import{_ as u}from"./plugin-vue_export-helper-DlAUqK2U.js";import{aj as m,am as n,as as g,ao as s,at as b,au as v,ap as o,al as a,an as t,aq as l,ar as f}from"./app-DpiNAgkx.js";const w={},y={id:"frontmatter-title-관련",tabindex:"-1"},q={class:"header-anchor",href:"#frontmatter-title-관련"},E={class:"table-of-contents"},T={href:"https://postgresql.org/docs/current/functions-math.html",target:"_blank",rel:"noopener noreferrer"},S={href:"https://ibm.com/think/topics/legacy-code",target:"_blank",rel:"noopener noreferrer"},I={href:"https://w3schools.com/sql/func_mysql_extract.asp",target:"_blank",rel:"noopener noreferrer"},C={href:"https://learn.microsoft.com/en-us/sql/t-sql/functions/cast-and-convert-transact-sql?view=sql-server-ver17",target:"_blank",rel:"noopener noreferrer"},P={href:"https://ibm.com/docs/en/db2-for-zos/12.0.0?topic=subqueries-correlated-non-correlated",target:"_blank",rel:"noopener noreferrer"},N={href:"https://arxiv.org/html/2406.13831v1",target:"_blank",rel:"noopener noreferrer"},R={href:"https://medium.com/learning-data/understanding-algorithmic-time-efficiency-in-sql-queries-616176a85d02",target:"_blank",rel:"noopener noreferrer"},k={href:"https://geeksforgeeks.org/sql/sql-with-clause/",target:"_blank",rel:"noopener noreferrer"},O={href:"https://stackoverflow.com/questions/49990666/trying-to-create-multiple-temporary-tables-in-a-single-query",target:"_blank",rel:"noopener noreferrer"},D={href:"https://dba.stackexchange.com/questions/212198/how-can-i-find-out-if-a-sql-function-can-be-inlined",target:"_blank",rel:"noopener noreferrer"},F={href:"https://dba.stackexchange.com/questions/115234/why-t-and-f-instead-of-true-and-false",target:"_blank",rel:"noopener noreferrer"},L={href:"https://docs.oracle.com/cloud/latest/big-data-discovery-cloud/BDDEQ/ceql_bp_filter_early.htm#BDDEQ-concept_F3B83B6965AC40429E5C68AB330BA74E",target:"_blank",rel:"noopener noreferrer"},A={href:"https://stackoverflow.com/questions/1111707/what-is-the-difference-between-a-hash-join-and-a-merge-join-oracle-rdbms",target:"_blank",rel:"noopener noreferrer"},x={href:"https://dba.stackexchange.com/questions/159413/exists-select-1-vs-exists-select-one-or-the-other",target:"_blank",rel:"noopener noreferrer"},B={href:"https://stackoverflow.com/questions/424212/performance-of-sql-exists-usage-variants",target:"_blank",rel:"noopener noreferrer"},K={href:"https://datacamp.com/tutorial/sql-limit",target:"_blank",rel:"noopener noreferrer"},M={href:"https://contrib.andrew.cmu.edu/~shadow/sql/sql1992.txt",target:"_blank",rel:"noopener noreferrer"},W={href:"https://cloud.google.com/products/compute",target:"_blank",rel:"noopener noreferrer"};function U(c,e){const h=l("VPCard"),r=l("router-link"),d=l("SiteInfo"),i=l("FontIcon"),p=l("RouteLink");return f(),m("div",null,[n("h1",y,[n("a",q,[n("span",null,g(c.$frontmatter.title)+" 관련",1)])]),s(h,b(v({title:"How to Design Structured Database Systems Using SQL [Full Book]",desc:"This book will guide you, step-by-step, through designing a relational database using SQL. SQL is one of the most recognized relational languages for managing and querying data in databases. You’ll learn the fundamental concepts related to both data ...",link:"/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/README.md",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",background:"rgba(10,10,35,0.2)"})),null,16),n("nav",E,[n("ul",null,[n("li",null,[s(r,{to:"#running-basic-queries"},{default:o(()=>e[0]||(e[0]=[t("Running Basic Queries")])),_:1})]),n("li",null,[s(r,{to:"#tuple-filtering"},{default:o(()=>e[1]||(e[1]=[t("Tuple Filtering")])),_:1})]),n("li",null,[s(r,{to:"#subqueries"},{default:o(()=>e[2]||(e[2]=[t("Subqueries")])),_:1})]),n("li",null,[s(r,{to:"#common-table-expressions"},{default:o(()=>e[3]||(e[3]=[t("Common Table Expressions")])),_:1})]),n("li",null,[s(r,{to:"#set-operations"},{default:o(()=>e[4]||(e[4]=[t("Set Operations")])),_:1})]),n("li",null,[s(r,{to:"#aggregation-queries"},{default:o(()=>e[5]||(e[5]=[t("Aggregation Queries")])),_:1})]),n("li",null,[s(r,{to:"#division-queries"},{default:o(()=>e[6]||(e[6]=[t("Division Queries")])),_:1})]),n("li",null,[s(r,{to:"#ranking-queries"},{default:o(()=>e[7]||(e[7]=[t("Ranking Queries")])),_:1})])])]),e[121]||(e[121]=n("hr",null,null,-1)),s(d,{name:"How to Design Structured Database Systems Using SQL [Full Book]",desc:"This book will guide you, step-by-step, through designing a relational database using SQL. SQL is one of the most recognized relational languages for managing and querying data in databases. You’ll learn the fundamental concepts related to both data ...",url:"https://freecodecamp.org/news/how-to-design-structured-database-systems-using-sql-full-book#heading-chapter-11-example-queries",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",preview:"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png"}),e[122]||(e[122]=a(`<p>Once we have done all this, we’ll have the database created and populated with tables. But these tables are empty, meaning they don&#39;t contain any tuples. So if we want to run queries on them that return any results, we need to execute <code>INSERT</code> statements to add tuples to all the tables.</p><p>In this case, since the database is an example, we don&#39;t have real data to use for populating the tables, and there&#39;s no simple and automatic way to fill them with synthetic data. The best option is to use the <strong>Python</strong> library <strong>faker</strong> and create a script to generate this synthetic data (I’ve explained this in this <strong>Jupyter Notebook</strong>).</p><p>There is also always the option to look for real data sources to populate our database. But when doing this, those data sources might provide information in table schemas that don&#39;t exactly match those of our database tables, requiring us to <strong>integrate</strong> and then <strong>insert</strong> the information through a process like an ETL. These <strong>ETL</strong> processes of integration and insertion are often applied in <strong>Data Warehouses</strong>, which can also be a database like ours.</p><hr><h2 id="running-basic-queries" tabindex="-1"><a class="header-anchor" href="#running-basic-queries"><span>Running Basic Queries</span></a></h2><p>So, assuming we already have the database populated with tables and tuples within them, we can run different queries on them. After all - the main operation that other services from other software layers use from the database is querying. This lets them obtain data that they can then transform, use to calculate certain metrics, or simply display to the end user.</p><p>For example, right after inserting the data, the first query we can run to ensure that the insertion process worked is the following:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT &#39;person&#39; AS tableName, COUNT(*) AS numberOfTuples</span>
<span class="line">FROM Person;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see, we use the FROM clause to get all the information stored in the <code>Person</code> table (which we could have written entirely in lowercase). Then, we use the <strong>aggregation function <code>COUNT(*)</code></strong> to count the total number of tuples in the table, naming the column where this number is stored <code>numberOfTuples</code>.</p><p>But, if we also want to display the table name in the same tuple as the previous count, we can add another column in the <code>SELECT</code> statement where all its values are <strong>&#39;person&#39;</strong>. This way, when the query is executed, it will return a table with two columns, one <code>tableName</code> and another <code>numberOfTuples</code>. Since the aggregation function only returns one value, the resulting table will have only one tuple, where the <code>tableName</code> column will have the value &#39;person&#39; and the other column will show the number of tuples in the <code>Person</code> table.</p><p>If we want to count the tuples of all the tables in the database, we have the option to create a larger query that gathers all the results of the <strong>sub-queries</strong> that count the tuples of each table. For this, we can use <code>UNION ALL</code>, which combines the tuples from all resulting tables into a single table. This works as long as all resulting tables have exactly the same schema, with the same column names and data types, as in this case.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT &#39;person&#39; AS tableName, COUNT(*) AS numberOfTuples</span>
<span class="line">FROM Person</span>
<span class="line">UNION ALL</span>
<span class="line">SELECT &#39;city&#39; AS tableName, COUNT(*) AS numberOfTuples</span>
<span class="line">FROM city;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Lastly, when we say &quot;obtain information&quot; about an element of the domain or database schema in this context, we mean getting its data stored in the attributes of the table that represents it.</p><p>For example, information about a person could be the <code>Name</code> or <code>Email</code> attribute of the <code>Person</code> table, among others. We won’t detail that info here, as in most cases, it’s easy to modify which attributes are selected to return as a query result. But in a real environment, it’s convenient and important to pay attention to the <strong>attributes</strong> the query should return, the <strong>names/aliases</strong> they should have, and the <strong>order</strong> in which they should be returned. The functionality of other software layers often depends on this step being performed correctly.</p><hr><h2 id="tuple-filtering" tabindex="-1"><a class="header-anchor" href="#tuple-filtering"><span>Tuple Filtering</span></a></h2><p>The query we just looked at is useful for managing the database. Knowing how much information is stored in each table helps us make sure that certain normalization or schema transformation operations have run correctly (and even that the information itself is correct).</p><p>Let’s now look at some other queries that allow us to execute services provided to the end user. We use these to operate on the domain according to its semantics, so they can be very diverse. Here, we’ll distinguish between different types of queries based on their approach and the SQL tools used in their construction.</p><p>First, we have a series of queries for tuple filtering. These queries apply a filter on a table to keep only certain tuples that meet specific conditions. Note that the table containing the tuples we want to filter can be generated in any way, whether through a <code>JOIN</code>, a set operation, or whatever we want to do. But if you need to perform a grouping with <code>GROUP BY</code>, the resulting table must be filtered using a <code>HAVING</code> clause, which differs from the usual <code>WHERE</code> clause used to filter tuples.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM person P</span>
<span class="line">WHERE P.name LIKE &#39;Carol%&#39;;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The above is a simple example that retrieves the tuples from the Person table for people whose names starts with <strong>“Carol“</strong>. As you can see, the only statement we need to filter tuples is <code>WHERE</code>. In it, all the conditions required for filtering are defined, regardless of their number or nature, as some will be performed using subquery results.</p><p>In this specific case, the query has the condition that a person&#39;s name must start with exactly the string that appears in the <code>LIKE</code> operator. Since it’s case-sensitive, the string has to match exactly what we want to search or filter. Then all tuples that meet this condition will be returned in the resulting table. We’ll get all of its attributes because of the <strong><code>SELECT *</code></strong> notation we used.</p><p>To illustrate that it doesn&#39;t matter whether you use uppercase or lowercase when naming schema elements in SQL statements, we can see in the below query that both the table <code>City</code> and its attributes are in lowercase (except for one that’s written exactly as it was declared, with the first letter capitalized). If we run this query, it will work the same as if we use <code>C.</code> to reference the attributes, since using only one table means there’s no ambiguity when referring to the table&#39;s columns.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM city C</span>
<span class="line">WHERE (population &gt; 20000 AND C.Latitude &gt;=0) OR C.longitude &lt;= 0;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Ultimately, with these conditions, we get all the tuples from <code>City</code> that have a population greater than 20,000 and a positive <code>latitude</code>, or those that simply have a negative <code>longitude</code>.</p><p>Let’s look at a similar example: here, we get all cruise bookings with a price below 500, an even cabin number, and a payment method of cash. In this case, we can see how we can apply different types of operators to build the condition.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM cruiseBooking CB </span>
<span class="line">WHERE CB.price &lt; 500 AND MOD(CB.cabinnumber, 2)=0 AND CB.paymentmethod=&#39;cash&#39;;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>On one hand, if we want to declare that all the conditions we impose must be met, we’ll use the logical operator <code>AND</code>. This performs a logical conjunction of those conditions so that the selected tuple is added to the resulting table only when all of them are met at the same time.</p><p>In other words, we can see the <code>WHERE</code> clause as a logical function that runs once for each tuple present in the table we want to filter. So if the result of that logical function is TRUE, then the tuple meets the conditions. Otherwise, it’s discarded and not included in the result table of the query.</p><p>So now we know that all the conditions we can define in a <code>WHERE</code> clause must be composed of a sequence of simpler logical conditions like <code>CB.price &lt; 500</code> joined by logical operators. Also, in each of these simpler conditions, we can find more logical operators, as they’re conditions that we can see as logical functions, which can themselves be composed of a sequence of even simpler conditions joined by logical operators. This allows for recursion, enabling us to use <strong>parentheses</strong> like in <code>(C1 AND (C2 OR C3))</code> to adjust the <strong>priority</strong> and <strong>precedence</strong> of these operators at different levels of recursion in our condition (just like in other programming languages).</p><p>On the other hand, we can also encounter conditions where arithmetic or comparison operators are used, such as in this case when checking if the string containing the payment method is exactly the value <code>‘cash‘</code>.</p><p>While in other languages we might write <code>CB.paymentmethod = &#39;cash&#39;</code>, in SQL we write the comparison operator with a single character <code>=</code>. If we want to negate it, we can do this either by using the logical operator <code>NOT</code> (affecting the entire equality condition) or by using <code>CB.paymentmethod &lt;&gt; &#39;cash&#39;</code> which represents the condition where it checks that the payment method is not <code>‘cash‘</code>, meaning it’s different from that value.</p><p>In addition to these operators, we also have a series of mathematical functions available. For example, to check if a number is even or odd, in most general-purpose programming languages we have the <strong>modulo operator <code>%</code></strong> which calculates the remainder of dividing the number by 2 - so if it’s 0, the number is even.</p>`,33)),n("p",null,[e[9]||(e[9]=t("But in SQL, these operations aren’t implemented by default with arithmetic operators, but rather with functions. Specifically, to calculate the modulo, we use ")),e[10]||(e[10]=n("code",null,"MOD(Dividend, Divisor)",-1)),e[11]||(e[11]=t(", although there are ")),n("a",T,[s(i,{icon:"iconfont icon-postgresql"}),e[8]||(e[8]=t("many other"))]),e[12]||(e[12]=t(" similar functions."))]),e[123]||(e[123]=a(`<p>We can use some of the operators mentioned earlier to perform calculations using entire columns. This results in other columns containing the results of those operations.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *, (CURRENT_DATE - CB.bookingDate) AS DateDifference1, ABS(CB.ArrivalDateFK-CB.DepartureDateFK)</span>
<span class="line">FROM cruiseBooking CB;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, in this query, we want to calculate several date differences, one being the number of days between the booking date and the current date, and another being the number of days between the departure and arrival dates of the cruise trip.</p><p>To do this for each tuple in the <code>CruiseBooking</code> table, the simplest way is to add several columns that take the results of these calculations as their values. Specifically, we create these columns in the <code>SELECT</code> statement. This selects the corresponding attributes from the resulting table of the query and displays them to the user. Only those attributes are visible to the user, even though we got them from a table with more attributes.</p><p>But, besides selecting attributes, we can also define new columns that didn&#39;t exist in the table we’re selecting from. For example, in this query, using the notation <code>*</code>, we select all the attributes present in the table from the <code>FROM</code> statement, which in this case is <code>CruiseBooking</code>.</p><p>In addition to those, we concatenate more attributes with a comma, like <code>DateDifference1</code> or the difference between the departure and arrival dates of the corresponding trip. If we look at the result of the query after adding these additional attributes, we’ll see a new column in the resulting table called <code>DateDifference1</code>, which will take as values the difference between the current date gotten with <code>CURRENT_DATE</code> and the booking date, which is <code>CB.bookingDate</code>.</p><p>So we see that in the SELECT statement, we can perform operations with the values of the tuples to generate new columns with intermediate calculations, or simply calculations required by the query, as in this case.</p><p>Specifically, the operation performed on each tuple to generate the value of the new column is defined in the <code>SELECT</code> statement itself. In this case, with <code>CURRENT_DATE - CB.bookingDate</code>, we define that the value of each tuple equals the current date minus the booking date. By default in SQL this returns the difference in days between the two dates.</p><p>Then to get the difference between the departure and arrival dates of the cruise trip, we use the values of the <code>DepartureDateFK</code> and <code>ArrivalDateFK</code> attributes from the foreign key pointing to Voyage. This avoids having to query data from other tables that contain them.</p><p>If we simply subtract them, depending on the order, we could get negative results, since one date is earlier than the other. So if we just want the absolute difference, we can wrap the operation with the <code>ABS()</code> function. And if we don&#39;t assign a specific name to that additional column, SQL by default assigns it the name <strong>“abs“</strong>. But we’ll want to change it sooner or later to avoid ambiguity problems if we use the <code>ABS()</code> function again to create another new column.</p><p>In the previous query, we saw that all the information we needed was present in the <code>CruiseBooking</code> table from the FROM clause - but this is not always the case.</p><p>For example, in the below query, we want to do a few things: first, we want to get all the bookings made by people whose names start with a letter that is later or equal to <code>&#39;L&#39;</code>. They should also meet a series of conditions like the ones we saw before. Finally, we want to calculate the difference in days between the current date and the booking date as we saw before.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *, (CURRENT_DATE - CB.bookingDate) AS DateDifferenceColumn </span>
<span class="line">FROM cruiseBooking CB INNER JOIN Person P ON CB.PersonFK = P.PersonID</span>
<span class="line">WHERE CB.price &lt; 2000</span>
<span class="line">    AND MOD(CB.cabinnumber, 2) = 0</span>
<span class="line">    AND CB.paymentmethod = &#39;cash&#39;</span>
<span class="line">    AND CB.bookingDate BETWEEN &#39;2025-01-01&#39; AND CURRENT_DATE</span>
<span class="line">    AND P.Name &gt; &#39;L&#39;;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For this, if we only use the <code>CruiseBooking</code> table in the <code>FROM</code> clause, we won&#39;t be able to access the name of the person who made the booking, as that’s an attribute of the <code>Person</code> table. We can get information from that table using the foreign key <code>PersonFK</code> from <code>CruiseBooking</code>. So to use the Name attribute from the <code>Person</code> in our query, we need to somehow &quot;concatenate&quot; or join the columns of the <code>Person</code> table with the information from the <code>CruiseBooking</code> table that we had before.</p><p>In SQL, the <code>JOIN</code> operation allows us to do this. We just need to choose a type and conditions that let us obtain only the tuples with the information we want.</p><p>Among all the types of <code>JOIN</code>s, the least likely to be used in production or in complex queries is the <strong>implicit join</strong>. When we use an <strong>implicit join</strong>, we are performing a Cartesian product between all the tuples involved in that <code>JOIN</code>. So if we want to keep only certain tuples from that Cartesian product, we have to use a <code>WHERE</code> clause to impose certain conditions on the attributes.</p><p>Implicit <code>join</code>s are harder to read and maintain. In large or complex queries, we need to separate the join itself from the conditions on the Cartesian product. That means the logic is split between the FROM list and the <code>WHERE</code> clause, so you have more places to check when you modify or refactor the query.</p><p>Also, in implicit <code>JOIN</code>s, we can’t perform operations equivalent to an <code>OUTER JOIN</code> because there’s no way to fill certain attributes with <code>NULL</code> if they’re not referenced in the other table of the <code>JOIN</code> (among other disadvantages). So the type of <code>JOIN</code> we choose will depend on the condition we need to impose on the tuples of the Cartesian product.</p>`,18)),n("p",null,[e[14]||(e[14]=t("Just keep in mind that there are certain cases where it might be convenient to use ")),e[15]||(e[15]=n("strong",null,[t("implicit "),n("code",null,"join"),t("s")],-1)),e[16]||(e[16]=t(", such as in queries involving very few tables (at most 2 to keep the code as simple as possible) with simple restrictions, or when maintaining ")),n("a",S,[s(i,{icon:"iconfont icon-ibm"}),e[13]||(e[13]=t("legacy code"))]),e[17]||(e[17]=t(", meaning old or inherited code that uses implicit ")),e[18]||(e[18]=n("code",null,"join",-1)),e[19]||(e[19]=t("s."))]),e[124]||(e[124]=a(`<p>In this case, when performing the Cartesian product, we’ll get a series of tuples that combine all those from <code>CruiseBooking</code> and <code>Person</code>. This will result in tuples with information about these two tables where the person&#39;s information does not correspond with the person referenced by the foreign key of the <code>CruiseBooking</code> tuple.</p><p>For that reason, we don&#39;t need those tuples from the Cartesian product - or in other words, we want to get all those where the foreign key <code>PersonFK</code> of <code>CruiseBooking</code> points to the person whose information is indeed in that same tuple of the Cartesian product.</p><p>Formally, we express this condition as <code>CB.PersonFK = P.PersonID</code>. In this case, we need to assign alias names to the tables to differentiate their attributes and resolve possible ambiguity issues. So the most suitable type of <code>JOIN</code> for this query is an <code>INNER JOIN</code>, as it allows us to declare this equality condition exactly as we have written it here in an <code>ON</code> clause, as seen above.</p><p>In this way, by using a specific type of <code>JOIN</code> that’s not implicit, we can isolate all the filtering conditions of the tuples in the <code>WHERE</code> clause (dedicating the <code>FROM</code> to obtaining the data). Through the <code>JOIN</code>, we can concatenate the attributes of other tables to the resulting table of the query, and apply a specific filter to the tuples of the Cartesian product of that operation with an equality condition.</p><p>Regarding the <code>WHERE</code> conditions in this query, we’ve added one that makes sure the booking date is between <code>&#39;2025-01-01&#39;</code> and the current date we get with <code>CURRENT_DATE</code>. We could’ve used the arithmetic operators <code>&lt;=</code> and <code>&lt;=</code> for this, but SQL offers us a more convenient alternative using <code>BETWEEN</code>, where we define that the date of the <code>bookingDate</code> attribute must be between <code>&#39;2025-01-01&#39;</code> and <code>CURRENT_DATE</code>, both included.</p><p>The <code>BETWEEN</code> operator would also work to check if a string is between a pair of strings, all compared alphabetically. In this query, the only condition we impose on the lexicographical order of a string is <code>P.Name &gt; &#39;L&#39;</code>. This ensures that the name of the person who made the booking starts with a letter <strong>greater than or equal</strong> to <code>L</code>. (If their name is composed of text that starts with <code>L</code> followed by more letters, that text will automatically be considered strictly greater than the text <code>&#39;L&#39;</code>.)</p><p>If we wanted to keep only the people whose names start strictly with a letter greater than <code>L</code>, we would have to use the condition <code>P.Name &gt; &#39;M&#39;</code>.</p><p>What if we need to get a list of all the people in the database, their information, and also the details of all the cruise bookings they have made? We’d need a list where all the registered people in the database appear.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM cruiseBooking CB RIGHT JOIN Person P ON CB.PersonFK = P.PersonID</span>
<span class="line">ORDER BY P.PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, if someone has made 2 bookings, there will be 2 rows with their information plus the details of the two bookings they made. Meanwhile, people who have never made a booking will appear in the list with a row containing their information and a series of <code>NULL</code> values in the columns where the booking information would be.</p><p>This query isn’t common in real cases, but this structure can be useful for solving other types of queries. So to build this list, the first operator we might think of is an OUTER JOIN. In this type of join, we specify the side of the table whose rows should always appear in the final list, filling in with <code>null</code>s in the other table when necessary.</p><p>To understand this, in this example, we see that a person doesn’t have to have any associated booking - so for each person, there doesn&#39;t necessarily have to be a booking in their name. So there may be some people who don’t have any bookings associated with them. So when we’re trying to do an <code>INNER JOIN</code> with the <code>CruiseBooking</code> table, they won&#39;t appear in the resulting table from the query.</p><p>That&#39;s why, instead of an <code>INNER JOIN</code> where we impose a strict condition that all tuples from the operation must meet, we use an <code>OUTER JOIN</code>. So, if we want all people to appear in the list even if they haven&#39;t made any bookings, we need to specify the side of the <code>OUTER JOIN</code> where we placed the <code>Person</code> table in the <code>JOIN</code> operation.</p><p>In this case, the <code>Person</code> table is on the right side, meaning its attributes are concatenated to the right of those in the <code>CruiseBooking</code> table. So in the <code>OUTER JOIN</code>, we must specify the <code>RIGHT</code> side so that all tuples from the table on the right side appear in the list, and for those people who don&#39;t have any associated bookings, their corresponding tuple will be filled with NULL values in the respective attributes that hold booking information.</p><p>If we had placed the Person table on the left side, then to achieve the same result as the previous query but with the columns of both tables reordered, we just need to change <code>RIGHT</code> to <code>LEFT</code> in the <code>JOIN</code> operation. This way, all tuples from the table on the left (meaning Person) must appear in the resulting table. The right side gets filled in with NULL values in this case, since that&#39;s where the attributes of the <code>CruiseBooking</code> table are.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Person P LEFT JOIN cruiseBooking CB ON CB.PersonFK = P.PersonID</span>
<span class="line">ORDER BY P.PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>On the other hand, in both queries, you can see that we used <code>ON</code> to define the equality condition on the tuples of the Cartesian product produced by <code>JOIN</code>. We have to do this because if we use <code>USING</code> instead of <code>ON</code>, both attributes on which we want to impose the equality condition must be named exactly the same - so we can’t use <code>USING</code> here.</p><p>Aside from the <code>JOIN</code> operation from which the data is extracted, we often need to return the result sorted by an attribute. It may also simply be useful to have the result sorted so we can make checks more quickly, as in this case.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.Birth</span>
<span class="line">FROM Person P LEFT JOIN cruiseBooking CB ON CB.PersonFK = P.PersonID</span>
<span class="line">ORDER BY P.PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To do this, at the end of the query, we can add an <code>ORDER BY</code> statement, which sorts the tuples of the resulting table according to the <code>PersonID</code> attribute of the person. This attribute doesn’t need to appear in the <code>SELECT</code>, as we might need other attributes that aren’t the ones defining the order, as shown above.</p><p>To finish with this type of <code>JOIN</code>, besides defining one side as <code>RIGHT</code> or <code>LEFT</code>, in an <code>OUTER JOIN</code> we might also need all the tuples from both sides&#39; tables to appear. In the query below, for example, we need to get a list of all driving license applications, so that all of them appear, one in each tuple, with all the information regarding their rejection or acceptance.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM DrivingLicense D FULL OUTER JOIN RejectedDrivingLicense R USING (LicenseID);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>To resolve this query, first, keep in mind that the schema constraints prevent us from having a driving license application both accepted and rejected at the same time. So for each application registered in the database, there will be either a tuple in <code>RejectedDrivingLicense</code> or in <code>DrivingLicense</code>, depending on whether it has been rejected or not. So when obtaining the query list, if the resulting table contains all the attributes from both tables, there will always be <code>NULL</code>s in some of them (either in <code>RejectedDrivingLicense</code> or in <code>DrivingLicense</code>).</p><p>To make sure that all applications appear, we can perform a <code>FULL OUTER JOIN</code>, where the <code>OUTER</code> specification is optional as we have seen on other occasions. This forces the tuples from both tables to appear in the final result, filling with NULL on the corresponding side for each tuple.</p><p>For example, if a license is accepted and we try to find it in the <code>RejectedDrivingLicense</code> table, it clearly won&#39;t be there. So, if we did an <code>INNER JOIN</code>, we wouldn&#39;t get a tuple for that application, which happens similarly with rejected applications and the <code>DrivingLicense</code> table. So with a <code>FULL OUTER JOIN</code>, we ensure that all applications appear, filling with <code>NULL</code> in <code>RejectedDrivingLicense</code> when the application is accepted and in the other table when it’s rejected. In this case, its also possible to use <code>USING</code> in the <code>JOIN</code>, since the equality condition is based on attributes in different tables that have exactly the same name.</p><p>Another <code>JOIN</code> we might encounter in real queries is the <code>NATURAL JOIN</code>, which is very similar to the <code>INNER JOIN</code> but with simpler syntax.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT PersonFK, RequestDate, Fee, ApprovalDate, Points</span>
<span class="line">FROM DrivingLicenseRequest NATURAL JOIN DrivingLicense;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, you can see in the example above a query that can help us verify that the schema&#39;s integrity constraints are met. In it, we get a list of all driving license requests that have been approved.</p><p>To do this, we perform a <code>NATURAL JOIN</code> between the <code>DrivingLicense</code> table and its superclass <code>DrivingLicenseRequest</code>. Since the only attributes with equivalent names are <code>LicenseID</code>, SQL automatically imposes the condition that the tuple with information from both tables has the same values in the <code>LicenseID</code> attributes of both tables, removing both attributes from the resulting table of the query.</p><p>This automatically imposed condition, as well as removing the attributes, is what characterizes the <code>NATURAL JOIN</code>. It’s often be preferable to an <code>INNER JOIN</code> because of these characteristics. By eliminating identical attributes, we eep the information that actually represents the people in those tuples. We can then use it to calculate various metrics or even as the result of a subquery in a more general query.</p><p>In this specific case, since all accepted requests have to be recorded in the <code>DrivingLicenseRequest</code> table, this query should return all tuples from <code>DrivingLicense</code>. But if any aren’t recorded in <code>DrivingLicenseRequest</code>, the foreign key won’t reference any valid tuple in <code>DrivingLicenseRequest</code>, revealing a database integrity issue.</p><p>Fortunately, we never have to manually check this situation with these queries, as the DBMS automatically verifies that all <strong>integrity constraints</strong> are met with each database modification, especially those related to keys.</p><p>In real queries, <strong>multiple <code>JOIN</code></strong> operations are usually used in the same <code>FROM</code> statement because we need to gather data from multiple tables (or even from within the same table).</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Person P</span>
<span class="line">    INNER JOIN Residence R1 ON (P.PersonID = R1.PersonFK)</span>
<span class="line">    INNER JOIN Residence R2 ON (</span>
<span class="line">        P.PersonID = R2.PersonFK</span>
<span class="line">        AND R1.CityFK &lt;&gt; R2.CityFK</span>
<span class="line">    )</span>
<span class="line">ORDER BY P.personID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, say we want to find people who have lived in several different cities at some point in their lives, regardless of when they did so. Since our schema lets people live in multiple cities at once, we’ll have to use several <code>JOIN</code> operations to gather data from <code>Person</code> and <code>Residence</code> and join them.</p><p>But given the condition we impose on the people, to know if someone has lived in more than one city, we need to check the <code>Residence</code> table and see if there are multiple <code>Residence</code> tuples for the same person with different cities.</p><p>Specifically, the query we want to make should get all those people who have lived in at least two <strong>different</strong> cities. If we only impose the condition that a person appears in at least two tuples of the <code>Residence</code> table, we’d get people who have had at least two residences - not those who have lived in different cities in those residences.</p><p>Therefore, the final condition ends up being that the person appears in at least two tuples of <code>Residence</code> where the associated city they have lived in is <strong>different</strong> in both tuples. Also, by checking this condition, we aren’t ensuring that the person only has those two tuples - we just need to know if they appear in at least two tuples with the previous characteristics (as a person may have had many residences).</p><p>To implement this query, we might first think of using set operations and subqueries - but there is a way to solve it using only <code>JOIN</code> operations.</p><p>When we do a <code>JOIN</code> between two tables, we are really doing the Cartesian product, from which we only keep some tuples that meet certain conditions. For example, when doing a <code>JOIN</code> between <code>Person</code> and <code>Residence</code>, the foreign key <code>PersonFK</code> in <code>Residence</code> must refer to the person from that same tuple in the <strong>Cartesian product</strong>. This means it must match the PersonID attribute from the <code>Person</code> table. With this, we can see that we obtain all the residences each person has or has had.</p><p>Then, from all of them, if we want to check that there are at least two with different <strong>foreign key <code>CityFK</code></strong> values (meaning that there are two residences in different cities), we can do another <code>JOIN</code> of the intermediate table resulting from the previous <code>JOIN</code> with the <code>Residence</code> table.</p><p>This way, in addition to saying that its <strong>foreign key</strong> <code>PersonFK</code> has to refer to the corresponding person from each tuple resulting from the <code>JOIN</code>, we’re also declaring that the city it refers to must be different from the city referenced by the previous <code>Residence</code> table used in the previous <code>JOIN</code>.</p><p>To understand this in a more programmatic way, when doing a <code>JOIN</code> between <code>Residence</code> and itself, we’re getting tuples that represent <strong>pairs of residences</strong>. So we’re obtaining a series of tuples that together represent the Cartesian product between the tuples of the <code>Residence</code> table with themselves.</p><p>In other words, we end up with a series of tuples where, in each one, we can find information from exactly 2 tuples of the <code>Residence</code> table, for each possible pair of <code>Residence</code> tuples (including cases where both tuples are the same). If we add the restriction that these pairs must refer to a certain person, then they will be all the possible pairs of residences that a person has had.</p><p>Then if we also add the condition that for each pair of residences the cities they refer to must be different, we‘ll end up with tuples where the person who has had those residences have lived in at least two different cities. This doesn’t ensure that it’s exactly two, as they may have lived in many more (which we can see in the resulting tuples from these <code>JOIN</code> operations).</p><p>When implementing this in SQL, we see that in both <code>ON</code> clauses, we declare the condition that the Residence tuples must refer to the same person of the tuple we want to construct - with that person and a pair of their residences. Also, in the second <code>JOIN</code>, we declare the condition that the cities of the pair of residences must be different using the operator <code>&lt;&gt;</code>. Finally, we order the result according to the values of the <code>PersonID</code> attribute.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT DISTINCT P.Name</span>
<span class="line">FROM Person P</span>
<span class="line">    INNER JOIN Residence R1 ON (P.PersonID = R1.PersonFK)</span>
<span class="line">    INNER JOIN Residence R2 ON (</span>
<span class="line">        P.PersonID = R2.PersonFK</span>
<span class="line">        AND R1.CityFK &lt;&gt; R2.CityFK</span>
<span class="line">    )</span>
<span class="line">ORDER BY P.personID; /*Error*/</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see from the query result, there are people who have had many residences, resulting in many pairs of residences that meet the imposed conditions. This creates multiple tuples in the resulting table where the same person&#39;s information appears.</p><p>So, if we only want to get the person&#39;s name, we can replace <code>*</code> with <code>P.Name</code> in the <code>SELECT</code> statement to select only that attribute. To avoid duplicate values, we can use <code>DISTINCT</code>. Without <code>DISTINCT</code>, the same person&#39;s name may appear multiple times, depending on the number of residence pairs they have had in different cities. This also happens because SQL by default models tables with multisets, allowing such duplicates.</p><p>If we care about removing duplicates, we should use <code>DISTINCT</code> - but this decision can affect other statements like <code>ORDER BY</code>. In this example, we’re ordering by the values of the <code>PersonID</code> attribute, which we don&#39;t need in the resulting table where only the Name attribute appears.</p><p>Since <code>PersonID</code> doesn’t appear in the <code>SELECT</code> after using <code>DISTINCT</code>, the DBMS will give us an error. We have several options to fix it.</p><p>On one hand, we can remove <code>DISTINCT</code>, which will result in duplicate person data but that’s ordered by their <code>PersonID</code> (even though it won&#39;t be shown in the result).</p><p>On the other hand, we can keep <code>DISTINCT</code> and remove <code>ORDER BY</code>, because if the attribute we are ordering by does not appear in the <code>SELECT</code> after using <code>DISTINCT</code>, we will get an error that will prevent us from executing the query.</p><p>Another alternative we have is to show all the information about the person, not just the name. This way, we can order the result by the <code>PersonID</code> attribute and remove duplicate people. Instead of writing the entire list of attributes from the Person table in the <code>SELECT</code>, we can use the notation <code>P.*</code> to refer to <strong>all the attributes of the table with alias <code>P</code></strong>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT DISTINCT P.*</span>
<span class="line">FROM Person P</span>
<span class="line">    INNER JOIN Residence R1 ON (P.PersonID = R1.PersonFK)</span>
<span class="line">    INNER JOIN Residence R2 ON (</span>
<span class="line">        P.PersonID = R2.PersonFK</span>
<span class="line">        AND R1.CityFK &lt;&gt; R2.CityFK</span>
<span class="line">    )</span>
<span class="line">ORDER BY P.personID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Finally, in SQL, it&#39;s common to encounter queries where we need to work with dates. For example, in our schema, we might have a query to get all the people who were born in May.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *, EXTRACT(MONTH FROM Birth) AS BirthMonth</span>
<span class="line">FROM Person</span>
<span class="line">WHERE EXTRACT(MONTH FROM Birth) = 5;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,57)),n("p",null,[e[21]||(e[21]=t("We can solve this by imposing a single condition on the birth date, with the peculiarity that we can't treat the data type exactly as if it were entirely numeric or text. Instead, we need to extract ")),n("a",I,[s(i,{icon:"fas fa-globe"}),e[20]||(e[20]=t("characteristics"))]),e[22]||(e[22]=t(" from the date to operate with."))]),e[125]||(e[125]=n("p",null,[t("In this case, the clearest characteristic to obtain is the month. By using the "),n("code",null,"EXTRACT()"),t(" function and the "),n("code",null,"MONTH"),t(" characteristic, we extract the month number from the Birth attribute's date to check if it’s May or not.")],-1)),n("p",null,[e[24]||(e[24]=t("Note that the function generally returns numbers for day, month, year, and so on, not strings. So we treat the month as if it were a number from 1 to 12. We can convert between number and string ")),n("a",C,[s(i,{icon:"fa-brands fa-microsoft"}),e[23]||(e[23]=t("using other SQL tools"))]),e[25]||(e[25]=t(", all in the appropriate format according to the time zone and geographic area. Then, if we want that date characteristic to appear as an additional attribute in the resulting table, we simply treat the ")),e[26]||(e[26]=n("code",null,"EXTRACT()",-1)),e[27]||(e[27]=t(" function as if it were any SQL function that returns a value when given certain values from a tuple."))]),n("p",null,[e[29]||(e[29]=t("But even if we assign it an alias, we can’t use that alias in the ")),e[30]||(e[30]=n("code",null,"WHERE",-1)),e[31]||(e[31]=t(" clause to declare the condition that it equals to be 5. Instead, we must write the entire calculation in the ")),e[32]||(e[32]=n("code",null,"WHERE",-1)),e[33]||(e[33]=t(" clause. Although this may seem inefficient in terms of readability, without using additional ")),s(p,{to:"/freecodecamp.org/mysql-common-table-expressions.html"},{default:o(()=>e[28]||(e[28]=[n("strong",null,"Common Table Eexpression (CTE) techniques",-1)])),_:1}),e[34]||(e[34]=t(" like those we will see later, we have no choice but to duplicate the attribute calculation in the ")),e[35]||(e[35]=n("code",null,"WHERE",-1)),e[36]||(e[36]=t(" clause if we want to impose a condition on it."))]),e[126]||(e[126]=a(`<div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *, EXTRACT(WEEK FROM Birth)</span>
<span class="line">FROM Person;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>In addition to the day, month, and year, the <code>EXTRACT()</code> function allows us to obtain all kinds of characteristics from a date, like the week number with <code>WEEK</code> as shown above, or the current quarter number with <code>QUARTER</code>.</p><hr><h2 id="subqueries" tabindex="-1"><a class="header-anchor" href="#subqueries"><span>Subqueries</span></a></h2><p>There are some SQL queries that require subqueries. A subquery is simply a query inside another query. It helps you solve a smaller problem so the main query can solve a bigger one.</p><p>Let’s dive in a little deeper. When you run a query in SQL, you get a result table (a multiset, since rows can repeat). A subquery lets the outer query use that result - for example, to check membership or existence.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Person P</span>
<span class="line">WHERE P.PersonID IN (SELECT PersonFK FROM Residence);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This returns every person whose identifier appears in <code>Residence.PersonFK</code> - that is, everyone who has (or had) a recorded residence. The subquery produces the set of referenced person IDs, while the outer query keeps rows where <code>p.PersonID</code> is in that set.</p>`,8)),n("p",null,[e[38]||(e[38]=t("Note that this is a ")),n("a",P,[s(i,{icon:"iconfont icon-ibm"}),e[37]||(e[37]=t("non-correlated subquery"))]),e[39]||(e[39]=a(" (it doesn’t reference the outer query), which many databases may <strong>materialize once</strong> or rewrite as a <strong>semi-join</strong> before applying the <code>IN</code> filter. In practice, this is usually comparable to an equivalent <code>EXISTS</code> or <code>JOIN</code>-based formulation. We’ll just choose the form that’s clearest and add appropriate indexes (for example, <code>Residence(PersonFK)</code>, <code>Person(PersonID)</code>) for speed.",15))]),e[127]||(e[127]=a(`<p>If the subquery can return <code>NULL</code>, <code>IN</code> uses three-valued logic. With a foreign key on <code>Residence.PersonFK</code>, <code>NULL</code> values are typically disallowed, so this isn’t an issue.</p><p>On the other hand, we can solve the query using <code>JOIN</code> operations as shown below:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT DISTINCT P.*</span>
<span class="line">FROM Person P INNER JOIN Residence R ON R.PersonFK = P.PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Here, we combine the data from <code>Person</code> and <code>Residence</code> using the equality condition that requires the foreign key of <code>Residence</code> to reference the person in the same tuple of the Cartesian product. This way, we only get those tuples that have the information of a residence and the person associated with it.</p><p>Then, to keep only the data of the people, we use <code>P.*</code> as before - but here we need to use <code>DISTINCT</code>, since a person may have multiple residences. Specifying <code>DISTINCT</code> prevents this from duplicating the data of the same person.</p>`,5)),n("p",null,[e[41]||(e[41]=t("The ")),e[42]||(e[42]=n("code",null,"JOIN",-1)),e[43]||(e[43]=t(" operation is often considered inefficient because it’s a Cartesian product that must construct all tuples of that product and then filter them using the conditions we declare. But we can make it faster with the right hardware, like ")),n("a",N,[s(i,{icon:"fas fa-globe"}),e[40]||(e[40]=t("GPUs"))]),e[44]||(e[44]=t("."))]),e[128]||(e[128]=a(`<p>Still here, we need to remove duplicates with <code>DISTINCT</code>, which involves additional processing of the query result. We also need another filter or process that eliminates duplicate tuples, so it seems less efficient at first glance.</p><p>But depending on how the DBMS implements these operations at a physical level, it can be more or less efficient than using subqueries (as the hardware also makes a difference).</p><p>Here’s another construction based on subqueries that we can use to solve the previous query. As you can see, we build a <strong>correlated subquery</strong> where we use the <code>PersonID</code> attribute from the &quot;higher-level&quot; query to get all the residences (tuples) from the Residence table that belong to the person indicated by the <code>PersonID</code> identifier. In other words, since the <code>WHERE</code> clause is executed for each tuple of Person, we can construct a subquery where, given a certain person with that identifier, we can get all the residences registered in their name. That would be those whose foreign key PersonFK refers to the <code>PersonID</code> identifier of the person.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Person P</span>
<span class="line">WHERE EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM Residence R</span>
<span class="line">        WHERE R.PersonFK = P.PersonID</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>With this correlated subquery, SQL must build its result for each person in the <code>Person</code> table, as the result depends on the specific person being processed. So to only keep those people who have a residence, we use the <code>EXISTS</code> operator to verify that the resulting multiset of the subquery contains at least one tuple (indicating that the person has a residence).</p><p>SQL has to go through the <code>Residence</code> table for each person in the <code>Person</code> table, although it only goes through <code>Residence</code> until it finds the first tuple whose foreign key points to the corresponding person. This avoids unnecessary checks of the rest of the tuples in <code>Residence</code> because <code>EXISTS</code> only requires at least one tuple in the subquery.</p>`,6)),n("p",null,[e[48]||(e[48]=t("Still, in the ")),n("a",R,[e[45]||(e[45]=t("worst-case scenario (")),s(i,{icon:"fa-brands fa-medium"}),e[46]||(e[46]=n("code",null,"learning-data",-1)),e[47]||(e[47]=t(")"))]),e[49]||(e[49]=t(", it would have to go through the entire table for each person if no person has or has had residences."))]),e[129]||(e[129]=a(`<p>Another way we can use membership or existence operators is on a list of values. This is declared very similarly to a tuple and a subquery but is not necessarily a tuple or a subquery.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Pool P</span>
<span class="line">WHERE Status IN (&#39;closed&#39;, &#39;renovation&#39;)</span>
<span class="line">    AND mindepth IN (</span>
<span class="line">        SELECT mindepth</span>
<span class="line">        FROM Pool</span>
<span class="line">        WHERE mindepth &gt; 4</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, above we have a query that returns all pools whose status is <code>&#39;closed&#39;</code> or <code>&#39;renovation&#39;</code> and whose minimum depth is greater than 4. To check the first condition, we could easily use the logical <code>OR</code> operator and declare two simpler conditions to check whether the Status value is either <code>&#39;closed&#39;</code> or <code>&#39;renovation&#39;</code>. But we can do this more simply using the <code>IN</code> operator. So by using the notation <code>(&#39;closed&#39;, &#39;renovation&#39;)</code>, we declare a list with those two values, checking with <code>IN</code> if the value contained in the Status attribute is in the list or not. This has the same effect as using the <code>OR</code> operator, but with clearer syntax and similar efficiency.</p><p>This check we do with <code>IN</code> is like a membership check on the result of a subquery, as the syntax is very similar. But don’t confuse the list declaration with a subquery, since <code>(&#39;closed&#39;, &#39;renovation&#39;)</code> doesn’t represent a multiset with tuples, but rather a list of values. We can also view it as if it were a column on which we perform a check.</p><p>On the other hand, the simplest way to check if the pool&#39;s minimum depth is greater than 4 is with the condition <code>mindepth &gt; 4</code> directly. But to show an equivalent way of checking with subqueries, you can see above that the subquery for the condition retrieves all <code>mindepth</code> values from the <code>Pool</code> table that are strictly greater than 4. Then it uses IN to check if the <code>mindepth</code> value from the outer query&#39;s <code>Pool</code> table is in the subquery&#39;s result.</p><p>So instead of writing <code>mindepth &gt; 4</code> directly, the subquery first selects all <code>mindepth</code> values greater than 4, and the outer query uses IN to keep a pool row only if its <code>mindepth</code> is in that set. In practice, although this can also be a solution to the query, we should keep the code as simple as possible. We generally avoid these techniques.</p><p>Also, we don’t need <code>alias P.</code> to refer to the <code>mindepth</code> of the outer query - as it’s the only one called that way in this query. But if we had to use it in the subquery, we’d need to use the <code>alias P.</code> to distinguish it from the <code>mindepth</code> attribute of the <code>Pool</code> table in the subquery. (This also doesn’t need an alias because it’s a simple subquery without another subquery inside it. This is possible to do, and sometimes even necessary.)</p><p>Here’s another equivalent way to solve the query using subqueries:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Pool P</span>
<span class="line">WHERE Status IN (&#39;closed&#39;, &#39;renovation&#39;)</span>
<span class="line">    AND P.mindepth &gt; ALL (</span>
<span class="line">        SELECT mindepth</span>
<span class="line">        FROM Pool</span>
<span class="line">        WHERE mindepth &lt;= 4</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The main difference is that here, the subquery gets all the <code>mindepth</code> values that are <code>&lt;= 4</code>, which is the opposite condition of what we want the tuples to meet. So in the outer query, we have the result of this subquery, which includes all the <code>mindepth</code> values we’re not interested in.</p><p>To check if a tuple meets the condition of having a minimum <code>depth &gt; 4</code> using these values, we use the <code>&gt; ALL</code> operator to verify if the <code>mindepth</code> of the tuple we are checking is strictly greater than all the values present in the subquery.</p><p>This equivalent way of solving the query is more elaborate than the simplest and most efficient solution, which is to use the <code>mindepth &gt; 4</code> condition directly. This is simply an example to demonstrate that there&#39;s often more than one way to get the <strong>same result</strong> for <strong>any state</strong> of the database. This is the definition of <strong>equivalent queries</strong>.</p><p>Also, in many situations, it’s useful to use operators like <code>ANY</code>, <code>IN</code>, <code>ALL</code>, <code>EXISTS</code>, and so on in combination with other arithmetic operators on a subquery to define conditions that certain tuples must meet, as shown in these examples.</p><p>So far, we’ve seen queries that use subqueries in their implementation, but those subqueries essentially behave as if they were queries themselves. This means we can execute them directly on the DBMS as if they were regular queries. So nothing prevents a subquery from being made up of subqueries at a &quot;lower&quot; level, meaning subqueries that are at a <strong>nesting level</strong> below the other <strong>subquery</strong>, which in turn is at a lower nesting level than the <strong>query</strong> it’s in.</p><p>Basically, SQL allows us to chain as many subqueries as we want within a query or subquery. This helps us solve problems like the query below, which retrieves a list with information on all the people who don’t have a valid driver&#39;s license:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Person P</span>
<span class="line">WHERE NOT EXISTS (</span>
<span class="line">    SELECT *</span>
<span class="line">    FROM DrivingLicense D</span>
<span class="line">    WHERE D.LicenseID IN (</span>
<span class="line">            SELECT LicenseID</span>
<span class="line">            FROM DrivingLicenseRequest R</span>
<span class="line">            WHERE R.PersonFK = P.PersonID</span>
<span class="line">        )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We could approach this query so that it&#39;d require <code>JOIN</code> operations to solve it. But in this case, it’s structured in a &quot;nested&quot; manner at the subquery level so that it requires the use of subqueries.</p><p>So to get this list, we first go through all the people in the Person table. For each one, we check that there is no driver&#39;s license whose associated request was created by that person. We can implement this condition by applying the <code>NOT EXISTS</code> operator to a subquery that returns all valid driver&#39;s licenses associated with a person. We get these by filtering <code>DrivingLicense</code> to licenses whose matching <code>DrivingLicenseRequest</code> row has <code>PersonFK = P.PersonID</code> - that is, licenses requested by the current person.</p><p>Regarding this last point, as you can see in the code, the simplest way to implement it with subqueries is to check that the <code>LicenseID</code> of the valid driver&#39;s license exists in the set of <code>LicenseID</code> values from the requests in the <code>DrivingLicenseRequest</code> table whose foreign key points to the person being iterated over in Person. That makes this subquery <strong>correlated</strong> with the outer query we are making, as it includes the attribute <code>P.PersonID</code>.</p><p>In short, we’ve implemented this query by <strong>nesting subqueries</strong>, where SQL allows us to reach an arbitrary level of nesting according to the needs of the query. But we could’ve done it in other ways like using <code>JOIN</code> operations, which in certain situations are easier to understand than the approach we just followed.</p><p>Just remember that nesting queries is not always the best way to solve a problem, especially when multiple levels of nesting are created (whether correlated or not with each other). We’re just showing what’s possible here. It’s only worthwhile when it improves the efficiency or clarity of the query sufficiently compared to other alternatives.</p><p>Let’s talk about where or in which statement subqueries can be nested. In the below code, you can see how the subquery is nested in the <code>FROM</code> clause.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.Name</span>
<span class="line">FROM Person P</span>
<span class="line">    INNER JOIN (</span>
<span class="line">        SELECT DISTINCT PersonFK</span>
<span class="line">        FROM Rental</span>
<span class="line">    ) R ON R.PersonFK = P.PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Since it returns a table with tuples, we’ll often use that query result in a <code>FROM</code> clause to get the information from the tuples and return it to the user through a <code>SELECT</code>. Or we could even combine it with another table using a <code>JOIN</code> operation, as in this case. Specifically, this query will get information about all the people who have rented a bike at some point at least one.</p><p>So the approach we follow to resolve the query is to perform a <code>JOIN</code> between the <code>Person</code> table (that contains all the people in the system) and a table that has the identifiers of the people pointed to by the <strong>foreign key <code>{PersonFK}</code></strong> of any tuple in Rental. This means anyone whose identifier is referenced by any tuple in Rental, implying that they’ve rented a bike at least once.</p><p>We can construct this list of person identifiers using a subquery that extracts all the <code>PersonFK</code> values from the Rental table while removing duplicates. A person may have made an arbitrary number of rentals throughout their history, but we’re interested in whether they have made at least one. So, we simply need to know if they appear in the list of <code>PersonFK</code> values.</p><p>Then, using an <code>INNER JOIN</code>, we combine the information of <code>PersonFK</code> returned by the subquery with the tuples from the <code>Person</code> table. This gives us all the information of the people identified by <code>PersonFK</code>, which in turn points to <code>PersonID</code>. But since we want, for example, the names of the people and not just their identifiers, both the <code>JOIN</code> and the <strong>subquery</strong> are essential, because if we only needed the identifier, it would be enough to return what the subquery provides.</p><p>In addition to nesting subqueries in the FROM clause, we can also do it in the <code>SELECT</code> clause, where the main goal is to calculate a metric or get more information for each tuple in the query. That is, if in the <code>SELECT</code> we get attributes <code>P.PersonID</code> and <code>P.Name</code> from each of the tuples returned to the user, we might want to get more information beyond these two attributes that needs to be calculated with a query. In this case, this query will be nested as a subquery in the <code>SELECT</code>, and it’s result will be the value added to the additional attribute representing the subquery in the <code>SELECT</code>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PersonID,</span>
<span class="line">    P.Name,</span>
<span class="line">    (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM Residence R</span>
<span class="line">        WHERE R.PersonFK = P.PersonID</span>
<span class="line">    ) AS NumResidences</span>
<span class="line">FROM Person P;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In these cases where the subquery is nested in the <code>SELECT</code> statement, the subquery must meet a basic requirement: it has to return at most one tuple and one column. This is because the result of the subquery will be added in a new <strong>additional column (and only one)</strong> in our <code>SELECT</code>. Then we’ll calculate its result and add it in each tuple of the outer query - so the subquery can’t return more than one tuple.</p><p>For example, in this query, we want to list all the people in the database along with a column that contains the number of residences they have had. To solve this, the simplest approach is to go through all the tuples of <code>Person</code> and, for each one, count how many tuples of <code>Residence</code> have their foreign key <code>PersonFK</code> referencing that person.</p><p>Going through the tuples of <code>Person</code> is simple: we just use a combination of <code>SELECT</code> and <code>FROM</code>. But in order to count how many tuples of <code>Residence</code> meet this condition for each person, we need a correlated subquery - specifically with the person being processed. We can uniquely identify this with <code>P.PersonID</code>.</p><p>We need to do this because to count tuples in <code>Residence</code>, we have to compare the values of their foreign key <code>PersonFK</code> with the identifier <code>P.PersonID</code>. To get the value of this count, we can use a subquery: the aggregation function <code>COUNT(*)</code> lets us count all the tuples present in <code>Residence</code>. It does this after filtering them with the condition that their foreign key <code>PersonFK</code> references the person being processed in the Person table.</p><p>It’s important to note that the subquery will only return one value generated by <code>COUNT()</code>, and only one column generated by this function. This meets the requirement that every subquery used in the <code>SELECT</code> statement must fulfill.</p><p>Finally, it’s worth mentioning that this value generated in the subquery populates an additional column which we’ve added by including the subquery itself in the <code>SELECT</code> for each tuple of our query. In other words, each tuple will need a value for this <strong>new column</strong>, which they’ll get by executing the correlated subquery on that specific tuple.</p><p><code>SELECT</code> and FROM aren’t the only statements where subqueries are allowed. We can also use them in a <code>WHERE</code>, <code>HAVING</code>, or even <code>ORDER BY</code> clause. More importantly, a query can have an arbitrary number of subqueries (nested or not) depending on its needs.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PoolID,</span>
<span class="line">    P.Name AS PoolName,</span>
<span class="line">    C.Name AS CityName,</span>
<span class="line">    P.Status,</span>
<span class="line">    (</span>
<span class="line">        SELECT PoolID</span>
<span class="line">        FROM CityPool C</span>
<span class="line">        WHERE C.PoolID = P.PoolID</span>
<span class="line">    ) AS CityPoolID</span>
<span class="line">FROM (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM Pool</span>
<span class="line">        WHERE Status = &#39;maintenance&#39;</span>
<span class="line">    ) AS P</span>
<span class="line">    JOIN City C ON P.CityFK = C.CityID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, in a query like the one above, we can see that there is not only a subquery in the <code>SELECT</code> but also one in the FROM.</p><p>In this specific case, the query gets information about all the pools currently under maintenance, including details about the city the pools are located in (such as its name). There’s also an additional column indicating the pool&#39;s identifier if it’s of the <code>CityPool</code> type, leaving it blank if it’s not.</p><p>So to resolve this query, we first need to get information about the pools under maintenance. This simply involves going through the tuples in the <code>Pool</code> table and selecting those whose Status value is <code>&#39;maintenance&#39;</code>.</p><p>Then, to gather information about the city where each pool is located (along with the <code>Pool</code> tuples we just obtained), we can use a <code>JOIN</code> that operates on the previous tuples and the <code>City</code> table. This is why we’re extracting all <code>Pool</code> tuples using a subquery.</p><p>So although the type of <code>JOIN</code> is not explicitly specified, by using the <code>ON</code> clause SQL automatically interprets it as an <code>INNER JOIN</code> (it would also be interpreted as <code>INNER</code> type if we had used the <code>USING</code> clause). But this practice is not recommended, as in most situations where the JOIN type is omitted, the readability of the code is compromised, especially when there are many <code>JOIN</code>s in the same query.</p><p>Here, in the <code>ON</code> clause, the <code>JOIN</code> condition states that in the same tuple of the Cartesian product, the foreign key <code>CityFK</code> - which represents the city where the pool is located - must have the same value as the <code>CityID</code> identifier of the city in the tuple.</p><p>Then, to attach the extra column with the pool identifier from <code>CityPool</code> for those tuples that represent pools of that type, respectively, we’ll use a subquery. This subquery searches the <code>CityPool</code> table for a tuple whose <code>PoolID</code> matches the <code>PoolID</code> from <code>Pool</code>. This checks if the pool from <code>Pool</code> is actually of the <code>CityPool</code> type or not.</p><p>In this way, the subquery will return the <strong>identifier value</strong> if it’s of the <strong><code>CityPool</code> type</strong> - otherwise, it will return <strong>nothing</strong>, meaning it will return a <strong>table without tuples</strong> (or in other words, an empty set or <strong>multiset</strong>, rather).</p><p>This is allowed in SQL, but it can sometimes cause errors, so it&#39;s generally not a good practice to use subqueries in the <code>SELECT</code> that aren’t guaranteed to return at least some tuple.</p><p>So for those pools that aren’t of the <code>CityPool</code> type, the subquery will return nothing. This means that the value of the extra column in the <code>SELECT</code> will be <code>NULL</code> as we can see when executing the query.</p><p>Since it doesn’t return any tuple with any value, we’ll insert an <strong>unknown</strong> value. The way to represent this in SQL is with the special value <code>NULL</code>. Also, this extra column by default has no name, so we can assign it a recognizable alias using the <code>AS</code> clause as shown in the query.</p><p>On the other hand, if we want to avoid having <code>NULL</code> values in the additional column, we can have this column contain boolean values where <code>TRUE</code> indicates that the pool is of the <code>CityPool</code> type and <code>FALSE</code> that it’s not.</p><p>Starting from the same query as before, the only change we need to make to achieve this is to add an <code>IS NOT NULL</code> check. For each tuple, it checks whether the value inserted in the additional <code>CityPoolType</code> column is <code>NULL</code> or not. Thus, if its type is indeed <code>CityPool</code>, the value in the additional column provided by the original subquery won’t be <code>NULL</code>. This meets the <code>IS NOT NULL</code> condition and returns <code>TRUE</code>. Conversely, if it’s not of that type, <code>IS NOT NULL</code> won’t be met, and the additional column in this case will be filled with FALSE.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PoolID,</span>
<span class="line">    P.Name AS PoolName,</span>
<span class="line">    C.Name AS CityName,</span>
<span class="line">    P.Status,</span>
<span class="line">    (</span>
<span class="line">        SELECT PoolID</span>
<span class="line">        FROM CityPool C</span>
<span class="line">        WHERE C.PoolID = P.PoolID</span>
<span class="line">    ) IS NOT NULL AS CityPoolType</span>
<span class="line">FROM (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM Pool</span>
<span class="line">        WHERE Status = &#39;maintenance&#39;</span>
<span class="line">    ) AS P</span>
<span class="line">    INNER JOIN City C ON P.CityFK = C.CityID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Here, we need to be careful about where we place the <code>IS NOT NULL</code> condition. On one hand, we might think of comparing the <code>PoolID</code> attribute of the <code>CityPool</code> table itself in the <code>SELECT</code> clause of the subquery. If we do this, we’ll be comparing a value that may or may not exist with <code>NULL</code>, so the final result of the subquery will be <code>FALSE</code> if the pool is of the <code>CityPool</code> type.</p><p>But if it’s of another type, there won&#39;t even be a value for that <code>PoolID</code> attribute in the <code>CityPool</code> table, so the comparison with <code>NULL</code> won’t be executed. This will result in the final query output having the additional column contain <code>NULL</code> values for pools that aren’t of the <code>CityPool</code> type and <code>FALSE</code> for those that are of the corresponding type.</p><p>This happens because we shouldn’t compare <code>PoolID</code> with <code>NULL</code>, as its value may or may not exist. And if it doesn&#39;t exist, the check won&#39;t be executed for all the tuples in our query.</p><p>Instead, we should perform this check on the result of the entire subquery. It can be <code>NULL</code> when the pool is not of type <code>CityPool</code> - and so we see values in the additional column filled with <code>NULL</code> in the final result. Or it can contain a valid identifier different from <code>NULL</code>, which violates the <code>IS NOT NULL</code> condition.</p><p>In short, the check to ensure that the additional column is of <strong>boolean</strong> type should compare the result of the entire subquery (which is either NULL or a specific value) with the <code>NULL</code> value itself. This checks to see if each tuple in our resulting table matches or not.</p><p>In summary, although it&#39;s not good practice to use subqueries in the <code>SELECT</code> clause that may result in an <strong>empty set</strong>, we can do so long as it doesn&#39;t make the readability or efficiency of the query worse. We also need to have certain guarantees that it does what it’s expected to do.</p><p>So far, we&#39;ve performed membership checks with IN, as well as checks with other operators. We’ve used individual attributes to verify if the value of a certain attribute was in a set formed by the values of an attribute, among other conditions. And sometimes we need these conditions to involve more than one attribute for verification.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT E.EntryTimestamp, E.PersonFK, E.PoolFK</span>
<span class="line">FROM Entry E</span>
<span class="line">WHERE (E.EntryTimestamp, E.PersonFK, E.PoolFK) IN (</span>
<span class="line">        SELECT PS.EntryFK, PS.PersonFK, PS.PoolFK</span>
<span class="line">        FROM PoolSanction PS</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, above we have a query that retrieves all the tuples from <code>Entry</code> that have been sanctioned with some pool sanction from the <code>PoolSanction</code> table. To do this, we simply need to go through the tuples in <code>Entry</code> and, for each one, check if it has a sanction. In other words, we verify if there is a tuple in <code>PoolSanction</code> whose foreign key to <code>Entry</code> references the tuple we’re examining.</p><p>When doing this, the first thing we notice is that the primary key of <code>Entry</code> doesn’t consist of a single attribute, but rather 3. This is just like the foreign key in <code>PoolSanction</code> - it determines that the entry that has been sanctioned doesn’t have one attribute, but three.</p><p>So under normal conditions, we could use a subquery to get all the foreign key values from <code>PoolSanction</code>, then check if the identifier (primary key) of each entry belongs to that set of values using the <code>IN</code> operator. But here we can’t do it the same way because we need to work with three attributes instead of one.</p><p>That&#39;s why, in the subquery, instead of returning a single attribute, we return all those that make up the foreign key to <code>Entry</code> (these are <strong><code>(EntryFK, PersonFK, PoolFK)</code></strong>). With this, we have a set of tuples where each one refers to a tuple in <code>Entry</code> that has been sanctioned.</p><p>Specifically, each of these tuples in the set refers to the three attributes that make up the primary key of <code>Entry</code>, which are <code>(EntryTimestamp, PersonFK, PoolFK)</code>. So to check if an entry belongs to this set, we simply go through it, looking to see if any of the tuples match exactly with the tuple of the entry&#39;s primary key (with all three attributes having equal values).</p><p>We do this using the <code>IN</code> operator, where instead of specifying a single attribute, we can specify an arbitrary number of them in parentheses. Thus, the <code>IN</code> operator will perform the same operation as in previous cases, taking the primary key <code>(EntryTimestamp, PersonFK, PoolFK)</code> of each entry and comparing it with each of the tuples from the subquery, attribute by attribute. If any of them match, then it belongs to the set, fulfilling the condition.</p><p>Here, it&#39;s very important to note that the tuples compared by <code>IN</code> must be the same size. This means that they need to have the same number of attributes, the same data type (or at least be comparable), and their semantics must be the same. That is, if for each tuple in <code>Entry</code> we use <code>(EntryTimestamp, PersonFK, PoolFK)</code> to check if that three-attribute tuple is in the subquery set, then that subquery must contain tuples of three attributes where:</p><ul><li>the first one is <code>EntryFK</code>, which refers to the <code>EntryTimestamp</code> attribute of the primary key of Entry,</li><li>The second one is <code>PersonFK</code>, referring to <code>PersonFK</code> of the primary key,</li></ul><p>and so on. This ensures that the comparison is semantically correct, even though in the DDL the primary key might have been defined in a completely different order.</p><p>Another variation of this query is to list all the sanctioned entries along with the information of the person who has the entry.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.*, E.EntryTimestamp, E.PersonFK, E.PoolFK</span>
<span class="line">FROM Person P INNER JOIN Entry E ON P.PersonID=E.PersonFK</span>
<span class="line">WHERE (E.EntryTimestamp, E.PersonFK, E.PoolFK) IN (</span>
<span class="line">        SELECT PS.EntryFK, PS.PersonFK, PS.PoolFK</span>
<span class="line">        FROM PoolSanction PS</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For this, based on the previous solution where we got the list of sanctioned entries, the only additional step we need to take is to perform a <code>JOIN</code> between <code>Entry</code> and Person. In doing this, we only keep those tuples from the Cartesian product where the foreign key <code>PersonFK</code> from Entry refers to the primary key <code>{PersonID}</code> from the information coming from Person.</p><p>We can also see that the condition checking whether the entry is sanctioned or not is the same. With this example, we can more clearly see the purpose of the <code>JOIN</code> operation, which is to gather information from multiple tables. So for each sanctioned entry we had before, if we now need to concatenate the information of the person to whom the foreign key <code>PersonFK</code> points, we can simply perform the Cartesian product between both tables and impose a condition to ensure that the reference of <code>PersonFK</code> is indeed the person present in the tuple.</p><p>Continuing with the uses of this last technique, where we use operators like IN to check if a certain combination of attribute values belongs to a set of tuples, in the following example we have a query that lists all the trips from the <code>Voyage</code> table for which there is a return trip. That is, we need to find all trips going from city A to city B for which there is at least one other different trip going from B to A.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Voyage V</span>
<span class="line">WHERE (V.DepartureCityFK, V.ArrivalCityFK) IN (</span>
<span class="line">        SELECT V2.ArrivalCityFK,</span>
<span class="line">            V2.DepartureCityFK</span>
<span class="line">        FROM Voyage V2</span>
<span class="line">    )</span>
<span class="line">ORDER BY (V.DepartureCityFK, V.ArrivalCityFK);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To do this, the first thing we need to realize is that the primary key of <code>Voyage</code> includes the attributes <code>DepartureCityFK</code> and <code>ArrivalCityFK</code>, which refer to the start and end cities of the trip, respectively. So if we have multiple trips with different values in these attributes, we’ll definitely know that both trips are different. This is because even if the rest of the primary key attributes were the same, as long as at least one of them is different, the trips must necessarily be different.</p><p>So we can formulate the query similarly to the previous ones, going through all the tuples in Voyage and for each one, checking if there is a trip whose start and end cities are the same the end and start cities of the trip being checked. So for each trip in Voyage, we construct a correlated subquery where we again go through all the tuples in Voyage and only get the values of the <code>DepartureCityFK</code> and <code>ArrivalCityFK</code> attributes. Then, we check if the values of these attributes from the trip in the &quot;higher level&quot; query are in the set of tuples we just built.</p><p>But in this case, if we look at the code, the order of the attributes is swapped compared to the order of those same attributes in the subquery. What we really want to check is that the value of the <code>DepartureCityFK</code> attribute of the tuple we are checking in the query matches the value of the <code>ArrivalCityFK</code> attribute of some tuple in the subquery. Also, we need to check that the value of the <code>ArrivalCityFK</code> attribute of the query&#39;s tuple matches the value of the <code>DepartureCityFK</code> attribute of the same tuple that matched the previous pair of attributes.</p><p>We can more easily understand this by viewing the pair <code>(V.DepartureCityFK, V.ArrivalCityFK)</code> as if they were the start and end cities, A and B, of a trip. What we want to check is if there is any tuple in the subquery that has B and A as the start and end cities, respectively.</p><p>The simplest way to make this check is either to reverse the order of the attributes in the tuple <code>(V.DepartureCityFK, V.ArrivalCityFK)</code> or in the attributes of the <code>SELECT</code> in the subquery. This is what we’ve decided to do here, which is why <code>ArrivalCityFK</code> is returned before <code>DepartureCityFK</code>.</p><p>Finally, to more easily check for the existence of these round trips, we can add the <code>ORDER BY</code> clause, which orders by multiple attributes instead of just one. That is, we use the attributes <code>(V.DepartureCityFK, V.ArrivalCityFK)</code> as the sorting criteria. SQL orders by the pairs of values for each tuple, as if each possible pair of values were considered a single value that could be compared with others.</p><p>By doing this, we can easily focus on the departure city of a trip and then look for another trip whose arrival city has the same value. Then we can find one whose departure city matches the arrival city of the original trip, thus finding a pair of trips that form a round trip to a city.</p><p>Finally, let’s look at another query where we need to compare values of multiple attributes at once. Here, all trips are listed whose associated cruise (the one making the trip) has been assigned to its cruise line on the same start date of the trip.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT V.*</span>
<span class="line">FROM Voyage V</span>
<span class="line">WHERE (V.ShipFK, V.DepartureDate)</span>
<span class="line">  IN (</span>
<span class="line">    SELECT SA.ShipFK, SA.StartDate</span>
<span class="line">    FROM ShipAssignment SA</span>
<span class="line">  );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To implement this, we need to consider all the attributes that <code>Voyage</code> has, including the information contained in the foreign keys, to avoid having to perform unnecessary operations like a JOIN with the <code>CruiseShip</code> or <code>CruiseLine</code> table.</p><p>In this case, we structure the query similarly, first going through all the tuples of <code>Voyage</code> and checking if the cruise has been assigned to its cruise line on the same date as the start of the trip.</p><p>To make this check easier, we construct a subquery that returns all the values of the <code>ShipFK</code> and <code>StartDate</code> attributes from the <code>ShipAssignment</code> table. This way, later in our query we can check if the cruise making the trip (which is referenced with the foreign key <code>ShipFK</code> of <code>Voyage</code>) was assigned on the DepartureDate of the <code>Voyage</code> tuple (start date of the trip) to any cruise line.</p><p>As you can see, we can simplify the query if we think of it as getting all trips for a cruise ship that has been assigned a start date with any cruise line. In other words, it doesn&#39;t have to be a specific line, but any line to which it was assigned on the date indicated by <code>DepartureDate</code> of <code>Voyage</code>. So in the <code>WHERE</code> clause, it checks if the pair of values taken by the attributes <code>(V.ShipFK, V.DepartureDate)</code>* are found in the subquery. And this time it maintains the correct order of the attributes, since <code>ShipFK</code> of Voyage must match <code>ShipFK</code> of <code>ShipAssignment</code>, and <code>DepartureDate</code> of <code>Voyage</code> must match StartDate of <code>ShipAssignment</code>, respectively.</p><p>On one hand, the match of <code>ShipFK</code> ensures that the cruise ship making the trip is the same as the one assigned in the <code>ShipAssignment</code> tuple. Likewise, the match of the date attributes ensures that this assignment was made on the start date of the trip.</p><p>We have also solved this query using a correlated subquery and the IN operator, although it&#39;s not the only way. As you can guess, there&#39;s always the option to use <code>JOIN</code> operations and conditions to filter the tuples, which can be more or less efficient in certain cases. This is why it&#39;s important to understand what SQL does under the hood, like whether it actually builds and stores all the tuples of a subquery or Cartesian product in memory, and when it does so.</p><hr><h2 id="common-table-expressions" tabindex="-1"><a class="header-anchor" href="#common-table-expressions"><span>Common Table Expressions</span></a></h2><p>We have seen that subqueries allow us to use the result of one query within another query. We can construct this once during the execution of the entire query if it’s not correlated, or once for each tuple of the table with which it’s correlated. In other words, we can see a subquery as a set of tuples that we operate with in a query.</p><p>But we don&#39;t always need queries to be correlated. We’ve seen that some queries can be resolved by non-correlated queries, meaning sets of tuples that are constructed only once and are sufficient to resolve the entire query in which they are contained.</p>`,93)),n("p",null,[e[52]||(e[52]=t("In these situations, to simplify notation, we can use a tool called ")),e[53]||(e[53]=n("strong",null,"CTE (Common Table Expression)",-1)),e[54]||(e[54]=t(" in SQL. These typically use the ")),n("a",k,[s(i,{icon:"fas fa-globe"}),e[50]||(e[50]=n("code",null,"WITH",-1))]),e[55]||(e[55]=t()),e[56]||(e[56]=n("strong",null,"clause",-1)),e[57]||(e[57]=t(". With this, we can define and store the result of a subquery in a temporary table that needs an alias. So instead of using a subquery in the construction of a query, we define a ")),n("a",O,[s(i,{icon:"fa-brands fa-stack-overflow"}),e[51]||(e[51]=t("temporary intermediate table"))]),e[58]||(e[58]=t()),e[59]||(e[59]=n("strong",null,"(Common Table Expression)",-1)),e[60]||(e[60]=t(" that only exists during the execution of the query and contains all the tuples generated by a certain subquery. Again, we need to use an alias to refer to it, just as we have to provide tables with a name and a schema when we create them in the DDL."))]),e[130]||(e[130]=a(`<p>To understand the <code>WITH</code> clause with an example, we can consider the query that gets information about all currently active cruises. Here, active means assigned to a cruise line at the current date when the query is executed.</p><p>Before writing code, it&#39;s helpful to think about how the query will be structured, meaning where we’ll get the data to respond, how we should combine the different tables with that data, what conditions or operations need to be applied to them or the tuples resulting from the operations performed, and so on.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH ActiveShips AS (</span>
<span class="line">    SELECT ShipFK</span>
<span class="line">    FROM ShipAssignment</span>
<span class="line">    WHERE StartDate &lt;= CURRENT_DATE</span>
<span class="line">        AND EndDate &gt;= CURRENT_DATE</span>
<span class="line">)</span>
<span class="line">SELECT CS.ShipID, CS.Speed, CS.PassengerCapacity</span>
<span class="line">FROM ActiveShips A INNER JOIN CruiseShip CS ON CS.ShipID = A.ShipFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this case, the information for all cruise assignments, whether current or not, is in the <code>ShipAssignment</code> table. So, to know which cruises are currently assigned to a cruise line, we can take advantage of the fact that this table has a <strong>foreign key <code>ShipFK</code></strong> that identifies the assigned cruise in each tuple of <code>ShipAssignment</code>.</p><p>So, if we set the condition that the <code>StartDate</code> of the assignment should be before the current date gotten from <code>CURRENT_DATE</code>, and that the <code>EndDate</code> is after the current date, we’ll get all those assignments that are valid on the current date. By extracting the values taken by the foreign key <code>ShipFK</code> for those assignments, we can identify the cruises that are currently assigned.</p><p>But the query not only asks us to <strong>identify them</strong> - but also to get <strong>information about them</strong> stored in <code>CruiseShip</code>. So, we save the identifiers of the cruises we got earlier in a temporary table to use in the query. In other words, we could make the conditions on <code>StartDate</code> and <code>EndDate</code> apply to <code>ShipAssignment</code> in a subquery. But to simplify the notation and demonstrate how to use <strong>CTEs</strong>, we’ll use the <code>WITH</code> clause where we define all the subquery code and assign an alias to that temporary table (see above code).</p><p>Specifically, by doing this, we’ll be saving the identifiers of the currently active cruises in the temporary table named <code>ActiveShips</code>. This is the alias we assigned using the <code>AS</code> operator - but it works in reverse in the <code>WITH</code> clause: first, you write the alias name and then you writethe code that gets the data from the intermediate table (the element to which the alias name is assigned).</p><p>So, when we use the <code>WITH</code> statement, we see that we have constructed an <code>ActiveShips</code> table with the result of what could be a <strong>non-correlated subquery</strong> - but for simplicity, we’ve refactored it so that its result is stored in an intermediate table with a certain alias.</p><p>Now, we can treat <code>ActiveShips</code> as if it were another table in the database, performing a <code>JOIN</code> between it and <code>CruiseShip</code> to get all the information about the active cruises. We impose an equality condition on the <code>ShipFK</code> and <code>ShipID</code> attributes of the <strong>ActiveShips</strong> and <code>CruiseShip</code> tables, respectively. This means we only keep those tuples from the Cartesian product where the foreign key <code>ShipFK</code> refers to the <code>ShipID</code> identifier of that same tuple. This allows us to find the complete information about a specific cruise.</p><p>In the previous query, we could have easily skipped using <code>WITH</code> and made <code>ActiveShips</code> a subquery to which we could’ve also assigned an alias. But when using a subquery, even if we assign it an alias, we can’t use it in just any part of the query. That is, if we have a subquery in a <code>FROM</code> or a <code>SELECT</code>, we can’t use it in other parts of the query in the same way as we can use an intermediate table defined in a <code>WITH</code>. This (<code>WITH</code>) we can reference at any point in the query, regardless of whether it’s formed by more subqueries.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH VoyageDistance AS (</span>
<span class="line">  SELECT * </span>
<span class="line">  FROM Voyage</span>
<span class="line">  WHERE Distance &gt; 1000</span>
<span class="line">)</span>
<span class="line">SELECT DepartureDate, ArrivalDate, Distance</span>
<span class="line">FROM VoyageDistance </span>
<span class="line">WHERE DepartureDate BETWEEN &#39;2025-01-01&#39; AND &#39;2025-06-30&#39;;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We have another similar example in the query above. Here, we consider a query that gets information about all voyages that started in the first half of 2025 (approximately) and have a distance greater than 1000 kilometers. The approach in this case is simpler since all the information we need is found in the <code>Voyage</code> table. So the condition that the distance is greater than 1000 kilometers is easily modeled with a <code>WHERE</code> clause and the expression <code>Distance &gt; 1000</code>.</p><p>Just like before, in this query we could also skip using <code>WITH</code> and include both the distance and the condition on the start date of the voyage in a single <code>WHERE</code>. But often we might need to modify or expand a query - for example, in the future we might be asked for a query based on this one, but with more or fewer conditions. So if we conducted an <strong>analysis</strong> of our domain, user requirements, and the query code, we might conclude that tuples with voyages over 1000 kilometers could be needed in multiple parts of the same query.</p><p>In this example, this phenomenon might not occur, but it illustrates that in a real situation, we may need to consider various factors that affect query design.</p><p>So, say we assume that the Voyage tuples with <code>Distance &gt; 1000</code> could potentially be used multiple times in a single query across multiple statements (in future modifications of this query). Then the most maintainable option is to use a <code>WITH</code> clause where we temporarily store these tuples and then use them in the query through the alias of this intermediate table (as if it were a regular database table). Then, we can add another <code>WHERE</code> clause at the very end of the query, declaring the condition that the start date of the voyage is in the first half of 2025. We can model this with the <code>BETWEEN</code> operator, the <code>EXTRACT()</code> function, or many other ways.</p><p>Finally, it’s worth noting that using the <code>WITH</code> clause without a clear reason isn’t considered a good practice. (Examples of such a clear reason might include a design decision based on user requirements or a thorough analysis of the query that concludes that it might be useful to have an intermediate table like <code>VoyageDistance</code> in the future).</p><p>This is mainly because, in situations like this, a <code>WHERE</code> clause is being used both in the construction of the intermediate table and in the resulting table from the query. This means multiple filters might be applied internally, which can be inefficient.</p>`,17)),n("p",null,[e[62]||(e[62]=t("But the DBMS often automatically applies certain techniques like ")),n("a",D,[s(i,{icon:"fas fa-globe"}),e[61]||(e[61]=t("inlining"))]),e[63]||(e[63]=t(" to optimize query execution through ")),e[64]||(e[64]=n("strong",null,"refactorizations",-1)),e[65]||(e[65]=t(" of the ")),e[66]||(e[66]=n("strong",null,"execution plan",-1)),e[67]||(e[67]=t(". In other words, even if our code is not the most optimal, the DBMS can automatically find an equivalent and more optimal way to resolve the query."))]),e[131]||(e[131]=a(`<p>To illustrate that the intermediate tables we define in the <code>WITH</code> clause can be constructed with subqueries as &quot;complex&quot; as we want, consider this query:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH Pending AS (</span>
<span class="line">    SELECT D.*</span>
<span class="line">    FROM DrivingLicenseRequest D</span>
<span class="line">        LEFT JOIN DrivingLicense A USING (LicenseID)</span>
<span class="line">        LEFT JOIN RejectedDrivingLicense R USING (LicenseID)</span>
<span class="line">    WHERE A.LicenseID IS NULL</span>
<span class="line">        AND R.LicenseID IS NULL</span>
<span class="line">)</span>
<span class="line">SELECT P.Name, Pending.*</span>
<span class="line">FROM Pending INNER JOIN Person P ON P.PersonID = Pending.PersonFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We’re getting information on all driving license requests currently being processed, meaning those that have not yet been accepted or rejected. We’re also including information about the person who made each request.</p><p>In this case, the key point is to realize that the requests in process are represented by tuples in <code>DrivingLicenseRequest</code> that aren’t referenced by any tuple in either <code>DrivingLicense</code> or <code>RejectedDrivingLicense</code> (since they aren’t yet accepted or rejected).</p><p>In this case, we can use <code>LEFT JOIN</code> so that by combining all these tables with <code>LEFT JOIN</code> operations, we can gather complete information about the requests. This means constructing a table formed by all the attributes of the three tables in the hierarchy, where some of them will be <code>NULL</code> or not in each tuple, depending on whether they represent accepted, rejected, or pending requests.</p><p>Specifically, since the foreign keys of the inheriting entities in the hierarchy are both called <code>LicenseID</code> (matching the identifier <code>{LicenseID}</code> of the superclass), the <strong><code>LEFT OUTER JOIN</code>s</strong> are performed by applying an equality condition on this attribute. This ensures that the tuples we get contain information about the same request, rather than multiple requests in the same tuple of the Cartesian product.</p><p>We use <code>LEFT JOIN</code> because the first table we combine is <code>DrivingLicenseRequest</code>. We know all its tuples are non-null because it represents the superclass of the hierarchy and contains information on all requests in the database, regardless of their status. So by placing this table on the left of the <code>JOIN</code> operation, we ensure that the information of all the tuples it contains appears - and it fills in <code>NULL</code> for the attributes from the other table, DrivingLicense.</p><p>Then, we do another <code>LEFT JOIN</code> with <code>RejectedDrivingLicense</code> following the same process. This results in a table where, despite using USING in the JOIN operations, we can impose conditions on the <code>LicenseID</code> attributes of all the tables. So for a tuple of the resulting Cartesian product to represent a pending request, the <code>LicenseID</code> attributes of the <code>DrivingLicense</code> and <code>RejectedDrivingLicense</code> tables must be <code>NULL</code>. This indicates that there are no tuples in the respective tables because the <code>LEFT JOIN</code> has been filled in with <code>NULL</code> if they didn&#39;t exist. We declare this condition using a WHERE clause and the IS operator, as you can’t compare an attribute with <code>NULL</code> directly using the <code>=</code> operator.</p><p>At this point, to simplify the query syntax and avoid chaining too many <code>JOIN</code>s, we can create an intermediate table with the result we got by performing these <code>LEFT JOIN</code>s and applying the previous condition. This way, we can later perform an <code>INNER JOIN</code> in the query to get the information of the person who made the request. We do this all through the <code>PersonID</code> attribute of the <code>Person</code> table and the <strong>foreign key <code>PersonFK</code></strong> of the intermediate table <code>Pending</code>, which comes from the <code>DrivingLicenseRequest</code> table and refers to the person associated with the request.</p><p>In this query, we could also consider combining all the joins in a single FROM clause and skipping the <code>WITH</code>. This would be correct, but it would complicate the code by having all the <code>JOIN</code>s chained. And, although this strategy can be more efficient under certain circumstances, we should seek a balance between code readability and efficiency.</p><p>To illustrate that the intermediate tables in the <code>WITH</code> clause can be defined by queries that contain subqueries, we’ll consider the same query as before and try to solve it using a different approach.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH Pending AS (</span>
<span class="line">    SELECT D.*</span>
<span class="line">    FROM DrivingLicenseRequest D</span>
<span class="line">    WHERE NOT EXISTS (</span>
<span class="line">            SELECT *</span>
<span class="line">            FROM DrivingLicense A</span>
<span class="line">            WHERE A.LicenseID = D.LicenseID</span>
<span class="line">        )</span>
<span class="line">        AND NOT EXISTS (</span>
<span class="line">            SELECT *</span>
<span class="line">            FROM RejectedDrivingLicense R</span>
<span class="line">            WHERE R.LicenseID = D.LicenseID</span>
<span class="line">        )</span>
<span class="line">)</span>
<span class="line">SELECT P.Name, Pending.*</span>
<span class="line">FROM Pending INNER JOIN Person P ON P.PersonID = Pending.PersonFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To get pending requests, keep the tuples in <code>DrivingLicenseRequest</code> whose primary key <code>{LicenseID}</code> is not referenced (via the foreign key <code>LicenseID</code>) by any tuple in <code>DrivingLicense</code> or <code>RejectedDrivingLicense</code>.</p><p>The simplest option to implement this is to go through all the tuples in <code>DrivingLicenseRequest</code> using the FROM clause, and for each of them, construct two very similar correlated queries.</p><ul><li>We can have one that gets all the tuples from <code>DrivingLicense</code> whose foreign key <code>LicenseID</code> refers to the primary key <code>LicenseID</code> of the tuple in <code>DrivingLicenseRequest</code> that we are going through, and</li><li>We can have another subquery that does the same but gets tuples from the <code>RejectedDrivingLicense</code> table.</li></ul><p>In this way, we can later check if any of the tables returned by the subqueries contain tuples or not using the <code>EXISTS</code> operator.</p><p>If any of the subqueries return tuples, then the request is either accepted or rejected. But if both subqueries return an empty set, it means that for a certain request in <code>DrivingLicenseRequest</code>, there is no tuple in the respective <code>DrivingLicense</code> or <code>RejectedDrivingLicense</code> tables that references it. This then indicates that the request is being processed.</p><p>With this process, we get the pending requests, which we store in an intermediate table using the <code>WITH</code> clause. To combine the information of the person who made each request, we use the intermediate table in the query, specifically in an <code>INNER JOIN</code> operation with the Person table, just as we did before.</p><p>So with this example, we’ve seen that there are multiple SQL constructions that lead to the same result - meaning a query doesn&#39;t necessarily have to be solved in just one way.</p><p>Also, by using the <code>WITH</code> clause, we can define each intermediate table with SQL code that’s as <strong>&quot;complex&quot;</strong> as we need it to be. We can include subqueries, conditions, and generally any SQL statement, except for a <code>WITH</code>, which by default can’t appear inside another <code>WITH</code>.</p><p>If we need to use an intermediate table to solve a query defined as a <strong>CTE</strong>, we need to define it at the same level as the other intermediate tables in our query, meaning in a single <code>WITH</code> statement (as we’ll see below).</p><p>So far, we have seen that we can use the <code>WITH</code> statement to define an intermediate table that we use to solve the query more comfortably and easily in certain situations. But, we might need several intermediate tables to solve a query, not just one.</p><p>For example, in the below code we have a query that gets information about people who have lived in at least two cities. We solved this query in the <strong>“Tuple filtering”</strong> section using <code>JOIN</code> operations - but we can also follow a similar approach where we first create several different intermediate tables and finally solve the query based on the results of these intermediate tables.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH R1 AS (</span>
<span class="line">    SELECT PersonFK, CityFK AS CityA</span>
<span class="line">    FROM Residence</span>
<span class="line">),</span>
<span class="line">R2 AS (</span>
<span class="line">    SELECT PersonFK, CityFK AS CityB</span>
<span class="line">    FROM Residence</span>
<span class="line">),</span>
<span class="line">CityPairs AS (</span>
<span class="line">    SELECT DISTINCT R1.PersonFK</span>
<span class="line">    FROM R1 INNER JOIN R2 ON (R1.PersonFK = R2.PersonFK</span>
<span class="line">        AND R1.CityA &lt;&gt; R2.CityB)</span>
<span class="line">)</span>
<span class="line">SELECT P.*</span>
<span class="line">FROM CityPairs MC INNER JOIN Person P ON MC.PersonFK = P.PersonID</span>
<span class="line">ORDER BY P.PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>First, we can create several intermediate tables that contain all the tuples from Residence - specifically the information about the person and city that make up each residence. We can do this by obtaining the attributes <code>PersonFK</code> and <code>CityFK</code>, which are foreign keys that refer to the person who has lived in a certain city during that residence. By constructing several intermediate tables with this information, we can rename <code>CityFK</code> with an alias like <code>CityA</code> in one of them and <code>CityB</code> in the other intermediate table, so that later the <code>JOIN</code> between them has a clearer syntax.</p><p>To construct several intermediate tables in a single <code>WITH</code> statement, we can chain them with commas. Instead of using the <code>WITH</code> keyword multiple times, we have to use it only once and chain all the intermediate tables we want with commas, as shown above.</p><p>Subsequently, with the intermediate tables <code>R1</code> and <code>R2</code> containing this information, we can create another intermediate table where we get the identifiers of all the people who have had a residence in several different cities (or in at least two cities).</p><p>To do this, we can perform an <code>INNER JOIN</code> between <code>R1</code> and <code>R2</code> (a Cartesian product of their tuples) and keep the tuples from the Cartesian product where the foreign key values <code>PersonFK</code> match and the <code>CityFK</code> values do not match. This way, we keep those tuples from the Cartesian product that represent information about several residences of the same person in different cities.</p><p>These identifiers are for the people whose information we need to get from the <code>Person</code> table. So now we can finally perform an <code>INNER JOIN</code> between the intermediate table <code>CityPairs</code> and <code>Person</code>, so that the final result of the query is the information of the people who have had at least two residences in different cities. (They would not have appeared in a tuple of the Cartesian product between <code>R1</code> and <code>R2</code> otherwise.)</p><p>The important point about this query is to note that we have used <strong>multiple intermediate tables</strong> in the same <code>WITH</code> clause to solve it - and this is entirely <strong>possible</strong> but not always recommended. We can resolve this query in various ways, each with its own advantages or disadvantages depending on the characteristics we need the code to have, such as clarity, efficiency, maintainability, and so on.</p><p>To conclude this CTE section, let&#39;s consider another query where we need to get information about bus trips that have taken place after 2025 and where the bus has WiFi. The simplest way to create this query would be to gather information from the <code>CityBus</code> and BusTrip tables using a JOIN, and then apply conditions on the tuples of the corresponding Cartesian product. But to illustrate using multiple <strong>intermediate tables (CTEs)</strong> in a single <code>WITH</code> clause, in this case, we’ll divide the query resolution into several parts.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH WifiBuses AS (</span>
<span class="line">    SELECT Plate, RouteNumber</span>
<span class="line">    FROM CityBus</span>
<span class="line">    WHERE FreeWifi = TRUE</span>
<span class="line">),</span>
<span class="line">AvailableTrips AS (</span>
<span class="line">    SELECT TripDate, StartAddress, EndAddress, PlateFK</span>
<span class="line">    FROM BusTrip</span>
<span class="line">    WHERE EXTRACT(YEAR FROM TripDate) &gt;= 2025 </span>
<span class="line">)</span>
<span class="line">SELECT T.TripDate, T.StartAddress, T.EndAddress, B.RouteNumber</span>
<span class="line">FROM AvailableTrips T INNER JOIN WifiBuses B ON B.Plate = T.PlateFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>First, we’ll get information about buses with <code>WiFi</code> in an intermediate table. To construct this table, we simply apply the condition <code>FreeWifi=TRUE</code> on the tuples of the <code>CityBus</code> table. In this case, when we do a <code>SELECT * FROM CityBus;</code> we can see that in the <code>FreeWifi</code> attribute, the boolean values are represented with the letters <code>&#39;t&#39;</code> or <code>&#39;f&#39;</code> - so we might think that in the query we should compare the attribute with <code>&#39;t&#39;</code>.</p>`,33)),n("p",null,[e[69]||(e[69]=t("But boolean values in SQL are ")),e[70]||(e[70]=n("code",null,"TRUE",-1)),e[71]||(e[71]=t(" and ")),e[72]||(e[72]=n("code",null,"FALSE",-1)),e[73]||(e[73]=t(", even though the DBMS ")),n("a",F,[s(i,{icon:"fas fa-globe"}),e[68]||(e[68]=t("represents"))]),e[74]||(e[74]=t(" them with another type of notation. So the correct way to check if the attribute contains the logical value ")),e[75]||(e[75]=n("code",null,"true",-1)),e[76]||(e[76]=t(" is to compare it with ")),e[77]||(e[77]=n("code",null,"TRUE",-1)),e[78]||(e[78]=t(". Even though the representation of the boolean value might change, in SQL we should always operate with boolean values using the literals ")),e[79]||(e[79]=n("code",null,"TRUE",-1)),e[80]||(e[80]=t(" and ")),e[81]||(e[81]=n("code",null,"FALSE",-1)),e[82]||(e[82]=t("."))]),e[132]||(e[132]=a("<p>Second, we construct another intermediate table with information about bus trips that have occurred in 2025 or later. We do this by getting all the tuples from BusTrip and filtering them using the <code>EXTRACT()</code> function and the <code>YEAR</code> feature of the date.</p><p>Finally, in the query, we perform a <code>JOIN</code> between both intermediate tables to gather all the information about trips and buses. This way, we get tuples with trips that occurred on dates equal to or after the year 2025, along with the information about the bus with <code>WiFi</code> that made that trip.</p><p>But in this case, we only return the route number of the bus to the user, which is also part of the information in the CityBus table. If this isn’t enough to identify the bus, we could also return its license plate in the <code>SELECT</code>, for example. This decision depends on what the end user needs.</p><p>Also, with this query, we can more clearly see the effect of coding a query using multiple intermediate tables on how efficiently it executes. For example, if we coded the query without <code>WITH</code> (and instead with <code>JOIN</code> operations between the respective CityBus and BusTrip tables and imposed conditions on the resulting tuples), we have to consider that the entire Cartesian product would be performed first and then filtered by the conditions.</p><p>But by using intermediate tables where each one imposes a certain condition on the tuples of each table, we can reduce the number of tuples in each intermediate table, since <strong>WifiBuses</strong> won’t contain all existing buses, but only those with <code>WiFi</code> (which will be fewer).</p>",5)),n("p",null,[e[84]||(e[84]=t("By applying this technique (known as ")),n("a",L,[s(i,{icon:"iconfont icon-oracle"}),e[83]||(e[83]=t("early filtering"))]),e[85]||(e[85]=t("), we ensure that when performing the final ")),e[86]||(e[86]=n("code",null,"JOIN",-1)),e[87]||(e[87]=t(" between the intermediate tables, the Cartesian product results in ")),e[88]||(e[88]=n("strong",null,"fewer tuples",-1)),e[89]||(e[89]=t(" - meaning it works with smaller tables and is therefore ")),e[90]||(e[90]=n("strong",null,"more efficient",-1)),e[91]||(e[91]=t("."))]),e[133]||(e[133]=a(`<p>Just keep in mind that in modern DBMS, this optimization can be carried out <a href="https://stackoverflow.com/questions/46727600/sql-performance-filter-first-or-join-first" target="_blank" rel="noopener noreferrer">automatically</a> even without using intermediate tables, depending on the nature of the query. So if it doesn’t significantly worsen the clarity of the query, we should filter the information from the tables to be combined via a <code>JOIN</code> as early as possible, which is why we have used multiple intermediate tables in a <code>WITH</code> clause.</p><hr><h2 id="set-operations" tabindex="-1"><a class="header-anchor" href="#set-operations"><span>Set Operations</span></a></h2><p>We have seen that in most queries, we need to impose conditions on table tuples to filter them and keep only those that interest us. Sometimes, we even need to use logical operators to chain multiple conditions together.</p><p>But using logical operators <code>AND</code>, <code>OR</code>, and <code>NOT</code> is not the only way to chain multiple conditions. We can also take a different approach where, instead of applying a filter on all tuples, we divide the conditions that must be met and apply multiple filters, one for each condition based on logical operators. Finally, we get the resulting tuples from those filters and combine them using <strong>set theory operators</strong>, which perform functions equivalent to <strong>logical operators</strong>.</p><p>In other words, in SQL, we can chain multiple conditions in a <code>WHERE</code> clause, for example, using logical operators. Or we can use set theory operators to combine the resulting tuples from multiple filters, each applying one of those conditions, all without using logical operators.</p><p>As we will see below, the decision to use logical or set operators largely depends on the clarity of the resulting code and the efficiency we want to achieve in the query.</p><p>To start, let&#39;s consider a query where we need to get information on all pools that are currently in a maintenance or closed state. If we wanted to do this with what we already know, the simplest way would be to use a filter with a <code>WHERE</code> clause, combining the conditions that the <code>Status</code> attribute is &#39;closed&#39; or &#39;maintenance&#39; using the logical <code>OR</code> operator.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT *</span>
<span class="line">FROM Pool</span>
<span class="line">WHERE Status=&#39;maintenance&#39;</span>
<span class="line">UNION</span>
<span class="line">SELECT *</span>
<span class="line">FROM Pool</span>
<span class="line">WHERE Status=&#39;closed&#39;;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Besides using this operator, we can rethink the query to solve it using set theory operators. In this case, using only one logical <code>OR</code> operator, we divide the <code>WHERE</code> condition into several conditions by removing that <code>OR</code> operator. This results in the conditions <code>Status = &#39;maintenance&#39;</code> and <code>Status = &#39;closed&#39;</code>, respectively.</p><p>Doing this, we can resolve two queries: one applying the first condition and another applying the second. This gives us two resulting tables, one with information about pools under maintenance and another table with all the closed pools.</p><p>But we wanted all of them in a single output table, not in several. So to combine all the tuples from both tables into one (so that they all appear in the resulting table), we use the <code>UNION</code> operator between both queries. It treats both queries as if they were <strong>multisets</strong> of tuples, resulting in another <strong>set</strong> of final tuples where the tuples from both tables are present. That is, all the tuples from both tables. This is just like in set theory where the union of sets <code>AUB</code> results in another set containing all the individuals from A, all from B, and all those in both <code>A</code> and <code>B</code>.</p><p>To apply a set theory operator, the schema of the tables returned by the queries we operate with has to be exactly the same. This means that they must have the same number of attributes with the same names and data types, and in the same order. Otherwise, we can’t compare tuples from both tables, and it wouldn’t be possible to determine if a tuple belongs to one of the sets involved in the operation.</p><p>When we run a query that includes a <code>UNION</code>, we can see that if there are duplicate tuples in the resulting tables from the queries we’re working with, those duplicate tuples disappear in the resulting table of the query. This happens because, by default, all set theory operators in SQL take multisets with tuples as input and produce a set of tuples, which means it won’t contain duplicate tuples. So if we want to force the appearance of duplicate tuples because the query requires it, we must add the <code>ALL</code> modifier after the corresponding <code>UNION</code>, <code>INTERSECT</code>, or <code>EXCEPT</code> operator.</p><p>For example, in this case, we want to get information about all the people who have rented at least one bike <strong>or</strong> owned at least one car in our system:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT PersonFK</span>
<span class="line">FROM Rental</span>
<span class="line">UNION ALL </span>
<span class="line">SELECT PersonFK</span>
<span class="line">FROM CarOwnership;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To do this, we first create a query that gets all the <code>PersonID</code> identifiers of people referenced by the tuples in the <code>Rental</code> table through their foreign key <code>PersonFK</code>. In other words, each tuple in <code>Rental</code> has a value in its foreign key <code>PersonFK</code> that corresponds to a certain person&#39;s identifier, which matches the value of their unique identifier <code>PersonID</code>. So selecting the <code>PersonFK</code> attribute is enough to get the identifiers of all the people who have at least one record in this table.</p><p>We do the same with another query on the <code>CarOwnership</code> table, which also has a foreign key <code>PersonFK</code> with the same characteristics.</p><p>Finally, when reviewing these partial results from the queries, we’ll see that some people may have rented several bikes or simply made several rentals, and they may also have multiple ownership records in <code>CarOwnership</code>, leading to duplicate tuples. So when building the final resulting table, we need to get the people who are present in one table or the other. This means we need to combine both tables with the <code>UNION</code> operator to get a final set with all the tuples from both tables.</p><p>In this specific case, we shouldn’t add the <strong><code>ALL</code> modifier</strong>, as we simply want to know which people meet the condition of having at least one rental and at least one car ownership (so a person who has made multiple rentals doesn’t need to appear multiple times in the final table).</p><p>But if we wanted to keep the duplicate tuples, this example clearly shows that by adding <code>ALL</code> after the <code>UNION</code> operator, all duplicate tuples from both tables are preserved, resulting in the final table showing as many tuples with the same person&#39;s information as the number of rentals and properties they have had. In other words, the ALL modifier forces <code>UNION</code> to return a <strong>multiset</strong>, not a <strong>set</strong> that removes <strong>duplicate tuples</strong>.</p><p>We can see the effect of the ALL modifier at a glance by examining the resulting table from the query. But if we are working with a very large query or a database with a lot of information, we may want to <strong>wrap</strong> our query in another outer query that uses the aggregation function <code>COUNT()</code> to count how many tuples it returns.</p><p>For example, in the below code you can see that we have used the query we just looked at as a subquery in the <code>FROM</code> clause, so we get all the tuples it contains. Then in the <code>SELECT</code>, we use the <code>COUNT(*)</code> function to count how many tuples there are.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT COUNT(*)</span>
<span class="line">FROM (</span>
<span class="line">    SELECT PersonFK</span>
<span class="line">    FROM Rental</span>
<span class="line">    UNION</span>
<span class="line">    SELECT PersonFK</span>
<span class="line">    FROM CarOwnership</span>
<span class="line">) AS PersonTable;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It’s also important here to provide an <strong>alias</strong> to the <strong>subquery</strong>, because when subqueries appear in the <code>FROM</code> clause, the DBMS needs them to have aliases to distinguish them and avoid ambiguities regarding the origin of the attributes that are later selected or used in other clauses.</p><p>Continuing with the different set theory operators that SQL offers, we have <code>INTERSECT</code>, which combines the results of several tables to return a set with all the tuples that appear in <strong>all the tables simultaneously</strong>. To understand this, we have the query below that retrieves information about all the people who have a driver&#39;s license and have also rented at least one bike.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT DR.PersonFK AS PersonID</span>
<span class="line">FROM DrivingLicenseRequest DR INNER JOIN DrivingLicense DL </span>
<span class="line">  ON DR.LicenseID = DL.LicenseID</span>
<span class="line">INTERSECT</span>
<span class="line">SELECT R.PersonFK</span>
<span class="line">FROM Rental R;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We could write this query using only <code>JOIN</code> operations. But this time it might be more natural and straightforward to think of it as a set operation.</p><p>First, as shown above, we can construct a query that returns the identifier of all the people who currently have an active driver&#39;s license. To do this, we perform a <code>JOIN</code> between <code>DrivingLicenseRequest</code> and <code>DrivingLicense</code>, so we can gather the driver&#39;s license information with the <strong>foreign key <code>PersonFK</code></strong> from <code>DrivingLicenseRequest</code>, which identifies the person who applied for the license. Then, we can construct another query that returns all the people who have rented at least one bike using the foreign key <code>PersonFK</code> from the Rental table.</p><p>And finally, to know which people have a driver&#39;s license and have also rented at least one bike, we need to keep the tuples that are in both tables. In other words, if the tables are multisets containing tuples, we need to keep those that appear in both multisets at the same time.</p><p>Instead of using the <code>UNION</code> operation, which represents the <strong>union of sets,</strong> we can use <code>INTERSECT</code>. It performs the <strong>intersection</strong> between sets, resulting in the final table of the query where we only have those people who meet all the conditions.</p><p>In the previous query, we only got the identifier of each person, since knowing the value of their primary key <code>{PersonID}</code> is enough to uniquely identify a person. With the foreign keys <code>PersonFK</code> pointing to the primary key <code>{PersonID}</code> of the Person table, we don&#39;t need the query to return more information about the person, as we can identify them with their primary key.</p><p>But there are situations where we may want to get more information about the person in the same query, such as their name in addition to the identification. So, if we needed to modify the query to return this, the simplest way would be to use a WITH clause that stores the identifiers of the people and then perform an INNER JOIN with the Person table in the body of the query, thus obtaining all the information present in the Person table.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT DR.PersonFK AS PersonID, (SELECT Name FROM Person WHERE PersonID=DR.PersonFK)</span>
<span class="line">FROM DrivingLicenseRequest DR INNER JOIN DrivingLicense DL </span>
<span class="line">  ON DR.LicenseID = DL.LicenseID</span>
<span class="line">INTERSECT</span>
<span class="line">SELECT R.PersonFK, (SELECT Name FROM Person WHERE PersonID=R.PersonFK)</span>
<span class="line">FROM Rental R</span>
<span class="line">ORDER BY PersonID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>But to illustrate other ways to get more information about people in the system from their identification in the same query without using <code>WITH</code>, we have the option to use subqueries in the SELECT clause, as you can see in the example above. Specifically, if we only need to return the name in addition to the <code>PersonID</code>, we can create a correlated subquery that, for each tuple to be returned, gets the name of the person with a specific identifier. In other words, it searches the Person table for the tuple with a certain <code>PersonID</code>, retrieves the name, and adds it as an additional column.</p><p>In general, using correlated subqueries in the SELECT is not a good practice because, as you might guess, the result of the subquery must be computed for each tuple to be returned. This also complicates maintenance, code clarity, and makes optimization by the DBMS more difficult in most cases.</p><p>Basically, with a simple <code>WITH</code> and an <code>INNER JOIN</code>, you can avoid having to traverse the entire Person table for each person to obtain a specific characteristic, instead gathering data from both the intermediate table of the <code>WITH</code> and the Person table.</p><p>Just like with other set theory operators, <code>INTERSECT</code> takes multisets as input and by default outputs a set, so there can&#39;t be duplicate tuples in the output table, as we saw earlier with <code>UNION</code>. So if we need to force the operator to always work with multisets and also return a multiset as the output of the <strong>intersection operation</strong>, we can add the <code>ALL</code> modifier, as shown in the example query below:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT BikeFK AS BikeID</span>
<span class="line">FROM Rental</span>
<span class="line">WHERE StartTimestamp &gt;= &#39;2024-01-01&#39;</span>
<span class="line">INTERSECT ALL</span>
<span class="line">SELECT BikeFK</span>
<span class="line">FROM Rental</span>
<span class="line">WHERE Duration &lt;= 3</span>
<span class="line">ORDER BY BikeID;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this query, we get information on bikes that have been rented at least once during or after 2024 and whose rental duration was at most 3 hours. To do this, we construct a query that gets all bikes that have had at least one rental on a date <code>&gt;= &#39;2024-01-01&#39;</code>, and then another that gets those that have had at least one rental with a maximum duration of 3 hours.</p><p>Then, to keep the bikes that meet both conditions at once, we perform the intersection between the multisets returned by the queries, keeping the tuples that are in both multisets at once. And, since the same bike may have had several rentals with these characteristics, there may be duplicates in the final query table. If we want to preserve them, we’ll have to add the <code>ALL</code> modifier.</p><p>By default, we don’t usually use <code>ALL</code> in this type of query, since we simply want to know which bikes meet the conditions. But it’s possible that, due to some user requirement, the query needs to return duplicates, in which case we’d use <code>ALL</code>.</p>`,42)),n("p",null,[e[93]||(e[93]=t("In most situations, the performance is similar enough to be negligible. But the more data there is in the database, the more noticeable the difference in performance will be between using ")),e[94]||(e[94]=n("code",null,"ALL",-1)),e[95]||(e[95]=t(" or not. This also depends on the ")),n("a",A,[s(i,{icon:"fa-brands fa-stack-overflow"}),e[92]||(e[92]=t("algorithms"))]),e[96]||(e[96]=t(" the DBMS uses internally to implement this operation."))]),e[134]||(e[134]=a(`<p>The last set operator we&#39;ll look at here is <code>EXCEPT</code>, which is called <code>MINUS</code> in some DBMS. Basically, it implements the difference operation between sets, meaning if we have several sets <code>A</code> and <code>B</code> with tuples, the difference <code>A</code>-<code>B</code> returns a set with all the tuples that <strong>are</strong> in <code>A</code> and <strong>are not</strong> in <code>B</code>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT p.EntryFK, p.PersonFK, p.PoolFK</span>
<span class="line">FROM PoolSanction p</span>
<span class="line">WHERE p.BanEndDate &lt; CURRENT_DATE</span>
<span class="line">EXCEPT</span>
<span class="line">SELECT p.EntryFK, p.PersonFK, p.PoolFK</span>
<span class="line">FROM PoolSanction p INNER JOIN Sanction s ON p.SanctionID = s.SanctionID</span>
<span class="line">WHERE s.Status = &#39;active&#39;;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, in the query above, we get information about all pool sanctions where the ban end date is before the current date when the query is run and that aren’t active.</p><p>As always, to do this, we might think the simplest way is through <code>JOIN</code> operations and a <code>WHERE</code> clause where the conditions are implemented. But to illustrate the use of <code>EXCEPT</code>, we can frame the query as finding all sanctions that meet the first condition of having a ban end date before <code>CURRENT_DATE</code>, and then from all of them, selecting only those that aren’t active. That is, from all those that meet the date condition, we keep only those that aren’t active.</p><p>Viewed another way, by getting all those that meet the date condition, we get a set <code>A</code> with tuples, where each one represents a sanction. Among all of them, there may be some that are active and some that are not. So to keep the ones we’re interested in, we need to remove from set <code>A</code> all those that are active. In other words, if we consider all the active ones to be in another set called <code>B</code>, then the sanctions we are interested in will be in the difference <code>A</code>-<code>B</code> (this means all the sanctions that meet the date condition (are in <code>A</code>) and are not active (are not in <code>B</code>)).</p><p>So in the query, you can see that we use the <code>EXCEPT</code> operator to work with the queries that get sets <code>A</code> and <code>B</code>, respectively. That is, the first query constructs set <code>A</code> by imposing the condition <code>p.BanEndDate &lt; CURRENT_DATE</code> on the tuples of <code>PoolSanction</code>, while the query following the <code>EXCEPT</code> operator constructs set B by imposing the condition <code>s.Status = &#39;active&#39;</code>, gathering data from <code>PoolSanction</code> and Sanction to filter by the Status attribute, which is in Sanction instead of <code>PoolSanction</code>.</p><p>To implement the difference A-B, we use <code>EXCEPT</code>, where the query above the <code>EXCEPT</code> is set <code>A</code> and the one below is set <code>B</code>. This is important to keep in mind because <code>EXCEPT</code> is the only operator where the order of the operands can change the result of the query.</p><p>For example, with the other operators <code>UNION</code> and <code>INTERSECT</code>, we can clearly see that it doesn&#39;t matter if we unite or intersect several sets A and <code>B</code> or <code>B</code> and <code>A</code> - in any order, the result will be the same. This is not the case with the difference <code>A</code>-<code>B</code>, which doesn&#39;t necessarily have to be equivalent to <code>B</code>-<code>A</code>. This property is called <strong>commutativity</strong>, and <code>EXCEPT</code> is the only set operator that <strong>is not commutative</strong>.</p><p>Ultimately, in this query, we can see that the table aliases are all in lowercase. This is allowed in SQL, and we can even declare an alias in uppercase and then use it in lowercase, or vice versa. But if we enclose the aliases in quotes, like in <strong>Person &quot;P&quot;</strong>, we can only refer to the table with the alias exactly as it’s written in the quotes.</p><p>On one hand, not quoting it provides flexibility when writing the code, as we don’t need to remember exactly how it was written. In most SQL code, quotes aren’t commonly used. But, this can cause ambiguity issues if the alias is named exactly the same as a table or another element in the database. Quoting it avoids these potential collisions with names of other elements.</p><p>In the end, the decision of which alias to assign to each table or element in the query mainly depends on its complexity and the style guide followed, among other factors.</p><p>Just like with other set operators, <code>EXCEPT</code> also takes multisets as input by default and returns sets, removing any duplicate tuples. So if we need to keep those duplicate tuples, we simply need to add the ALL modifier.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT BikeFK AS BikeID</span>
<span class="line">FROM Rental</span>
<span class="line">EXCEPT ALL</span>
<span class="line">SELECT BikeFK</span>
<span class="line">FROM Rental</span>
<span class="line">WHERE Duration &lt; 2;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, in this query, we get information on all the bikes that have been rented <strong>at least</strong> once and have never been rented for less than 2 hours.</p><p>To implement this, we first construct a query that retrieves all the bikes that have been rented at least once using the foreign key attribute <code>BikeFK</code> from the <code>Rental</code> table. Then, with another query, we get all the bikes that have been rented at least once for less than 2 hours. Finally, to keep only those we’re interested in, we get the difference between the first set and the second.</p><p>As you can see, a bike may have been rented an arbitrary number of times, so the first query might return many duplicate tuples. If we don&#39;t use the <code>ALL</code> modifier, all those duplicates will be lost, resulting in a table where each bike is guaranteed to appear at most once.</p><p>But if we want to keep the duplicates, simply using <code>ALL</code> will show that the query returns a result table with many more tuples, as many of them correspond to duplicate bikes.</p><p>Here, we also need to consider that, despite the possibility of duplicate bikes existing in both set <code>A</code> and set <code>B</code>, when we perform the difference <code>A</code>-<code>B</code> using <code>EXCEPT</code>*, we won&#39;t get any bike that&#39;s in B, regardless of how many times it&#39;s duplicated in both sets, one, or neither.</p><p>But when performing the operation using <code>EXCEPT ALL</code>, if we have <strong>x repetitions</strong> of a tuple in set <code>A</code> and <strong>y repetitions</strong> of that same tuple in B, then in the resulting table, we will get <code>max{x-y,0}</code> repetitions of that tuple. That is, when there are more repetitions in A than in B, we will get <strong>x-y</strong> repetitions of the tuple in the final table. If there are more repetitions in B than in <code>A</code>, then <strong>x-y</strong> is <strong>negative</strong>, so we will simply get 0 repetitions of the tuple. This means that tuple won’t appear in the resulting table of the <strong>difference operation</strong> implemented with <code>EXCEPT ALL</code>.</p><p>To correctly understand the <strong>difference operator</strong>, let&#39;s consider a query where we need to get information on all cruise trips for which there is no return trip. In other words, we want to find all trips going from a <strong>city x</strong> to another <strong>city y</strong> with no return to city x.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT V.DepartureCityFK, V.ArrivalCityFK</span>
<span class="line">FROM Voyage V</span>
<span class="line">EXCEPT</span>
<span class="line">SELECT V2.ArrivalCityFK, V2.DepartureCityFK</span>
<span class="line">FROM Voyage V2</span>
<span class="line">ORDER BY DepartureCityFK, ArrivalCityFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The approach we&#39;ll take for this query is based on set theory, as it’s particularly easy to solve in this case. First, we construct a query that returns all existing trips. From these, we can get all the information present in the <code>Voyage</code> table - but for simplicity, we&#39;ll only focus on the attributes that determine the departure and arrival cities of the trip (which are their foreign keys <code>DepartureCityFK</code> and <code>ArrivalCityFK</code>).</p><p>Then, from all those trips returned by the first query, we need to remove the return trips. That is, for each existing trip that departs from city x and arrives at city y, we look for a <strong>return trip</strong> in the <code>Voyage</code> table that departs from city y and arrives at city x. If it exists, we remove the original trip from the result table of the first query.</p><p>We could implement this using the <code>IN</code> operator and a subquery. But it&#39;s simpler and more efficient to build a second query that gets all the trips from <code>Voyage</code> but swaps the departure and arrival cities for each trip as shown above. This second query is responsible for getting all possible return trips that might exist by swapping the values of the foreign keys <code>DepartureCityFK</code> and <code>ArrivalCityFK</code>, meaning swapping the departure cities with the arrival cities.</p><p>Finally, with these two queries, we apply the <code>EXCEPT</code> operator, where we remove from the result table of the first query (which contains all the trips from <code>Voyage</code>) all those contained in the result table of the second query. In other words, from all existing trips, we are removing those considered return trips because they were generated by the second query by swapping the departure and arrival cities.</p><p>Even if some of those return trips don&#39;t exist in <code>Voyage</code> (which can happen), the <code>EXCEPT</code> operator will simply ignore that tuple to remove since it doesn&#39;t exist in the set from which it needs to be removed.</p><p>To wrap up this type of query, so far we have seen some that apply a single set operator. But SQL allows us to use any number of them in the same query or subquery as needed. This is applied in the query below, which retrieves information about all the people who have or have had a driver&#39;s license application registered in the database, or an approved license, and who have never had a rejected application.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH Persons AS (</span>
<span class="line">    SELECT DR.PersonFK</span>
<span class="line">    FROM DrivingLicenseRequest DR</span>
<span class="line">        INNER JOIN DrivingLicense DL ON DR.LicenseID = DL.LicenseID</span>
<span class="line">    UNION</span>
<span class="line">    SELECT DR2.PersonFK</span>
<span class="line">    FROM DrivingLicenseRequest DR2</span>
<span class="line">    EXCEPT</span>
<span class="line">    SELECT DR3.PersonFK</span>
<span class="line">    FROM DrivingLicenseRequest DR3</span>
<span class="line">        INNER JOIN RejectedDrivingLicense RD ON DR3.LicenseID = RD.LicenseID</span>
<span class="line">)</span>
<span class="line">SELECT PersonFK</span>
<span class="line">FROM Persons</span>
<span class="line">ORDER BY PersonFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Regarding the implementation, we can see that in the first query used to build the intermediate table Persons, we get all the people who have or have had an accepted driver&#39;s license by using an <code>INNER JOIN</code> between the <code>DrivingLicenseRequest</code> and <code>DrivingLicense</code> tables. This way, with the data from <code>DrivingLicenseRequest</code>, we can access the foreign key <code>PersonFK</code> that identifies the person who made the application.</p><p>Then, we make a union with a second query that retrieves people who have made an application, whether pending, accepted, or rejected. This time we get the data from the <code>DrivingLicenseRequest</code> table, which encompasses all existing applications in the database.</p><p>By performing the union, we get all the people who have or have had pending, accepted, or rejected applications, since the first query returns only those with an approved license - but the second returns people who have also had rejected applications.</p><p>To exclude those with rejected applications, the <code>EXTENT</code> operator is used along with another query that retrieves these people with rejected applications. So they are all excluded from the final query result - or rather from the intermediate table Persons. From this table, we finally get all its tuples and order them by the attribute <code>PersonFK</code> - that is, by the identifier of the people we obtain.</p><p>As you can see, the order in which set operations are performed is from top to bottom. That is, these operators act on tables containing query results, so SQL performs these operations in a top-down order (although we can use parentheses to change the precedence of the operators according to the needs of the query). Also, in this case, we can see that the <code>UNION</code> operation is redundant since everything contained in the first query is also contained in the second.</p><p>In other words, the second query retrieves people who have or have had applications of any type, whether pending, accepted, or rejected, so all the people who have had accepted applications are included in this set of people generated by the second query. In a real-world environment, we should optimize it by dispensing with the first query, which also implies the elimination of the <code>UNION</code> operation. But here we leave it as is to illustrate its equivalence with the optimized query we have described.</p><hr><h2 id="aggregation-queries" tabindex="-1"><a class="header-anchor" href="#aggregation-queries"><span>Aggregation Queries</span></a></h2><p>Next, we’ll look at a type of query that’s often used in temporal data analysis, calculating metrics, building dashboards aimed at strategic decision-making, and so on. These are <strong>aggregation queries</strong>, and they’re based on treating the tuples of a table as if they were groups on which we can perform certain operations. For example, we can use them to sum all the values of an attribute in a certain group, find their average, and calculate the maximum or minimum value, among others.</p><p>As you might guess, the basic statements to implement this type of query are <code>GROUP BY</code>, <code>HAVING</code>, and the different aggregation functions offered by SQL.</p><p>With <code>GROUP BY</code>, we can choose a series of attributes whose values will determine how we form groups of tuples in a particular table. This means that each of these groups will be formed by a combination of values taken by the selected attributes. Then with the aggregation functions, we will calculate a certain metric for each group.</p><p>With <code>HAVING</code>, we’ll impose conditions related to the characteristics of each group, mainly the value taken by the metrics we calculate on them.</p><p>To understand all this with examples, let&#39;s first consider a query where we want to get a list of all the nationalities present in the Person table and the number of people with each nationality.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT Nationality, COUNT(*) AS NumPersons</span>
<span class="line">FROM Person</span>
<span class="line">GROUP BY Nationality</span>
<span class="line">ORDER BY NumPersons DESC;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>At first glance, we realize that we need to count a certain number of people for each value that the <code>Nationality</code> attribute takes. So, the approaches we&#39;ve seen so far aren’t straightforward for performing this operation (which in some cases may not be possible without grouping).</p><p>For example, here we could list all the different nationalities that appear in <code>Nationality</code> and, for each one, use a subquery in the <code>SELECT</code> statement to count how many people in the Person table have that nationality.</p><p>But this approach would be very inefficient since, for each different nationality, we would have to go through the entire Person table looking for people with that nationality. Even though these searches can be optimized, they are generally not as efficient as the approach we’re going to follow using grouping.</p><p>Instead, we use <strong>grouping</strong> with the <code>GROUP BY</code> statement. Specifically, we indicate the table attributes that guide generating the tuple groups in the table. In this case, since we want to calculate a metric for each value of the <code>Nationality</code> attribute, we use that attribute to group the tuples. This way, for <strong>each value</strong> of that attribute, a <strong>group of tuples</strong> is generated, represented by that value, which will represent all the people with that same nationality.</p><p>Then, if we want to count how many people have that nationality, we simply count how many tuples each group has. So, in the <code>SELECT</code> statement, we add an extra attribute where we use <code>COUNT(*)</code>. This time it won’t count all the tuples in the table, but those in each group. Since using <code>GROUP BY</code> makes it mandatory to return in the <code>SELECT</code> the attributes we are grouping by, the final table will only show the distinct values of <code>Nationality</code>, meaning the &quot;representative&quot; values of each group of tuples.</p><p>For each of those values, we’ll attach the value of <code>COUNT(*)</code> in the same tuple of the output table, which will correspond to the <strong>number of tuples</strong> in the corresponding <strong>group</strong>. This conceptually represents the number of people with that nationality.</p><p>Finally, we can apply sorting with the <code>ORDER BY</code> statement - but we should keep in mind that we can only sort in this case with respect to the attributes we return in <code>SELECT</code>. This is because in the query, we’re creating groups represented by <code>Nationality</code> values, which means we can’t &quot;return&quot; the rest of the attributes in the <code>SELECT</code> as we did before.</p><p>We can only calculate metrics with them and return those - but not the attributes themselves with all their values. This is because when grouping, the resulting table necessarily contains <strong>only the &quot;representative&quot; values</strong> of each group and metrics of other attributes calculated from those groups (or metrics of the group itself, such as the number of tuples it has in this case).</p><p>There are various metrics we can calculate with the basic <strong>aggregation functions</strong> that SQL provides by default. Below we see a query where we get, for each possible pool status, the smallest minimum depth and the largest maximum depth of the pools with that status, as well as the average depth and the number of pools in that status.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT Status,</span>
<span class="line">    MIN(MinDepth) AS Shallowest,</span>
<span class="line">    AVG((MinDepth + MaxDepth) / 2.0) AS AvgDepth,</span>
<span class="line">    MAX(MaxDepth) AS Deepest,</span>
<span class="line">    COUNT(*) AS NumPools</span>
<span class="line">FROM Pool</span>
<span class="line">GROUP BY Status;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The implementation of this query is very similar to the previous one, as we need to group the <code>Pool</code> tuples by the values of their <code>Status</code> attribute, which determines the status of the pools.</p><p>So in the <code>GROUP BY</code> clause, we only specify the <code>Status</code> attribute. This way, we group the tuples into as many groups as there are values present in the <code>Status</code> attribute in the table, and in each of these groups, we have all the tuples representing pools in that status.</p><p>So along with the information for each status, we can calculate metrics for its associated group of tuples - that is, for the pools in that status. For example, with <code>MIN(MinDepth)</code>, we get the smallest value of the <code>MinDepth</code> attribute present in the group for which this metric is being calculated. In this case, it represents the smallest minimum depth of all pools in a certain status.</p><p>Similarly, with the aggregation operation <code>MAX(MaxDepth)</code>, we get the largest maximum depth, or in other words, the largest value of the <code>MaxDepth</code> attribute in the corresponding group of pools. With <code>COUNT(*)</code>, we get the number of pools in each group.</p><p>On the other hand, the average depth associated with the pools in each group is calculated with <code>AVG((MinDepth + MaxDepth) / 2.0)</code>. First, it’s worth noting that both in the <code>SELECT</code> clause and in the input argument of an aggregation function like <code>AVG()</code>, we can perform arithmetic operations on the attributes.</p><p>For example, in this case, with <code>(MinDepth + MaxDepth) / 2.0</code>, we calculate the average value between the minimum and maximum depth of <strong>each pool</strong> - not of each group, but of each tuple in the group - all using decimal values like 2.0 so that the result isn’t automatically rounded to an integer. Then, with this value calculated for each tuple, we use the aggregation function <code>AVG()</code> to calculate the average of this value for each group.</p><p>That is, with <code>(MinDepth + MaxDepth) / 2.0</code>, we get a certain value for each tuple, and then with <code>AVG()</code>, we take all those calculated values for the tuples of a certain group and calculate their average. Thus, for each possible state of a pool, we obtain the average depth of all the pools in that state, first calculating the average depth of each pool and then calculating the average of these depths across all pools in a certain state.</p><p>But, in addition to calculating metrics for each group of tuples, we might need to keep only those groups whose metrics meet certain conditions, depending on the query to be resolved. For example, here we consider a query where we get, for each person, the number of bike rentals they have made since records began, as long as that person has made at least three rentals.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PersonID,</span>
<span class="line">    P.Name,</span>
<span class="line">    COUNT(*) AS RentalCount</span>
<span class="line">FROM Rental R</span>
<span class="line">    INNER JOIN Person P ON R.PersonFK = P.PersonID</span>
<span class="line">GROUP BY P.PersonID, P.Name</span>
<span class="line">HAVING COUNT(*) &gt; 2</span>
<span class="line">ORDER BY RentalCount;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To implement this query, we might have considered using a correlated subquery in the <code>SELECT</code> statement that counts how many tuples in Rental have their foreign key <code>PersonFK</code> pointing to each person. But this would be inefficient, since groupings are usually much faster for this type of task.</p><p>It’s also <strong>not possible</strong> to impose a condition <code>WHERE COUNT(*) &gt; 2</code>, either in the subquery of the <code>SELECT</code> clause or in the main query (in general, conditions on aggregation functions can’t be imposed in a <code>WHERE</code> clause). So in this case, we would have to use another subquery in the <code>WHERE</code> clause that counts the number of rentals each person has and that then checks that this number is <code>&gt; 2</code>. To avoid using subqueries and make our implementation as fast as possible, we first perform an <code>INNER JOIN</code> between the Rental and Person tables. We combine all their information into tuples of their Cartesian product where we have rentals and data about the person who made them. We can do this by imposing the condition in the <code>JOIN</code> that the foreign key <code>PersonFK</code> of Rental points to the <code>PersonID</code> identifier of its same tuple in the Cartesian product.</p><p>After performing the <code>JOIN</code>, we use the <code>GROUP BY</code> clause to group the resulting tuples by the <code>PersonID</code> and ₩ attributes of the person table. We do this because we want to calculate a metric for each person, so we have to include their identifier (primary key) in the grouping of the <code>GROUP BY</code> statement (meaning all the attributes that form their primary key).</p><p>Also, since we want to return each person&#39;s name along with their identifier, we can include the ₩ attribute in the ₩. But it&#39;s important to note that the attributes we group by must uniquely identify each group of tuples that is formed.</p><p>In other words, by grouping by <strong>PersonID</strong>, we are forming groups of tuples that contain all the rentals made by a certain person, identified by a value of their primary key PersonID. This serves as the &quot;representative&quot; of the group of tuples.</p><p>But since this <code>PersonID</code> attribute is enough to identify the group, it&#39;s fine if we include more information about the person in this &quot;representative value&quot; of the group. So instead of containing only their primary key, it includes more information about the person, like their name.</p><p>As you can guess, if instead of grouping by <code>{PersonID}</code> we group by a <strong>candidate key</strong> (or rather a <strong>superkey</strong> as in this case <code>{PersonID, Name}</code>), we’ll get the same groups as grouping by <code>{PersonID}</code>. This means that the same number of groups will still be generated as there are people in the table (since with a superkey we can <strong>uniquely identify each person, and therefore each group)</strong>.</p><p>Adding the <code>Name</code> attribute to the grouping is not an arbitrary decision - we have to use the Name attribute in the <code>SELECT</code> statement. When using <code>GROUP BY</code>, we can only return in the <code>SELECT</code> statement those attributes that we have used in the <code>GROUP BY</code> clause (so, those we have used for grouping). So to get the person&#39;s name and not just their identifier, one option is to include the attribute in the <code>GROUP BY</code> so we can return it in the <code>SELECT</code> - or in other words, use the <code>Name</code> attribute for grouping.</p><p>But, this won’t always work because there are times when we group by an attribute A and want to return information about another attribute B. But for <strong>each value</strong> of <strong>attribute <code>A</code></strong>, we have <strong>multiple tuples</strong> with <strong>multiple different values</strong> in attribute <code>B</code>. This prevents us from using <code>B</code> for grouping, although we can still calculate metrics on B.</p><p>On the other hand, to count how many rentals each person has made, we just need to use the aggregation function <code>COUNT(*)</code> after grouping by <code>{PersonID, Name}</code>. This forms groups of tuples from the Cartesian product where we have the same information for the same person, but each represents a different rental. By counting how many tuples each group has, we get the number of rentals made.</p><p>To get only those groups (people) who have made more than 2 rentals, we use the <code>HAVING</code> clause to impose that condition, since aggregation functions can’t be used in the <code>WHERE</code> clause. Also, we can’t use the alias given to the attribute constructed with <code>COUNT(*)</code> that’s returned in the <code>SELECT</code> in <code>HAVING</code>. Instead, we need to rewrite the definition of the attribute in <code>HAVING</code>.</p><p>That is, just like with <code>WHERE</code>, we can’t impose conditions on the attributes or columns of the resulting table we return by simply referring to their aliases - we have to use their definitions, as in this case with <code>COUNT(*)</code>.</p><p>It&#39;s worth noting that including the Name attribute in the <code>GROUP BY</code> to <strong>&quot;return&quot;</strong> it in the <code>SELECT</code> isn’t the only option we have to do this (or even to get more information about the person). We always have the option to save the query result in an intermediate table with a <code>WITH</code> clause and then join it with the Person table or the appropriate one.</p><p>But we have another option, as shown below, which involves grouping only by the <strong><code>PersonID</code> attribute</strong> and then using correlated subqueries in the <code>SELECT</code> to get the rest of the information for each person.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PersonID,</span>
<span class="line">    (SELECT Name FROM Person WHERE PersonID=P.PersonID),</span>
<span class="line">    COUNT(*) AS RentalCount</span>
<span class="line">FROM Rental R</span>
<span class="line">    INNER JOIN Person P ON R.PersonFK = P.PersonID</span>
<span class="line">GROUP BY P.PersonID</span>
<span class="line">HAVING COUNT(*) &gt; 2</span>
<span class="line">ORDER BY RentalCount;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>But this option isn’t the most optimal: as with a <strong>correlated subquery</strong> in the <code>SELECT</code>, we can only add one attribute of information per subquery. This forces us to use one subquery per attribute we want to add, which is very inefficient as you can see. Also, correlating the subqueries reduces maintainability and possibly also the clarity of the code, which are qualities worth considering.</p><p>With these queries, we have seen how we can use <code>GROUP BY</code> to group tuples by one attribute, or even several if we need to add more information to the resulting table from the query. Also, we’ve seen the correct way to impose conditions on expressions with aggregation functions, which is by using the <code>HAVING</code> clause.</p><p>But, we’re not always trying to return more information to the user every time we use multiple attributes in the <code>GROUP BY</code> statement. Sometimes, we need to group tuples by more than one attribute.</p><p>For example, in the query below, we get all the <strong>person-pool pairs</strong> (only those present in <code>CityPool</code>) that exist in the system. Then for each of them, we calculate the average duration the user has spent in that pool across all their entries.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT E.PersonFK AS PersonID,</span>
<span class="line">    E.PoolFK AS PoolID,</span>
<span class="line">    COUNT(*) AS VisitCount,</span>
<span class="line">    AVG(E.Duration) AS AvgDuration</span>
<span class="line">FROM Entry E</span>
<span class="line">GROUP BY E.PersonFK, E.PoolFK</span>
<span class="line">ORDER BY E.PersonFK, E.PoolFK;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see, we get the data from the Entry table, where we have to perform a grouping with the attributes <code>PersonFK</code> and <code>PoolFK</code>, since we need to calculate metrics for each person-pool pair. With this grouping, each pair of person-pool values is a group formed by all the tuples in Entry that represent times the person entered that pool.</p><p>In this way, with <code>AVG(E.Duration)</code>, we calculate the average of the <code>Duration</code> attribute for each group (so how long, on average, a person stayed at the pool on each visit) while <code>COUNT(*)</code> counts the number of those entries.</p><p>Finally, it&#39;s important to note that in this query, we’re only getting the person-pool pairs that appear in the Entry table - we’re not constructing all possible pairs. So we won&#39;t find any tuple in the resulting table of the query where a person has never entered a certain pool.</p><p>If we wanted to include this information, we would need to structure the query differently, constructing all combinations of person-pool in an intermediate table and then calculating how many entries each person has in each pool in another way (either using subqueries, <code>OUTER JOIN</code> operations, or even more advanced functions that aren’t covered here).</p><p>In the <code>GROUP BY</code> clause, we can use an arbitrary number of attributes for grouping. The query below shows how we can get the number of times the cruise has traveled that route and the sum of the distances covered on those trips for each cruise and route between two ports. We can also display the information of the cities where those ports are located.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT V.ShipFK AS ShipID,</span>
<span class="line">    V.DepartureNameFK AS DeparturePort,</span>
<span class="line">    V.DepartureCityFK AS DepartureCity,</span>
<span class="line">    V.ArrivalNameFK AS ArrivalPort,</span>
<span class="line">    V.ArrivalCityFK AS ArrivalCity,</span>
<span class="line">    COUNT(*) AS TripCount,</span>
<span class="line">    SUM(V.Distance) AS TotalDistance</span>
<span class="line">FROM Voyage V</span>
<span class="line">GROUP BY V.ShipFK,</span>
<span class="line">    V.DepartureNameFK,</span>
<span class="line">    V.DepartureCityFK,</span>
<span class="line">    V.ArrivalNameFK,</span>
<span class="line">    V.ArrivalCityFK</span>
<span class="line">ORDER BY V.ShipFK, TotalDistance DESC;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see, in this case, we get all this information from the <code>Voyage</code> table, as it has multiple foreign keys to <code>CruiseShip</code> and <code>Port</code>, as well as <code>City</code>. These can help us implement this query easily.</p><p>Of all the attributes it has, we group by <code>ShipFK</code>, <code>DepartureNameFK</code>, <code>DepartureCityFK</code>, <code>ArrivalNameFK</code>, and <code>ArrivalCityFK</code>. This allows us to group the tuples of the Voyage table based on the combinations of values representing a <strong>cruise-route pair</strong> (where a <strong>route</strong> is considered as a <strong>pair of ports</strong> along with the city values where they are located).</p><p>These are <strong>redundant</strong> for the grouping itself, as clearly all ports belong to one and only one city (according to the domain). But if we want to know the city where the port is located, the simplest option is to include the <code>DepartureCityFK</code> and <code>ArrivalCityFK</code> attributes in the grouping so we can return them in the <code>SELECT</code>.</p><p>So for each cruise-route pair, we can count how many trips the cruise has made on that route using <code>COUNT(*)</code>, and with <code>SUM(V.Distance)</code> we can get the sum of all distances covered on those trips (as the tuples of each group in this case are the trips the cruise makes or has made on the corresponding route).</p><p>On the other hand, in this type of query, it’s also common to use the <code>DISTINCT</code> modifier to count values in a group or perform a specific aggregation operation on them. For example, in the query below, we get all the people who have ever lived in a city. Then for each of them, we count how many <strong>different cities</strong> they have lived in.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT R.PersonFK AS PersonID,</span>
<span class="line">    COUNT(DISTINCT R.CityFK) AS NumCities</span>
<span class="line">FROM Residence R</span>
<span class="line">GROUP BY R.PersonFK</span>
<span class="line">ORDER BY NumCities DESC;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To do this, we use the data stored in the <code>Residence</code>* table, which has a foreign key <code>PersonFK</code> that can determine, in each tuple, the person associated with that residence record. Since we want to calculate a certain metric for each person who has had at least one residence, we group by the <code>PersonFK</code> attribute, as selecting data from the <code>Residence</code> table ensures that all these people have or have had at least one residence record.</p><p>Then, for each group of tuples formed, we could use <code>COUNT(*)</code> to count how many residences the &quot;representative&quot; person of each group has had. But in this case, we want to count the number of <strong>different cities</strong> they have lived in.</p><p>To do this, we will give <code>COUNT()</code> the <code>CityFK</code> attribute as the input argument, which is a foreign key that determines the city associated with a residence record. This would count all the values the <code>CityFK</code> attribute takes for each group, but not the distinct values. So, we’ll need to add the <code>DISTINCT</code> modifier before the attribute and inside the <code>COUNT()</code> aggregation function so that it only counts the distinct values that the <code>CityFK</code> attribute takes in each group. This corresponds to the number of different cities a certain person has been associated with through <code>Residence</code>, meaning where they have lived.</p><p>When using the <code>DISTINCT</code> modifier in aggregation functions, we may also need to apply conditions on these aggregation functions. So we’ll need to use the same <code>DISTINCT</code> modifier in other clauses like <code>HAVING</code>, in addition to <code>SELECT</code> where most aggregations are calculated and returned.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH PersonsTable AS (</span>
<span class="line">    SELECT CB.PersonFK AS PersonID,</span>
<span class="line">        COUNT(DISTINCT CB.PaymentMethod) AS NumPaymentMethods</span>
<span class="line">    FROM CruiseBooking CB</span>
<span class="line">    GROUP BY CB.PersonFK</span>
<span class="line">    HAVING COUNT(DISTINCT CB.PaymentMethod) &gt; 1</span>
<span class="line">    ORDER BY NumPaymentMethods DESC</span>
<span class="line">)</span>
<span class="line">SELECT *</span>
<span class="line">FROM PersonsTable PT</span>
<span class="line">    INNER JOIN Person P USING (PersonID);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, above we have a query that retrieves information about all the people who have made at least one cruise booking. It also gets the number of different payment methods they have used to pay for all those bookings, as long as that number is at least two different payment methods.</p><p>First, to implement this query, we use the <code>CruiseBooking</code> table and group by <code>PersonFK</code> - as when needing to calculate a number for each person, we should group the tuples of the table by the <code>PersonFK</code> attribute. This way, each group corresponds to the bookings made by a certain person.</p><p>So we can easily use <code>COUNT(DISTINCT CB.PaymentMethod)</code> to count how many distinct values the <code>PaymentMethod</code> attribute takes in each group of tuples. This corresponds to the number of different payment methods the representative person of that group of tuples has used to pay for their bookings.</p><p>Also, to require that they’ve used at least two payment methods, we use a <code>HAVING</code> clause where we declare that the value returned by the aggregation function <code>COUNT(DISTINCT CB.PaymentMethod)</code> must be <code>&gt;1</code>.</p><p>We can’t use its alias name <code>NumPaymentMethods</code> to declare this condition (some DBMS allow it, but for portability reasons, it’s better to code it without using the alias in the condition), nor can we use a <code>WHERE</code> because it’s an aggregation function. The correct way is using a <code>HAVING</code>.</p><p>Although it may seem that the value of <code>NumPaymentMethods</code> is being calculated multiple times unnecessarily, internally the DBMS can optimize the query automatically to avoid this type of unnecessary calculation. But we still have to write the aggregation function multiple times: once to define the attribute <code>NumPaymentMethods</code> in the <code>SELECT</code> and another in the <code>HAVING</code> to impose a filtering condition on the tuples of the resulting table from a grouping.</p><p>Finally, we save the result of this grouping in an intermediate table called <code>PersonsTable</code>, where only the identifier of each person and their corresponding number of payment methods are stored. Later, we can use this intermediate table in a <code>JOIN</code> operation with the <code>Person</code> table. This will gather all the information of each person along with the attribute containing the number of payment methods in one table. Then this is ultimately returned as the query output to the user.</p><p>So as expected, if we use the <code>DISTINCT</code> modifier on an attribute in an aggregation function in the <code>SELECT</code> clause and want to impose a condition on it, we have to write it exactly as it appears in a <code>HAVING</code> clause - regardless of whether it uses the modifier or not, since we write it exactly as it appears in the SELECT.</p><p>So far, we have seen that we can give an aggregation function input attributes with which it will perform the corresponding aggregation operation. Also, if we need only the distinct values of a certain attribute or <strong>result of an arithmetic operation between attributes</strong>, we can add the DISTINCT modifier.</p><p>But DISTINCT is not only used for a single attribute - we can also use it to. getunique combinations of values from a series of attributes, or even unique results obtained from an arithmetic operation involving multiple attributes. For example, in the query below, we want to get all the cruises along with the number of pairs of departure and arrival cities they have visited throughout their journeys.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT V.ShipFK,</span>
<span class="line">    COUNT(DISTINCT (V.DepartureCityFK, V.ArrivalCityFK)) AS NumRoutes</span>
<span class="line">FROM Voyage V</span>
<span class="line">GROUP BY V.ShipFK</span>
<span class="line">ORDER BY NumRoutes DESC;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To do this, we simply group the tuples of the Voyage table by the <code>ShipFK</code> attribute, since we want to calculate a number for each cruise (and the <code>ShipFK</code> foreign key is what determines the cruise that made the journey). Thus, each group of tuples will be “represented” by a certain value of <code>ShipFK</code> that uniquely identifies a cruise. Those tuples, in turn, will represent all the journeys that cruise has made.</p><p>So to count how many <strong>distinct</strong> pairs of departure and arrival cities each cruise has traveled to, we can use the aggregation function <code>COUNT(DISTINCT (V.DepartureCityFK, V.ArrivalCityFK))</code>.</p><p>As you might guess, a cruise can make the same trip several times, which means that within the same group of tuples, we might find the same combination of values for the attributes <code>(V.DepartureCityFK, V.ArrivalCityFK)</code> multiple times. These attributes uniquely identify the departure and arrival cities of the trip, so if the trip is made several times, there must be several &quot;duplicate&quot; tuples - or at least tuples with the same values in those attributes, since there can be multiple different ports in the same city.</p><p>If we look at all the combinations of values that the attributes <code>(V.DepartureCityFK, V.ArrivalCityFK)</code> take in each group, we’ll see that they represent the departure and arrival cities of the cruise in each trip it has made. By applying the <code>DISTINCT</code> modifier, we treat each pair of values as if it were unique, and we keep all those that are unique. This refers to pairs of different departure and arrival cities in the group on which this aggregation operation is calculated, ignoring all duplicate pairs (which would inflate the count artificially). This would represent the total trips the ship has made.</p><p>Finally, when solving a query using grouping, we might need to calculate metrics using aggregation functions with the <code>DISTINCT</code> modifier. Because of this, we have to consider that if we need to impose a condition on that metric, we’ll need to use the aggregation function in the HAVING clause exactly as declared in the <code>SELECT</code> (with the same <code>DISTINCT</code> modifier and attributes present in the input argument of the aggregation function).</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT CB.PersonFK AS PersonID,</span>
<span class="line">    COUNT(DISTINCT (CB.CabinNumber, CB.PaymentMethod))</span>
<span class="line">FROM CruiseBooking CB</span>
<span class="line">GROUP BY CB.PersonFK</span>
<span class="line">HAVING COUNT(DISTINCT (CB.CabinNumber, CB.PaymentMethod)) &gt; 1</span>
<span class="line">ORDER BY count DESC;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, in the query above, we get the identifiers of certain people, and for each one, we calculate the number of distinct pairs composed of <strong>cabin number-payment method</strong> that the person has generated through their various <code>CruiseBooking</code> reservations in their name. Here, we’re only interested in those people whose number of pairs is at least two.</p><p>To implement this, we first group by the <code>PersonFK</code> attribute of the <code>CruiseBooking</code> table, as it contains all the information we need to calculate the previous metric for each person. This is why we only need to group by the foreign key <code>PersonFK</code> attribute. So for each group of <code>CruiseBooking</code> tuples, we’ll have all the reservations associated with a single person.</p><p>Then, with <code>COUNT(DISTINCT (CB.CabinNumber, CB.PaymentMethod))</code>, we can calculate the number of distinct combinations of values that the attributes <code>(CB.CabinNumber, CB.PaymentMethod)</code> take in the group of tuples. As you can see, when we want to count combinations of values from several attributes, we declare both attributes in a &quot;tuple&quot; <code>(CB.CabinNumber, CB.PaymentMethod)</code>, which we provide as input to the <code>COUNT()</code> aggregation function. We use the <code>DISTINCT</code> modifier to ensure it only counts distinct combinations of these two attributes.</p><p>Later, if we want to say that the result of the aggregation function has to be greater than 1 to consider the corresponding group of tuples in constructing the query output, we use the <code>HAVING</code> clause. In it, we can declare the condition by rewriting the aggregation function again in the <code>HAVING</code> clause in the same way we declared it in the <code>SELECT</code>.</p><p>Note that we haven&#39;t assigned an alias to the attribute we built with the aggregation function, so SQL automatically assigns the name <strong>“count”</strong> to this additional attribute. This corresponds to the name of the aggregation function used in its construction.</p><p>But if we create more attributes using the same aggregation function <code>COUNT()</code>, then all those attributes will be called <strong>“count”</strong> by default at the same time, creating an ambiguity problem. This is why it&#39;s essential to use aliases for attributes that are expected to be named the same way by SQL (the DBMS is responsible for assigning default aliases).</p><p>Finally, it&#39;s important to note that queries don&#39;t necessarily have to contain only one grouping. The <code>GROUP BY</code> clause can be used an indefinite number of times in a query, especially if it includes a subquery, which can use the <code>GROUP BY</code> clause again.</p><p>But it&#39;s important to remember that you can&#39;t have multiple groupings <strong>“at the same time”</strong> in the same query. In other words, if we use <code>GROUP BY</code> multiple times, it must be because our query consists of subqueries, with each subquery performing a grouping, avoiding doing them all at the same level. In other words, for each <code>GROUP BY</code> clause, there must be one and only one <code>SELECT</code> clause.</p><hr><h2 id="division-queries" tabindex="-1"><a class="header-anchor" href="#division-queries"><span>Division Queries</span></a></h2><p>At this point, with everything we&#39;ve seen, we have enough tools to solve practically any query with SQL. (But there are some that we won&#39;t focus on here because they require operating on data recursively or hierarchically, which is more complex.)</p><p>Now, we’ll look at a series of queries where we use these previous tools to implement a relational algebra operator that doesn&#39;t have a direct implementation in SQL. For example, we have seen that the <code>SELECT</code> statement represents the implementation of the <strong>projection</strong> operator in relational algebra. And other statements such as certain types of JOIN or UNION, INTERSECT, and EXCEPT have direct equivalent operators in relational algebra as well.</p><p>But the <strong>division</strong> operator doesn’t have an equivalent clause or function in SQL due to its nature. In short, if we have several tables, this operator is responsible for getting all the tuples from one of the tables that are <strong>“associated”</strong> with each and every tuple of the other table.</p><p>To understand how this works, let&#39;s consider the following example:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT R.PersonFK</span>
<span class="line">FROM Rental R</span>
<span class="line">GROUP BY R.PersonFK</span>
<span class="line">HAVING COUNT(DISTINCT R.BikeFK) = (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM Bike</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Say we want to find the people who have rented each and every bike registered in our database. To implement this, we’ll have to use the <strong>division</strong> operator, since in the query setup we will have two tables: one with tuples containing information about a person and a bike, indicating that the person has rented the bike, and another table with all the bikes registered in the system. If we apply a division operation from relational algebra on these tables, we can find all the people who appear in enough tuples in the first table to have rented all the bikes in the second table.</p><p>This can be implemented in many ways, and here we will focus on two. The first method involves <strong>counting</strong> how many different/distinct bikes each person has rented and checking if that number matches the total number of bikes in the system (we’ll see how to do this below). If it matches, then we know that person has rented all the bikes.</p><p>To do this, we can group the tuples in the <code>Rental</code> table by the foreign key <code>PersonFK</code> attribute, since we need to calculate how many bikes <strong>each person</strong> has rented. So we form groups of tuples representing the rentals of each person who has rented at least once (people who have never rented don’t appear in <code>PersonFK</code> of the <code>Rental</code> table).</p><p>Next, using the <code>HAVING</code> clause, we count how many different bikes each person has rented with <code>COUNT(DISTINCT R.BikeFK)</code>. This means that for each group of tuples, we count how many different values the BikeFK attribute takes in that group. This represents the number of different bikes rented, since BikeFK is the foreign key pointing to Bike and uniquely identifies the bike that has been rented.</p><p>Finally, we compare this number with the total number of bikes in our database, which we can get through a subquery using the aggregation function <code>COUNT(*)</code>. Remember that we can use <code>COUNT(*)</code> without the GROUP BY clause being present in the subquery.</p><p>We can also approach the division of tables from a perspective closer to set theory. For example, below is the same query solved using <code>NOT EXISTS</code> and subqueries only:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PersonID</span>
<span class="line">FROM Person P</span>
<span class="line">WHERE NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM Bike B</span>
<span class="line">        WHERE NOT EXISTS (</span>
<span class="line">                SELECT *</span>
<span class="line">                FROM Rental R</span>
<span class="line">                WHERE R.PersonFK = P.PersonID</span>
<span class="line">                    AND R.BikeFK = B.BikeID</span>
<span class="line">            )</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Here, if each person has rented every bike in the database, then there&#39;s not a bike that exists that they haven&#39;t rented. Let’s try to translate this concept into a SQL query literally: first, with a <code>SELECT</code> and <code>FROM</code>, we can <strong>“traverse”</strong> all the tuples of <code>Person</code>. Then for each one, we check that there is no bike the person hasn&#39;t rented. To do this, we construct a <strong>correlated subquery</strong> that returns all bikes that person hasn’t rented.</p><p>This subquery is simply constructed by &quot;traversing&quot; all the tuples of <code>Bike</code> and checking that there is no rental record between the person and the bike being traversed. We do this another correlated subquery that traverses the tuples of <code>Rental</code> and returns only those in which the person has rented the bike. If it doesn&#39;t return any tuples, then there were none with those characteristics, which tells us that the person traversed has never rented the bike traversed. In this case, we’re not interested in that person since they have not rented all the bikes in the database.</p><p>If someone really had rented them all, then the <strong>correlated subquery</strong> that traverses the tuples of <code>Rental</code> would always return at least one tuple. Then the the correlated subquery that traverses the tuples of <code>Bike</code> would never return tuples. And this would satisfy the <code>NOT EXISTS</code> condition that we imposed on the people.</p><p>If we read the SQL code we’ve implemented &quot;literally,&quot; we’re <strong>traversing the people, and for each one we’re checking that they’ve rented every bike</strong>. So we finally get the same people as we do with the query that uses a grouping and a count of bikes rented by each person.</p><p>If we execute either of these queries, they probably won&#39;t return any results. After all, the probability of a person having rented each and every bike registered in the database is small.</p><p>But if we want to check whether the queries work or not, we can always manually insert tuples into <code>Person</code>, <code>Bike</code>, and <code>Rental</code>, especially in <code>Rental</code>. Then there would a person who has a tuple in <code>Rental</code> for each bike in the <code>Bike</code> table, and thus can be present in the result of the <strong>division</strong> operation.</p><p>Another query we can solve using a division operation from relational algebra is the one below. In it, we find all the people who have entered <strong>all</strong> the pools in a certain city (specifically the one with the <code>CityID</code> value of 55).</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT E.PersonFK</span>
<span class="line">FROM Entry E</span>
<span class="line">    INNER JOIN CityPool CP ON E.PoolFK = CP.PoolID</span>
<span class="line">    INNER JOIN Pool P ON CP.PoolID = P.PoolID</span>
<span class="line">WHERE P.CityFK = 55</span>
<span class="line">GROUP BY E.PersonFK</span>
<span class="line">HAVING COUNT(DISTINCT E.PoolFK) = (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM CityPool CP2</span>
<span class="line">            INNER JOIN Pool P2 ON CP2.PoolID = P2.PoolID</span>
<span class="line">        WHERE P2.CityFK = 55</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this case, we first gather all the information from the <code>Entry</code>, <code>CityPool</code>, and <code>Pool</code> tables. This lets us get the information of the people who have entered the pools and the information of the city where the pool is located. So after gathering this information with the <code>INNER JOIN</code> operations, we impose the condition that the foreign key <code>CityFK</code> must reference a certain city, specifically the one identified with the value 55 in its primary key <code>{CityID}</code>. We do this to filter the resulting tuples from the <code>JOIN</code>s, so that we only have those where people have entered pools in the specific city we are considering in the query.</p><p>But this condition doesn’t necessarily have to be in that <code>WHERE</code> clause, as there are other equally valid alternatives (using subqueries, CTE, and so on.).</p><p>Then we group the tuples by PersonFK, so that each group of tuples represents all the pool entries of a certain person (specifically entries in pools of the city identified by the value <code>CityID=55</code>). To find only the people who have entered all the pools in that city, we use <code>COUNT(DISTINCT E.PoolFK)</code> to count the number of different pools they have entered. This equals the number of distinct values taken by the <strong>foreign key <code>PoolFK</code></strong> in the <strong><code>Entry</code></strong> table. We then compare this number with the total number of city pools located in the city with <code>CityID=55</code>, all obtained through a simple uncorrelated subquery.</p><p>In this subquery, we perform another <code>INNER JOIN</code> between <code>CityPool</code> and Pool to gather data on all city pools with the foreign key <code>CityFK</code> that determines the city they are located in. This lets us declare the condition <code>P2.CityFK = 55</code> to count all the pools in that city using <code>COUNT(*)</code>. Also, the advantage of the subquery being uncorrelated is that it only needs to be computed once, since the number of pools in that city doesn&#39;t change while the query is running.</p><p>If we try to solve the previous query using the approach closest to set theory, as we did earlier, we will end up with an implementation that mainly uses the <code>NOT EXISTS</code> operator and correlated subqueries.</p><p>Conceptually, we can solve the query by going through all the people in the database and checking that there’s no city pool located in the city with <code>CityID=55</code> for which there is no entry associating the person with the pool. In other words, for each person, there must be an entry for every city pool in the city with <code>CityID=55</code>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT * </span>
<span class="line">FROM Person P</span>
<span class="line">WHERE NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM CityPool CP</span>
<span class="line">            INNER JOIN Pool PL ON CP.PoolID = PL.PoolID</span>
<span class="line">        WHERE PL.CityFK = 55</span>
<span class="line">            AND NOT EXISTS (</span>
<span class="line">                SELECT *</span>
<span class="line">                FROM Entry E</span>
<span class="line">                WHERE E.PersonFK = P.PersonID</span>
<span class="line">                    AND E.PoolFK = CP.PoolID</span>
<span class="line">            )</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Now, when coding all this, we arrive at a query very similar to the previous one where we looked for people who had rented all the bikes. As you can see, we first go through all the tuples of <code>Person</code>, and for each one, we construct a correlated subquery that gets all the city pools the corresponding person has not entered.</p><p>To do this, we perform a <code>JOIN</code> between <code>CityPool</code> and Pool and impose the condition that ensures all the city pools we consider are located in the city with <code>CityID=55</code>. Also, we verify with another correlated subquery that there is no entry of the person in the pool we are examining.</p><p>Each of these approaches to the same query have significant performance differences. In this last implementation, we nest several subqueries, leading to traversing many tuples, which is often unnecessary. On the other hand, using grouping tends to be faster since the traversal of tuples mainly depends on how the <code>GROUP BY</code> operation is implemented (which usually provides adequate performance for most queries).</p><p>Besides performance, in this last query, we can easily return all the information for each person because we directly use the <code>Person</code> table in the implementation. In contrast, in the previous approach using <code>GROUP BY</code>, we only returned each person&#39;s identifier, forcing us to use CTEs to perform a <code>JOIN</code> with the <code>Person</code> table if we want to return more information besides the identifier.</p><p>So when performing a division in SQL, we should consider not only the efficiency of the implementation but also the ease of modifying the query, as well as its clarity and maintainability.</p><p>The previous two queries were formulated considering the city with <code>CityID=55</code>, although this is an arbitrary decision. If we want to choose an appropriate value for <code>CityID</code> so that the two previous queries return data, since there may be cities where no person has entered all their pools, we can use the query below. For each person and city, it gets the number of pools in that city the person has entered, along with the total number of pools in that city.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT E.PersonFK,</span>
<span class="line">    P.CityFK,</span>
<span class="line">    COUNT(DISTINCT E.PoolFK) AS EnteredPools,</span>
<span class="line">    (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM CityPool AS CP2</span>
<span class="line">            INNER JOIN Pool AS P2 ON CP2.PoolID = P2.PoolID</span>
<span class="line">        WHERE P2.CityFK = P.CityFK</span>
<span class="line">    ) AS TotalPools</span>
<span class="line">FROM Entry AS E</span>
<span class="line">    INNER JOIN CityPool AS CP ON E.PoolFK = CP.PoolID</span>
<span class="line">    INNER JOIN Pool AS P ON CP.PoolID = P.PoolID</span>
<span class="line">GROUP BY E.PersonFK, P.CityFK</span>
<span class="line">ORDER BY EnteredPools, TotalPools;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see, several <code>JOIN</code> operations are first performed to gather all the information from <code>Entry</code>, <code>CityPool</code>, and <code>Pool</code>, so we can later group the resulting tuples by <code>PersonFK</code> and <code>CityFK</code>. This means grouping the tuples into groups where each represents the entries a certain person has made in the pools of a certain city. Then, with <code>COUNT(DISTINCT E.PoolFK)</code>, we count the pools they have entered, since <code>PoolFK</code> is the foreign key in the <code>Entry</code> table that determines the pool the person has entered. Finally, with a correlated subquery in the <code>SELECT</code>, we get the total number of pools in the city identified by <code>CityFK</code>.</p><p>Finally, it&#39;s important to note that with this query, we will never get a value of 0 in the <code>EnteredPools</code> attribute. If a person has never entered any pool in a certain city, there will be no resulting tuples from these <code>JOIN</code> operations with <code>(E.PersonFK, P.CityFK)</code> attributes that reference both the person and the city.</p><p>This happens because no entry (<code>Entry</code> tuple) will have its foreign key <code>PersonFK</code> as the person and its foreign key <code>PoolFK</code> as a pool in the corresponding city that the person has visited (since they haven&#39;t visited any pool in that city).</p><p>So if we also want to include in our query&#39;s resulting table tuples with <code>(E.PersonFK, P.CityFK)</code> pairs of people who have never visited any pool in the city <code>CityFK</code>, we would need to use set operations to find those <code>(E.PersonFK, P.CityFK)</code> pairs and add the tuples constructed from these pairs to the final resulting table.</p><p>We can see another fundamental case we might encounter when implementing a division operation in SQL below. Here, we have a query where we get all the people who have or have had pool sanctions in all possible states.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT PS.PersonFK</span>
<span class="line">FROM PoolSanction PS</span>
<span class="line">    INNER JOIN Sanction S ON PS.SanctionID = S.SanctionID</span>
<span class="line">GROUP BY PS.PersonFK</span>
<span class="line">HAVING COUNT(DISTINCT S.Status) = 3;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To implement this, we first perform a <code>JOIN</code> between <code>PoolSanction</code> and <code>Sanction</code>, ensuring with the first table that the sanction occurred in a pool and using the second table to get the sanction&#39;s state (which is stored in the <code>Status</code> attribute).</p><p>Then, as we need to count how many states each person has sanctions in, we group by the <code>PersonFK</code> attribute, creating groups of tuples that represent the sanctions each person has or has had. This way, we can use <code>HAVING</code> to require that the number of states in which a person has sanctions is equal to the total number of possible states a sanction can have.</p><p>On one hand, with <code>COUNT(DISTINCT S.Status)</code>, we can count how many different values the <code>Status</code> attribute takes in each group - or in other words, the number of states of the sanctions associated with a person. And, since there are three possible states (&#39;created&#39;, &#39;active&#39;, &#39;expired&#39;), we simply compare the resulting count from the aggregation function with 3. But if we use the constant 3 in the comparison and later modify the database to include more or fewer states in the sanctions, we will be forced to change that number. This makes the query not as maintainable as it could be.</p><p>So another another option we have for declaring the condition in the <code>HAVING</code> clause is to compare the result of the aggregation function <code>COUNT(*)</code> with the result of a subquery that calculates how many possible states a sanction can have.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT PS.PersonFK</span>
<span class="line">FROM PoolSanction PS</span>
<span class="line">    INNER JOIN Sanction S ON PS.SanctionID = S.SanctionID</span>
<span class="line">GROUP BY PS.PersonFK</span>
<span class="line">HAVING COUNT(DISTINCT S.Status) = (SELECT COUNT(DISTINCT Status) FROM Sanction);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As up can see above, this subquery is non-correlated, as it simply counts how many <strong>distinct values</strong> the <code>Status</code> attribute takes in the <code>Sanction</code> table. But implementing the query this way has a problem: we’re assuming that in the <code>Sanction</code> table, specifically in the <code>Status</code> attribute, we can find all the possible values that <code>Status</code> can take. But this might not be the case, as if the table is empty, no distinct values can be counted in the <code>Status</code> attribute.</p><p>This means that this last implementation of the query only works when the <code>Sanction</code> table contains tuples representing sanctions where there is at least one sanction in all possible states. If we can guarantee that the database meets this condition, then the above implementation is more convenient for us because it requires no maintenance.</p><p>But this condition is not usually met, so it’s not a good practice to assume that we’ll find all possible values an attribute can take in that attribute. For example, if we think of an <strong>integer</strong> attribute, it’s clear that there don’t have to be tuples that take a different value for every possible value the attribute can have.</p><p>Another option we have is to skip grouping and use a set theory-based approach. As you can see, in the implementation above, we go through all the tuples of Person, and for each one, we check that there <strong>exists</strong> a pool sanction with the status <strong>‘created’</strong>. Also, using the logical operator <code>AND</code>, we require that at the same time there exists another pool sanction with the status <strong>‘active’</strong>. Finally, we use another logical operator <code>AND</code> to also require that for that person there exists another pool sanction in the status <strong>‘expired’</strong>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT p.PersonID</span>
<span class="line">FROM Person p</span>
<span class="line">WHERE EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM PoolSanction ps</span>
<span class="line">            INNER JOIN Sanction s ON ps.SanctionID = s.SanctionID</span>
<span class="line">        WHERE ps.PersonFK = p.PersonID</span>
<span class="line">            AND s.Status = &#39;created&#39;</span>
<span class="line">    )</span>
<span class="line">    AND EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM PoolSanction ps</span>
<span class="line">            INNER JOIN Sanction s ON ps.SanctionID = s.SanctionID</span>
<span class="line">        WHERE ps.PersonFK = p.PersonID</span>
<span class="line">            AND s.Status = &#39;active&#39;</span>
<span class="line">    )</span>
<span class="line">    AND EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM PoolSanction ps</span>
<span class="line">            INNER JOIN Sanction s ON ps.SanctionID = s.SanctionID</span>
<span class="line">        WHERE ps.PersonFK = p.PersonID</span>
<span class="line">            AND s.Status = &#39;expired&#39;</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see above, this implementation is equivalent to the previous one where we used groupings and counts to implement the division operation. But this approach is clearly less maintainable, though possibly easier to understand in some aspects.</p><p>For example, in this implementation, the names of the different <code>Status</code> values appear explicitly in the conditions we impose in each correlated subquery, which is generally not a good practice. If you want to modify the database domain, you will also have to modify these values.</p><p>Also, we’re duplicating the same code multiple times, making the query code as a whole less maintainable. This is because if the query itself needs to be modified, it’s very likely that we’ll need to make changes in all three subqueries, slowing down the management process.</p><p>So although the set theory-based approach may be impractical in certain situations, it can work for a small database like the one we&#39;re dealing with here. But, whenever possible, it&#39;s best to choose solutions that are more maintainable and require fewer changes in the future.</p><p>In this specific case, the best option would be to use the grouping approach where the number of sanction statuses for a person is compared with the total number of statuses, as changing that number in the query is easier than modifying the code of several subqueries. This also avoids having to make assumptions about the <code>Sanction</code> tuples.</p><p>Let’s look at another query that’s similar to the previous ones, where we can see a different way the division operator can appear. It retrieves all the people who, for every city they have lived in, have visited at least one pool located in that city.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT P.PersonID</span>
<span class="line">FROM Person P</span>
<span class="line">WHERE NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM Residence R</span>
<span class="line">        WHERE R.PersonFK = P.PersonID</span>
<span class="line">            AND NOT EXISTS (</span>
<span class="line">                SELECT *</span>
<span class="line">                FROM Entry E</span>
<span class="line">                    INNER JOIN Pool PO ON E.PoolFK = PO.PoolID</span>
<span class="line">                WHERE E.PersonFK = P.PersonID</span>
<span class="line">                    AND PO.CityFK = R.CityFK</span>
<span class="line">            )</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For each person, keep them only if there is no residence of theirs that lacks a matching pool-visit in the same city. Equivalently: for every city a person has lived in (from <code>Residence</code>), there must be at least one record showing they visited a pool in that city.</p><p>The implementation of this approach is very similar to how we express it in natural language. On one hand, we go through the tuples of Person with a <code>SELECT</code> and a <code>FROM</code>, and we set the condition that the result of a subquery is empty using <code>NOT EXISTS</code>.</p><p>In this correlated subquery, we go through the tuples of <code>Residence</code> for the person we are currently checking the condition for, so to keep only the residences we are interested in, we impose the condition <code>R.PersonFK = P.PersonID</code> in the subquery. This ensures that the selected <code>Residence</code> tuples have their foreign key <code>PersonFK</code> pointing to the person we are going through, whose identification is given by <code>P.PersonID</code>.</p><p>On the other hand, within this subquery, we also check that another correlated and nested subquery doesn’t return any tuples either. This last subquery is dedicated to getting all the entries where the person identified by <code>P.PersonID</code> has entered a pool located in the city identified by <code>R.CityFK</code> - that is, the city of the residence we are going through at the time of executing this subquery.</p><p>In summary, in this query, we have seen that divisions don’t always refer to situations where the tuples we want to obtain are &quot;associated&quot; with all the tuples of another table. Instead, as in this case, they can also refer to the output tuples of our query needing to meet a certain condition in relation to all the tuples of another table.</p><p>Similar to the previous query, we can consider another one where we need to find people who have or have had at least one travel booking in all existing cruise classes.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT CB.PersonFK</span>
<span class="line">FROM CruiseBooking CB</span>
<span class="line">    INNER JOIN CruiseShip CS ON CB.ShipFK = CS.ShipID</span>
<span class="line">GROUP BY CB.PersonFK</span>
<span class="line">HAVING COUNT(DISTINCT CS.Class) = (</span>
<span class="line">        SELECT COUNT(DISTINCT Class)</span>
<span class="line">        FROM CruiseShip</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this case, we start by setting up the division operation through grouping and counting. First, we perform an <code>INNER JOIN</code> between the <code>CruiseBooking</code> and <code>CruiseShip</code> tables. This allows us to gather information about the person who made each travel booking using the foreign key <code>PersonFK</code> from <code>CruiseBooking</code> and the information about the cruise class for the trip. This same table has a foreign key <code>ShipFK</code> that uniquely identifies the cruise ship for the trip, from which we can determine its class.</p><p>So after this operation, we group by the <code>PersonFK</code> attribute, as we’ll need to count how many different cruise classes each person has booked to perform the division.</p><p>Regarding this quantity, we calculate it using the aggregation function <code>COUNT(DISTINCT CS.Class)</code>, which is executed once for each group of tuples. Then we compare it with the total number of cruise classes in our database.</p><p>In this case, we could have directly written the number instead of using an uncorrelated subquery to get the total number of classes by looking at the distinct values of the <code>Class</code> attribute from the <code>CruiseShip</code> table. So as it stands, with the subquery, we’re implicitly assuming that the <code>CruiseShip</code> table contains cruises in all existing classes (but this may not be the case).</p><p>Imagine if the table is empty, for example - the subquery would result in a total of 0 cruise classes, when in reality, there may be more (the domain of the <code>Class</code> attribute may contain more values than those actually appearing in the table).</p><p>But it’s important to clarify here that by “all cruise classes” we mean all possible values that the <code>Status</code> attribute can take - that is, the values we define as the domain of the attribute. On the other hand, in some circumstances, we can assume that all cruise classes correspond to the distinct values that the Status attribute takes in the <code>CruiseShip</code> table, all depending on the domain we are working with.</p><p>For simplicity, from now on in this domain, we’ll assume that the distinct values of an attribute like <code>Class</code> found in its corresponding table are equivalent to all the values it can take. If we think about it, this makes sense because if there are only 2 distinct values in the <code>Class</code> attribute of the <code>CruiseShip</code> table, then all the bookings made throughout the time the database has existed will have to reference some cruise in the <code>CruiseShip</code> table (whose <code>Class</code> will have one of those two values). There might be no bookings referencing cruises of a certain class, but if there are cruises of two classes, then it makes sense to assume that those two classes make up <strong>“all the classes the <code>Class</code> attribute can hold.”</strong></p><p>So, by assuming that the <code>Class</code> attribute of the <code>CruiseShip</code> table contains &quot;all the classes&quot; of cruises, we can solve the query using a set theory approach as shown in the query below.</p><p>Here, we first go through all the tuples in <code>CruiseBooking</code> that represent bookings. In each one, we check that there is no cruise (of any class, rather) for which no booking has been made by the person referenced by the foreign key <code>PersonFK</code> of the <code>CruiseBooking</code> tuple we are examining.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT DISTINCT CB.PersonFK</span>
<span class="line">FROM CruiseBooking CB</span>
<span class="line">WHERE NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM CruiseShip C</span>
<span class="line">        WHERE NOT EXISTS (</span>
<span class="line">                SELECT *</span>
<span class="line">                FROM CruiseBooking CB2</span>
<span class="line">                    INNER JOIN CruiseShip CS2 ON CB2.ShipFK = CS2.ShipID</span>
<span class="line">                WHERE CB2.PersonFK = CB.PersonFK</span>
<span class="line">                    AND CS2.Class = C.Class</span>
<span class="line">            )</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>That is, since we are only interested in getting the people who have ever booked, we start the query by going through <code>CruiseBooking</code>, not Person, because there may be people in <code>Person</code> who have never booked.</p><p>So to check that there is no cruise with these characteristics, we use the <code>NOT EXISTS</code> operator and a correlated subquery in which we go through all the cruises registered in the <code>CruiseShip</code> table. For each one, we check that there is no travel booking where the cruise is the one <strong>whose class is the same</strong> as the cruise and person we are currently examining in the query.</p><p>By doing this, we ensure that for all the people returned by our query, there is no cruise of any class that hasn’t been booked at least once by that person. But we can do this correctly only if we are sure that the Class attribute in <code>CruiseShip</code> includes what we consider as <strong>“all possible cruise classes“</strong>. If we didn&#39;t have this assurance, then this set theory approach would not be correct, because the correlated subquery that goes through the tuples of <code>CruiseShip</code> might not be covering all possible cruise classes.</p><p>For example, imagine the <code>CruiseShip</code> table is empty. In that case, this approach would return more people than it should, since that subquery would never return tuples.</p><p>On the contrary, in the other approach based on groupings, if the <code>CruiseShip</code> table is empty, then the uncorrelated subquery that counts the total number of classes would return 0. Also, the <code>HAVING</code> condition would never be met, preventing the return of people who do not meet the condition defined in the query statement.</p><p>So as you can see, it’s not always better to use just one approach based on either <strong>groupings</strong> or <strong>set theory</strong> - it varies depending on the situation.</p><p>In this specific case, it’s more practical to use groupings - mainly for efficiency (since internally a grouping is usually faster than executing a correlated subquery multiple times) but also for simplicity of maintenance and code clarity.</p><p>To wrap up our discussion on the division operator of relational algebra, it’s important to note that there are times when we have to do division using intermediate tables (CTE) instead of tables from the database itself.</p><p>For example, in the query below, we obtain the ships that have made at least one trip departing from and arriving at each pair of cities with an area of at most 11 km². In other words, all ships that have made at least one trip between each pair of cities with this characteristic.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH AllPairs AS (</span>
<span class="line">    SELECT C1.CityID AS Dep, C2.CityID AS Arr</span>
<span class="line">    FROM City C1 CROSS JOIN City C2</span>
<span class="line">    WHERE C1.CityID &lt;&gt; C2.CityID AND C1.Area&lt;11 AND C2.Area&lt;11</span>
<span class="line">),</span>
<span class="line">ShipVisits AS (</span>
<span class="line">    SELECT V.ShipFK,</span>
<span class="line">        V.DepartureCityFK AS Dep,</span>
<span class="line">        V.ArrivalCityFK AS Arr</span>
<span class="line">    FROM Voyage V</span>
<span class="line">        INNER JOIN City C1 ON V.DepartureCityFK = C1.CityID</span>
<span class="line">        INNER JOIN City C2 ON V.ArrivalCityFK = C2.CityID</span>
<span class="line">    WHERE C1.Area&lt;11 AND C2.Area&lt;11</span>
<span class="line">)</span>
<span class="line">SELECT SV.ShipFK</span>
<span class="line">FROM ShipVisits SV</span>
<span class="line">GROUP BY SV.ShipFK</span>
<span class="line">HAVING COUNT(DISTINCT (SV.Dep, SV.Arr)) = (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM AllPairs</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see in the implementation, to make the code simpler, we can first build an intermediate table with all possible pairs of cities with an area value <code>&lt;11</code>. We. cando this by executing a <code>CROSS JOIN</code> between the <code>City</code> table and itself, as it contains all the cities registered in our database. Then we require that both cities in the pair have an area <code>&lt;11</code>. It’s important to note that the <code>Area</code> attribute of the <code>City</code> table contains values representing square kilometers, so it’s straightforward to declare the <code>&lt;11</code> condition in our query. But if this attribute had values in other units, we’d need to adapt to them or convert them to other units that we could easily work with. It’s crucial to consider the units in which the values we compare are measured to correctly code the query.</p><p>Finally, this table includes all possible pairs of cities that meet the area characteristic, meaning it doesn&#39;t matter if the same pair of cities <code>(A,B)</code> also appears in the table as <code>(B,A)</code>.</p><p>Then, we build another intermediate table where we store the different pairs of cities each cruise has visited throughout all its trips, considering only those cities that meet the query conditions (area <code>&lt;11</code>).</p><p>To do this, we simply extract the foreign key attributes <code>ShipFK</code>, <code>DepartureCityFK</code>, and <code>ArrivalCityFK</code>. These determine the cruise that made the trip and the departure and arrival cities of the trip from the resulting table of the <code>INNER JOIN</code> operations between the <code>Voyage</code> table and the <code>City</code> table itself.</p><p>We perform these operations to access the area information of each city, allowing us to impose the same area conditions as in the first intermediate table <code>AllPairs</code>. If we didn&#39;t do this, we might consider cruise trips between cities that don&#39;t meet the conditions we are looking for. This would increase the number of <strong>“valid”</strong> city pairs the cruise has traveled between. Since we’re going to structure the query using a grouping and a <strong>count</strong>, it’s essential not to count irrelevant elements for our query.</p><p>Once both CTEs are built, we perform a grouping on the <code>ShipVisits</code> tables based on the <code>ShipFK</code> attribute. We do this to calculate, for each cruise, the number of distinct pairs of cities it has traveled between. We easily calculate this using the aggregation function <code>COUNT(DISTINCT (SV.Dep, SV.Arr))</code>. Then we can compare the returned value with the total number of city pairs that exist and that we have stored in the first CTE called <code>AllPairs</code>, all within the <code>HAVING</code> clause.</p><p>To keep only those cruises that have traveled through every pair of cities calculated in <code>AllPairs</code>, we compare the output of the <code>COUNT()</code> aggregation function with the result of an uncorrelated subquery that simply counts how many tuples the intermediate table <code>AllPairs</code> has.</p><p>In the total count of pairs, we don’t have to use the <code>DISTINCT</code> modifier, since the <code>CROSS JOIN</code> never generates repeated city pairs given the very definition of the cross product operation. And there there are no identical tuples in the City table, meaning there are no identical cities in our database (much less with the same value of their primary key <code>CityID</code>). But if we wanted to use the <code>DISTINCT</code> modifier to count how many distinct tuples are in <code>AllPairs</code>, we could use the syntax <code>COUNT(DISTINCT AllPairs.*)</code>.</p><p>Regarding this last subquery, we could have avoided explicitly constructing all the city pairs in <code>AllPairs</code> if we had directly performed the same computation as in <code>AllPairs</code> - but returning only <code>COUNT(*)</code>. This would directly count all the city pairs with the characteristics we are looking for. But we can only do this if we code the query using grouping and counting, as we’ll see that it can also be implemented based on set operations, for which we’ll necessarily need to construct and store the pairs in <code>AllPairs</code>.</p><p>So just as we have shown with other queries, we can also approach this one using <strong>set theory operators</strong>. As you can see below, the intermediate tables are constructed in the same way except for <code>ShipVisits</code>, where we don&#39;t need the cities involved in the trips to meet the condition of having an <code>area &lt; 11</code>. This is because those <code>ShipVisits</code> tuples will later be compared with the city pairs in <code>AllPairs</code>, which do meet the condition. This way, we end up with cruises that have made a trip in all the pairs of <code>AllPairs</code>, regardless of additional tuples in <code>ShipVisits</code> with trips between cities that don&#39;t meet the condition we&#39;re looking for.</p><p>Although this isn&#39;t crucial for resolving the query, it&#39;s important to note that <code>ShipVisits</code> contains more tuples than necessary, which might slow down the query since <code>ShipVisits</code> will later be used in a <strong>correlated subquery</strong>, resulting in multiple scans of its tuples.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH AllPairs AS (</span>
<span class="line">    SELECT C1.CityID AS Dep, C2.CityID AS Arr</span>
<span class="line">    FROM City C1 CROSS JOIN City C2</span>
<span class="line">    WHERE C1.CityID &lt;&gt; C2.CityID AND C1.Area&lt;11 AND C2.Area&lt;11</span>
<span class="line">),</span>
<span class="line">ShipVisits AS (</span>
<span class="line">    SELECT V.ShipFK,</span>
<span class="line">        V.DepartureCityFK AS Dep,</span>
<span class="line">        V.ArrivalCityFK AS Arr</span>
<span class="line">    FROM Voyage V</span>
<span class="line">)</span>
<span class="line">SELECT SV.ShipFK</span>
<span class="line">FROM ShipVisits SV</span>
<span class="line">WHERE NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM AllPairs AP</span>
<span class="line">        WHERE NOT EXISTS (</span>
<span class="line">                SELECT *</span>
<span class="line">                FROM ShipVisits SV2</span>
<span class="line">                WHERE SV2.ShipFK = SV.ShipFK</span>
<span class="line">                    AND SV.Dep = AP.Dep</span>
<span class="line">                    AND SV.Arr = AP.Arr</span>
<span class="line">            )</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>After constructing the CTEs, we solve the query in a way similar to the other divisions we&#39;ve seen. First, we go through the tuples of <code>ShipVisits</code> (although we could also choose to go through those of <code>CruiseShip</code>, since what we want is to go through all the cruises in the database, or at least those that have made a trip). So instead of using <code>CruiseShip</code>, which might contain cruises that have never made a trip, we choose to go through the tuples of <code>ShipVisits</code>, where we can find cruises referenced by the <strong>foreign key <code>ShipFK</code></strong> from the <code>Voyage</code> table, which we know have made at least one trip.</p><p>In each of these tuples, we check that there is no pair of cities from <code>AllPairs</code> for which there is no trip made by the cruise we are currently going through between the cities of that pair.</p><p>To do this, we use the <code>NOT EXISTS</code> operator and two nested correlated subqueries. In the first, we go through the tuples of <code>AllPairs</code> - that is, the pairs of cities that do meet the condition of having an <code>area &lt; 11</code>. Then for each pair, we use <code>NOT EXISTS</code> again on another correlated subquery that gets all the trips made by the cruise currently being processed in the query execution over the cities of the corresponding pair from <code>AllPairs</code>.</p><p>In a more intuitive way, we’re getting all the cruises for which there is no pair of cities from <code>AllPairs</code> where the cruise hasn&#39;t traveled at least once. As you can guess, since the cities in <code>AllPairs</code> do meet the condition of having an area less than 11 km², it doesn&#39;t matter that ShipVisits has trips with cities that don&#39;t meet this condition - because in the query we check that for a certain pair of cities from <code>AllPairs</code> there is no trip of a cruise in those cities. So it’s really indifferent which cities are present in the trips of <code>ShipVisits</code>, as those that meet the condition will definitely be there since we don&#39;t impose any condition when constructing that intermediate table.</p><p>In summary, with this approach, we can solve the query just as we did before using groupings and counts. But the difference here is that we can save the conditions (<code>area &lt; 11</code>) that we imposed when constructing the tuples of <code>ShipVisits</code>.</p><p>At first glance, this might seem like an improvement in code clarity, as it’s shorter. This makes it more maintainable in this case because fewer operations and statements are needed to construct the CTE. But the resulting CTE contains more tuples, specifically those that represent all the trips each cruise has made, not just those made between cities that meet the condition of having an <code>area &lt; 11</code>. This additional number of tuples impacts the computation of the query. But to analyze this impact, we must also consider that in constructing <code>ShipVisits</code>, we are saving two <code>JOIN</code> operations, which are highly costly, in addition to the expected amount of data with which the query will be executed.</p><p>For example, if the amount of data in the involved tables is small, the performance difference won’t be significantly noticeable. But if it’s large, it’s more beneficial to have the smallest possible number of tuples in <code>ShipVisits</code>, even if it requires performing an additional <code>JOIN</code>.</p><p>This is because the correlated subquery that goes through the tuples of <code>ShipVisits</code> is executed once for each tuple of <code>AllPairs</code>, and all of this is executed once for each tuple of <code>ShipVisits</code> (we could have replaced this last one with <code>CruiseShip</code> to improve performance, as the number of cruises is fixed and tends to be smaller than the number of trips).</p><p>So the computation involved in going through all the tuples of <code>ShipVisits</code> is much greater than the computation of a simple <code>JOIN</code> used to construct the CTE itself - which, despite being computationally costly, only needs to be executed once (not multiple times depending on the number of tuples in other tables).</p><p>To finish with the division operation, we&#39;ve seen that we can implement it in SQL using the <code>EXISTS</code> operator (either as is or negated with the logical <code>NOT</code> operator) and a correlated subquery. In it, the <code>SELECT</code> statement uses the <code>*</code> notation to return all the attributes of the corresponding table. This means that to check if the subquery returns any tuple or not, we construct its result so that each tuple possibly has multiple attributes - meaning all those that result from using the <code>SELECT *</code> notation. But sometimes instead of returning several attributes, it simply returns a column with a fixed value like the integer 1. In general, using the <code>SELECT *</code> notation in a correlated subquery to which the EXISTS operator is applied is considered good practice, so it’s coded this way by default. But there are also other possibilities like <code>SELECT 1</code>, which at first glance might seem more efficient because it doesn&#39;t return unnecessary attributes since it only checks if the subquery results in any tuple or not.</p>`,231)),n("p",null,[e[99]||(e[99]=t("In summary, the decision on which attributes to return in a correlated subquery using the ")),e[100]||(e[100]=n("code",null,"EXISTS",-1)),e[101]||(e[101]=t(" operator is mainly determined by the characteristics of the DBMS, as each ")),n("a",x,[s(i,{icon:"fas fa-globe"}),e[97]||(e[97]=t("implementation"))]),e[102]||(e[102]=t(" of the ")),n("a",B,[s(i,{icon:"fa-brands fa-stack-overflow"}),e[98]||(e[98]=t("DBMS"))]),e[103]||(e[103]=t(" handles these operations differently at the physical level."))]),e[135]||(e[135]=a(`<hr><h2 id="ranking-queries" tabindex="-1"><a class="header-anchor" href="#ranking-queries"><span>Ranking Queries</span></a></h2><p>To conclude with the different &quot;types&quot; of queries we might encounter, there are queries where we need to calculate a <strong>ranking</strong> - that is, ordering elements based on the value they have for a certain metric. For example, ordering people by the number of bike rentals they have made, allowing us to find out who has made the most or fewest rentals, among many other similar tasks.</p><p>In this case, these approaches don’t have any equivalent operator in relational algebra. This is because the calculation of rankings is based on the combination of multiple techniques and tools like groupings, aggregations, or uncorrelated subqueries that aren’t present in relational algebra as specific operators.</p><p>This is mainly because in relational algebra, there is no concept of order, and since tables are treated as sets of tuples, there is no unique way to number the tuples positionally to establish an <strong>internal order of the set</strong>. In other words, within a set, its elements don’t necessarily have an order among them unless we explicitly define it.</p><p>We can start by finding the maximum value of whatever we’re ranking for (and, optionally, where in the table this occurs). For example, in the query below, we get the maximum passenger capacity among all the cruise ships in the <code>CruiseShip</code> table.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT MAX(PassengerCapacity) AS MaxCapacity</span>
<span class="line">FROM CruiseShip;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>In terms of approach, solving this query involves establishing a ranking of the cruise ships based on their passenger capacity (this is their metric). The one with the highest capacity occupies the first place in the ranking, followed by the other cruise ships. So if we take the first in the ranking and access its metric, we will have the maximum passenger capacity, which is what we want to obtain.</p><p>In SQL, implementing this query is very simple if we only want to get the metric value and its values are already calculated in an attribute. As you can see, we simply use the <code>MAX()</code> aggregation function, which we give the attribute where the metric values are calculated as an input argument. Finally, when we execute the query, we will see that only one tuple is returned with that maximum value in the attribute we have named with the alias <code>MaxCapacity</code>.</p><p>But the implementation is not always that simple. For example, if we want to get not only the maximum value of the metric but also the specific element associated with that metric - in this case, the cruise ship with the highest passenger capacity - we first need to go through the tuples in <code>CruiseShip</code> and check each one to see if it corresponds to the cruise ship with the highest passenger capacity.</p><p>Specifically, what we check in each tuple is whether the passenger count is equal to the maximum or not, so that we only keep those tuples where the <code>PassengerCapacity</code> value is exactly equal to the maximum value of that attribute.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT ShipID, PassengerCapacity</span>
<span class="line">FROM CruiseShip</span>
<span class="line">WHERE PassengerCapacity = (</span>
<span class="line">    SELECT MAX(PassengerCapacity)</span>
<span class="line">    FROM CruiseShip</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This is reflected in the query code above. In the <code>WHERE</code> clause, we check that the <code>PassengerCapacity</code> attribute value is equal to the result of the uncorrelated subquery that returns its maximum value. We use the <code>MAX()</code> aggregation function for this just like before.</p><p>If the values match, we will have found the tuple of the cruise ship with the highest passenger capacity. But there may be several cruise ships with that same capacity, so our query will return them as well.</p><p>If we want to get only one cruise ship, we have the option to add an additional clause <code>LIMIT 1</code> at the very end of the query, which basically returns only the first tuple of the resulting table from the query.</p>`,15)),n("p",null,[e[106]||(e[106]=t("This ")),n("a",K,[s(i,{icon:"fas fa-globe"}),e[104]||(e[104]=t("LIMIT clause"))]),e[107]||(e[107]=t(", it’s not part of the ")),n("a",M,[s(i,{icon:"fas fa-globe"}),e[105]||(e[105]=t("SQL-92 standard"))]),e[108]||(e[108]=t(", but it can still be used in any query we need as long as the DBMS supports it (all ")),e[109]||(e[109]=n("strong",null,"modern",-1)),e[110]||(e[110]=t(" DBMSs support it). Its use is simple: we just give it a number that indicates the number of tuples from the resulting table of the query that we want to get from the first tuple located at the top of the table, ignoring the rest."))]),e[136]||(e[136]=a(`<p>Another option we have is to do without the <code>MAX()</code> aggregation function. As you can see below, most of the query code is the same, except for the subquery. Instead of returning the maximum value of the <code>PassengerCapacity</code> attribute, it returns the attribute itself - meaning all the values in its corresponding column in the CruiseShip table.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT ShipID, PassengerCapacity</span>
<span class="line">FROM CruiseShip</span>
<span class="line">WHERE PassengerCapacity &gt;= ALL (</span>
<span class="line">    SELECT PassengerCapacity</span>
<span class="line">    FROM CruiseShip</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this way, the condition of the <code>WHERE</code> clause uses the operator &gt;= along with the <code>ALL</code> modifier, which establishes that, for a certain tuple of <code>CruiseShip</code>, its <code>PassengerCapacity</code> value must be greater than all the values returned by the subquery. Or put another way, our query retrieves information on all cruise ships whose passenger capacity is greater than or equal to each and every capacity stored in the <code>PassengerCapacity</code> attribute of the <code>CruiseShip</code> table.</p><p>Specifically, here we have to use the operator <code>&gt;=</code>, not <code>&gt;</code>, because if we are going through the tuple of a cruise ship that does have the highest passenger capacity, its capacity will at most be equal to the maximum capacity of the <code>CruiseShip</code> table (but never greater). That is, if the maximum is a value <code>X</code>, then there will be no cruise ship with a capacity <code>&gt;X</code>, but there will be one or more with a capacity <code>=X</code>, which are the ones we want to find.</p><p>At the same time, these have a capacity <code>X</code> that is greater than the rest of the capacities of the other cruise ships, which is why we use the operator <code>&gt;=</code>.</p><p>The <code>ALL</code> modifier is necessary to ensure that the value of the <code>PassengerCapacity</code> attribute meets the condition imposed by the <code>&gt;=</code> operator with respect to each and every tuple returned by the subquery. In this case, it only returns tuples with an attribute or column with the values of all the passenger capacities that it must be compared with.</p><p>As you can guess, even though this way of implementing the query is equivalent to the previous one, here the subquery returns a series of values that are compared for each tuple of <code>CruiseShip</code>. That is, for each cruise ship, all the tuples of the subquery are traversed to perform the comparison declared in the <code>WHERE</code> clause. This requires much more computation than simply comparing with a number like the maximum capacity obtained from the <code>MAX()</code> function.</p><p>So since the <strong>subquery</strong> is <strong>non-correlated</strong> and is computed only once, it’s more optimal to use the previous approach where we used <code>MAX()</code> than this one, as this approach uses more space to store the subquery tuples and more time unnecessarily traversing them to make the comparisons.</p><p>Continuing with queries where we need to calculate a <strong>maximum</strong>, here we have another one where we get the person (or people) who have had the most residences in cities, along with that maximum number of residences.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT R.PersonFK AS PersonID, COUNT(*) AS NumResidences</span>
<span class="line">FROM Residence AS R</span>
<span class="line">GROUP BY R.PersonFK</span>
<span class="line">HAVING COUNT(*) &gt;= ALL (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM Residence</span>
<span class="line">        GROUP BY PersonFK</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>We can find the information to solve this query in the <code>Residence</code> table - specifically in the tuples themselves, where each one represents a residence. The person is referenced by the foreign key <code>PersonFK</code> and the city where the person has lived is referenced by the foreign key <code>CityFK</code>.</p><p>So, in this table, we don&#39;t have a number in an attribute that tells us the number of residences a person has had. Instead, the tuples themselves represent the residences of the people, and we need to count them to know which person has or has had the most residences.</p><p>To do this, we can group the tuples in Residence by the attribute <code>PersonFK</code>, since we need to count residences for each person. In this way, we form groups of tuples that represent all the residences a person has had.</p><p>Once the groups are made, we can use <code>COUNT(*)</code> to count how many residences the &quot;representative&quot; person of that group of tuples has or has had. Then, to ensure that this number is the maximum, we use the operator <code>&gt;=</code> along with the <code>ALL</code> modifier and a subquery.</p><p>In this case, the subquery calculates, for each person, the total number of residences they have or have had in the same way as in the main query, using a grouping by the <code>PersonFK</code> attribute of Residence and the aggregation function <code>COUNT(*)</code>.</p><p>With this, we can verify, in the <code>HAVING</code> clause, that the number of residences of a certain person is greater than or equal to all the numbers of residences that all the people present in the Residence table have or have had.</p><p>On the other hand, we could try to implement the query without using the <code>&gt;=</code> operator and the <code>ALL</code> modifier, and instead use only a non-correlated subquery and the aggregation function <code>MAX()</code>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT</span>
<span class="line">  R.PersonFK   AS PersonID,</span>
<span class="line">  COUNT(*)     AS NumResidences</span>
<span class="line">FROM Residence AS R</span>
<span class="line">GROUP BY R.PersonFK</span>
<span class="line">HAVING COUNT(*) = (</span>
<span class="line">  SELECT MAX(COUNT(*))</span>
<span class="line">  FROM Residence</span>
<span class="line">  GROUP BY PersonFK</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see above, the query construction is very similar, except that in the <code>HAVING</code> clause, we directly compare <code>COUNT(*)</code>, which returns the number of residences a person has or has had with the result of the subquery, which seems to obtain the maximum number of residences any person has had.</p><p>But if we look at the <code>SELECT</code> clause of the subquery, several nested aggregation functions like <code>MAX(COUNT(*))</code> appear, intending to calculate the maximum value of the numbers of residences people have had. But <strong>this is not allowed in SQL</strong>. In fact, if we run the query, the DBMS will give us an error because <strong>an aggregation function can’t be used as an input argument to another aggregation function</strong>.</p><p>If we really want to use the aggregation function <code>MAX()</code> to solve the query, we have no choice but to first build a CTE where we store all the people who have ever had a residence and their respective number of residences.</p><p>You can see this in the code below, and it’s very similar to the approach we followed before to solve the query. This involves grouping the residence tuples by their foreign key attribute <code>PersonFK</code> and using <code>COUNT(*)</code> to count how many tuples each group has, that is, how many residences each person has.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">WITH ResCount AS (</span>
<span class="line">    SELECT PersonFK AS PersonID, COUNT(*) AS NumResidences</span>
<span class="line">    FROM Residence</span>
<span class="line">    GROUP BY PersonFK</span>
<span class="line">)</span>
<span class="line">SELECT RC.PersonID,</span>
<span class="line">    RC.NumResidences</span>
<span class="line">FROM ResCount RC</span>
<span class="line">WHERE RC.NumResidences = (</span>
<span class="line">        SELECT MAX(NumResidences)</span>
<span class="line">        FROM ResCount</span>
<span class="line">    );</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Then, once this intermediate table <code>ResCount</code> is built, we are in the same situation as in the queries at the beginning of this section, where the numbers of residences are now values stored in an attribute.</p><p>So we can follow the usual approach to get the tuple or tuples from ResCount with the maximum value in their attribute <code>NumResidences</code>. This involves going through all its tuples and checking if their NumResidences value matches the maximum. We can easily calculate this with a non-correlated subquery and the aggregation function <code>MAX()</code>.</p><p>After these queries, we can consider solving them by obtaining the element with the lowest value in its metric in the ranking.</p><p>For example, in this last case, it would correspond to finding the person or people who have had the fewest residences (which doesn&#39;t make much sense in this query, but it does in others).</p><p>So, to calculate <strong>minimums</strong> instead of <strong>maximums</strong> in SQL, you use exactly the same constructions we just saw, with the difference that the operators and aggregation functions used change, such as the operator <code>&gt;=</code> to <code>&lt;=</code> and the aggregation function <code>MIN()</code> is used instead of <code>MAX()</code>.</p><p>In addition to calculating maximums and minimums, in SQL it&#39;s sometimes useful to calculate the <strong>ranking positions</strong> of elements based on the value of their metrics.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT</span>
<span class="line">  P1.PoolID,</span>
<span class="line">  P1.Name,</span>
<span class="line">  P1.MaxDepth,</span>
<span class="line">  (</span>
<span class="line">    SELECT COUNT(*) + 1</span>
<span class="line">    FROM Pool AS P2</span>
<span class="line">    WHERE P2.MaxDepth &gt; P1.MaxDepth</span>
<span class="line">  ) AS DepthRank</span>
<span class="line">FROM Pool AS P1</span>
<span class="line">ORDER BY DepthRank;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, in the query above, we get a list of all the pools in the database, where for each one, we calculate its position in the pool ranking ordered by the value of its <code>MaxDepth</code> attribute, that is, by its maximum depth.</p><p>Also, since there can be multiple pools with the same <code>MaxDepth</code> value, in that case, both pools will have the same position in the ranking. So the next position with a lower <code>MaxDepth</code> value won’t be the immediate next position - instead, you must add the number of pools from the previous position that had the same <code>MaxDepth</code> value to that ranking position.</p><table><thead><tr><th><code>PoolID</code></th><th><code>Name</code></th><th><code>MaxDepth</code></th><th><code>DepthRank</code></th></tr></thead><tbody><tr><td>1</td><td>Sample Pool Name 1</td><td>5</td><td>1</td></tr><tr><td>2</td><td>Sample Pool Name 2</td><td>5</td><td>1</td></tr><tr><td>3</td><td>Sample Pool Name 3</td><td>3</td><td>3</td></tr><tr><td>4</td><td>Sample Pool Name 4</td><td>2</td><td>4</td></tr><tr><td>5</td><td>Sample Pool Name 5</td><td>2</td><td>4</td></tr></tbody></table><p>To understand this, here we have a table where you can see that the first two pools have the same position (<code>DepthRank</code>) in the pool ranking because they have the same <code>MaxDepth</code> value. Then, the next pool with <code>PoolID=3</code> has position 3 in the ranking, as there are two pools before it in the ranking. Finally, the next two pools with <code>PoolID=4</code> and <code>PoolID=5</code> again have the same position in the ranking for the same reason as before.</p><p>As we can see, this way of defining and building the ranking is not what we might expect, where each pool has a unique position. Instead, we slightly modify the ranking definition to allow pools with the same <code>MaxDepth</code> value to share the same position in the ranking, so SQL implementation doesn&#39;t require more advanced functions.</p><p>Regarding the implementation, if we look at the attributes of the example table, specifically <code>MaxDepth</code> and its relationship with <code>DepthRank</code>, we can conclude that the position we should assign to each pool in the ranking matches the number of pools with a <code>MaxDepth</code> strictly greater than its own <strong>plus 1</strong>.</p><p>For example, for the pool with <code>PoolID=2</code>, we see that there is no pool with a <code>MaxDepth</code> greater than its own - at most, there are some with an equivalent <code>MaxDepth</code>, but never greater because this pool has the highest <code>MaxDepth</code> value (meaning the maximum). Meanwhile, the pool with <code>PoolID=3</code> has two pools with a <code>MaxDepth</code> greater than its own.</p><p>So if we <strong>add one</strong> to the number of pools with a metric value, which in this case we can find in the <code>MaxDepth</code> attribute, greater than the <code>MaxDepth</code> value of a certain pool, then the amount we obtain is the ranking position of that pool.</p><p>The simplest way to implement this calculation in SQL is through a correlated subquery in the <code>SELECT</code>, where, as you can see, we get all <code>Pool</code> tuples with a <code>MaxDepth</code> greater than the pool we are iterating over in the query. And finally, with <code>COUNT(*)+1</code>, we add 1 to the number of tuples returned by the subquery, thus generating the <strong>position</strong> in the ranking of the pool being iterated over in the query.</p><p>Continuing with the idea of getting the ranking position of the elements, we also have the option to select only those elements with a ranking position greater or less than a certain amount we need to set.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">SELECT PoolID, Name, MaxDepth</span>
<span class="line">FROM Pool AS P</span>
<span class="line">WHERE (</span>
<span class="line">        SELECT COUNT(*)</span>
<span class="line">        FROM Pool AS P2</span>
<span class="line">        WHERE P2.MaxDepth &gt; P.MaxDepth</span>
<span class="line">    ) &lt; 5</span>
<span class="line">ORDER BY MaxDepth DESC;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For example, above we have a query where we get the pools that are among the top 5 distinct positions in the ranking. In other words, we don’t get the first 5 rows with pools ordered in the ranking according to their MaxDepth value, but we get all those whose ranking position is among the top 5 distinct positions.</p><p>As you can see, the implementation is simple. We go through all the <code>Pool</code> tuples and for each one, we execute a subquery like the one we saw in the previous query: it gets the number of pools with a MaxDepth greater than the pool we are iterating over - that is, its position in the ranking. Then, we compare that number with 5 to ensure it’s strictly less.</p><p>Also, here we need to note that we have not added 1 to <code>COUNT(*)</code>, which means the ranking starts counting at <strong>position 0</strong>, not 1, so we can later check that the position is among the top 5 distinct ones with <code>&lt; 5</code> and not <code>&lt; 6</code>. This doesn&#39;t have to be done this way necessarily, as we could have added 1 to <code>COUNT(*)</code> and declared the comparison using <code>&lt; 6</code>, or <code>&lt;=5</code>.</p><p>In summary, in this query, we used a correlated subquery to get the ranking position (starting from position 0) of each pool, so we only keep those whose position is strictly less than 5. But we could have also pre-calculated the positions of each pool in a CTE and then applied this condition to an attribute instead of the value returned by a subquery.</p><p>This alternative will likely use more memory than is necessary, since the computing the execution of the subquery that calculates the ranking position will be present whether we use a CTE or not. So the most optimal approach would be to avoid wasting memory unless we really need an intermediate table with that information for other uses.</p><p>So now we’ve have seen a series of queries that follow certain patterns that are the most basic and fundamental in SQL. But there are many other queries we could perform on the schema of this example with a wide variety of purposes. These are <strong>essential</strong> to know how to formulate and code.</p>`,47)),n("p",null,[e[111]||(e[111]=t("To learn more queries, you can visit the following resource: ")),s(i,{icon:"iconfont icon-jupyter"}),e[112]||(e[112]=n("code",null,"PostgreSQL.ipynb",-1)),e[113]||(e[113]=t("."))]),s(d,{name:"sql-storage/PostgreSQL.ipynb at main · cardstdani/sql-storage",desc:"Contribute to cardstdani/sql-storage development by creating an account on GitHub.",url:"https://github.com/cardstdani/sql-storage/blob/main/PostgreSQL.ipynb/",logo:"https://github.githubassets.com/favicons/favicon-dark.svg",preview:"https://opengraph.githubassets.com/7bcbcfa51cd3a5f4cd6363d5e76f8da7e21b4f70750fca25e82020150d9a6311/cardstdani/sql-storage"}),n("p",null,[e[115]||(e[115]=t("This is a Jupyter notebook that you can run from Google Collab. It contains Python code and Bash commands that allow you to install the ")),e[116]||(e[116]=n("strong",null,"PostgreSQL DBMS",-1)),e[117]||(e[117]=t(" on a ")),e[118]||(e[118]=n("strong",null,"Linux virtual machine",-1)),e[119]||(e[119]=t(" like those used by ")),n("a",W,[s(i,{icon:"iconfont icon-gcp"}),e[114]||(e[114]=t("Google Compute Engine"))]),e[120]||(e[120]=t(" (the backend of Google Collab). You can also execute SQL code to create the database from the DDL and then run queries and obtain their results."))]),e[137]||(e[137]=n("p",null,"The notebook contains a series of query statements with solutions, along with everything needed to execute them. These queries aren’t ordered or classified like those we saw in the last chapter, as the goal is for you to try to solve them from the statements without looking at the solution. This way, you can later see how they were solved and gain practice in formulating queries, which is one of the most valuable skills for providing services to end users from the database.",-1)),e[138]||(e[138]=n("p",null,[t("You don’t necessarily have to do this in a Google Collab environment - you can also do it on a PostgreSQL installation on a "),n("strong",null,"local machine"),t(" and execute the queries by copying and pasting the query code into the PostgreSQL terminal. But doing it in a remote environment like the one offered by Google Collab has certain advantages, such as not having to worry about installing anything manually, as everything is set up automatically by simply running the code cells or being able to see the text of the statements in the notebook rendered with markdown.")],-1)),e[139]||(e[139]=n("p",null,"Still, there are some disadvantages, such as the database being stored on a Google virtual machine, which means you don't have full control over the machine and environment in which the DBMS runs. Its execution can also be interrupted depending on how you use the virtual machine and the plan you have with Google Collab.",-1)),e[140]||(e[140]=n("p",null,"So even though it may not be an environment where you can deploy a fully functional production database, it’s sufficiently similar to a real environment where you might have a database deployed for a project, making it worthwhile to work in Google Collab.",-1))])}const J=u(w,[["render",U],["__file","chapter-11-example-queries.html.vue"]]),j=JSON.parse('{"path":"/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-11-example-queries.html","title":"Chapter 11: Example Queries","lang":"en-US","frontmatter":{"lang":"en-US","title":"Chapter 11: Example Queries","description":"Article(s) > (12/12) How to Design Structured Database Systems Using SQL [Full Book]","category":["Data Science","PostgreSQL","Design","System"],"tag":["blog","freecodecamp.org","data-science","sql","postgres","posgtresql","design","system"],"head":[[{"meta":null},{"property":"og:title","content":"Article(s) > (12/12) How to Design Structured Database Systems Using SQL [Full Book]"},{"property":"og:description","content":"Chapter 11: Example Queries"},{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-11-example-queries.html"}],["meta",{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-11-example-queries.html"}],["meta",{"property":"og:site_name","content":"📚Bookshelf"}],["meta",{"property":"og:title","content":"Chapter 11: Example Queries"}],["meta",{"property":"og:description","content":"Article(s) > (12/12) How to Design Structured Database Systems Using SQL [Full Book]"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:image:src","content":"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png"}],["meta",{"name":"twitter:image:alt","content":"Chapter 11: Example Queries"}],["meta",{"property":"article:author","content":"Daniel García Solla"}],["meta",{"property":"article:tag","content":"blog"}],["meta",{"property":"article:tag","content":"freecodecamp.org"}],["meta",{"property":"article:tag","content":"data-science"}],["meta",{"property":"article:tag","content":"sql"}],["meta",{"property":"article:tag","content":"postgres"}],["meta",{"property":"article:tag","content":"posgtresql"}],["meta",{"property":"article:tag","content":"design"}],["meta",{"property":"article:tag","content":"system"}],["meta",{"property":"article:published_time","content":"2025-08-14T00:00:00.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Chapter 11: Example Queries\\",\\"image\\":[\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png\\"],\\"datePublished\\":\\"2025-08-14T00:00:00.000Z\\",\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Daniel García Solla\\",\\"url\\":\\"https://freecodecamp.org/news/author/cardstdani/\\"}]}"]],"next":"/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/README.md#conclusion","date":"2025-08-14T00:00:00.000Z","isOriginal":false,"author":[{"name":"Daniel García Solla","url":"https://freecodecamp.org/news/author/cardstdani/"}],"cover":"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png","gitInclude":[]},"headers":[{"level":2,"title":"Running Basic Queries","slug":"running-basic-queries","link":"#running-basic-queries","children":[]},{"level":2,"title":"Tuple Filtering","slug":"tuple-filtering","link":"#tuple-filtering","children":[]},{"level":2,"title":"Subqueries","slug":"subqueries","link":"#subqueries","children":[]},{"level":2,"title":"Common Table Expressions","slug":"common-table-expressions","link":"#common-table-expressions","children":[]},{"level":2,"title":"Set Operations","slug":"set-operations","link":"#set-operations","children":[]},{"level":2,"title":"Aggregation Queries","slug":"aggregation-queries","link":"#aggregation-queries","children":[]},{"level":2,"title":"Division Queries","slug":"division-queries","link":"#division-queries","children":[]},{"level":2,"title":"Ranking Queries","slug":"ranking-queries","link":"#ranking-queries","children":[]}],"readingTime":{"minutes":115.45,"words":34636},"filePathRelative":"freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-11-example-queries.md","localizedDate":"August 14, 2025","excerpt":"\\n","copyright":{"author":"Daniel García Solla"}}');export{J as comp,j as data};
