import{_ as l}from"./plugin-vue_export-helper-DlAUqK2U.js";import{aj as p,am as t,as as h,ao as o,at as m,au as g,ap as n,al as u,aq as i,ar as f,an as s}from"./app-DpiNAgkx.js";const b={},y={id:"frontmatter-title-관련",tabindex:"-1"},w={class:"header-anchor",href:"#frontmatter-title-관련"},v={class:"table-of-contents"};function k(c,e){const r=i("VPCard"),a=i("router-link"),d=i("SiteInfo");return f(),p("div",null,[t("h1",y,[t("a",w,[t("span",null,h(c.$frontmatter.title)+" 관련",1)])]),o(r,m(g({title:"Gitting Things Done - A Visual and Practical Guide to Git [Full Book]",desc:"Introduction Git is awesome. Most software developers use Git on a daily basis. But how many truly understand Git? Do you feel like you know what's going on under the hood as you use Git to perform various tasks? For example, what happens when you us...",link:"/freecodecamp.org/gitting-things-done-book/README.md",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",background:"rgba(10,10,35,0.2)"})),null,16),t("nav",v,[t("ul",null,[t("li",null,[o(a,{to:"#chapter-12-git-log"},{default:n(()=>e[0]||(e[0]=[s("Chapter 12 - Git Log")])),_:1}),t("ul",null,[t("li",null,[o(a,{to:"#filtering-commits"},{default:n(()=>e[1]||(e[1]=[s("Filtering Commits")])),_:1})]),t("li",null,[o(a,{to:"#formatting-log"},{default:n(()=>e[2]||(e[2]=[s("Formatting Log")])),_:1})]),t("li",null,[o(a,{to:"#setting-an-alias"},{default:n(()=>e[3]||(e[3]=[s("Setting an alias")])),_:1})])])]),t("li",null,[o(a,{to:"#chapter-13-git-bisect"},{default:n(()=>e[4]||(e[4]=[s("Chapter 13 - Git Bisect")])),_:1}),t("ul",null,[t("li",null,[o(a,{to:"#how-does-git-bisect-work"},{default:n(()=>e[5]||(e[5]=[s("How Does git bisect Work?")])),_:1})]),t("li",null,[o(a,{to:"#git-bisect-example"},{default:n(()=>e[6]||(e[6]=[s("git bisect Example")])),_:1})]),t("li",null,[o(a,{to:"#automatic-git-bisect"},{default:n(()=>e[7]||(e[7]=[s("Automatic git bisect")])),_:1})])])]),t("li",null,[o(a,{to:"#chapter-14-other-useful-commands"},{default:n(()=>e[8]||(e[8]=[s("Chapter 14 - Other Useful Commands")])),_:1}),t("ul",null,[t("li",null,[o(a,{to:"#git-cherry-pick"},{default:n(()=>e[9]||(e[9]=[s("git cherry-pick")])),_:1})]),t("li",null,[o(a,{to:"#git-revert"},{default:n(()=>e[10]||(e[10]=[s("git revert")])),_:1})]),t("li",null,[o(a,{to:"#git-add-p"},{default:n(()=>e[11]||(e[11]=[s("git add -p")])),_:1})])])])])]),e[12]||(e[12]=t("hr",null,null,-1)),o(d,{name:"Gitting Things Done - A Visual and Practical Guide to Git [Full Book]",desc:"Introduction Git is awesome. Most software developers use Git on a daily basis. But how many truly understand Git? Do you feel like you know what's going on under the hood as you use Git to perform various tasks? For example, what happens when you us...",url:"https://freecodecamp.org/news/gitting-things-done-book#heading-part-4-amazing-and-useful-git-tools",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",preview:"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png"}),e[13]||(e[13]=u(`<p>Git has lots of commands, and these commands have so many options and arguments. I could try to cover them all (though they do change over time), but I don&#39;t see a point in that. You should probably know a subset of these commands really well, those that you use regularly. Then, you can always search for a specific command to perform a task at hand.</p><p>This part relies on the basics you acquired in the previous parts, and covers specific commands and options that you may find useful. Given your understanding of how Git works, having these small tools can make you a real pro in Gitting things done.</p><hr><h2 id="chapter-12-git-log" tabindex="-1"><a class="header-anchor" href="#chapter-12-git-log"><span>Chapter 12 - Git Log</span></a></h2><p>You used <code>git log</code> many times across different chapters, and you had probably used it many times before reading this book.</p><p>Most developers use <code>git log</code>, few use it effectively. In this chapter you will learn useful tweaks for making the most of <code>git log</code>. Once you feel comfortable with the different switches of this command, it will be a game changer in your day to day work with Git.</p><p>Thinking about it, <code>git log</code> encompasses the essence of every version control system - that is, to record changes in versions. You record versions so that you can consider the history of your project - perhaps revert or apply specific changes, prefer to switch to a different point in time and test things there. Perhaps you would like to know who contributed a certain piece of code or when they did that.</p><p>While <code>git</code> does preserve this information by using commit objects, that also point to their parent commits, and references to commit objects (such as branches or <code>HEAD</code>), this storing of versions is not enough. Without being able to find the relevant commit you would like to consider, or gather the relevant information about it, having this data stored is pretty useless.</p><p>You can think of your commit objects as different books that pile up in a huge stack, or in a library, filling long shelves. The information you might need is in these books, but if you don&#39;t have an index - a way to know in which book the information you seek lies, or where this book is located within the library - you wouldn&#39;t be able to make much use of it. <code>git log</code> is this indexing of your library - it&#39;s a way to find the relevant commits and the information about them.</p><p>The useful arguments for <code>git log</code> that you will learn in this chapter either format how commits are displayed in the log, or filter specific commits.</p><p><code>git lol</code>, an alias which I have used throughout the book, uses some of these switches, as I will demonstrate. Feel free to tweak this alias (or create another from scratch) after reading this chapter.</p><p>As in other chapters, the goal is not to provide a complete reference, therefore I will not provide <em>all</em> different switches of <code>git log</code>. I will focus on the switches I believe you will find useful.</p><h3 id="filtering-commits" tabindex="-1"><a class="header-anchor" href="#filtering-commits"><span>Filtering Commits</span></a></h3><p>Consider the default output of <code>git log</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_1.png" alt="The output of  without additional switches" loading="lazy"> <em>The output of <code>git log</code> without additional switches</em></p><p>The log starts from <code>HEAD</code>, and follows the parent chain.</p><h4 id="commits-not-reachable-from" tabindex="-1"><a class="header-anchor" href="#commits-not-reachable-from"><span>Commits (Not) Reachable From...</span></a></h4><p>When you write <code>git log &lt;revision&gt;</code>, <code>git log</code> will include all entries reachable from <code>&lt;revision&gt;</code>. By &quot;reachable&quot;, I refer to reachable by following the parent chain. So running <code>git log</code> without any arguments is equivalent to running <code>git log HEAD</code>.</p><p>You can specify multiple revisions for <code>git log</code> - if you write <code>git log branch_1 branch_2</code>, you ask <code>git log</code> to include every commit that is reachable from <code>branch_1</code> or <code>branch_2</code> (or both).</p><p><code>git log</code> will <strong>exclude</strong> any commits that are reachable from revisions preceded by a <code>^</code>.</p><p>For example, the following command:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log branch_1 ^branch_2</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>asks <code>git log</code> to include every commit that is reachable from <code>branch_1</code>, but not those reachable from <code>branch_2</code>.</p><p>Consider the history when I use <code>git log feature_branch_1</code> on this repo:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_2-1.png" alt="Image" loading="lazy"> <em><code>git log feature_branch_1</code></em></p><p>The history includes all commits reachable by <code>feature_branch_1</code>. Since this branch &quot;branched off&quot; <code>main</code> (that is, &quot;Commit 12&quot;, which <code>main</code> points to, is reachable from the parent chain) - the log also includes the commits reachable from <code>main</code>.</p><p>What would happen if I ran this command?</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log feature_branch_1 ^main</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_3.png" alt="Image" loading="lazy"> <em><code>git log feature_branch_1 ^main</code></em></p><p>Indeed, <code>git log</code> outputs only &quot;Commit 13&quot; and &quot;Commit 16&quot;, which are reachable from <code>feature_branch_1</code> but not from <code>main</code>.</p><h4 id="git-log-all" tabindex="-1"><a class="header-anchor" href="#git-log-all"><span><code>git log --all</code></span></a></h4><p>To follow commits that are reachable from any named reference or (any refs in <code>refs/</code>) or <code>HEAD</code>.</p><h4 id="by-author" tabindex="-1"><a class="header-anchor" href="#by-author"><span>By Author</span></a></h4><p>If you know you are looking for a commit that a specific person has authored, you can filter these commits by using that user&#39;s name or email, like so:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--author</span><span class="token operator">=</span><span class="token string">&quot;Name&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>You can use regular expressions to look for author names that match a specific pattern, for example:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--author</span><span class="token operator">=</span><span class="token string">&quot;John\\|Jane&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>will filter commits authored by either John or Jane.</p><h4 id="by-date" tabindex="-1"><a class="header-anchor" href="#by-date"><span>By Date</span></a></h4><p>When you know that the change you are looking for has been committed within a specific timeframe, you can use <code>--before</code> or <code>--after</code> to filter commits from that timeframe.</p><p>For example, to get all commits introduced after April 12th, 2023 (inclusive), use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--after</span><span class="token operator">=</span><span class="token string">&quot;2023-04-12&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h4 id="by-paths" tabindex="-1"><a class="header-anchor" href="#by-paths"><span>By Paths</span></a></h4><p>You can ask <code>git log</code> to only show commits where <em>changes</em> to files in specific paths have been introduced. Notice that this does not mean any commit that points to a tree that includes the files in question, but rather that if we compute the difference between the commit in question and its parent, we would see that at least one of the paths has been modified.</p><p>For example, you can use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--all</span> -- <span class="token number">1</span>.py</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>to find all commits that are reachable from any named pointer, or <code>HEAD</code>, and introduce a change to <code>1.py</code>. You can specify multiple paths:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--all</span> -- <span class="token number">1</span>.py <span class="token number">2</span>.py</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The previous command will make <code>git log</code> include reachable commits that introduced a change to <code>1.py</code> or <code>2.py</code> (or both).</p><p>You can also use a glob pattern, for example:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log -- *.py</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>will include commits reachable from <code>HEAD</code> that include a change to any file in the root directory whose name ends with a <code>.py</code>. To look for any file whose name ends with <code>.py</code>, you can use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log -- **/*.py</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h4 id="by-commit-message" tabindex="-1"><a class="header-anchor" href="#by-commit-message"><span>By Commit Message</span></a></h4><p>If you know the commit message (or parts of it) of the commit you are searching, you can use the <code>--grep</code> switch for &quot;git log&quot;, for example:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--grep</span><span class="token operator">=</span><span class="token string">&quot;Commit 12&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>yields back the commit with the message &quot;Commit 12&quot;.</p><h4 id="by-diff-content" tabindex="-1"><a class="header-anchor" href="#by-diff-content"><span>By Diff Content</span></a></h4><p>This one is super useful, and it saved me countless times. By using <code>git log -S</code>, you can search for commits that introduce or remove a particular line of source code.</p><p>This comes in handy, for example, when you know you have created something in the repo, but you don&#39;t know where it is now. You can&#39;t find it anywhere on your filesystem (it&#39;s not in <code>HEAD</code>), and you know it must be there - lurking somewhere in this library (bunch of commits) that you have.</p><p>Say I remember I wrote a line with the text <code>Git is awesome</code>, but I can&#39;t find it now. I could run:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--all</span> -S<span class="token string">&quot;Git is awesome&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Notice I used <code>--all</code> to avoid restraining myself to commits reachable from <code>HEAD</code>.</p><p>You can also search for a regular expression, using <code>-G</code>:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--all</span> -G<span class="token string">&quot;Git .* awesome&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h3 id="formatting-log" tabindex="-1"><a class="header-anchor" href="#formatting-log"><span>Formatting Log</span></a></h3><p>Consider the default output of <code>git log</code> again:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_1-1.png" alt="The output of  without additional switches" loading="lazy"> <em>The output of <code>git log</code> without additional switches</em></p><p>The log starts from <code>HEAD</code>, and follows the parent chain.</p><p>Each log entry begins with a line starting with <code>commit</code> and then the SHA-1 of the commit, perhaps followed by additional pointers that point to this commit.<br> It is then followed by the author, date, and commit message.</p><h4 id="oneline" tabindex="-1"><a class="header-anchor" href="#oneline"><span><code>--oneline</code></span></a></h4><p>The main difficulty with the default output of <code>git log</code> is that it is hard to understand a history with more than a few commits, as you simply don&#39;t see them all.</p><p>In the output of <code>git log</code> shown before, only four commit objects appeared on my screen. Using <code>git log --oneline</code> provides a more concise view, showing the SHA-1 of the commit, next to its message, and named references if relevant:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_5.png" alt="The output of " loading="lazy"> <em>The output of <code>git log --oneline</code></em></p><p>If you wish to omit the named references, you can add the <code>--no-decorate</code> switch:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_6.png" alt="The output of " loading="lazy"> <em>The output of <code>git log --oneline --no-decorate</code></em></p><p>To explicitly ask for <code>git log</code> to show decorations, you can use <code>git log --decorate</code>.</p><h4 id="graph" tabindex="-1"><a class="header-anchor" href="#graph"><span><code>--graph</code></span></a></h4><p><code>git log --oneline</code> shows a compact representation. That is great when we have a linear history, perhaps on a single branch. But what happens when we have multiple branches, that may diverge from one another?</p><p>Consider the output of the following command on my repository:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--oneline</span> feature_branch_1 feature_branch_2</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_7.png" alt="The output of " loading="lazy"> <em>The output of <code>git log --oneline feature_branch_1 feature_branch_2</code></em></p><p><code>git log</code> outputs any commit reachable by <code>feature_branch_1</code>, <code>feature_branch_2</code>, or both. But what does the history look like? Did <code>feature_branch_2</code> diverge from <code>feature_branch_1</code>? Or did it diverge from <code>main</code>? It is impossible to tell from this view.</p><p>This is where <code>--graph</code> comes in handy, drawing an ASCII graph representing the branch structure of the commit history. If we add this option to the previous command:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_8.png" alt="The output of " loading="lazy"> <em>The output of <code>git log --oneline --graph feature_branch_1 feature_branch_2</code></em></p><p>You can actually <em>see</em> that <code>feature_branch_1</code> branched from <code>main</code> (as &quot;Commit 12&quot;, <code>main</code>, is the parent of &quot;Commit 13&quot;), and also that <code>feature_branch_2</code> branched from <code>main</code> (as the parent of &quot;Commit 14&quot; is also &quot;Commit 12&quot;).</p><p>The <code>*</code> symbol tells us which branch a certain commit is &quot;on&quot;, so you can know for sure that &quot;Commit 13&quot; is on <code>feature_branch_1</code>, and not <code>feature_branch_2</code>.</p><h4 id="pretty-format" tabindex="-1"><a class="header-anchor" href="#pretty-format"><span><code>--pretty=format</code></span></a></h4><p>The above result is already very useful! Yet, it lacks a few things. We don&#39;t know the author or the time of the commit. These two information details were included in the default output of <code>git log</code> which was very long. Perhaps we can add them in a more compact way?</p><p>By using <code>--pretty=format:</code>, you can display the information of each commit in various ways using <code>printf</code>-style placeholders.</p><p>In the following command, the <code>%s</code>, <code>%an</code> and <code>%cd</code> placeholders are replaced by the commit&#39;s subject (message), author name, and the commit&#39;s date, respectively.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--oneline</span> <span class="token parameter variable">--graph</span> feature_branch_1 feature_branch_2 <span class="token parameter variable">--pretty</span><span class="token operator">=</span>format:<span class="token string">&quot;%s (%an) [%cd]&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The output looks like this:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_9.png" alt="Image" loading="lazy"> <em><code>git log --oneline --graph feature_branch_1 feature_branch_2 --pretty=format:&quot;%s (%an) [%cd]</code></em></p><p>That&#39;s useful, but not really great to look at. We can then use other formatting tricks, specifically <code>%C(color)</code> that will switch the color to <code>color</code>, until reaching a <code>%Creset</code> that resets the color. To make the author name&#39;s yellow, you can use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--oneline</span> <span class="token parameter variable">--graph</span> feature_branch_1 feature_branch_2 <span class="token parameter variable">--pretty</span><span class="token operator">=</span>format:<span class="token string">&quot;%s %C(yellow)(%an)%Creset [%cd]&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_10.png" alt="Image" loading="lazy"> <em><code>git log --oneline --graph feature_branch_1 feature_branch_2 --pretty=format:&quot;%s %C(yellow)(%an)%Creset [%cd]&quot;</code></em></p><p>For some colors, like <code>red</code> or <code>green</code>, it is unnecessary to include the parenthesis, so <code>Cred</code> is enough.</p><h4 id="how-is-git-lol-structured" tabindex="-1"><a class="header-anchor" href="#how-is-git-lol-structured"><span>How is <code>git lol</code> Structured?</span></a></h4><p>When I run <code>git lol</code>, it actually executes the following:</p><p><code>git log --graph --pretty=format:&#39;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#39; --abbrev-commit</code></p><p>Can you take this bit by bit?</p><p>You already know <code>--graph</code>, which makes the output include an ASCII graph.</p><p><code>--abbrev-commit</code> uses a short prefix from the full SHA-1 of the commit (in my configuration, the first seven characters).</p><p>The rest is just coloring of various details about the commit:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> lol <span class="token parameter variable">--all</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_11.png" alt="Image" loading="lazy"> <em><code>git lol --all</code></em></p><p>I like this output because I find it clear. It gives me the information I need, with enough coloring so that every detail stands out without hurting my eyes. But if you prefer other information, other colors, a different order, or anything else - go ahead and tweak it to your liking.</p><h3 id="setting-an-alias" tabindex="-1"><a class="header-anchor" href="#setting-an-alias"><span>Setting an alias</span></a></h3><p>As you know, I set <code>git lol</code> as an alias - that is, when I run <code>git lol</code>, it executes the long command I provided previously.</p><p>How can you create an alias in Git?</p><p>The easiest way is to use <code>git alias</code>, like so:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> alias.co checkout</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>This command sets <code>co</code> to be an alias for the command <code>checkout</code>, so you can use <code>git co main</code> instead of <code>git checkout main</code>.</p><p>To define <code>git lol</code> as an alias, you can use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> alias.lol <span class="token string">&#39;log --graph --pretty=format:&#39;</span>%Cred%h%Creset -%C<span class="token punctuation">(</span>yellow<span class="token punctuation">)</span>%d%Creset %s %Cgreen<span class="token punctuation">(</span>%cr<span class="token punctuation">)</span> %C<span class="token punctuation">(</span>bold blue<span class="token punctuation">)</span><span class="token operator">&lt;</span>%an<span class="token operator">&gt;</span>%Creset<span class="token string">&#39; --abbrev-commit&#39;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><hr><h2 id="chapter-13-git-bisect" tabindex="-1"><a class="header-anchor" href="#chapter-13-git-bisect"><span>Chapter 13 - Git Bisect</span></a></h2><p>Oops.</p><p>I have a bug.</p><p>Yes, that happens some times, to all of us. Something in my system is broken, and I can&#39;t tell why. I have been debugging for a while, but the solution is not clear.</p><p>I can tell that two weeks ago, this didn&#39;t happen. Luckily for me, I have been using Git (obviously, I know...), so I can go back in time and test a past version of my code. Indeed, in this version - everything worked fine.</p><p>But... I have made many changes in these two weeks. Alas, not just me - my entire team has contributed commits that add, delete, or modify parts of the code base. Where do I begin? Should I go over every change introduced in those two weeks?</p><p>Enter - <code>git bisect</code>.</p><p>The goal of <code>git bisect</code> is help you find the commit where a bug was introduced, in an effective manner.</p><h3 id="how-does-git-bisect-work" tabindex="-1"><a class="header-anchor" href="#how-does-git-bisect-work"><span>How Does <code>git bisect</code> Work?</span></a></h3><p><code>git bisect</code> first asks you to mark one commit as &quot;bad&quot; (where the bug occurs), and another commit as &quot;good&quot; (one without the bug). Then, it checks out a commit halfway between these two commits, and then asks you to identify the commit as either &quot;good&quot; or &quot;bad&quot;. This process is repeated until you find the first &quot;bad&quot; commit.</p><p>The key here is using binary search - by looking at the halfway point and deciding if it is the new top or bottom of the list of commits, you can find the right commit efficiently. Even if you have 10,000 commits to hunt through, it only takes a maximum of 13 steps to find the first commit that introduced the bug.</p><h3 id="git-bisect-example" tabindex="-1"><a class="header-anchor" href="#git-bisect-example"><span><code>git bisect</code> Example</span></a></h3><p>For this example, I will use the repository on <a href="https://github.com/Omerr/bisect-exercise.git" target="_blank" rel="noopener noreferrer">https://github.com/Omerr/bisect-exercise.git</a>. To create it, I adapted the open source repository <a href="https://github.com/bast/git-bisect-exercise" target="_blank" rel="noopener noreferrer">https://github.com/bast/git-bisect-exercise</a> (according to its license).</p><p>In this repository, we have a single python file that is used to compute the value of pi (which is approximately <code>3.14</code>). If you run <code>python3 get_pi.py</code> on <code>main</code>, however, you will get a wrong result:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/bisect_1.png" alt="A wrong result, we have a bug" loading="lazy"> <em>A wrong result, we have a bug</em></p><p>This branch consists of more than 500 commits.</p><p>Find the first commit on this branch by using:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> log <span class="token parameter variable">--oneline</span> <span class="token operator">|</span> <span class="token function">tail</span> <span class="token parameter variable">-n</span> <span class="token number">1</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/bisect_2.png" alt="Image" loading="lazy"> <em><code>git log --oneline | tail -n 1</code></em></p><p>If you <code>checkout</code> to this commit and run <code>python3 get_pi.py</code> again, the result is correct:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_1_pi.png" alt="From the first commit, the result is valid" loading="lazy"> <em>From the first commit, the result is valid</em></p><p>So somewhere between <code>HEAD</code> and commit <code>f0ea950</code>, a change was introduced that resulted in this wrong output.</p><p>To find it using <code>git bisect</code>, <code>start</code> the bisect process, and mark this commit as &quot;good&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> bisect start</span>
<span class="line"><span class="token function">git</span> bisect good</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>By default, <code>git bisect good</code> would take <code>HEAD</code> as the &quot;good&quot; commit. To mark <code>main</code> as &quot;bad&quot;, you can use <code>git bisect bad main</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/bisect_3.png" alt="Image" loading="lazy"> <em><code>git bisect bad main</code></em></p><p><code>git bisect</code> checked out commit number <code>251</code>, the &quot;middle point&quot; of <code>main</code> branch. Does the state in this commit produce the right or wrong output?</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/bisect_4.png" alt="Trying again..." loading="lazy"> <em>Trying again...</em></p><p>We still get the wrong output, which means we can discard commits <code>252</code> through <code>500</code> (and additional commits after that), and narrow our search to commits <code>2</code> through <code>251</code>. Mark this as <code>bad</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/bisect_5.png" alt="Mark as " loading="lazy"> <em>Mark as <code>bad</code></em></p><p><code>git bisect</code> checked out the &quot;middle&quot; commit (number <code>126</code>), and running the code again results in the right answer! This means that this commit is &quot;good&quot;, and that the first &quot;bad&quot; commit is somewhere between <code>127</code> and <code>251</code>. Mark it as &quot;good&quot;:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/bisect_6.png" alt="Mark as " loading="lazy"> <em>Mark as <code>good</code></em></p><p>Nice, <code>git bisect</code> takes us to commit <code>188</code>, as this is the &quot;middle&quot; commit between <code>127</code> and <code>251</code>. By running the code again, you can see that the result is wrong, so this is actually a &quot;bad&quot; commit, which means the first faulty commit is somewhere between <code>127</code> and <code>188</code>. As you can see, <code>git bisect</code> narrows down the search space by half on each iteration.</p><p>Come on, now it&#39;s your turn - keep going from here! Test the result of <code>python3 get_pi.py</code> and use <code>git bisect good</code> or <code>git bisect bad</code> to mark the commit accordingly. What is the faulty commit?</p><p>When you are done, use <code>git bisect reset</code> to stop the bisect process.</p><h3 id="automatic-git-bisect" tabindex="-1"><a class="header-anchor" href="#automatic-git-bisect"><span>Automatic <code>git bisect</code></span></a></h3><p>In the previous example, you could simply run <code>python3 get_pi.py</code> and check the result. Other times, the process of validating whether a certain commit is &quot;good&quot; or &quot;bad&quot; can be tricky, error prone, or just time consuming.</p><p>It is possible to automate the process of <code>git bisect</code> by creating code that would be executed on each iteration, returning <code>0</code> when the current commit is &quot;good&quot;, and a value between <code>1-127</code> (inclusive), except <code>125</code>, if it should be considered &quot;bad&quot;.</p><p>The syntax is:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> bisect run my_script arguments</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>As this book is not about programming and doesn&#39;t assume you know a specific programming language, I will not show an example of implementing <code>my_script</code>. The <code>README.md</code> file in the repository used in this chapter (<a href="https://github.com/Omerr/bisect-exercise.git" target="_blank" rel="noopener noreferrer">https://github.com/Omerr/bisect-exercise.git</a>) includes an example for a script that you can run with <code>git bisect run</code> to automatically find the faulty commit for the previous example.</p><hr><h2 id="chapter-14-other-useful-commands" tabindex="-1"><a class="header-anchor" href="#chapter-14-other-useful-commands"><span>Chapter 14 - Other Useful Commands</span></a></h2><p>This chapter highlights a few commands that had have already been mentioned in previous chapters. I am putting them here together so that you can come back to them as a reference when needed.</p><h3 id="git-cherry-pick" tabindex="-1"><a class="header-anchor" href="#git-cherry-pick"><span><code>git cherry-pick</code></span></a></h3><p>Introduced in <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a>, this command takes a given commit, computes the <strong>patch</strong> this commit introduces by computing the difference between the parent&#39;s commit and the commit itself, and then <code>cherry-pick</code> &quot;replays&quot; this difference. It is like &quot;copy-pasting&quot; a commit, that is, the diff this commit introduced.</p><p>In <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a> we considered the difference introduced by &quot;Commit 5&quot; (using <code>git diff main &lt;SHA_OF_COMMIT_5&gt;</code>):</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_diff_main_commit_5-1.png" alt="Running  to observe the patch introduced by &quot;Commit 5&quot;" loading="lazy"> <em>Running <code>git diff</code> to observe the patch introduced by &quot;Commit 5&quot;</em></p><p>You can see that in this commit, John started working on a song called &quot;Lucy in the Sky with Diamonds&quot;:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_diff_main_commit_5_output-1.png" alt="The output of  - the patch introduced by &quot;Commit 5&quot;" loading="lazy"> <em>The output of <code>git diff</code> - the patch introduced by &quot;Commit 5&quot;</em></p><p>As a reminder, you can also use the command <code>git show</code> to get the same output:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> show <span class="token operator">&lt;</span>SHA_OF_COMMIT_<span class="token operator"><span class="token file-descriptor important">5</span>&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Now, if you <code>cherry-pick</code> this commit, you will introduce <em>this change</em> specifically, on the active branch. You can switch to <code>main</code> branch:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> checkout main <span class="token punctuation">(</span>or <span class="token function">git</span> switch main<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>And create another branch:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> checkout <span class="token parameter variable">-b</span> my_branch <span class="token punctuation">(</span>or <span class="token function">git</span> switch <span class="token parameter variable">-c</span> my_branch<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/create_my_branch-1.png" alt="Creating  that branches from " loading="lazy"> <em>Creating <code>my_branch</code> that branches from <code>main</code></em></p><p>Next, <code>cherry-pick</code> &quot;Commit 5&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> cherry-pick <span class="token operator">&lt;</span>SHA_OF_COMMIT_<span class="token operator"><span class="token file-descriptor important">5</span>&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/cherry_pick_commit_5-1.png" alt="Using  to apply the changes introduced in &quot;Commit 5&quot; onto " loading="lazy"> <em>Using <code>cherry-pick</code> to apply the changes introduced in &quot;Commit 5&quot; onto <code>main</code></em></p><p>Consider the log (output of <code>git lol</code>):</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_lol_commit_5-1.png" alt="The output of " loading="lazy"> <em>The output of <code>git lol</code></em></p><p>It seems like you <em>copy-pasted</em> &quot;Commit 5&quot;. Remember that even though it has the same commit message, and introduces the same changes, and even points to the same tree object as the original &quot;Commit 5&quot; in this case - it is still a different commit object, as it was created with a different timestamp.</p><p>Looking at the changes, using <code>git show HEAD</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_show_HEAD-3.png" alt="The output of " loading="lazy"> <em>The output of <code>git show HEAD</code></em></p><p>They are the same as &quot;Commit 5&quot;&#39;s.</p><h3 id="git-revert" tabindex="-1"><a class="header-anchor" href="#git-revert"><span><code>git revert</code></span></a></h3><p><code>git revert</code> is essentially the reverse of <code>git cherry-pick</code>, introduced in <a href="#heading-chapter-10-additional-tools-for-undoing-changes">chapter 10</a>. This command takes the commit you&#39;re providing it with and computes the diff from its parent commit, just like <code>git cherry-pick</code>, but this time, it computes the <em>reverse</em> changes. That is, if in the specified commit you added a line, the reverse would delete the line, and vice versa.</p><h3 id="git-add-p" tabindex="-1"><a class="header-anchor" href="#git-add-p"><span><code>git add -p</code></span></a></h3><p>Staging changes is an integral part of introducing changes to Git. Sometimes, you wish to stage all changes together (with <code>git add .</code>), or perhaps stage all changes of a specific file (using <code>git add &lt;file_path&gt;</code>). Yet there are times where it would be convenient to stage only certain parts of modified files.</p><p>In <a href="https://freecodecamp.org/news/p/f7b355ea-3f22-4613-8218-e95c67779d9f/chapter-6-diffs-and-patches" target="_blank" rel="noopener noreferrer">chapter 6</a>, we introduced <code>git add -p</code>. This command allows you to stage certain parts of files, by splitting them into hunks (<code>p</code> stands for <code>patch</code>). For example, say you have this file, <code>my_file.py</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/my_file_py_1.png" alt="Image" loading="lazy"> <em><code>my_file.py</code></em></p><p>You then modify this file - by changing text within <code>function_1</code>, and also adding a new function, <code>function_5</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/my_file_py_2.png" alt=" after the changes" loading="lazy"> <em><code>my_file.py</code> after the changes</em></p><p>If you used <code>git add my_file.py</code> at this point, you would stage both of these changes together. In case you want to separate them into different commits, you could use <code>git add -p</code>, which splits these two changes and asks you about each one as a standalone hunk:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/add_p_1.png" alt="Image" loading="lazy"> <em><code>git add -p</code></em></p><p>By typing <code>?</code>, you can see what the different options stand for:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/add_p_2.png" alt="Using a  to get a description of the different options" loading="lazy"> <em>Using a <code>?</code> to get a description of the different options</em></p><p>In this case, say we only want to stage the change introducing <code>function_5</code>. We do not want to stage the change of <code>function_1</code>, so we select <code>n</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/add_p_3.png" alt="Not staging the change to " loading="lazy"> <em>Not staging the change to <code>function_1</code></em></p><p>Next, we are prompted for the second change - the one introducing <code>function_5</code>. We want to stage this hunk indeed, to can do so we can type <code>y</code>.</p>`,198))])}const x=l(b,[["render",k],["__file","part-4-amazing-and-useful-git-tools.html.vue"]]),T=JSON.parse('{"path":"/freecodecamp.org/gitting-things-done-book/part-4-amazing-and-useful-git-tools.html","title":"Part 4 - Amazing and Useful Git Tools","lang":"en-US","frontmatter":{"lang":"en-US","title":"Part 4 - Amazing and Useful Git Tools","description":"Article(s) > (5/6) Gitting Things Done - A Visual and Practical Guide to Git [Full Book]","category":["Git","Article(s)"],"tag":["blog","freecodecamp.org","git"],"head":[[{"meta":null},{"property":"og:title","content":"Article(s) > (5/6) Gitting Things Done - A Visual and Practical Guide to Git [Full Book]"},{"property":"og:description","content":"Part 4 - Amazing and Useful Git Tools"},{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/gitting-things-done-book/part-4-amazing-and-useful-git-tools.html"}],["meta",{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/gitting-things-done-book/part-4-amazing-and-useful-git-tools.html"}],["meta",{"property":"og:site_name","content":"📚Bookshelf"}],["meta",{"property":"og:title","content":"Part 4 - Amazing and Useful Git Tools"}],["meta",{"property":"og:description","content":"Article(s) > (5/6) Gitting Things Done - A Visual and Practical Guide to Git [Full Book]"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:image:src","content":"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png"}],["meta",{"name":"twitter:image:alt","content":"Part 4 - Amazing and Useful Git Tools"}],["meta",{"property":"article:author","content":"Omer Rosenbaum"}],["meta",{"property":"article:tag","content":"blog"}],["meta",{"property":"article:tag","content":"freecodecamp.org"}],["meta",{"property":"article:tag","content":"git"}],["meta",{"property":"article:published_time","content":"2024-01-09T00:00:00.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Part 4 - Amazing and Useful Git Tools\\",\\"image\\":[\\"https://freecodecamp.org/news/content/images/2023/12/git_log_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_2-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_1-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_5.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_6.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_7.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_8.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_9.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_10.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_11.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/bisect_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/bisect_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_1_pi.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/bisect_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/bisect_4.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/bisect_5.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/bisect_6.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_diff_main_commit_5-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_diff_main_commit_5_output-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/create_my_branch-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/cherry_pick_commit_5-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_lol_commit_5-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_show_HEAD-3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/my_file_py_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/my_file_py_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/add_p_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/add_p_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/add_p_3.png\\"],\\"datePublished\\":\\"2024-01-09T00:00:00.000Z\\",\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Omer Rosenbaum\\",\\"url\\":\\"https://freecodecamp.org/news/author/omerros/\\"}]}"]],"date":"2024-01-09T00:00:00.000Z","isOriginal":false,"author":[{"name":"Omer Rosenbaum","url":"https://freecodecamp.org/news/author/omerros/"}],"cover":"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png","gitInclude":[]},"headers":[{"level":2,"title":"Chapter 12 - Git Log","slug":"chapter-12-git-log","link":"#chapter-12-git-log","children":[{"level":3,"title":"Filtering Commits","slug":"filtering-commits","link":"#filtering-commits","children":[]},{"level":3,"title":"Formatting Log","slug":"formatting-log","link":"#formatting-log","children":[]},{"level":3,"title":"Setting an alias","slug":"setting-an-alias","link":"#setting-an-alias","children":[]}]},{"level":2,"title":"Chapter 13 - Git Bisect","slug":"chapter-13-git-bisect","link":"#chapter-13-git-bisect","children":[{"level":3,"title":"How Does git bisect Work?","slug":"how-does-git-bisect-work","link":"#how-does-git-bisect-work","children":[]},{"level":3,"title":"git bisect Example","slug":"git-bisect-example","link":"#git-bisect-example","children":[]},{"level":3,"title":"Automatic git bisect","slug":"automatic-git-bisect","link":"#automatic-git-bisect","children":[]}]},{"level":2,"title":"Chapter 14 - Other Useful Commands","slug":"chapter-14-other-useful-commands","link":"#chapter-14-other-useful-commands","children":[{"level":3,"title":"git cherry-pick","slug":"git-cherry-pick","link":"#git-cherry-pick","children":[]},{"level":3,"title":"git revert","slug":"git-revert","link":"#git-revert","children":[]},{"level":3,"title":"git add -p","slug":"git-add-p","link":"#git-add-p","children":[]}]}],"readingTime":{"minutes":13.87,"words":4162},"filePathRelative":"freecodecamp.org/gitting-things-done-book/part-4-amazing-and-useful-git-tools.md","localizedDate":"January 9, 2024","excerpt":"\\n","copyright":{"author":"Omer Rosenbaum"}}');export{x as comp,T as data};
