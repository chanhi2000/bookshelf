import{_ as u}from"./plugin-vue_export-helper-DlAUqK2U.js";import{aj as m,am as i,as as y,ao as n,at as g,au as b,ap as a,al as r,an as t,aq as l,ar as f}from"./app-DpiNAgkx.js";const v={},w={id:"frontmatter-title-관련",tabindex:"-1"},k={class:"header-anchor",href:"#frontmatter-title-관련"},E={class:"table-of-contents"},T={href:"https://en.wikipedia.org/wiki/Datalog",target:"_blank",rel:"noopener noreferrer"},N={href:"https://geeksforgeeks.org/postgresql/postgresql-serial/",target:"_blank",rel:"noopener noreferrer"},L={href:"https://postgresql.org/message-id/49DCDA27.4090901@megafon.hr",target:"_blank",rel:"noopener noreferrer"},C={href:"https://ibm.com/docs/en/db2/11.5.x?topic=concepts-primary-key-referential-integrity-check-unique-constraints",target:"_blank",rel:"noopener noreferrer"},R={href:"https://jcsites.juniata.edu/faculty/rhodes/dbms/eermodel.htm",target:"_blank",rel:"noopener noreferrer"},I={href:"https://softwareengineering.stackexchange.com/questions/227832/single-table-w-extra-columns-vs-multiple-tables-which-duplicate-schema?utm_source=chatgpt.com",target:"_blank",rel:"noopener noreferrer"},D={href:"https://gist.github.com/cardstdani/1247573e1ef2f6ea9ab99b82c5761ad6",target:"_blank",rel:"noopener noreferrer"},S={href:"https://medium.com/@tharinduimalka915/how-kahns-algorithm-helped-me-solve-database-schema-dependencies-2b7e54142fd5",target:"_blank",rel:"noopener noreferrer"},A={href:"https://en.wikipedia.org/wiki/Topological_sorting",target:"_blank",rel:"noopener noreferrer"},P={href:"https://softwareengineering.stackexchange.com/questions/359107/resolving-foreign-keys-breaking-cycles-to-enable-a-topological-sort",target:"_blank",rel:"noopener noreferrer"},F={href:"https://postgresql.org/docs/current/ddl-priv.html",target:"_blank",rel:"noopener noreferrer"},q={href:"https://stackoverflow.com/questions/50883645/is-postgres-a-default-and-special-user-of-postgresql",target:"_blank",rel:"noopener noreferrer"};function O(c,e){const d=l("VPCard"),s=l("router-link"),h=l("SiteInfo"),o=l("FontIcon"),p=l("RouteLink");return f(),m("div",null,[i("h1",w,[i("a",k,[i("span",null,y(c.$frontmatter.title)+" 관련",1)])]),n(d,g(b({title:"How to Design Structured Database Systems Using SQL [Full Book]",desc:"This book will guide you, step-by-step, through designing a relational database using SQL. SQL is one of the most recognized relational languages for managing and querying data in databases. You’ll learn the fundamental concepts related to both data ...",link:"/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/README.md",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",background:"rgba(10,10,35,0.2)"})),null,16),i("nav",E,[i("ul",null,[i("li",null,[n(s,{to:"#database-levels"},{default:a(()=>e[0]||(e[0]=[t("Database Levels")])),_:1})]),i("li",null,[n(s,{to:"#the-database-design-process"},{default:a(()=>e[1]||(e[1]=[t("The Database Design Process")])),_:1})]),i("li",null,[n(s,{to:"#entity-relationship-to-logical-model"},{default:a(()=>e[2]||(e[2]=[t("Entity-Relationship to Logical Model")])),_:1}),i("ul",null,[i("li",null,[n(s,{to:"#person-entity"},{default:a(()=>e[3]||(e[3]=[t("Person entity")])),_:1})]),i("li",null,[n(s,{to:"#rental-entity"},{default:a(()=>e[4]||(e[4]=[t("Rental entity")])),_:1})]),i("li",null,[n(s,{to:"#carownership-entity"},{default:a(()=>e[5]||(e[5]=[t("CarOwnership entity")])),_:1})]),i("li",null,[n(s,{to:"#residence-entity"},{default:a(()=>e[6]||(e[6]=[t("Residence entity")])),_:1})]),i("li",null,[n(s,{to:"#shipassignment-entity"},{default:a(()=>e[7]||(e[7]=[t("ShipAssignment entity")])),_:1})]),i("li",null,[n(s,{to:"#city-entity"},{default:a(()=>e[8]||(e[8]=[t("City entity")])),_:1})]),i("li",null,[n(s,{to:"#port-entity"},{default:a(()=>e[9]||(e[9]=[t("Port entity")])),_:1})]),i("li",null,[n(s,{to:"#cruiseline-entity"},{default:a(()=>e[10]||(e[10]=[t("CruiseLine entity")])),_:1})]),i("li",null,[n(s,{to:"#vehicle-entity"},{default:a(()=>e[11]||(e[11]=[t("Vehicle entity")])),_:1})]),i("li",null,[n(s,{to:"#how-is-this-is-a-hierarchy-implemented-with-tables"},{default:a(()=>e[12]||(e[12]=[t("How is this IS-A hierarchy implemented with tables?")])),_:1})]),i("li",null,[n(s,{to:"#cruiseship-entity"},{default:a(()=>e[13]||(e[13]=[t("CruiseShip entity")])),_:1})]),i("li",null,[n(s,{to:"#bike-entity"},{default:a(()=>e[14]||(e[14]=[t("Bike entity")])),_:1})]),i("li",null,[n(s,{to:"#car-entity"},{default:a(()=>e[15]||(e[15]=[t("Car entity")])),_:1})]),i("li",null,[n(s,{to:"#citybus-entity"},{default:a(()=>e[16]||(e[16]=[t("CityBus entity")])),_:1})]),i("li",null,[n(s,{to:"#carregistration-entity"},{default:a(()=>e[17]||(e[17]=[t("CarRegistration entity")])),_:1})]),i("li",null,[n(s,{to:"#drivinglicenserequest-entity"},{default:a(()=>e[18]||(e[18]=[t("DrivingLicenseRequest entity")])),_:1})]),i("li",null,[n(s,{to:"#how-can-we-model-the-driving-license-requests-hierarchy-at-the-logical-level"},{default:a(()=>e[19]||(e[19]=[t("How can we model the driving license requests hierarchy at the logical level?")])),_:1})]),i("li",null,[n(s,{to:"#rejecteddrivinglicense-entity"},{default:a(()=>e[20]||(e[20]=[t("RejectedDrivingLicense entity")])),_:1})]),i("li",null,[n(s,{to:"#drivinglicense-entity"},{default:a(()=>e[21]||(e[21]=[t("DrivingLicense entity")])),_:1})]),i("li",null,[n(s,{to:"#bustrip-entity"},{default:a(()=>e[22]||(e[22]=[t("BusTrip entity")])),_:1})]),i("li",null,[n(s,{to:"#busticket-entity"},{default:a(()=>e[23]||(e[23]=[t("BusTicket entity")])),_:1})]),i("li",null,[n(s,{to:"#buspass-entity"},{default:a(()=>e[24]||(e[24]=[t("BusPass entity")])),_:1})]),i("li",null,[n(s,{to:"#voyage-entity"},{default:a(()=>e[25]||(e[25]=[t("Voyage entity")])),_:1})]),i("li",null,[n(s,{to:"#cruisebooking-entity"},{default:a(()=>e[26]||(e[26]=[t("CruiseBooking entity")])),_:1})]),i("li",null,[n(s,{to:"#pool-entity"},{default:a(()=>e[27]||(e[27]=[t("Pool entity")])),_:1})]),i("li",null,[n(s,{to:"#how-can-we-translate-the-pool-hierarchy-entities-at-the-conceptual-level-to-tables"},{default:a(()=>e[28]||(e[28]=[t("How can we translate the pool hierarchy entities at the conceptual level to tables?")])),_:1})]),i("li",null,[n(s,{to:"#cruisepool-entity"},{default:a(()=>e[29]||(e[29]=[t("CruisePool entity")])),_:1})]),i("li",null,[n(s,{to:"#citypool-entity"},{default:a(()=>e[30]||(e[30]=[t("CityPool entity")])),_:1})]),i("li",null,[n(s,{to:"#olympicpool-entity"},{default:a(()=>e[31]||(e[31]=[t("OlympicPool entity")])),_:1})]),i("li",null,[n(s,{to:"#entry-entity"},{default:a(()=>e[32]||(e[32]=[t("Entry entity")])),_:1})]),i("li",null,[n(s,{to:"#team-entity"},{default:a(()=>e[33]||(e[33]=[t("Team entity")])),_:1})]),i("li",null,[n(s,{to:"#membership-entity"},{default:a(()=>e[34]||(e[34]=[t("Membership entity")])),_:1})]),i("li",null,[n(s,{to:"#participation-entity"},{default:a(()=>e[35]||(e[35]=[t("Participation entity")])),_:1})]),i("li",null,[n(s,{to:"#swimmingcompetition-entity"},{default:a(()=>e[36]||(e[36]=[t("SwimmingCompetition entity")])),_:1})]),i("li",null,[n(s,{to:"#sanction-entity"},{default:a(()=>e[37]||(e[37]=[t("Sanction entity")])),_:1})]),i("li",null,[n(s,{to:"#how-is-the-is-a-hierarchy-implemented-with-tables"},{default:a(()=>e[38]||(e[38]=[t("How is the IS-A hierarchy implemented with tables?")])),_:1})]),i("li",null,[n(s,{to:"#drivingsanction-entity"},{default:a(()=>e[39]||(e[39]=[t("DrivingSanction entity")])),_:1})]),i("li",null,[n(s,{to:"#sportsanction-entity"},{default:a(()=>e[40]||(e[40]=[t("SportSanction entity")])),_:1})]),i("li",null,[n(s,{to:"#poolsanction-entity"},{default:a(()=>e[41]||(e[41]=[t("PoolSanction entity")])),_:1})])])]),i("li",null,[n(s,{to:"#how-to-create-the-database"},{default:a(()=>e[42]||(e[42]=[t("How to Create the Database")])),_:1})])])]),e[115]||(e[115]=i("hr",null,null,-1)),n(h,{name:"How to Design Structured Database Systems Using SQL [Full Book]",desc:"This book will guide you, step-by-step, through designing a relational database using SQL. SQL is one of the most recognized relational languages for managing and querying data in databases. You’ll learn the fundamental concepts related to both data ...",url:"https://freecodecamp.org/news/how-to-design-structured-database-systems-using-sql-full-book#heading-chapter-10-database-design-process-example",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",preview:"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png"}),e[116]||(e[116]=r(`<p>So far, you’ve learned about the entire relational model and some basic SQL. Now you can create a relational database on the PostgreSQL DBMS, manage it, and perform queries on it. So let’s apply all this knowledge to a real-world use case.</p><hr><h2 id="database-levels" tabindex="-1"><a class="header-anchor" href="#database-levels"><span>Database Levels</span></a></h2><p>To do this, we need to remember some of the different levels of the database design process. First, we have the <strong>analysis</strong> phase where we gather the project requirements from the end user or client. Then we create a <strong>conceptual</strong> design, which we subsequently transform into a <strong>logical</strong> design that we can implement on a DBMS.</p><p>These are the main levels we need to worry about here. But in addition to these, we have the <strong>physical</strong> level, which focuses on the internal representation of the logical model implementation of the database in the DBMS using DBMS objects like indexes. We also have the <strong>storage</strong> level, which is the closest to the hardware, and is mainly dedicated to organizing the disk files that implement the database functionality on the DBMS. Lastly, we also have the <strong>application</strong> design level that aims to provide the database as a service to the user.</p><p>We won’t cover these additional levels in this example due to their complexity and because they aren’t as closely related to the actual database design.</p><hr><h2 id="the-database-design-process" tabindex="-1"><a class="header-anchor" href="#the-database-design-process"><span>The Database Design Process</span></a></h2><p>When faced with a real problem that requires designing a database, the first thing we need to do is gather as much information as possible from the user or client. We do this to formalize the requirements of the system we’re going to build.</p><p>We can interview the client, survey potential users of the service, or use other similar methods. In this case, we won’t directly perform any of these tasks. Instead, we’ll assume that we have certain requirements, and from them we’ve been able to construct an <strong>entity-relationship</strong> diagram that captures them and correctly models the domain of our system. Let’s say it looks like this:</p><figure><img src="https://cdn.hashnode.com/res/hashnode/image/upload/v1754234568579/a4165e66-85f1-4a81-b85d-a2be8ea19db3.jpeg" alt="Entity-relationship diagram that we will work with in this chapter. It represents various entities and relationships, where entities include &quot;Vehicle,&quot; &quot;Person,&quot; &quot;Car,&quot; &quot;Pool,&quot; &quot;City,&quot; and others." tabindex="0" loading="lazy"><figcaption>Entity-relationship diagram that we will work with in this chapter. It represents various entities and relationships, where entities include &quot;Vehicle,&quot; &quot;Person,&quot; &quot;Car,&quot; &quot;Pool,&quot; &quot;City,&quot; and others.</figcaption></figure><p>As you can see from the diagram above (you can enlarge it by opening it in a new tab), the project we’ll work on in this example is an extension of the bike rental domain we’ve used so far.</p><p>In addition to a bike rental service, we’ll include other elements that may be present in a real world database model, such as vehicles, places, cities, and so on. We’ll also include actions that can be performed between these elements, like owning a car, residing in a city, booking a cruise trip, or getting a bus pass, among others.</p><p>When we’re building this diagram, our most significant decisions involve which concepts are modeled with entities, which are represented through relationships, and which aren’t worth including in our system.</p><p>From the entire domain, it&#39;s common to encounter a lot of information provided by the client or users that doesn&#39;t directly help us model the system, as they don’t expect it to be stored in the database. So all concepts related to information that is not intended to be stored <strong>persistently</strong> are usually not included in the design.</p><p>As for the other issues, they are very subjective, and there is no set of rules to follow to know unequivocally which concepts to model with <strong>entities</strong> or <strong>relationships</strong> - or even to determine the <strong>degree</strong> of these relationships (which in this context we will assume is always 2 to avoid complicating the design with relationships involving more than two classes).</p><hr><h2 id="entity-relationship-to-logical-model" tabindex="-1"><a class="header-anchor" href="#entity-relationship-to-logical-model"><span>Entity-Relationship to Logical Model</span></a></h2><p>But to understand how we can and should make these design decisions, it&#39;s useful to understand the <strong>purpose</strong> of each entity in this entity-relationship diagram, as well as the meaning of the elements it comprises or relates to. We also need to understand how it’s been translated to the logical design level.</p><p>Before explaining each of the entities, below is the relational diagram we have after the entire logical design phase:</p><figure><img src="https://cdn.hashnode.com/res/hashnode/image/upload/v1753355497082/c7f6079b-a88b-4651-a92b-36761151aa80.png" alt="Relational diagram derived from the previous entity-relationship one." tabindex="0" loading="lazy"><figcaption>Relational diagram derived from the previous entity-relationship one.</figcaption></figure><p>This diagram is what we will gradually build as we transform entities into tables. Make sure you have both this diagram and the entity-relationship diagram open in separate windows so you can refer to them during the following chapters. This will make everything we discuss easier to understand.</p><p>As you can see in the diagram above, it includes some modifications like foreign keys pointing to &quot;loose&quot; attributes such as <code>Sanction.SanctionID</code>, instead of the same attribute in the table of the diagram. This aims to prevent the foreign key arrows from crossing excessively. Although this isn’t a standard way to represent the relational logical model, as long as its meaning is specified it’s completely valid.</p><p>Some constraints aren’t modeled in the system due to their complexity, which we’ll see as we explain all the entities. That&#39;s why there are no notes included in the relational diagram, and we don’t indicate the attributes that can or can’t be NULL. These are helpful to show in the diagram, but it’s not required.</p><p>Lastly, during the explanations, we’ll show the SQL code used to create each table. You’ll find the SQL script for creating the entire database at the very end, after I’ve explained all the entities. This is because we’re not going to discuss them in the order they need to be created, in order to respect referential integrity constraints that would cause errors in the DBMS if tables were created in a different order.</p><h3 id="person-entity" tabindex="-1"><a class="header-anchor" href="#person-entity"><span><code>Person</code> entity</span></a></h3><p>First, we have the entity <code>Person</code>, whose main goal is to model the existence of people in our system. It&#39;s important to note that in our domain, there are physical people, where each one is a physical entity that we can abstract through the concept of a person, which has a set of associated characteristics. In other words, even though there are many different people, they all share a set of characteristics that define them as people.</p><p>These characteristics are what we’ll model as the attributes of the <code>Person</code> entity. These can then be &quot;instantiated,&quot; as we saw earlier, resulting in a set of entity occurrences - or in other words, specific people defined by the values of their characteristics or attributes.</p><p>To better understand this, we can translate this entity to the logical design level, where, being a single entity, we model it with a single table named <code>Person</code> with the corresponding attributes and data types that match the characteristics of people. In this way, the table schema will be the structure that defines &quot;all people,&quot; like a template, while the specific people whose information we want to store in the system correspond to the tuples of the table, which will be inserted as we register people in the system.</p><p>For the attributes of the entity, we’ll include those that need to be stored persistently, such as name, date of birth, email, and so on. Among all of them, we choose <code>PersonID</code> as the <strong>primary key</strong>, which we assume holds the person&#39;s government ID. But to illustrate the concept of <strong>surrogate key</strong> in SQL, in the implementation on the DBMS, we’ll implement the <code>PersonID</code> attribute as a surrogate key instead of the person&#39;s actual ID (since both can uniquely identify each person). So each tuple in <code>Person</code> will have a unique and distinct value in that attribute, serving as a superkey, candidate key, and ultimately being selected as the primary key.</p><p>In addition to the attributes represented in the entity-relationship diagram, the table we use to model the <code>Person</code> entity has other attributes that help implement associations with other entities, specifically foreign keys.</p><p>If we look only at the entity-relationship diagram, we will see a series of associations that &quot;leave&quot; or &quot;enter&quot; the Person entity. In other words, all the relationships this entity has are <code>1-*</code>, which means the maximum cardinalities on both sides are <code>1</code> and <code>*</code>, respectively. These maximum cardinalities tell us how many occurrences of the entities can be related to each other. So with this information, we can determine where to place the foreign keys and which attributes they should reference from specific entities.</p><p>In the case of Person, we have 12 associations with such multiplicities, of which only one is a relationship where the <strong>&quot;many&quot;</strong> side (the side with the maximum cardinality <code>*</code>) is in the Person entity itself. This means that to implement the association between Person and <strong>CruiseLine</strong>, for example, at the logical level, there should be a foreign key on the many side pointing to the entity on the 1 side. Otherwise, if we place the foreign key in CruiseLine and have it reference Person, its attribute could contain an arbitrary number of references to people, leading to the appearance of a repeating group.</p><p>On the other hand, the other 11 associations have the <strong>&quot;1 side&quot;</strong> in Person, indicating that there are 11 entities that must have a <strong>foreign key</strong> pointing to Person.</p><p>Thus, we know that Person has a foreign key pointing to CruiseLine, even though the attributes that make it up do not appear explicitly in the conceptual diagram. And, since the foreign key has to reference tuples from CruiseLine, it will consist of as many attributes as the primary key of CruiseLine, with the same data types, respectively.</p><p>This happens because the foreign key must uniquely reference tuples. So the values of the foreign key attributes should allow us to go to the CruiseLine table, look at the columns of its primary key attributes, and easily find the referenced tuple. So the foreign key in Person will have 2 attributes, not just one.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Person (</span>
<span class="line">    PersonID SERIAL PRIMARY KEY,</span>
<span class="line">    Name VARCHAR(32) NOT NULL,</span>
<span class="line">    Birth DATE NOT NULL CHECK (Birth &lt; CURRENT_DATE),</span>
<span class="line">    Email VARCHAR(32) NOT NULL,</span>
<span class="line">    Phone BIGINT NOT NULL CHECK (Phone &gt; 0),</span>
<span class="line">    Nationality VARCHAR(32) NOT NULL,</span>
<span class="line">    NameFK VARCHAR(32),</span>
<span class="line">    FoundationDateFK DATE,</span>
<span class="line">    FOREIGN KEY (NameFK, FoundationDateFK) REFERENCES CruiseLine(Name, FoundationDate)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Furthermore, as you can see in its DDL, the attributes <code>(NameFK, FoundationDateFK)</code> that make up the foreign key don’t have the <code>NOT NULL</code> constraint. This is because the foreign key in Person may not reference any tuple from CruiseLine due to the minimum multiplicity of the association on the CruiseLine side (which, being 0, implies that a person might not be a customer of any cruise line).</p><p>Semantically, this association, implemented with the foreign key, represents the possibility that a person can be a customer of a certain cruise line, where if they aren’t a customer of any, their foreign key will be <code>NULL</code>.</p><p>At the same time, a cruise line does not necessarily have to have any customers, as it can be related to zero people at a minimum, according to the minimum multiplicity on the other side. So with both minimum multiplicities at 0, the association as a whole becomes optional, meaning it may not exist at all, as there is nothing that requires it to exist.</p>`,40)),i("p",null,[e[44]||(e[44]=t("If we look at the relational diagram, to represent this entity or table, it's enough to write it in ")),i("a",T,[n(o,{icon:"fa-brands fa-wikipedia-w"}),e[43]||(e[43]=t("Datalog"))]),e[45]||(e[45]=t(" notation, with its name and attributes. The only thing to keep in mind is that the attributes that make up the primary key are underlined, and those that represent foreign keys each have an arrow coming from them pointing to the corresponding attribute of the primary key of the entity or table they reference."))]),e[117]||(e[117]=i("p",null,"In cases like this where the foreign key is composite, each of its attributes has an arrow pointing to the corresponding attribute of the referenced entity. But the order in which the attributes are written in this diagram is not entirely relevant - meaning we can write them in any order as long as we correctly represent which are primary or foreign keys.",-1)),i("p",null,[e[47]||(e[47]=t("Regarding the DDL, since we will consider ")),e[48]||(e[48]=i("strong",null,"PersonID",-1)),e[49]||(e[49]=t(" as a ")),e[50]||(e[50]=i("strong",null,"surrogate key",-1)),e[51]||(e[51]=t(", we declare it as ")),i("a",N,[n(o,{icon:"fas fa-globe"}),e[46]||(e[46]=t("SERIAL"))]),e[52]||(e[52]=t(" so the column stores ")),e[53]||(e[53]=i("strong",null,"auto-incrementing",-1)),e[54]||(e[54]=t(" values. This way, to uniquely identify each tuple, the attribute will use an integer value that increases by one as tuples are inserted. This allows us to differentiate all of them by that number."))]),e[118]||(e[118]=r(`<p>We’ll specify the primary key with <code>PRIMARY KEY</code>, which we can place directly in the attribute declaration if it’s not composite. We’ll specify the foreign key with <code>FOREIGN KEY</code>, indicating which attributes reference the primary key of CruiseLine.</p><p>The only thing to be careful about is the order of the attributes. Although you can arrange the foreign key in any order in <code>FOREIGN KEY</code>, in <code>REFERENCES</code>, we must ensure that the attributes of the primary key of CruiseLine are in the same order as those of the foreign key in order to be referenced correctly.</p><p>For example, if <code>NameFK</code> should reference <code>Name</code>, then those attributes will occupy the same position in the tuples where we declare the foreign key and the primary key it points to, without needing to appear in a specific position, as long as the correspondence is maintained.</p><p>Now let’s look at what a Person can do.</p><h3 id="rental-entity" tabindex="-1"><a class="header-anchor" href="#rental-entity"><span>Rental entity</span></a></h3><p>In our domain, people can rent bikes, and for each bike rental, we want to store certain information like the time the rental occurred, the duration in hours, the price per hour, and so on. So if we modeled this as an M-N association between <code>Bike</code> and <code>Person</code>, we couldn&#39;t store all this information unless we used an associative entity (which is only valid when the entity itself is weak in identification). But here we prefer to use a surrogate key to uniquely identify the rentals, which avoids making the entity representing them weak in identification.</p><p>This is necessary because each rental requires storing associated information, in addition to the person and the bike involved. So an we’ll introduce an entity that relates to both <code>Bike</code> and <code>Person</code> through <code>1-*</code> associations** (each <code>Rental</code> associates a bike with a person), storing information about that &quot;event.&quot; Then, as it has two associations with the many side in Rental, this entity will have two foreign keys - one to implement each association. One will reference the primary key of the Bike entity and the other the primary key of the <code>Person</code> entity.</p><p>Here, we need to distinguish between both foreign keys, as each is composed of one attribute, unlike the previous case where <code>Person</code> had only one foreign key composed of several attributes. That is, regardless of the attributes that comprise each foreign key, it’s important to distinguish that one aims to uniquely identify a bike while the other uniquely identifies a person.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Rental (</span>
<span class="line">    RentalID SERIAL PRIMARY KEY,</span>
<span class="line">    StartTimestamp TIMESTAMP NOT NULL,</span>
<span class="line">    Duration INT NOT NULL CHECK (Duration &gt;= 0), /*Duration of the rental period in hours*/</span>
<span class="line">    HourPrice DOUBLE PRECISION NOT NULL CHECK (HourPrice &gt;= 0),</span>
<span class="line">    BikeFK INT NOT NULL,</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (BikeFK) REFERENCES Bike(BikeID),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When writing your DDL, the attributes are declared the same as before - the main difference here being that each foreign key has its own <code>FOREIGN KEY</code> constraint, which references the primary key attribute of the corresponding table. This is the case because here, both <code>Bike</code> and Person have primary keys with a single attribute.</p><p>Another important detail to consider is the minimum multiplicity on the Person and <code>Bike</code> sides in the associations of the conceptual diagram, where the 1 side of the associations has a minimum multiplicity of 1. This means that a Rental must always be associated with a person and a bike, so their foreign keys can never be <code>NULL</code>. This is why the <code>NOT NULL</code> constraint is used in the attributes.</p><p>As before, at the conceptual level, we don’t show the attributes that form the foreign keys, as the associations themselves and their cardinalities implicitly indicate the existence of foreign keys. But in the relational diagram, we do show these attributes, where arrows indicate the primary key attributes of other entities to which they point. And, since the entity is not weak in identification, none of the foreign key attributes should be underlined.</p><p>Regarding the other constraints, we don’t allow any attribute to be <code>NULL</code>, as it doesn&#39;t make sense for a timestamp to be null, for example, when it’s precisely the valuable information about a rental that we want to store in the database. The other attributes also have constraints like non-negativity, since the duration or the hourly rate can’t be negative amounts.</p><p>This way, if someone tries to insert negative values for these attributes, the DBMS will automatically know that the inserted data isn’t valid or correct, since the actual numbers for duration and price can never be negative. This implies that the values for those attributes must be positive to be correct.</p><h3 id="carownership-entity" tabindex="-1"><a class="header-anchor" href="#carownership-entity"><span><code>CarOwnership</code> entity</span></a></h3><p>Another entity related to Person in the diagram - that is, representing something else a Person can do - is <code>CarOwnership</code>. This aims to model that people can have cars, whether bought, rented, or leased. For this, we use the same conceptual structure as with <code>Rental</code>, where a person can have multiple cars and a car can belong to many people.</p><p>As before, this implicit <strong><code>N-M</code> association</strong> between <code>Car</code> and <code>Person</code> must store information about the ownership, such as its type, start date, price, and so on. So we’ll use an intermediate entity with <code>1-*</code> associations towards both entities, with the 1 side on them.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE CarOwnershipType AS ENUM(&#39;buy&#39;, &#39;rental&#39;, &#39;lease&#39;);</span>
<span class="line">CREATE TABLE CarOwnership (</span>
<span class="line">    InsuranceID SERIAL PRIMARY KEY,</span>
<span class="line">    BuyDate TIMESTAMP NOT NULL, /*Date when ownership starts*/</span>
<span class="line">    BuyPrice DOUBLE PRECISION NOT NULL CHECK (BuyPrice &gt;= 0), /*Ownership price, if rental or lease, this price represents a monthly amount*/</span>
<span class="line">    WarrantyEndDate DATE NOT NULL CHECK (WarrantyEndDate &gt;= DATE(BuyDate)),</span>
<span class="line">    OwnershipType CarOwnershipType NOT NULL,</span>
<span class="line">    PlateFK VARCHAR(32) NOT NULL,</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (PlateFK) REFERENCES Car(Plate),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The table implemented at the logical level is very similar to Rental, as we have a surrogate key that uniquely identifies the tuples, thus preventing the entity from being weak in identification. You can see this directly in the conceptual diagram. There, we have an attribute marked with <code>{id}</code> that we provide with semantics equivalent to that of a surrogate key. This means we don&#39;t need its identification to depend on any other entity.</p>`,19)),i("p",null,[e[56]||(e[56]=t("In other words, at the conceptual level, ")),e[57]||(e[57]=i("code",null,"InsuranceID",-1)),e[58]||(e[58]=t(" is a unique identifier provided by an insurance company. To to generate it, they likely used a technique similar to SQL's ")),e[59]||(e[59]=i("code",null,"SERIAL",-1)),e[60]||(e[60]=t(" auto-increment, although it doesn't necessarily have to be that, as there are ")),n(p,{to:"/freecodecamp.org/how-to-effectively-manage-unique-identifiers-at-scale.html"},{default:a(()=>e[55]||(e[55]=[i("strong",null,"many others",-1)])),_:1}),e[61]||(e[61]=t(" with very specific applications."))]),e[119]||(e[119]=i("p",null,[t("The value of "),i("code",null,"InsuranceID"),t(" might be provided to us when inserting tuples into our system, where this value would have to meet the primary key constraint and not repeat for any pair of possible tuples. But still, we decided to implement it with a "),i("code",null,"SERIAL"),t(" to make the generation of synthetic data for this database simpler.")],-1)),e[120]||(e[120]=i("p",null,[t("Just keep in mind that, in a real situation, if we are provided with this value, we should avoid using "),i("code",null,"SERIAL"),t(" and save the identifier that each tuple has. Since "),i("code",null,"InsuranceID"),t(" is the primary key, no pair of tuples can have the same value in this attribute, but they can have the same start date, price, and so on.")],-1)),i("p",null,[e[63]||(e[63]=t("In this table, to restrict the values that the attribute OwnershipType can take, instead of using a ")),e[64]||(e[64]=i("code",null,"CHECK",-1)),e[65]||(e[65]=t(", we’ll create a new data type. We could have done this perfectly using a ")),e[66]||(e[66]=i("code",null,"CREATE DOMAIN",-1)),e[67]||(e[67]=t(". But instead, we’ll use a ")),i("a",L,[n(o,{icon:"iconfont icon-postgresql"}),e[62]||(e[62]=i("code",null,"TYPE ENUM",-1))]),e[68]||(e[68]=t(" to show another way of defining the set of values an attribute can take. It defines the possible values for the attribute, representing an ownership where a person buys, rents, or leases a car. Finally, that ")),e[69]||(e[69]=i("code",null,"TYPE ENUM",-1)),e[70]||(e[70]=t(" is assigned as the data type of the attribute."))]),e[121]||(e[121]=r(`<p>We’ve implemented the most basic domain constraints and problem requirements here, which only involve the <code>CarOwnership</code> table itself. For example, we have those requiring the price to be positive or the warranty end date to be after the ownership start date.</p><p>On the other hand, we can see that the attribute <code>BuyDate</code> has been assigned a <code>TIMESTAMP</code> data type, which doesn&#39;t exactly match the attribute&#39;s name. In this example, such details aren’t as important, since the <code>TIMESTAMP</code> was declared this way to provide a time in addition to the date of purchase. But in a real project, you should be stricter with naming attributes according to their characteristics. This will help improve schema clarity and make database management easier.</p><h3 id="residence-entity" tabindex="-1"><a class="header-anchor" href="#residence-entity"><span><code>Residence</code> entity</span></a></h3><p>A person can also reside in a city, so our database must be able to store information about a person&#39;s stay in a certain city. We’ll do this using the entity <code>Residence</code>, which functions similarly to the previous entities <code>Rental</code> and <code>CarOwnership</code>, but with some differences.</p><p>First, the attributes it stores are:</p><ul><li>the start date of a person&#39;s stay in a city (which can’t be null because if the stay exists, it must have started on a date),</li><li>the end date of the stay (which can be NULL because the person may reside in the city for an indefinite amount of time), and</li><li>the address where they reside within the city.</li></ul><p>When the EndDate attribute is NULL, it means the person is still residing in the city, as the end date of the stay is not defined. Also, if this date exists and is later than the current date, we can also know that the person still lives in the city until the specified date.</p><p>This has implications for identifying the <code>Residence</code> entity, as there is no set of attributes within the entity itself that uniquely identifies the tuples of <code>Residence</code>. Instead, it’s the start date, along with the references to the person and city, that together uniquely identify it. So since the identification of the entity depends on other entities, <code>Residence</code> is a weak entity in terms of identification.</p><p>These references work similarly to what we saw earlier in <code>Rental</code>, for example, where we had several <code>1-*</code> associations with the many side in the <code>Residence</code> entity. This implies that for each association, the foreign key is located in the <code>Residence</code> entity, pointing to the entity on the other side of the association.</p><p>Since there are two such associations in total, there are two foreign keys, each formed by one attribute, because the primary keys of the entities they point to are also formed by a single attribute.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Residence (</span>
<span class="line">    StartDate DATE NOT NULL,</span>
<span class="line">    EndDate DATE CHECK (</span>
<span class="line">        EndDate IS NULL</span>
<span class="line">        OR EndDate &gt;= StartDate</span>
<span class="line">    ),</span>
<span class="line">    Address VARCHAR(32) NOT NULL,</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    CityFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (StartDate, PersonFK, CityFK),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (CityFK) REFERENCES City(CityID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If we look at the relational diagram, we’ll see that the table implementing this entity has its foreign keys underlined because they’re part of the primary key. This helps us identify that the corresponding conceptual entity is <strong>weak in identification</strong>, with some of its primary key attributes being foreign keys.</p><p>Also, if we wanted to reconstruct the conceptual entity from the relational diagram, it would be enough to look at the table&#39;s foreign keys, which other entities they reference, whether their attributes are underlined or not, and any possible constraints indicated in the relational diagram.</p><p>With this, if any of the foreign keys are underlined, the entity is necessarily <strong>weak</strong> in identification, and the <strong>«weak»</strong> role would be specifically placed on the association modeled by that foreign key. The <strong>many</strong> side of that association would be placed on the side of the entity from which the foreign key originates. And we wouldn’t include its foreign key attributes in the conceptual diagram entity.</p><p>In its DDL, we can see that the primary key is composed of StartDate along with the foreign key attributes, where each one represents a different foreign key pointing to a certain entity like <code>Person</code> or <code>City</code> - hence the addition of two FOREIGN KEY constraints. We’ve also added the <code>NOT NULL</code> constraint to both foreign keys due to the minimum multiplicity of the 1 side of the associations, which requires a <code>Residence</code> tuple to relate a person with a city. If we had <code>0..1</code> instead of <code>1..1</code> on those sides of the associations, then each foreign key of <code>Residence</code> might not reference any person or city, meaning it could be <code>NULL</code>.</p><p>Regarding the remaining constraints, no attribute can be null except <code>EndDate</code>. If it’s not <code>NULL</code>, then the date it stores must be after the date the residence began, as it wouldn&#39;t make sense for it to be earlier than the start date.</p><h3 id="shipassignment-entity" tabindex="-1"><a class="header-anchor" href="#shipassignment-entity"><span><code>ShipAssignment</code> entity</span></a></h3><p>Another entity that is practically the same as the previous one is <code>ShipAssignment</code>, responsible for modeling the assignment of certain cruise ships to cruise lines. That is, a cruise can belong to or be assigned to a cruise line that operates it under its brand for a certain period, just like a person can reside in a city for a certain period.</p><p>Being a weak entity in identification, as we can see in its conceptual diagram, we could have represented it with an associative entity and an N-M association between CruiseShip and CruiseLine. But to be consistent with the notation we used in <code>Residence</code>, we won’t use an associative entity. Instead, we’ll have the entity interpose in the N-M association, resulting in two <code>1-*</code> associations with the <strong>many</strong> side in <code>ShipAssignment</code>.</p><p>This implicitly indicates that there are two foreign keys pointing to CruiseShip and CruiseLine, respectively.</p><p>Also, note that just focusing on the <strong>many</strong> side (which is an easy rule to apply to determine where to place foreign keys just by looking at the conceptual diagram) isn’t by itself a good practice without further consideration. When you have a conceptual diagram, you should look at all the elements of the entity to make informed and reasoned decisions about its logical design.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE ShipAssignment (</span>
<span class="line">    StartDate DATE NOT NULL,</span>
<span class="line">    EndDate DATE NOT NULL CHECK (EndDate &gt;= StartDate),</span>
<span class="line">    NameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    FoundationDateFK DATE NOT NULL,</span>
<span class="line">    ShipFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (StartDate, NameFK, FoundationDateFK, ShipFK),</span>
<span class="line">    FOREIGN KEY (NameFK, FoundationDateFK) REFERENCES CruiseLine(Name, FoundationDate),</span>
<span class="line">    FOREIGN KEY (ShipFK) REFERENCES CruiseShip(ShipID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Here, we assume the end date of the assignment is always defined, meaning cruises are assigned to cruise lines through &quot;contracts&quot; that always start and end on specific dates, and assignments don’t last indefinitely. This implies that <code>EndDate</code> can never be <code>NULL</code>. So in the DDL, we include the <code>NOT NULL</code> constraint and a <code>CHECK</code> to ensure that <code>EndDate</code> is after the start date, guaranteeing that only valid tuples are inserted into the database.</p><p>The foreign keys are formed solely by the attribute ShipFK, which refers to the CruiseShip entity. We use it to reference the cruise assigned to a certain cruise line. But the other foreign key, which is used to implement the other <code>1-*</code> association, is composed of the attributes <strong>(<code>NameFK</code>, <code>FoundationDateFK</code>)</strong>, which refer to the primary key of <code>CruiseLine</code> - and this, in turn, is composite and contains two attributes <strong>(<code>Name</code>, <code>FoundationDate</code>)</strong>.</p><p>If we only look at the relational diagram, we’ll see that three attributes are part of foreign keys because there are arrows coming from them. Specifically, the arrow from one of them (<code>ShipFK</code>) will point to an attribute in a certain table. So we know that this attribute forms a foreign key by itself, while the other two have arrows pointing to attributes of another different entity (but both referencing the same one).</p><p>So together, they form another foreign key because the entity or table they reference is <strong>different</strong> from the one referenced by the other attribute <code>ShipFK</code>.</p><p>These attributes, in turn, serve to uniquely identify each tuple in ShipAssignment - because, with just the start and end dates, we can’t distinguish between any possible pair of tuples.</p><p>For example, if several ships are assigned to the same cruise line during the same time period, the start and end dates will match in both tuples, but they’ll represent different assignments even though the dates are the same. So the primary key of the table includes the attributes of the foreign keys, so that their values can distinguish any pair of tuples we might have in the table. Specifically, we include the foreign keys because a cruise can be or have been assigned to several cruise lines, just as a cruise line can have had multiple cruises assigned to it.</p><p>The values of both foreign keys are necessary to uniquely identify this &quot;event&quot; between a cruise and a cruise line, because according to the domain, the same cruise can’t be assigned multiple times to the same cruise line on the same date.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE ASSERTION EveryCruiseLineHasAssignment CHECK (</span>
<span class="line">    NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM CruiseLine cl</span>
<span class="line">        WHERE NOT EXISTS (</span>
<span class="line">                SELECT *</span>
<span class="line">                FROM ShipAssignment sa</span>
<span class="line">                WHERE sa.NameFK = cl.Name</span>
<span class="line">                    AND sa.FoundationDateFK = cl.FoundationDate</span>
<span class="line">            )</span>
<span class="line">    ) </span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Lastly, given the minimum multiplicity of <code>1..*</code> on the ShipAssignment side, we need to implement a constraint to ensure that all cruise lines have at least one cruise assignment, which is always associated with a cruise.</p><p>To do this, we can use either an <code>ASSERTION</code> or a <code>TRIGGER</code>, as this is a constraint involving multiple tables. But for simplicity, we’ll assume that the data inserted always meets this constraint. This means we don’t need to include assertions and triggers in the DDL.</p><p>Now let’s discuss some other important entities in our system.</p><h3 id="city-entity" tabindex="-1"><a class="header-anchor" href="#city-entity"><span><code>City</code> entity</span></a></h3><p>This entity is similar to Person and is used to store information about cities in the system. Specifically, for each city, it stores its name, the country where it’s located, population, area, and coordinates in latitude and longitude. Each physical city in our domain within the system will be represented by a tuple in the <code>City</code> table, which is how we implement this entity at the logical level.</p><p>Of all the associations that this entity has, none are of the <code>1-*</code> type with the <strong>many</strong> side in <code>City</code>. Instead, they all have their 1 side in <code>City</code>. This means there will be exactly 4 foreign keys from other entities pointing to <code>City</code>, but the <code>City</code> table itself won’t have any foreign keys pointing to another entity.</p><p>This might not be straightforward to see at the conceptual level, as we need to look at the maximum cardinalities of the associations to know which ones result in foreign keys in the entity we’re implementing.</p><p>In contrast, in the relational diagram, this is more direct. References implemented with foreign keys are arrows, so we can directly know how many arrows refer to the primary key of a certain table or how many point to other tables, also clearly indicating from which attributes and tables they originate.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE City (</span>
<span class="line">    CityID SERIAL PRIMARY KEY,</span>
<span class="line">    Name VARCHAR(32) NOT NULL,</span>
<span class="line">    Country VARCHAR(32) NOT NULL,</span>
<span class="line">    Population INT NOT NULL CHECK (Population &gt;= 0),</span>
<span class="line">    Area DOUBLE PRECISION NOT NULL CHECK (Area &gt;= 0),</span>
<span class="line">    Latitude DOUBLE PRECISION NOT NULL CHECK (</span>
<span class="line">        Latitude BETWEEN -90 AND 90</span>
<span class="line">    ),</span>
<span class="line">    Longitude DOUBLE PRECISION NOT NULL CHECK (</span>
<span class="line">        Longitude BETWEEN -180 AND 180</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Regarding the DDL, we implement the identifier <code>CityID</code> with a <code>SERIAL</code> surrogate key, as at the conceptual level we have defined that the attribute <code>CityID</code> is the primary key of <code>City</code>.</p><p>It&#39;s important to note that when modeling a domain or solving a problem for a client, we might be required to use identifiers specific to the domain we are modeling, which would mean <code>CityID</code> would be of the same type as the identifier to be stored. But for simplicity, let’s assume that we construct the city identifiers ourselves using a surrogate key.</p><p>In addition to the <code>NOT NULL</code> constraints that prevent all attributes from being <code>NULL</code>, since it doesn&#39;t make sense for a city to have no name or a defined population number, we impose a restriction on the range of values that Latitude and Longitude can take. This is to ensure the values are valid, even though we can&#39;t verify if they are correct, as this mainly depends on the data source.</p><p>To do this, we can use the <code>BETWEEN</code> operator, which performs the same check as <code>Latitude &gt;= -90 AND Latitude &lt;= 90</code> but in a more readable way.</p><h3 id="port-entity" tabindex="-1"><a class="header-anchor" href="#port-entity"><span><code>Port</code> entity</span></a></h3><p>In addition to cities, our domain also includes ports, which are represented by the entity <code>Port</code>. Like before, each port will be a tuple in the table, with its primary key composed of the port&#39;s name stored in the <code>Name</code> attribute and a foreign key that references <code>City</code>, modeling the city where the port is located.</p><p>We can infer the existence of this foreign key by looking at the entity&#39;s associations, where all of them are of the <code>1-*</code> type, and only one has the <strong>many side</strong> in <code>Port</code>. This precisely models this relationship between <code>Port</code> and <code>City</code>. The others have their <strong>1 side</strong> in <code>Port</code>, indicating that they point to <code>Port</code>, meaning they reference some tuple in the <code>Port</code> table.</p><p>At the same time, the foreign key of <code>Port</code> is also part of its primary key because a port can’t be identified by its name alone - we also need to know the city where it’s located.</p><p>For example, in this domain, we assume that there can be several ports with the same name, but not located in the same city. So if two ports are in the same city, according to the domain, we have the guarantee that their names can’t be the same. This allows us to define the primary key as the combination <strong>(<code>Name</code>, <code>CityFK</code>)</strong>.</p><p>We’re making these assumptions here as an example, but in a real project they would need to be confirmed with domain experts and the client&#39;s requirements to ensure they are met. This would allow us to make design decisions such as establishing the keys of an entity. So once we know that Port has a foreign key that is part of its primary key, we know that the entity is weak in identification. In the relational diagram, we will have to underline not only <code>Name</code> but also the <code>CityFK</code> attribute.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Port (</span>
<span class="line">    Name VARCHAR(32),</span>
<span class="line">    TerminalCount INT NOT NULL CHECK (TerminalCount &gt;= 0),</span>
<span class="line">    MaxShipLength INT NOT NULL CHECK (MaxShipLength &gt;= 0),</span>
<span class="line">    Area DOUBLE PRECISION NOT NULL CHECK (Area &gt;= 0),</span>
<span class="line">    CityFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY(Name, CityFK),</span>
<span class="line">    FOREIGN KEY (CityFK) REFERENCES City(CityID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The DDL is similar to the previous ones: we have the declaration of attributes and constraints like <code>PRIMARY KEY</code>, where the set of attributes <strong>(<code>Name</code>, <code>CityFK</code>)</strong> is defined as those that uniquely identify the tuples of <code>Port</code>. We also have the corresponding <code>FOREIGN KEY</code> that references the <code>CityID</code> attribute, the primary key of the <code>City</code> table.</p>`,51)),i("p",null,[e[72]||(e[72]=r("A peculiarity of this <code>CREATE TABLE</code> statement is that we don’t add a <code>NOT NULL</code> constraint to the <code>Name</code> attribute because we don’t need to declare it explicitly in this case. That is, since <code>Name</code> is part of the primary key, and a primary key never allows <code>NULL</code> values in its attributes, we can skip declaring <code>NOT NULL</code>, as <code>PRIMARY KEY</code> does so implicitly to ensure the ",15)),i("a",C,[n(o,{icon:"iconfont icon-ibm"}),e[71]||(e[71]=t("primary key integrity constraint"))]),e[73]||(e[73]=t("."))]),e[122]||(e[122]=r(`<p>This also applies to the foreign key attribute, which can’t be NULL due to the minimum cardinality (minimum cardinality 1 in <code>1..1</code>) on the <code>City</code> entity side, which requires all ports to be associated with a city. But to more clearly reflect this minimum cardinality, we add <code>NOT NULL</code> explicitly to the CityFK attribute, even though it’s not strictly necessary.</p><p>Lastly, if we want to ensure that the logical design represented in the relational diagram is correct with respect to the conceptual diagram, we can try reconstructing the conceptual entity from the table in the relational diagram.</p><p>To do this, after creating the entity with its name and attributes (except for those that are foreign keys), we have to infer the associations implemented through these foreign keys. So for each of them, we introduce an association that relates <code>Port</code> to the entity the foreign key points to, where the many side is on <code>Port</code> and the 1 is on the other entity&#39;s side.</p><p>In addition to the maximum cardinalities <code>1</code> and <code>*</code>, we also have to define the minimums, which we can determine through the constraints indicated in the relational diagram.</p><p>For example, if one of the foreign keys can be <code>NULL</code>, then its minimum cardinality on the 1 side of the association will be 0, resulting in that side having a cardinality of <code>0..1</code>. On the other hand, if it can’t be <code>NULL</code>, the minimum cardinality is 1. On the other side of the association, we’ll default to a minimum cardinality of 0 unless there are constraints requiring cities to have at least one port, for example. This means the minimum cardinality would be 1, resulting in the Port side of the association having a cardinality of <code>1..*</code>.</p><p>Finally, we can repeat this process with the foreign keys that point to the primary key of <code>Port</code>, leading to more associations with other entities.</p><p>For example, if we are reconstructing the conceptual entity <code>City</code> from the relational diagram, we will see that there is a foreign key from <code>Port</code> pointing to <code>CityID</code> of City. So City will have a <code>1-*</code> association with <code>Port</code>, where the many side is on the <code>Port</code> side because the foreign key originates from <code>Port</code>.</p><p>In this way, when we have fully reconstructed the conceptual entity, we’ll determine if it’s weak in identification by checking if any of its foreign keys is underlined. This means it’s also part of the primary key. In that case, we’ll add the role <strong>«weak»</strong> to the associations that have arisen from these foreign keys, always on the side from which the foreign key originates.</p><h3 id="cruiseline-entity" tabindex="-1"><a class="header-anchor" href="#cruiseline-entity"><span>CruiseLine entity</span></a></h3><p>This entity is responsible for representing cruise lines in our system, which can have customers and cruises assigned. Conceptually, this entity is very similar to those we have already seen, as it has a primary key made up of two attributes of the entity itself, and no foreign keys pointing to other entities. But there are foreign keys in other entities that <strong>point</strong> to <strong>CruiseLine</strong>, which we can see from the <code>1-*</code> type associations.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE CruiseLine (</span>
<span class="line">    Name VARCHAR(32) NOT NULL,</span>
<span class="line">    FoundationDate DATE NOT NULL,</span>
<span class="line">    ContactPhone BIGINT NOT NULL CHECK (ContactPhone &gt; 0),</span>
<span class="line">    Rating DOUBLE PRECISION NOT NULL CHECK (Rating &gt;= 0),</span>
<span class="line">    PRIMARY KEY (Name, FoundationDate)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Specifically, the primary key of this entity is made up of the company name and the foundation date. This combination of values might seem unique across the tuples we can store in the table, as it’s very unlikely that multiple cruise lines with the same name would be founded on the same date. But we shouldn’t make these assumptions ourselves - instead, we have to ensure that these conditions are met with the client, target user, or domain experts of our system.</p><p>Here, for simplicity, we directly assume that no cruise line has the same name as another founded on the same date, but you should always verify if this holds true in the domain.</p><p>So we set <strong>(<code>Name</code>, <code>FoundationDate</code>)</strong> as the primary key, which in turn imposes the implicit <code>NOT NULL</code> constraint on both attributes (meaning we don’t need to declare it explicitly). In the DDL, we can also see that the <code>ContactPhone</code> attribute is not of type <code>INTEGER</code>, but <code>BIGINT</code>. This is because phone numbers are usually long numbers representing large numeric quantities that would exceed the range representable by a more basic type like <code>INTEGER</code>. For text-type attributes, a fixed maximum length of 32 characters is used for all strings, which is enough to accommodate any cruise line name or similar information.</p><p>We could also represent the phone number with a string, allowing the storage of the country code in text format, but this can complicate processing since the number would need to be parsed from text.</p><h3 id="vehicle-entity" tabindex="-1"><a class="header-anchor" href="#vehicle-entity"><span><code>Vehicle</code> entity</span></a></h3><p>In our domain, there can be certain types of vehicles, such as cars, cruise ships, bikes, or city buses. They all share a series of <strong>common characteristics</strong> like model, weight, color, or odometer reading to know how far they have traveled since they were manufactured.</p><p>These attributes are common to all vehicles in our domain, as they will always have a model name or weight, among other things, regardless of the type of vehicle they are. Becuase of this, we’ve decided to abstract these common characteristics in the conceptual design into a <strong>superclass</strong> entity called <code>Vehicle</code>. And from this, all entities representing specific types of vehicles must inherit.</p><p>In other words, at a conceptual level, we have an <strong>IS-A hierarchy</strong> where the <strong>parent entity</strong> is <code>Vehicle</code>, which contains all the characteristics that define all vehicles. From it, a series of entities inherit that represent specific types of vehicles (where each has more specific characteristics of the corresponding vehicle type).</p>`,19)),i("p",null,[e[75]||(e[75]=t('In summary, we use an IS-A hierarchy because we need to model a situation where a series of "individuals" in our domain share a set of ')),e[76]||(e[76]=i("strong",null,"common features",-1)),e[77]||(e[77]=t(". Formally, an IS-A hierarchy can be defined as a ")),i("a",R,[n(o,{icon:"fas fa-globe"}),e[74]||(e[74]=t("specialization/generalization"))]),e[78]||(e[78]=t(" relationship between a ")),e[79]||(e[79]=i("strong",null,"superclass",-1)),e[80]||(e[80]=t(" entity and some ")),e[81]||(e[81]=i("strong",null,"inheriting",-1)),e[82]||(e[82]=t(" entities. The inheriting entities are composed of all the characteristics or attributes of the superclass plus some of their own attributes."))]),e[123]||(e[123]=r('<p>But, practically, what matters to us is that a hierarchy allows us to have a superclass (the Vehicle entity in this case) where we have attributes corresponding to these common characteristics, and then a series of entities that inherit from it and represent specific types of individuals (each having specific characteristics depending on their type).</p><p>With this, we gain clarity and maintainability in the diagram, as adding a new common characteristic to all vehicles only requires adding it to Vehicle - not to each and every inheriting entity. Similarly, if a new type of vehicle needs to be added to the system, we won’t need to include all the common attributes of vehicles in that entity.</p><h3 id="how-is-this-is-a-hierarchy-implemented-with-tables" tabindex="-1"><a class="header-anchor" href="#how-is-this-is-a-hierarchy-implemented-with-tables"><span>How is this IS-A hierarchy implemented with tables?</span></a></h3><p>At this point, we need to decide how to implement the hierarchy using tables in the logical model. Specifically, we have to determine the number of tables to use and the keys each will have concerning the implementation of the hierarchy itself.</p><p>To start, it&#39;s important to see that Vehicle has VehicleID as its primary key, which we assume is a surrogate key. With this, we know that if we had to implement any table for the inheriting entities, they should have a foreign key pointing to VehicleID, as it’s the primary key that can uniquely identify tuples of Vehicle.</p><p>We see that the hierarchy here is <strong>complete</strong> and <strong>disjoint</strong>. It’s complete because all the &quot;individuals&quot; in the hierarchy must always be represented by the inheriting entities. In other words, we will never find a vehicle that only has the attributes of Vehicle - instead, all vehicles in our domain are necessarily of one of the types defined in the inheriting entities (or so we assume). It’s <strong>disjoint</strong> because a vehicle can’t be of multiple types at once, meaning it can’t be both a car and a cruise ship, which makes sense.</p><p>All this means that each of them will be implemented with a specific table. Our system stores many types of vehicles and will likely need to expand with even more types of vehicles. To simplify this process of adding new types of vehicles and to avoid the appearance of too many NULL values in tables, we’ll implement a table for each inheriting entity of the hierarchy.</p><p>For the superclass, we’ll also implement a specific table, as each vehicle that exists in our system will be represented in one of the tables of the inheriting entities - but it’ll need to take values in the characteristics (attributes) of the superclass.</p><p>Here, we have several options. One option is not to implement a table for the superclass, duplicating all its attributes in each of the tables of the inheriting entities. This is easy to understand and initially seems practical, but it has significant drawbacks.</p><p>Another option is to implement a table for the superclass and include a foreign key in all the inheriting entities that point to the primary key of Vehicle.</p>',10)),i("p",null,[e[84]||(e[84]=t("We can easily dismiss the first option because duplicating attributes in all the tables for different types of vehicles leads to a lot of ")),i("a",I,[n(o,{icon:"fas fa-globe"}),e[83]||(e[83]=t("redundancy at the metadata level"))]),e[85]||(e[85]=t(" or ")),e[86]||(e[86]=i("strong",null,"schema",-1)),e[87]||(e[87]=t(", meaning duplicated attributes in multiple tables without a clear need for duplication."))]),e[124]||(e[124]=r(`<p>Beyond the redundancy issue, duplicating the same attributes in multiple tables makes certain schema modifications more complicated. For example, adding an additional common feature in Vehicle would require adding an attribute in each table. Or we could change how common attributes like <strong>color</strong> are represented, such as switching color names from uppercase to lowercase (or any change in their representation). We’d need to make these changes across all the vehicle type tables.</p><p>With the other option, we implement a specific table for the superclass, avoiding these problems by centralizing the storage of common features in a single table. This makes it easier to perform the operations mentioned before, or even additional ones like counting how many vehicles are in our system.</p><p>We can easily do this by counting the tuples in the Vehicle table, instead of adding up the tuple counts from each of the tables for different types of vehicles. We can resolve this query this way because all vehicles will have a <strong>tuple in Vehicle</strong> that stores the common features, as well as one in their specific vehicle type table that stores the rest of the features defining it as a car, cruise, bike, and so on.</p><p>In this tuple, there’s a <strong>foreign key</strong> that references the tuple in the superclass table, thus associating the information from both tuples so it can query it and know all the information about a vehicle - both its <strong>common</strong> features to all vehicles and the <strong>specific</strong> ones of its type.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE ColorType AS ENUM ( &#39;red&#39;, &#39;green&#39;, &#39;blue&#39;, &#39;yellow&#39;, &#39;black&#39;, &#39;white&#39; ); </span>
<span class="line">CREATE TABLE Vehicle (</span>
<span class="line">    VehicleID SERIAL PRIMARY KEY,</span>
<span class="line">    Model VARCHAR(32) NOT NULL,</span>
<span class="line">    Weight DOUBLE PRECISION NOT NULL CHECK (Weight &gt;= 0),</span>
<span class="line">    Color ColorType NOT NULL,</span>
<span class="line">    Odometer DOUBLE PRECISION NOT NULL CHECK (Odometer &gt;= 0)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Finally, we decide to implement a table for all entities in the hierarchy, using <strong>foreign keys</strong> in the tables of the inheriting entities to reference tuples in <code>Vehicle</code> that store the common features of the vehicles.</p><p>In its DDL, we can see that the primary key is implemented with an attribute of type <code>SERIAL</code> because it’s a <strong>surrogate key</strong>. For the <code>Color</code> attribute, we create a <code>TYPE ENUM</code> with the possible colors in our system. This is a good practice because if we need a color attribute in more areas of the system, we’ll have its domain (or data type) defined in <code>ColorType</code>. And this allows us to reuse it and ensure that all color attributes can take values from exactly the same data set.</p><p>But if we try to reconstruct the IS-A hierarchy from the entity-relationship diagram just by looking at the implementation represented in the relational diagram, we’ll realize that it’s somewhat more complex than what we saw before.</p><p>This is because there’s not a single way to translate an IS-A hierarchy into a relational diagram. Depending on the semantics of the features and entities, plus the system requirements, it may be better to use more or fewer tables to implement it. But in cases like this where we have a table for each entity in the hierarchy, we can clearly see that there’s a <code>Vehicle</code> table with a primary key <code>VehicleID</code> (which is referenced by multiple tables, each having exactly the same foreign key referencing <code>Vehicle</code>).</p><p>If we only look at this, we might think that <code>Vehicle</code> is an entity that has <code>1-*</code> associations with other entities - and this is entirely possible when looking only at the relational diagram.</p><p>But to derive the conceptual design from the logical one and infer the existence of an IS-A hierarchy, we have to focus on the semantics of the tables and attributes. That&#39;s where we&#39;ll see that <code>Vehicle</code> contains attributes common to all types of vehicles that have foreign keys pointing to <code>Vehicle</code>. This gives us clues that <code>Vehicle</code> could be the superclass of a hierarchy, and the rest of the tables with foreign keys pointing to <code>Vehicle</code> could be inheriting entities.</p><p>But inferring the existence of an IS-A hierarchy in the conceptual design simply by observing the logical implementation is not always unequivocal. This is because, for example, here we could perfectly consider that <code>Vehicle</code> is an entity associated with the other inheriting entities through <code>1-*</code> type associations. This would also be correct from a conceptual and logical point of view.</p><p>Still, even though conceptually we can transform the hierarchy into a series of <code>1-*</code> associations between <code>Vehicle</code> and the other entities, this is only true to the implementation if we implement one table per entity. Otherwise, we wouldn’t be correctly reflecting in the conceptual design what is actually implemented in the logical one.</p><p>In summary, when we see an IS-A hierarchy, it doesn&#39;t necessarily mean there are foreign keys between the inheriting entities and the superclass, as not always as many tables as entities are used to implement the hierarchy. So to reconstruct a hierarchy at the conceptual level from the logical one, the most reliable thing to focus on is the constraints, notes, or indications left in the relational diagram explaining why certain tables were implemented - that is, where they come from.</p><p>Implementing a hierarchy at the logical level usually involves a series of design decisions that must be properly justified, which we can then use to infer the existence of the hierarchy at the conceptual level.</p><p>This exercise of trying to reconstruct the conceptual level is important to approach clearly, as understanding this reverse process is key to comprehending the elements of the different design levels and how they translate from one to another.</p><h3 id="cruiseship-entity" tabindex="-1"><a class="header-anchor" href="#cruiseship-entity"><span><code>CruiseShip</code> entity</span></a></h3><p>To illustrate how we’ve implemented the IS-A hierarchy of Vehicle, let&#39;s look at the different inheriting entities that make it up.</p><figure><img src="https://cdn.hashnode.com/res/hashnode/image/upload/v1754729845339/090a3c2f-0833-4f14-8a58-52fb1a1606ea.png" alt="Part of the entity-relationship diagram where the IS-A hierarchy of vehicles is represented according to their type." tabindex="0" loading="lazy"><figcaption>Part of the entity-relationship diagram where the IS-A hierarchy of vehicles is represented according to their type.</figcaption></figure><p>First, we have <code>CruiseShip</code>, which models the existence of cruise ship-type vehicles in our system, where each cruise ship is a tuple in the table. Regarding the specific features of the cruise ship that make it a cruise ship-type vehicle, we have its length, passenger capacity, or even the speed at which it travels. It also has features that all vehicles must have in the Vehicle table, such as model, color, and so on, specifically in tuples that store the values of each cruise&#39;s features.</p><p>To relate this information from both tables, there is a foreign key in <code>CruiseShip</code> that points to Vehicle, meaning it references the tuple in Vehicle where these feature values are stored, for each cruise ship (tuple of <code>CruiseShip</code>).</p><p>This way, we ensure that the attributes repeated in all vehicle types are centralized in one table where they can be easily modified or consulted, much better than having them all duplicated in the different tables of vehicle types.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE ClassType AS ENUM(&#39;first&#39;, &#39;second&#39;, &#39;third&#39;, &#39;economy&#39;);</span>
<span class="line">CREATE TABLE CruiseShip (</span>
<span class="line">    ShipID SERIAL PRIMARY KEY,</span>
<span class="line">    Speed DOUBLE PRECISION NOT NULL CHECK (Speed &gt;= 0),</span>
<span class="line">    Length DOUBLE PRECISION NOT NULL CHECK (Length &gt;= 0),</span>
<span class="line">    PassengerCapacity INT NOT NULL CHECK (PassengerCapacity &gt;= 0),</span>
<span class="line">    Class ClassType NOT NULL,</span>
<span class="line">    VehicleID INT UNIQUE NOT NULL,</span>
<span class="line">    FOREIGN KEY (VehicleID) REFERENCES Vehicle(VehicleID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the DDL, we see that the attributes and their types are declared, where ShipID is defined as a surrogate key using the <code>SERIAL</code> data type. This allows us to uniquely identify each cruise ship. But since every cruise ship is also a vehicle, we could also identify it by making its primary key <code>{VehicleID}</code>, because this attribute, even though it’s a foreign key, will never be NULL since a cruise ship needs to have the features that classify it as a vehicle.</p><p>So the foreign key must reference a valid tuple in Vehicle where the values for the features common to all vehicles are stored. Consequently, <code>VehicleID</code> is an alternative key declared with the <code>UNIQUE</code> constraint, although we aren’t required to add this constraint since the surrogate key <code>ShipID</code> is sufficient to identify it.</p><p>The important thing about this attribute is to correctly define the <code>NOT NULL</code> and FOREIGN KEY constraints, ensuring it correctly references the primary key <code>VehicleID</code> of the Vehicle table.</p><p>In the conceptual design, we see that this entity has multiple <code>1-*</code> associations, which indicate that there are three foreign keys from other entities pointing to <code>CruiseShip</code>. But if we only have the conceptual design, we can’t say anything about the possible foreign key generated by the IS-A hierarchy. That is, if we only have the conceptual diagram, we can’t &quot;guess&quot; how many tables have been used to implement the hierarchy - we only know that after creating the logical design. At most, we could consider all possible options for implementing the hierarchy and, for each one, analyze whether there is a foreign key coming from <code>CruiseShip</code>.</p><p>But if in addition to the entity-relationship diagram we know that there is a foreign key originating from <code>CruiseShip</code> and pointing to another entity, then the entity it points to must necessarily be Vehicle. This is because <code>1-*</code> type associations are elements that we know will generate foreign keys. But certain types of associations like 1-1 or <code>0..1</code>-<code>0..1</code> can lead to ambiguities, as we have seen before when trying to infer the existence of a hierarchy at the conceptual level.</p><p>So by discarding entities related through <code>1-*</code> associations, the only option left would be Vehicle. With all this, we can also know that the implementation of the hierarchy at the logical level has been done by creating a table for the superclass and for the <code>CruiseShip</code> entity - but we couldn’t be sure whether the other entities have also been implemented with a table or not, as that heavily depends on the semantics.</p><h3 id="bike-entity" tabindex="-1"><a class="header-anchor" href="#bike-entity"><span><code>Bike</code> entity</span></a></h3><p>Continuing with the different types of vehicles, we also have bicycles represented in the entity <code>Bike</code>, which inherits from Vehicle. Here, it’s clearer that the attributes of the inheriting entities are more specific than those of the superclass, as only bikes have features like <code>FrameHeight</code> or <code>Foldable</code>.</p><p>If we only look at the conceptual diagram, we can’t be certain if <code>Bike</code> has a foreign key pointing to Vehicle. This is precisely because, as mentioned before, without knowing the specific implementation at the logical level, we can’t guarantee that there is a foreign key in <code>Bike</code>. But considering the semantics of the hierarchy, we could propose the different options available for implementing it and determine in each case whether such a foreign key exists.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Bike (</span>
<span class="line">    BikeID SERIAL PRIMARY KEY,</span>
<span class="line">    Electric BOOLEAN NOT NULL,</span>
<span class="line">    Foldable BOOLEAN NOT NULL,</span>
<span class="line">    HasLights BOOLEAN NOT NULL,</span>
<span class="line">    FrameHeight DOUBLE PRECISION NOT NULL CHECK (FrameHeight &gt;= 0),</span>
<span class="line">    VehicleID INT UNIQUE NOT NULL,</span>
<span class="line">    FOREIGN KEY (VehicleID) REFERENCES Vehicle(VehicleID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Since we decided to use a table to implement each table in the hierarchy, in this case, there is indeed such a foreign key, just as in <code>CruiseShip</code>. And we can see it declared in the same way as the <code>FOREIGN KEY</code> constraint.</p><p>Also, the primary key of <code>Bike</code> is not the foreign key that uniquely identifies the vehicles. Instead, it’s the <code>BikeID</code> identifier. Here we’re assuming that our domain requires each type of vehicle to have its own identifier. That is, in addition to the <code>VehicleID</code> identifier that serves for any type of vehicle, each of these types must have its own <strong>type-specific identifier</strong>. This means that cruise ships, bikes, cars, and buses will each have a way to identify themselves (even though all of them can be distinguished from each other by the <code>VehicleID</code> identifier they possess indirectly through their foreign key referencing Vehicle. This is why the foreign key attribute is declared as UNIQUE.).</p><p>And since this foreign key is not part of the primary key, the entity is not weak in identification. Even if it were, it wouldn’t be marked in any way at the conceptual level. This is because at that level, we have a hierarchy of entities that can be implemented at the logical level in many ways, and not all of them will have entities weak in identification.</p><figure><img src="https://cdn.hashnode.com/res/hashnode/image/upload/v1752833986569/a77fb4b1-3632-4d63-ad28-750c6768ef0d.png" alt="Entity-relationship diagram with inheritance where  and  are subclasses of Vehicle." tabindex="0" loading="lazy"><figcaption>Entity-relationship diagram with inheritance where <code>Bike</code> and <code>Car</code> are subclasses of Vehicle.</figcaption></figure><p>To understand this, we can consider a simpler example of a hierarchy with only two inheriting entities (as you can see in the diagram above). If we only have the conceptual design, we still won&#39;t know which tables we&#39;ll use to implement the hierarchy - although we know we have several options, such as:</p><ul><li>implementing or not implementing a table for the superclass</li><li>implementing a table to represent all inheriting entities, or just one table for each entity</li><li>or even more complex implementations like using a single table for the superclass and some of the inheriting entities.</li></ul><p>Each of these options has its peculiarities. If we don&#39;t implement a table for the superclass, then there will necessarily be no foreign keys pointing to it. Or if we decide to create a table to represent the superclass and some inheriting entity together, then that inheriting entity won’t have any foreign key pointing to the superclass.</p><p>Regarding weakness in identification, depending on whether we are required to have each type of vehicle with its own identifier, we could have a global identifier in the superclass, or as in our diagram, multiple identifiers, one for each type of vehicle in addition to the Vehicle superclass identifier that identifies any vehicle. So we see that weakness in identification does not always exist, as it mainly depends on the domain and the requirements of the problem.</p><p>For example, if we see identifiers in each of the inheriting entities, and we know that those identifiers can serve as primary keys, then this suggests that the inheriting entities may have been implemented with a table each, where their foreign key initially does not include other foreign key attributes. But the conceptual diagram can’t guarantee this, as the existence or absence of a foreign key that may or may not be part of the primary key when implementing a hierarchy is a design decision specific to the logical level.</p><p>Another aspect we can infer from the conceptual diagram is the <code>1-*</code> associations where the 1 is in one of the entities of the hierarchy. Necessarily, if any foreign key points to the superclass (the 1 side of the association is in the superclass), then a table must be implemented for it.</p><p>On the other hand, if it points to one of the inheriting entities, it’s not a sufficient condition to infer that there is a table for that inheriting entity. This is because there may be a table representing the superclass along with that inheriting entity, with the foreign key perfectly pointing to the identifier of that table.</p><p>So in the IS-A hierarchies of the conceptual model, the &quot;weak&quot; role is never used to indicate possible identification weakness that the tables implementing the entities might have. There are many ways to implement the hierarchy with tables, and the chosen method is not 100% determined by the conceptual diagram.</p><p>But it’s very important to be clear that the entities in the hierarchy can have associations with other entities that make them weak in identification. In that case, even though they are part of an IS-A hierarchy, the &quot;weak&quot; role would be used to indicate that the entity is weak in identification (but not due to the hierarchy - rather because of an association with another entity).</p><h3 id="car-entity" tabindex="-1"><a class="header-anchor" href="#car-entity"><span><code>Car</code> entity</span></a></h3><p>Similar to the previous entities, we have <code>Car</code>, which represents the existence of cars in the system. Its primary key is <code>Plate</code>, which we assume is unique for each car. As we can see in the DDL, the data type of <code>Plate</code> is <code>VARCHAR</code>. This makes it perfectly possible for the attribute to be part of a primary key, as they don’t necessarily have to be integers or numeric to be part of a key. Any data type whose values are unique across the tuples stored in the table can serve.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE CarFuelType AS ENUM ( &#39;gas&#39;, &#39;diesel&#39;, &#39;electric&#39;, &#39;hybrid&#39;, &#39;hydrogen&#39; ); </span>
<span class="line">CREATE TABLE Car (</span>
<span class="line">    Plate VARCHAR(32) PRIMARY KEY,</span>
<span class="line">    FuelType CarFuelType NOT NULL,</span>
<span class="line">    DoorCount INT NOT NULL CHECK (DoorCount &gt;= 0),</span>
<span class="line">    TrunkCapacity INT NOT NULL CHECK (TrunkCapacity &gt;= 0),</span>
<span class="line">    HorsePower INT NOT NULL CHECK (HorsePower &gt;= 0),</span>
<span class="line">    Doors INT NOT NULL CHECK (Doors &gt; 0),</span>
<span class="line">    AirConditioning BOOLEAN NOT NULL,</span>
<span class="line">    VehicleID INT UNIQUE NOT NULL,</span>
<span class="line">    FOREIGN KEY (VehicleID) REFERENCES Vehicle(VehicleID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Regarding the foreign keys related to this entity, we have the same one as before, which serves to reference a tuple of <code>Vehicle</code> that stores information about the car model, color, and so on. But looking at the conceptual diagram, we can see that there are two other foreign keys in other entities pointing to <code>Car</code>, since the 1 side of the corresponding <code>1-*</code> associations is in <code>Car</code>.</p><p>Even though we can’t see this in the DDL of the <code>Car</code> table, these foreign keys are in other entities referencing <code>Car</code>. But we can see them in the relational diagram as arrows pointing to the attributes of the primary key of <code>Car</code>.</p><p>Lastly, we also create an <code>ENUM TYPE</code> to restrict the domain of the FuelType attribute. We could implement this perfectly with a constraint, but to reuse this data type in other entities that might need it, we should define a <code>DOMAIN</code> or an <code>ENUM TYPE</code> (as in this case, that can be assigned as a data type to the attribute).</p><p>Also, defining a set of values this way is especially useful when the attribute holds text, as in other numeric attributes it may be easier to restrict their possible values with conditions like <code>(HorsePower &gt;= 0)</code>.</p><h3 id="citybus-entity" tabindex="-1"><a class="header-anchor" href="#citybus-entity"><span><code>CityBus</code> entity</span></a></h3><p>To finish with the vehicle hierarchy, we have <code>CityBus</code>, which represents city buses in our domain. In this entity, we also have <code>Plate</code> as the primary key, which stores the bus&#39;s license plate and serves to uniquely identify it (meaning it differentiates it from any other city bus).</p><p>But the license plate does not directly differentiate it from other types of vehicles like cars or bikes, as the semantics of each attribute are different for each type of vehicle, as mentioned before.</p><p>For example, although cars and buses both have a license plate, if we try to differentiate cars from buses using their <code>Plate</code> attributes, we will see that cars may have a different license plate structure than buses, as determined by the domain and project requirements.</p><p>So to distinguish and uniquely identify them, we need to use the <code>VehicleID</code> identifier, since <code>Plate</code> is specific to the vehicle types <code>Car</code> and <code>CityBus</code>.</p><p>In addition to representing the existence of buses, this entity has <code>1-*</code> type associations with <code>Person</code> and <code>City</code> that model the person driving each bus and the city where it operates. So in the conceptual diagram, we can see that the association with <code>Person</code> has the role <strong>“drives”</strong>. This indicates that a person can drive an arbitrary number of buses, while a bus is driven by one and only one person.</p><p>This association results in <code>CityBus</code> having a foreign key pointing to <code>Person</code>, allowing us to know, given a bus, the person who drives it by accessing the <code>Person</code> tuple referenced by the foreign key attribute.</p><p>Similarly, <code>CityBus</code> also has an association with <code>City</code> that represents the city to which each bus belongs and operates. Conceptually, we can see this as each bus having to operate in only one city, and each city having an arbitrary number of buses operating in it, including none (since the minimum cardinality on the <code>CityBus</code> side is 0).</p><p>Logically, this is implemented with a foreign key in <code>CityBus</code> pointing to <code>City</code>. So if we need to know the city where a certain bus operates, we simply check the value of its foreign key, which will uniquely identify a tuple in <code>City</code>, indicating the city we are looking for.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE CityBus (</span>
<span class="line">    Plate VARCHAR(32) PRIMARY KEY,</span>
<span class="line">    RouteNumber INT NOT NULL CHECK (RouteNumber &gt;= 0),</span>
<span class="line">    Seats INT NOT NULL CHECK (Seats &gt; 0),</span>
<span class="line">    FreeWifi BOOLEAN NOT NULL,</span>
<span class="line">    VehicleID INT UNIQUE NOT NULL,</span>
<span class="line">    DriverFK INT NOT NULL,</span>
<span class="line">    CityFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (VehicleID) REFERENCES Vehicle(VehicleID),</span>
<span class="line">    FOREIGN KEY (DriverFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (CityFK) REFERENCES City(CityID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In addition to the previous foreign keys, there is another one in the BusTrip entity that references <code>CityBus</code>, which we can see with the <code>1-*</code> type association it has with that entity. This last one isn’t directly reflected in the DDL of <code>CityBus</code>, but it’s in the relational diagram where we have an arrow pointing to the primary key Plate. And, as usual, we do not add the <code>NOT NULL</code> constraint to the <code>Plate</code> attribute, since we are imposing the <code>PRIMARY KEY</code> constraint, which implicitly includes <code>NOT NULL</code> in all the attributes that comprise it.</p><p>The foreign keys for <code>Person</code> and <code>City</code> also can’t be <code>NULL</code> due to the minimum cardinalities of the associations, where having <code>1..1</code> implies that a city bus must have a driver and a city to operate in, hence <code>NOT NULL</code> is explicitly added in the DDL.</p><h3 id="carregistration-entity" tabindex="-1"><a class="header-anchor" href="#carregistration-entity"><span><code>CarRegistration</code> entity</span></a></h3><p>In our domain, cars can belong to a person through a record in the <code>Carownership</code> table. They can also be registered as fit to drive through <code>CarRegistration</code>, which associates cars with driver&#39;s licenses to model their legal registration. That is, a car can exist at any time, but to be able to drive, it must be registered and associated with a driver&#39;s license. This is why <code>CarRegistration</code> is dedicated to associating cars with driver&#39;s licenses.</p><p>The entity is very similar to some we have seen before, like <code>Residence</code> (while the entities it relates to here are different, as well as the reason for its existence). Implicitly, a car can be registered and associated with many driver&#39;s licenses, while the same driver&#39;s license can have an arbitrary number of cars associated with it. We can determine this by observing the <strong>cardinalities</strong> and <strong>navigability</strong> of the associations in the conceptual diagram.</p><p>For example, if we have a car, then by conducting an exhaustive search in the tuples of <code>CarRegistration</code>, we can find out how many records it’s in or has participated in. Also, for each of those records, we automatically know the driver&#39;s license it has been associated with - so from one car, we can learn about many driver&#39;s licenses.</p><p>Conversely, the same applies: if we have a certain license, we can indirectly find out by looking in the <code>CarRegistration</code> table how many records associate cars with that license. And for each of those records, we would obtain the associated car.</p><p>We’ve now analyzed navigability at the logical level. Previously, we saw the concept of navigability at the conceptual level, where associations could only be traversed in a certain direction depending on their cardinality. But in the logical model, we have access to all the tuples of all the tables in the database schema.</p><p>So, even though the <code>Car</code>-<code>CarRegistration</code> association is not navigable towards <code>CarRegistration</code> at the conceptual level, it is at the logical level. That is, if we have a car, we can find out which tuples in <code>CarRegistration</code> refer to that car, using the foreign keys that implement the association. With that information, we can then navigate to <code>DrivingLicense</code> once we know which tuples in <code>CarRegistration</code> pointed to the car.</p><p>This type of navigation is considered more typical of the logical level. With it, we can obtain information from other entities in a broader way than with the concept of navigation we saw at the conceptual level.</p><p>Here, on the <strong>entity-relationship diagram</strong>, we can see that there is an implicit N-M association between Car and <code>DrivingLicense</code>, which we just navigated through.</p><p>To do this, we had to go through the <code>1-*</code> type associations, which are divided so that there can be an &quot;intermediate&quot; entity that stores information related to the N-M association, and to enable its implementation at the logical level. But we need to keep in mind the cardinalities of the <code>1-*</code> associations that make up the implicit N-M association, where on the <code>CarRegistration</code> side we have optionality because the minimum cardinality is 0. This means that a car may not be registered, so there would be no tuple in <code>CarRegistration</code> referring to a certain car, thus preventing navigation to <code>DrivingLicense</code>.</p><p>This is completely valid because if a car is not registered, it won’t be associated with any driving license, and so we won’t be able to access any information from <code>DrivingLicense</code>.</p><p>Despite this, since there is a possibility that it’s registered, we consider these associations at the logical level to be navigable (all of this is equivalent to analyzing it in the opposite direction, from <code>DrivingLicense</code> to <code>Car</code> through <code>CarRegistration</code>).</p><p>For this process to be carried out, <code>CarRegistration</code> needs to have two foreign keys: one pointing to the primary key of <code>Car</code> to uniquely identify the car being registered, and another pointing to <code>DrivingLicense</code> to identify the driver&#39;s license associated with the car.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE CarRegistration (</span>
<span class="line">    RegistrationID SERIAL PRIMARY KEY,</span>
<span class="line">    RegistrationDate DATE NOT NULL,</span>
<span class="line">    ExpirationDate DATE NOT NULL CHECK (ExpirationDate &gt; RegistrationDate),</span>
<span class="line">    PlateFK VARCHAR(32) NOT NULL,</span>
<span class="line">    LicenseFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (PlateFK) REFERENCES Car(Plate),</span>
<span class="line">    FOREIGN KEY (LicenseFK) REFERENCES DrivingLicense(LicenseID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When implementing the <code>CarRegistration</code> table, we see in its DDL that the primary key is declared as <code>SERIAL</code> because it’s a surrogate key. Also, the foreign keys all have the <code>NOT NULL</code> constraint due to the minimum multiplicity of 1 for the corresponding associations, which requires every tuple in <code>CarRegistration</code> to reference exactly one car and one driving license.</p><p>Regarding the information stored in the car registration, we mainly have the registration date or expiration date. Neither can be <code>NULL</code>, since we assume that in our domain, cars are always registered for a certain period that can later be extended through other registrations.</p><p>Here, we could have defined the <code>CarRegistration</code> entity as weak in identification, including both foreign keys in a primary key like <code>{RegistrationDate, PlateFK, LicenseFK}</code>. But for simplicity, a surrogate key is preferred, which simplifies database operations. In fact, the only advantage of not using the surrogate key would be saving the space occupied by the values of that additional column (and we could remove it if need be). But doing so would complicate the identification of <code>CarRegistration</code> tuples, as well as make certain queries less efficient and less readable.</p><p>And if we delve into the physical level, we would realize that having a primary key composed of more attributes would cause the DBMS to use more space to manage it. This would counteract the savings from removing the surrogate key - so the surrogate key remains the preferred option.</p><p>In summary, at the conceptual level, we’ve learned that navigation from <code>Car</code> to <code>DrivingLicense</code> is not entirely possible, as there is no foreign key in <code>Car</code> pointing to <code>CarRegistration</code>. But at the logical level, we can get information from <code>CarRegistration</code> because we can examine all the tuples of <code>CarRegistration</code>, allowing us to know which of them has their corresponding foreign key referencing the car we started from.</p><p>That is, conceptually, <code>1-*</code> type associations are only navigable from the many side to the one side, but at the logical level, they are considered bidirectional. Also, the <code>1-*</code> associations surrounding <code>CarRegistration</code> are both of the <code>1-*</code> type and implicitly give rise to an N-M type, which is another reason why we can actually navigate from Car to <code>DrivingLicense</code> through <code>CarRegistration</code>.</p><h3 id="drivinglicenserequest-entity" tabindex="-1"><a class="header-anchor" href="#drivinglicenserequest-entity"><span><code>DrivingLicenseRequest</code> entity</span></a></h3><p>In our domain, people can request a driver&#39;s license from a public entity, which in this case doesn&#39;t matter to us - we only care that it’s responsible for accepting or rejecting these requests. If a request is accepted, it should become the driver&#39;s license of the person who requested it, while if it is rejected, it will remain in the database as a failed request.</p><p>To model this in our database, we have many options:</p><ul><li>creating a <code>DrivingLicenseRequest</code> entity with a boolean attribute <code>Accepted</code> to represent whether the request status is accepted or not, or</li><li>creating an IS-A hierarchy as seen in the conceptual diagram, where we have a superclass <code>DrivingLicenseRequest</code> dedicated to recording all requests that exist or have existed. In turn, we have inheriting entities that are created once the request has been resolved, with one entity representing accepted requests and the other modeling those that are rejected.</li></ul><p>On one hand, using a single entity with attributes that determine its status is not the best option, because besides knowing if it has been rejected or not, the request can be in process. This would mean that it’s neither accepted nor rejected yet.</p><p>This causes multiple problems that complicate implementation, such as needing to make the <code>Accepted</code> attribute <code>NULL</code> until the request has been resolved, or even using this <code>NULL</code> value to represent the request&#39;s status. This &quot;mixes&quot; the semantics of the <code>Accepted</code> attribute with the representation of the request&#39;s status. This is not necessarily a serious problem, just a lack of clarity in representing the status and outcome of a request.</p><p>This option would also generate <code>NULL</code> values in the <strong>specific attributes</strong> of rejected or accepted requests, since each of them requires specific attributes that the other type does not have (such as the <strong>number of points</strong> for an accepted driver&#39;s license). So with this option, besides distinguishing the type of request, you would need to manage the <code>NULL</code> values in all attributes that don’t correspond with the type represented in the <code>Accepted</code> attribute. And this greatly complicates the semantics and operations when managing the data, as well as wasting unnecessary space storing these <code>NULL</code>s.</p><p>On the other hand, using an IS-A hierarchy to conceptually model driver&#39;s license requests can bring other disadvantages, such as greater complexity in the schema from the high number of tables that can be generated. You can also have more complexity when adding constraints to ensure that a request is not accepted and rejected at the same time. Or you can even have data fragmentation across multiple tables, where part of the information is stored in a superclass and the rest in an entity representing the specific type of request, whether accepted or rejected.</p><p>Still, using an IS-A hierarchy solves all the problems we saw with the previous option, providing a simpler and more consistent semantics with which we can operate on the database more easily. It also keeps us from having to worry about managing <code>NULL</code> values in certain attributes or the consistency between attributes that determine the request&#39;s status, as there are none of those here.</p><p>Thus, in the conceptual model, we have an IS-A hierarchy representing these requests, where a superclass represents requests that have just been created and are in process. In inheriting entities, these same requests are represented once they have been resolved. If they are rejected, they become a specific type <code>RejectedDrivingLicense</code>, and if accepted, another type called simply <code>DrivingLicense</code>.</p><p>In other words, at the conceptual level, we can view each request as an &quot;individual&quot; that can be found in the set of entity occurrences of the superclass, indicating that the request is in process. When it’s rejected or accepted, that individual then belongs to the set of the corresponding inheriting entity.</p><h3 id="how-can-we-model-the-driving-license-requests-hierarchy-at-the-logical-level" tabindex="-1"><a class="header-anchor" href="#how-can-we-model-the-driving-license-requests-hierarchy-at-the-logical-level"><span>How can we model the driving license requests hierarchy at the logical level?</span></a></h3><p>As we have seen before, an IS-A hierarchy doesn’t have a direct and unique translation at the logical level, as its semantics and domain requirements determine which possibilities are better or worse in aspects like query efficiency, ease of management, and so on.</p><p>So to translate this entity hierarchy formed by the superclass entity <code>DrivingLicenseRequest</code> and the inheriting entities <code>RejectedDrivingLicense</code> and <code>DrivingLicense</code> into tables in a DBMS, we need to analyze its characteristics to determine what implementation best suits the domain we’re modeling. We also need to analyze the other entities and the associations that connect with them, such as the association between Person and <code>DrivingLicense</code>, which models the relationship between a person and their driving license.</p><p>The first thing we need to check is whether the hierarchy is complete or not. In this case, there can be requests in process that haven’t been accepted or rejected, and so aren’t represented in any of the inheriting entities.</p><p>Since there are requests that don’t necessarily need to be represented by any inheriting entity, we see that the hierarchy is <strong>not complete (partial)</strong>. Given that it’s not complete, the only way for our database to correctly store those requests that are in process is to create a specific table for the superclass <code>DrivingLicenseRequest</code>. Without it, it would be more complicated to know when a request has been resolved or not.</p><p>Later, knowing that all requests are stored in <code>DrivingLicenseRequest</code>, our system must be able to store information that determines whether it has been resolved or not, as well as the result of its resolution. For this, when a request is resolved and accepted, an occurrence of the entity <code>DrivingLicense</code> is created. But if it’s rejected, an occurrence of the other inheriting entity is created.</p><p>So in no case will the request be represented by occurrences of multiple inheriting entities at the same time, so the hierarchy is <strong>disjoint</strong>. This ensures that the previous decision to implement a table for the superclass is the correct option.</p><p>To translate the inheriting entities to the logical level, we need to decide whether to implement a table for each one, a single table for both, or even a table with all the entities in the hierarchy.</p><p>The most important thing to consider in this decision is the number of attributes each entity has and the tuples expected to exist in the database if we implement a table for each entity. In other words, we need to consider how many occurrences of each entity are expected to exist in the domain.</p><p>Initially, we can assume that there are always more rejected licenses than accepted ones, as it’s very likely to be rejected at least once before being accepted. With this, we could decide to implement a table for the <code>DrivingLicenseRequest</code> and <code>RejectedDrivingLicense</code> entities together (since there are more rejected than accepted) and another for <code>DrivingLicense</code> that has a <strong>foreign key</strong> pointing to that table. But this would generate <code>NULL</code> values in the attributes from <code>RejectedDrivingLicense</code> when representing accepted driving licenses.</p><p>Since implementing the entire hierarchy with a single table also leads to too many <code>NULL</code> values in the attributes when representing accepted or rejected licenses, the best solution in this case is again to implement a table for each entity in the hierarchy.</p><p>The main reason for choosing this option is the number of <code>NULL</code> values generated when representing accepted or rejected licenses. In general, if the inheriting entities had only one attribute, then it would be clear that it could be implemented more simply with a single table for the entire hierarchy, or two.</p><p>But when there is more than one attribute, the queries become especially complicated because we need to check if several attributes are <code>NULL</code> at the same time (and manage the database and the possible extension of the hierarchy to more types of requests).</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE DrivingLicenseRequest (</span>
<span class="line">    LicenseID SERIAL PRIMARY KEY,</span>
<span class="line">    RequestDate DATE NOT NULL,</span>
<span class="line">    Fee DOUBLE PRECISION NOT NULL CHECK (Fee &gt;= 0),</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Once we decide to use a table for each entity in the hierarchy, we need to reflect this decision in both the relational diagram and the SQL DDL. This is mainly because it’s <strong>not complete</strong>, <strong>disjoint</strong>, and has multiple attributes in the inheriting entities that would lead to too many <code>NULL</code> values.</p><p>So in the relational diagram, we create the corresponding tables, where <code>DrivingLicense</code> and <code>RejectedDrivingLicense</code> add foreign keys pointing to <code>DrivingLicenseRequest</code> to identify the request that has been rejected or accepted.</p><p>In other words, all requests are stored in the superclass table. Then when they are accepted or rejected, a tuple is added to the corresponding table so that its foreign key references the <code>DrivingLicenseRequest</code> tuple representing the request itself. This way, the superclass table is dedicated to storing requests, while the other tables focus on representing which requests have been rejected or accepted.</p><p>Regarding the foreign keys pointing to or present in any of the tables in the hierarchy, we can see that to know which person a certain request belongs to, there is a <strong>foreign key</strong> in <code>DrivingLicenseRequest</code> pointing to <strong>Person</strong>. So for every request, that foreign key indicates the person associated with that request.</p><p>On the other hand, given the associations we see in the conceptual diagram, there are two other foreign keys from other entities pointing to DrivingLicense. We need to consider all of this because it can affect the decision we made earlier about how to implement the hierarchy. If there are foreign keys pointing to the superclass, for example, we would necessarily have to implement a table for it.</p><p>Finally, we identify the requests using a surrogate key in <code>DrivingLicenseRequest</code>, which uniquely identifies all requests, regardless of their status. We can also see this in the inheriting entities, which don’t have any type of identification on their own, but are assumed to be identified by the primary key of <code>DrivingLicenseRequest</code>.</p><p>In other words, even though there is no clear identifier in the inheriting entities, it’s important to remember that the attributes of the superclass are inherited. So when we’re implementing the hierarchy at the logical level, no matter how we do it, we will always do it in such a way that each accepted or rejected request can be identified by the primary key of <code>DrivingLicenseRequest</code>. This is the table that stores the resolved request.</p><h3 id="rejecteddrivinglicense-entity" tabindex="-1"><a class="header-anchor" href="#rejecteddrivinglicense-entity"><span><code>RejectedDrivingLicense</code> entity</span></a></h3><p>Continuing with the previous hierarchy, given its implementation, we have the table <code>RejectedDrivingLicense</code>, which represents its corresponding entity. Its tuples will store information regarding the rejection of requests that have been denied, such as the date or reason for rejection.</p><p>Also, to know which request each tuple&#39;s information corresponds to, there is a foreign key pointing to <code>DrivingLicenseRequest</code>, specifically referencing the tuple in that table that stores the rest of the request information (including the primary key that identifies it).</p><p>To avoid having to include a surrogate key in this table or define a primary key from the entity&#39;s attributes, we’ll choose the <strong>primary key</strong> to be the <strong>foreign key</strong> itself. This in turn references the primary key of the superclass table that uniquely identifies all requests, regardless of their status.</p><p>This means that the RejectedDrivingLicense table is weak in identification, as it requires the primary key of the owning entity <code>DrivingLicenseRequest</code> to identify it. But as we’ve seen before, this shouldn’t be reflected in the conceptual diagram because this way of implementing the hierarchy is not always unique. So depending on how we do it, the table may cease to be weak in identification.</p><p>In summary, the variability that exists when implementing an IS-A hierarchy with tables means that concepts like identification weakness aren’t indicated in the conceptual diagram, as they are only generated by certain implementations.</p><p>Generally, if we only look at the diagram, all we can do is consider all the options available for implementing the hierarchy, analyze each one, and even decide which one to implement in the end. But this is a decision we make and doesn’t imply what we have represented in the hierarchy of the diagram.</p><p>In other words, from the diagram, it’s impossible to infer which specific logical implementation has been used, although in very specific cases, it can be easier and more straightforward to &quot;guess.&quot;</p><figure><img src="https://cdn.hashnode.com/res/hashnode/image/upload/v1752916101394/f9b94685-4891-4174-b38d-35c6d164744b.png" alt="Entity-relationship diagram with incomplete and disjoint inheritance where  is a subclass of ." tabindex="0" loading="lazy"><figcaption>Entity-relationship diagram with incomplete and disjoint inheritance where <code>AcceptedRequest</code> is a subclass of <code>Request</code>.</figcaption></figure><p>For example, say we have a hierarchy with only one superclass <code>Request</code> that is pointed to by a foreign key and an inheriting entity <code>AcceptedRequest</code> that’s very similar to the one in our domain. We can see at the conceptual level that the hierarchy is incomplete, as there may be requests that have not yet been accepted. It’s also disjoint, which in this case makes analyzing this aspect of the hierarchy irrelevant given the number of inheriting entities it has.</p><p>So, since it’s incomplete, we’ll need a table for the superclass. Also, to avoid the appearance of <code>NULL</code> values if the hierarchy is implemented with a single table, we’ll use a table for the inheriting entity.</p><p>But if the hierarchy were complete, we would only have one clear way to implement the hierarchy: with a single table. This is because there would never be <code>NULL</code>s in the attributes of <code>AcceptedRequest</code>, despite the option of using a table for each entity and complicating the database logic by inserting a tuple in each table for each request.</p><p>With this example, we see that in very specific cases, it’s possible to infer the clearest way to implement a hierarchy at the logical level, even though there will always be some variability that prevents us from &quot;guessing&quot; the exact implementation chosen for the DBMS. Finally, it’s also important to consider the identification of the tuples with which we model the hierarchy, where multiple options also arise.</p><p>On one hand, if the domain or requirements dictate that certain entities need to have their own identifiers, then we’ll have to define them as primary keys of the corresponding entities. In our domain, all requests must be uniquely identified by an attribute in <code>DrivingLicenseRequest</code>, so we add a surrogate key to that entity.</p><p>If the requirements tell us that some of the attributes of an entity serve as an identifier, then we’ll use them as the primary key - but here for simplicity, we assume there is no domain-specific identifier, and we are the ones adding the surrogate key as an identifier to store the data in our system.</p><p>On the other hand, if we don&#39;t have information on how the entities should be identified, then we have the freedom to do so however we want, mainly depending on the implementation chosen in the end.</p><p>But regardless of the source of this identification, in general, it all comes down to whether or not each inheriting entity can be identified by its own attributes. This determines if the table it converts to at the logical level is weak in identification or not - because if we ultimately decide to define a primary key for an inheriting entity, then we will necessarily implement it with a concrete table.</p><p>As you can see, the identification of each entity can give us clues about how the hierarchy will be implemented, but it’s not something unequivocal that always guarantees a single way to implement it.</p><p>Sometimes, it’s our task to define how we identify them, and that will depend on how many tables we choose for the implementation and how we associate them with each other.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE RejectedDrivingLicense (</span>
<span class="line">    LicenseID SERIAL PRIMARY KEY,</span>
<span class="line">    RejectionDate DATE NOT NULL,</span>
<span class="line">    ReapplicationDate DATE NOT NULL CHECK (ReapplicationDate &gt;= RejectionDate),</span>
<span class="line">    Reason VARCHAR(32) NOT NULL,</span>
<span class="line">    FOREIGN KEY (LicenseID) REFERENCES DrivingLicenseRequest(LicenseID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the DDL corresponding to this entity, we see that a specific table is created for it with the respective attributes shown in the conceptual diagram. Also, we include one that serves as a foreign key to reference the tuple of <code>DrivingLicenseRequest</code> that represents the rejected application (and also serves as the primary key of this table).</p><p>We could include a surrogate key here as well, but since we already have the <code>LicenseID</code> value from the superclass table, we don’t need to do so (and the domain doesn’t require us to identify rejected applications in a special way).</p><p>So we add the <code>PRIMARY KEY</code> and <code>FOREIGN KEY</code> constraints to that attribute at the same time, so it can’t contain NULL values because of the <strong>implicit <code>NOT NULL</code> restriction</strong> added by <code>PRIMARY KEY</code>. It must also reference the <strong>primary key <code>LicenseID</code></strong> attribute of <code>DrivingLicenseRequest</code>.</p><p>To reflect this in the relational diagram, we can just underline the foreign key attribute, indicating that the table is weak in identification and that attribute can’t take <code>NULL</code> values. But to clearly indicate that other attributes that aren’t part of the key (like <code>RejectionDate</code>) can’t be <code>NULL</code>, we need to use other elements like margin notes or any other technique that clearly reflects this condition.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE ASSERTION RejectionDateConstraint CHECK (</span>
<span class="line">    NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM RejectedDrivingLicense R</span>
<span class="line">            JOIN DrivingLicenseRequest D USING (LicenseID)</span>
<span class="line">        WHERE R.RejectionDate &lt; D.RequestDate</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line">CREATE ASSERTION ApprovalDateConstraint CHECK (</span>
<span class="line">    NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM DrivingLicense D</span>
<span class="line">            JOIN DrivingLicenseRequest R USING (LicenseID)</span>
<span class="line">        WHERE D.ApprovalDate &lt; R.RequestDate</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Finally, although we define constraints on the table - such as that the reapplication date can’t be earlier than the rejection date - there are also other constraints (like the rejection date must be after the application date).</p><p>These types of constraints involving information from multiple tables need to be implemented with assertions or triggers. The simplest option is to use assertions as shown above, although we haven’t yet implemented the <code>ASSERTION</code> statements in PostgreSQL, so attempting to define them will result in an error from the DBMS. It’ll simply ignore these definitions.</p><p>So we’ll choose not to implement these types of constraints at this point, assuming that the inserted data already meets them for simplicity.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE ASSERTION NoSimultaneousApprovalRejection CHECK (</span>
<span class="line">    NOT EXISTS (</span>
<span class="line">        SELECT *</span>
<span class="line">        FROM DrivingLicense d</span>
<span class="line">            JOIN RejectedDrivingLicense r USING (LicenseID)</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Also, accepted and rejected applications can’t exist at the same time, so with this assertion, we could prevent this inconsistency. Basically, we define here that there can’t be any tuple in either <code>DrivingLicense</code> or <code>RejectedDrivingLicense</code> with the same <code>LicenseID</code>. This means that no application (<code>LicenseID</code>) can appear simultaneously in both tables, as the domain requires people to submit a new application when the one they have submitted is rejected.</p><h3 id="drivinglicense-entity" tabindex="-1"><a class="header-anchor" href="#drivinglicense-entity"><span><code>DrivingLicense</code> entity</span></a></h3><p>To conclude with this hierarchy, when a driver&#39;s license application is accepted, a tuple gets created in the <code>DrivingLicense</code> table with which we have implemented its respective entity. Thus, the main goal of this entity is to model a person&#39;s driver&#39;s license, because once it’s accepted, it can be used to register cars, and is indirectly associated with the person who holds the license.</p><p>To do this, first, the <code>DrivingLicense</code> has a foreign key pointing to <code>DrivingLicenseRequest</code>, just like the previous inherited entity in the hierarchy. In turn, the request, regardless of its status, always refers to a person through its foreign key, to model whose license it is. And for cars to be registered in association with a person&#39;s driver&#39;s license, the <code>CarRegistration</code> entity has a foreign key pointing to <code>DrivingLicense</code>, so each registration necessarily refers to a specific license.</p><p>We can see all of this in the entity-relationship diagram through the <code>1-*</code> type associations, as well as in their minimum cardinalities. But we can’t directly infer the existence of the foreign key in <code>DrivingLicense</code>, specific to the hierarchy implementation, because we can implement the hierarchy in many ways.</p><p>To understand this last point, imagine being told that there is a foreign key in <code>DrivingLicense</code> pointing to another entity. With this information, we can directly know that this foreign key points to the superclass of the hierarchy and exists because of the implementation where there is at least a specific table for <code>DrivingLicense</code>.</p><p>This is because the rest of the associations of the <code>DrivingLicense</code> entity are of the <code>1-*</code> type, with the 1 on the <code>DrivingLicense</code> side - so these associations result in foreign keys pointing to <code>DrivingLicense</code>, not the other way around. In summary, with just the conceptual diagram, you can’t know exactly how a hierarchy has been implemented, but with some additional information, you can.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE DrivingLicense (</span>
<span class="line">    LicenseID SERIAL PRIMARY KEY,</span>
<span class="line">    ApprovalDate DATE NOT NULL CHECK (ApprovalDate &lt;= CURRENT_DATE),</span>
<span class="line">    Points INT NOT NULL CHECK (</span>
<span class="line">        Points BETWEEN 0 AND 15</span>
<span class="line">    ),</span>
<span class="line">    FOREIGN KEY (LicenseID) REFERENCES DrivingLicenseRequest(LicenseID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The DDL of this entity is very similar to the previous one, where we have a primary key composed of the <code>LicenseID</code> attribute, which is also a foreign key that identifies the request that has been accepted. This refers to the tuple in the superclass table where the request is stored and uniquely identified, along with the entity&#39;s own attributes.</p><p>The table constraints in this case declare that the approval date must be earlier than the current date, as it’s impossible for a request to have been accepted on a date that has not yet occurred.</p><p>For this, we use <code>CURRENT_DATE</code> to get the current date in SQL and compare it with another date like the one stored in the attribute. There’s also the Points attribute, which determines the remaining points on the person&#39;s driver&#39;s license. According to the domain, this value is an integer between 0 and 15, so we restrict the possible values it can take with a <code>CHECK</code>, as well as with the <code>INTEGER</code> data type itself, preventing it from taking decimal values.</p><p>Given the simplicity of the attribute&#39;s domain, using a <code>CHECK</code> is the easiest option, although we could have defined a <code>DOMAIN</code> or <code>TYPE ENUM</code> and assigned it as the data type to the attribute. This would be useful if we had more attributes with the same domain in the rest of the schema.</p><h3 id="bustrip-entity" tabindex="-1"><a class="header-anchor" href="#bustrip-entity"><span><code>BusTrip</code> entity</span></a></h3><p>People in our domain can use <code>CityBus</code> buses as a means of transportation. To this end, we have an entity called <code>BusTrip</code> that models specific routes buses take across the city. Each time a bus travels from one point to another, it’s considered a trip recorded in this entity through a tuple. This tuple stores information such as the starting and ending addresses of the trip, the date it takes place, and the time it took.</p><p>To uniquely identify the tuples in this table, the primary key uses the attributes <code>TripDate</code>, the starting and ending addresses, and the foreign key attribute that identifies the specific bus that made the trip. We have to include the foreign key in the primary key because there could be several <code>BusTrips</code> with the same date and starting and ending addresses, all conducted by different buses.</p><p>So to uniquely distinguish all of them, we need to include the information of the bus making the trip, which means the value of the foreign key pointing to <code>CityBus</code>.</p><p>Regarding the semantics of this entity, we can see that no bus can make the same trip multiple times on the same date, as this would result in duplicate tuples, violating the primary key constraint. We assume that this is the case because of the characteristics of the domain.</p><p>In the design process, sometimes we have to model situations that may not be entirely intuitive, such as a bus not making the same trip more than once a day.</p><p>Since the <code>TripDate</code> attribute is of type <code>DATE</code>, it can only store dates with a resolution up to days. This means that we can’t represent the exact moment the trip occurs in our database (in the same way we could using the <code>TIMESTAMP</code> data type, which allows representing moments in time with date and time).</p><p>So, given the granularity of the <code>DATE</code> data type, we comply with the restriction that a bus can’t make the same trip multiple times a day (beause in that case, several tuples with exactly the same date would be stored, since <code>DATE</code> can only represent up to days).</p><p>This is an example of a restriction that is implicitly modeled by the data type of the attribute itself. If it were <code>TIMESTAMP</code>, we could have multiple trips by the same bus on the same day but at different times.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE BusTrip (</span>
<span class="line">    TripDate DATE NOT NULL,</span>
<span class="line">    StartAddress VARCHAR(32) NOT NULL,</span>
<span class="line">    EndAddress VARCHAR(32) NOT NULL,</span>
<span class="line">    Duration INT NOT NULL CHECK (Duration &gt;= 0),</span>
<span class="line">    PlateFK VARCHAR(32) NOT NULL,</span>
<span class="line">    PRIMARY KEY (TripDate, StartAddress, EndAddress, PlateFK),</span>
<span class="line">    FOREIGN KEY (PlateFK) REFERENCES CityBus(Plate)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When constructing the relational diagram, we must also underline the foreign key attribute that points to <code>CityBus</code>, since it’s part of the primary key (it’s the weak entity in identification). More specifically, we can infer this from the entity-relationship diagram by looking at where the «weak» role is located, which indicates the <strong>owner entity</strong> of BusTrip, meaning the one it depends on for identification.</p><p>In the DDL, this is reflected in the attributes that make up the primary key, where we find the three from the table itself and <code>PlateFK</code>, which is the foreign key responsible for referencing the bus that makes the trip. We won’t impose any additional restrictions on the <code>StartAddress</code> and EndAddress attributes, even though just any text can’t be stored in them - only texts that represent valid addresses in a city (specifically where the bus operates).</p><p>For simplicity, we’ll assume that if an address is not valid, it’s the responsibility of another part of the system to check this, such as software in the application layer that validates addresses before inserting tuples into the database.</p><p>On the other hand, we will add the non-negativity restriction on the duration, as it doesn&#39;t make sense for it to be negative. We could name these restrictions to make database administration easier, but since we aren’t going to work on them here, we won’t do so.</p><h3 id="busticket-entity" tabindex="-1"><a class="header-anchor" href="#busticket-entity"><span><code>BusTicket</code> entity</span></a></h3><p>For a person to travel on a bus route, they must have a ticket that allows them to board a bus represented in the <code>CityBus</code> table. So in our domain, we’ll model the existence of tickets with the <code>BusTicket</code> entity. Its only attribute is used to store the timestamp when it was issued.</p><p>It’s important to use the <code>TIMESTAMP</code> data type here and not <code>DATE</code> because a person can buy multiple tickets on the same day for different routes, which is why we need to clarify which ticket was generated first.</p><p>When we see how this is represented in the conceptual diagram, you might notice the XOR restriction that appears between the associations connecting <code>BusTicket</code> with <code>Person</code> and <code>BusPass</code>. This restriction represents that all existing tickets are either directly associated with a person who owns the ticket or are associated with a <code>BusPass</code> that’s owned by a person and allows multiple trips with a pass.</p><p>This is how we’d semantically explain the restriction we want to model - but conceptually, when we have a restriction represented by a dashed line and a logical condition like XOR, it means that either the <strong><code>BusTicket</code>-<code>Person</code></strong> association exists, or the <strong><code>BusTicket</code>-<code>BusPass</code></strong> association exists. It’s not possible for neither to exist or for both to exist at the same time.</p><p>Because these associations exist, the foreign key in <code>BusTicket</code> pointing to the respective entity is not <code>NULL</code>. That is, both associations are of type <code>1-*</code>, so they are clearly implemented with foreign keys in <code>BusTicket</code>. But the minimum cardinalities on both sides are 0, indicating that the associations as a whole may not exist. In other words, it means that the values of the foreign key attributes can be <code>NULL</code>.</p><p>At this point, if we didn&#39;t have the XOR restriction, the foreign keys could both be <code>NULL</code> at the same time, indicating that a ticket is not associated with any person or pass, making it impossible to identify the passenger taking the trip.</p><p>On the other hand, if both foreign keys had values in their attributes, we would be modeling that the person has used the pass to travel by bus through the non-nullity of the <strong><code>BusTicket</code>→<code>BusPass</code></strong> foreign key, while at the same time modeling that the same person has not used the pass but has obtained a ticket directly through the non-nullity of <strong><code>BusTicket</code>→<code>Person</code></strong>.</p><p>That is, if the foreign key <strong><code>BusTicket</code>→<code>BusPass</code></strong> is not <code>NULL</code>, then we are modeling the situation where a person uses their pass to travel by bus, while the other foreign key, when not <code>NULL</code>, represents that the person is not using a pass to travel but is doing so directly with a ticket.</p><p>So both situations can’t occur at the same time thanks to the domain restrictions that dictate that a person either travels with a ticket or with a pass - but not both at once, and not neither. This is because a ticket is necessary to travel. This is why we use the XOR condition to represent that either one association exists or the other, but not both at the same time. It also prohibits neither from existing.</p><table><thead><tr><th><strong>PersonFK</strong></th><th><strong>PassFK</strong></th><th><strong>Valid</strong></th><th><strong>Meaning</strong></th></tr></thead><tbody><tr><td>No NULL</td><td>NULL</td><td>✔️</td><td>Ticket purchased directly by the person.</td></tr><tr><td>NULL</td><td>No NULL</td><td>✔️</td><td>Ticket charged to the person&#39;s BusPass.</td></tr><tr><td>NULL</td><td>NULL</td><td>❌</td><td>There is no information on which person is traveling (orphan ticket).</td></tr><tr><td>No NULL</td><td>No NULL</td><td>❌</td><td>Indicates both direct purchase and use of a pass at the same time (inconsistent).</td></tr></tbody></table><p>It’s also important to emphasize that for the foreign keys to be <code>NULL</code>, the minimum cardinality on the side of Person and <code>BusPass</code> in the associations must be 0. To model that a person travels using a pass, we might consider associating the <code>BusPass</code> entity directly with <code>BusTrip</code> instead of with <code>BusTicket</code>. But doing this would result in an N-M relationship between <code>BusPass</code> and <code>BusTrip</code>, since a pass can lead to an indefinite number of trips, while multiple people can travel using their pass on a single trip.</p><p>To avoid having to add another intermediate entity to implement the N-M association, let’s associate BusTicket with <code>BusPass</code>, so that we can see each trip made using a pass by checking the foreign key values of the ticket.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE BusTicket (</span>
<span class="line">    IssueTime TIMESTAMP,</span>
<span class="line">    TripDateFK DATE,</span>
<span class="line">    StartAddressFK VARCHAR(32),</span>
<span class="line">    EndAddressFK VARCHAR(32),</span>
<span class="line">    PlateFK VARCHAR(32),</span>
<span class="line">    PersonFK INT,</span>
<span class="line">    PassFK INT,</span>
<span class="line">    PRIMARY KEY(</span>
<span class="line">        IssueTime,</span>
<span class="line">        TripDateFK,</span>
<span class="line">        StartAddressFK,</span>
<span class="line">        EndAddressFK,</span>
<span class="line">        PlateFK</span>
<span class="line">    ),</span>
<span class="line">    FOREIGN KEY (</span>
<span class="line">        TripDateFK,</span>
<span class="line">        StartAddressFK,</span>
<span class="line">        EndAddressFK,</span>
<span class="line">        PlateFK</span>
<span class="line">    ) REFERENCES BusTrip(TripDate, StartAddress, EndAddress, PlateFK),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (PassFK) REFERENCES BusPass(PassID),</span>
<span class="line">    CONSTRAINT XORConstraint CHECK (</span>
<span class="line">        (</span>
<span class="line">            PersonFK IS NULL</span>
<span class="line">            AND PassFK IS NOT NULL</span>
<span class="line">        )</span>
<span class="line">        OR (</span>
<span class="line">            PersonFK IS NOT NULL</span>
<span class="line">            AND PassFK IS NULL</span>
<span class="line">        )</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To uniquely identify each ticket, we have to use both the IssueTime attribute of the table and the foreign key pointing to <code>BusTrip</code>, which determines which trip will be made with that ticket. So we have a weak entity in identification again - and it’s peculiar in that the foreign key in this case is composed of several attributes, since the primary key of <code>BusTrip</code> (which is the owning entity on which it depends for identification) is itself composed of multiple attributes. Specifically, this primary key has 4 attributes - so in <code>BusTicket</code>, as the foreign key must reference the primary key of <code>BusTrip</code>, it will be composed of exactly 4 attributes (meaning as many as the primary key it points to).</p><p>To declare this foreign key, we use the same FOREIGN KEY constraint as always, the only difference being that here we use several attributes instead of just one.</p><p>The most important thing about this constraint when we have multiple attributes is to declare them in order. For example, if we want <code>TripDateFK</code> to point to the <code>TripDate</code> attribute of the <code>BusTrip</code> primary key, then we must put those two attributes in the same order in the constraint tuple. Here, for example, they are in the first position, but we could place them both in the second position after <code>StartAddressFK</code> and <code>StartAddress</code>, or in the third (and so on), as long as they correspond.</p><p>Since this is the only foreign key that can’t be <code>NULL</code> in the table, we need to ensure that all its attributes have the <code>NOT NULL</code> constraint. But since they’re part of the primary key, we don’t need to explicitly declare the constraint.</p><p>On the other hand, for the other foreign keys that model associations with Person and BusPass, we shouldn’t add this constraint because these foreign keys will need to take a <code>NULL</code> value in certain situations. So none of the attributes require us to declare constraints.</p><p>Finally, the <code>TIMESTAMP</code> data type isn’t the only one that can store date and time in the <code>IssueTime</code> attribute - we also have alternatives like <code>DATETIME</code> or <code>TIMESTAMP WITH TIME ZONE</code>. These have specific uses, such as storing the time zone in addition to the time itself. For simplicity, in this example, we’ll use <code>TIMESTAMP</code> for all attributes that need to store date and time.</p><h3 id="buspass-entity" tabindex="-1"><a class="header-anchor" href="#buspass-entity"><span><code>BusPass</code> entity</span></a></h3><p>We can already infer the semantics of this entity from what we’ve just seen. Specifically, if a person plans to take multiple bus trips and doesn’t want to buy individual tickets for each of those trips, they can purchase a pass (represented by the <code>BusPass</code> entity) which allows them to take multiple trips without worrying about tickets.</p><p>If we look at the conceptual diagram, we’ll see that it has several associations of type <code>1-*</code>, where one of them is affected by the XOR constraint. So given the minimum cardinalities of this association, it may not exist, as we explained earlier. So we know that there won&#39;t always be a foreign key pointing to <code>BusPass</code>, since the association is optional due to its minimum cardinalities.</p><p>But on the other hand, we have the other association with <code>Person</code> that results in a foreign key in <code>BusPass</code> that always has to exist, because all passes must have an associated person (meaning every pass must have an owner).</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE ModalityType AS ENUM( &#39;single&#39;, &#39;round_trip&#39;, &#39;daily&#39;, &#39;weekly&#39;, &#39;monthly&#39;, &#39;annual&#39; ); </span>
<span class="line">CREATE TABLE BusPass (</span>
<span class="line">    PassID SERIAL PRIMARY KEY,</span>
<span class="line">    IssueDate DATE NOT NULL,</span>
<span class="line">    ExpirationDate DATE NOT NULL CHECK (ExpirationDate &gt; IssueDate),</span>
<span class="line">    Modality ModalityType NOT NULL,</span>
<span class="line">    RemainingTrips INT NOT NULL CHECK (RemainingTrips &gt;= 0),</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For the identification of this entity, we include a surrogate key to simplify the process. We could have selected another set of attributes as the primary key, although this would result in the entity being weak in identification, as well as a primary key with more attributes than it has using a surrogate key. So the simpler solution is generally preferred.</p><p>We should include a <code>CHECK</code> to ensure that the expiration date of the pass is after the issue date to prevent inserting tuples with inconsistent dates. Also, none of the attributes can be <code>NULL</code>. The foreign key also can’t be <code>NULL</code> because of the minimum cardinality that prevents it from being <code>NULL</code>. And finally, other attributes like modality can’t be <code>NULL</code> either. For these, we implement a custom <code>ENUM TYPE</code> where we define the different pass modalities that determine how a person can use that pass.</p><p>Lastly, we can indicate the constraint that we modeled at the conceptual level with an XOR in the same way in the relational diagram using a dashed line between the foreign keys involved. We can also indicate it with a textual note. But in the DDL, the simplest way to code it is with a <code>CHECK</code> in <code>BusTicket</code>, which is where the foreign keys involved in the integrity condition originate.</p><h3 id="voyage-entity" tabindex="-1"><a class="header-anchor" href="#voyage-entity"><span><code>Voyage</code> entity</span></a></h3><p>Continuing with the ways people in our domain travel by cruise, we have the entity <code>Voyage</code>. This models the trips taken by the cruises. Specifically, the entity stores information about the trip, such as the departure and arrival dates, as well as the ports where the trip begins and ends.</p><p>We can also see that it has an attribute called <code>Distance</code>, which might initially seem irrelevant - but <code>Distance</code> records the total distance traveled by the cruise during the trip. And this doesn’t necessarily have to match the shortest distance between the departure and arrival ports.</p><p>The decision to use this meaning for this attribute came from our domain and its constraints. That is, if we are required to record the total distance the cruise travels, in addition to the distance between both ports, the simplest option would be to add an attribute in this entity that records that magnitude.</p><p>In other words, if we didn&#39;t need to know the distance traveled by the cruise itself, we could be satisfied with knowing the distance between the departure and arrival ports (which we can determine from the port information). But we’ll use the attribute <code>Distance</code> here, which records the actual distance traveled by the cruise during the trip (since we need this information).</p><p>If we look at the conceptual model, we’ll see that this entity has two identical associations of the same type <code>1-*</code> with the entity <code>Port</code>, all with the aim of conceptually modeling that a voyage is associated with two ports, one for departure and one for arrival, where both can be the same. Regarding this last point, if they could not be the same, we would need to indicate that restriction with a note, as there are no standard elements in an entity-relationship diagram or in the relational model to represent such a situation.</p><p>On the other hand, we could also consider modeling the trip so that is has departure and arrival ports through a single <code>Voyage</code>-<code>Port</code> association with a cardinality of 2 on the <code>Port</code> side. But if we did this, conceptually, we wouldn&#39;t distinguish which port was for departure and which was for arrival. Rather, we would be modeling that the cruise passes through two ports on that trip - but we wouldn&#39;t know for sure which was the arrival or departure port (at least at the conceptual level) since at the logical level there would necessarily have to be two foreign keys pointing to Port.</p><p>So to easily distinguish between the arrival and departure ports for a trip and to clarify the semantics of the association between <code>Voyage</code> and Port, we’ll use multiple associations, each with a role that explicitly indicates the relationship the port has with the trip.</p><p>In addition to these associations, the <code>Voyage</code> entity needs to reference <code>CruiseShip</code> to know which cruise has made that trip. That&#39;s why, in the conceptual diagram, there is a <code>1-*</code> association with <code>CruiseShip</code>, where one cruise ship can make many trips, but a trip is only made by one cruise ship.</p><p>To identify this entity, we’ll take advantage of the fact that both the start and end dates of the trip are always defined to include them in the primary key. This means both dates can’t be <code>NULL</code> as they define the trip&#39;s duration.</p><p>But, to truly uniquely identify the <code>Voyage</code> tuples, we have to distinguish them using the departure and arrival ports of the trip, as well as the cruise ship that performs it. That&#39;s why we include all foreign keys in the primary key. If we didn’t do this, there could be several tuples of different trips made by different cruise ships or passing through different ports that could still have the same value in the departure and arrival dates. So we need to include information about the cruise ship making the trip, as well as the ports involved.</p><p>By defining the primary key this way, we are making the entity weak in identification, where its owning entities are <code>CruiseShip</code> and Port, even though part of its primary key is composed of attributes from the entity itself.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Voyage (</span>
<span class="line">    DepartureDate DATE,</span>
<span class="line">    ArrivalDate DATE CHECK (ArrivalDate &gt;= DepartureDate),</span>
<span class="line">    Distance DOUBLE PRECISION NOT NULL CHECK (Distance &gt;= 0),</span>
<span class="line">    DepartureNameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    DepartureCityFK INT NOT NULL,</span>
<span class="line">    ArrivalNameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    ArrivalCityFK INT NOT NULL,</span>
<span class="line">    ShipFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (</span>
<span class="line">        DepartureDate,</span>
<span class="line">        ArrivalDate,</span>
<span class="line">        DepartureNameFK,</span>
<span class="line">        DepartureCityFK,</span>
<span class="line">        ArrivalNameFK,</span>
<span class="line">        ArrivalCityFK,</span>
<span class="line">        ShipFK</span>
<span class="line">    ),</span>
<span class="line">    FOREIGN KEY (ShipFK) REFERENCES CruiseShip(ShipID),</span>
<span class="line">    FOREIGN KEY (DepartureNameFK, DepartureCityFK) REFERENCES Port(Name, CityFK),</span>
<span class="line">    FOREIGN KEY (ArrivalNameFK, ArrivalCityFK) REFERENCES Port(Name, CityFK)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To implement this entity at the logical level, in its DDL, you can see that we first define the attributes of the entity itself, as well as those of the foreign key for the departure port, which are <strong>(<code>DepartureNameFK</code>, <code>DepartureCityFK</code>)</strong>.</p><p>Note that the foreign keys pointing to <code>Port</code> must have two attributes since the primary key of <code>Port</code> has two attributes. So we’ll need a total of 4 attributes to model the foreign keys that reference the departure and arrival ports of the trip, both referencing the <code>Name</code> and <code>CityFK</code> attributes of the <code>Port</code> table (which make up its primary key as we saw earlier). Also, we need another attribute, <code>ShipFK</code>, to reference <code>CruiseShip</code> and thus determine which cruise ship made the trip.</p><p>With all this, the primary key of <strong><code>Voyage</code></strong> is defined as the set of attributes <strong>(<code>DepartureDate</code>, <code>ArrivalDate</code>, <code>DepartureNameFK</code>, <code>DepartureCityFK</code>, <code>ArrivalNameFK</code>, <code>ArrivalCityFK</code>, <code>ShipFK</code>)</strong>.</p><p>If we had to infer the attributes that make up the primary key using only the conceptual diagram, we would need to look at the entities that the foreign keys reference. These are represented with the <code>1-*</code> associations.</p><p>For example, in <code>CruiseShip</code>, we would see that its primary key has only one attribute, so necessarily in <code>Voyage</code>, the corresponding primary key that references it must have one attribute, <code>ShipFK</code>. Meanwhile, the other two foreign keys that reference <code>Port</code> need to have two attributes each, since we can see that <code>Port</code> is identified by its name and the city where it’s located. So its primary key has two attributes <strong>(<code>Name</code>, <code>CityFK</code>)</strong> that we will need to reference from <code>Voyage</code>.</p><p>In the relational diagram, this is easier to interpret. We’ll see that one attribute references an attribute of the <code>CruiseShip</code> table, so we know it’s a foreign key that leads to a <code>1-*</code> association in the conceptual model.</p><p>Also, there are two other attributes that together reference two attributes of <code>Port</code> - and together, they also form a foreign key that creates a <code>1-*</code> association in the conceptual diagram, where the many side is in the entity from which the foreign key originates ( that is, in <code>Voyage</code>).</p><p>With this last foreign key, we can represent the departure port of the trip. There’s another pair of attributes <strong>(<code>ArrivalNameFK</code>, <code>ArrivalCityFK</code>)</strong> that follow the same pattern to represent the arrival port of the trip. From them, we can also infer that at the conceptual level, there’s another association with the same characteristics.</p><p>And, since all these foreign keys are underlined, this implies they are part of the primary key of <code>Voyage</code>. From that we can infer that <code>Voyage</code> is weak in identification.</p><p>Lastly, if we look at the data types of the foreign keys, we’ll see that they match exactly with the types of the attributes they reference. This is especially important because a foreign key, by definition, is an attribute that holds the value of another attribute it references, so both must be of the same type for this to be possible.</p><p>Since foreign keys are made up of multiple attributes, in this case, we also need to consider the relative order of the attributes that form the foreign key with the order of the attributes they reference. This is unlike what happens in the <code>PRIMARY KEY</code> constraint, where the order in which the primary key attributes are declared doesn’t matter. In that case, in <code>PRIMARY KEY</code>, we are declaring a set of attributes, where what matters is that they appear in the constraint (not that they follow a specific order).</p><h3 id="cruisebooking-entity" tabindex="-1"><a class="header-anchor" href="#cruisebooking-entity"><span><code>CruiseBooking</code> entity</span></a></h3><p>For a person to travel on a cruise, they must make a reservation for a specific voyage. So in our domain, we have the entity <code>CruiseBooking</code>, which is responsible for storing the reservations people make to travel on a cruise.</p><p>The data stored for each reservation includes the booking date, cabin number, price, and payment method. To know which person has booked which voyage, the entity has <code>1-*</code> associations with <code>Person</code> and <code>Voyage</code>, which logically translate into two foreign keys pointing to the respective entities.</p><p>To uniquely identify each booking, we could choose the easy option of including a surrogate key attribute to serve as the primary key. But to illustrate the complexity of not doing this, we’ll use only attributes from the table itself to identify its tuples. So the primary key of this entity is composed of the attributes <code>BookingDate</code>, <code>CabinNumber</code>, the foreign key to <code>Person</code>, and the other foreign key to <code>Voyage</code>.</p><p>We do this because we assume that multiple people can book the same cabin for the same voyage, all on the same date. For example, this can happen if several people from the same family decide to book a certain voyage. Each of those people will have a record or tuple in the <code>CruiseBooking</code> table with the same attributes in <code>BookingDate</code>, <code>CabinNumber</code>, and the foreign key of <code>Voyage</code>, but a different value in the foreign key of <code>Person</code>. This allows the tuples to be uniquely distinguished.</p><p>The foreign key to <code>Person</code> has a single attribute since the primary key of <code>Person</code> has only one attribute. But the other foreign key that refers to the voyage being booked has exactly 7 attributes (as the <code>Voyage</code> entity requires 7 attributes to be uniquely identified).</p><p>With this, we realize that the primary key of <code>CruiseBooking</code> will have a total of 10 attributes, making it a much more complex solution than simply using a surrogate key. So you can see why it’s very convenient to use surrogate keys whenever possible for this type of entity - especially when the foreign keys that will be part of the primary key have too many attributes, as in this case.</p><div class="language-pgsql line-numbers-mode has-collapsed-lines collapsed" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql" style="--vp-collapsed-lines:15;"><pre><code><span class="line">CREATE TYPE PaymentMethodType AS ENUM (&#39;card&#39;, &#39;paypal&#39;, &#39;bank&#39;, &#39;cash&#39;, &#39;mobile&#39;);</span>
<span class="line">CREATE TABLE CruiseBooking (</span>
<span class="line">    BookingDate DATE NOT NULL,</span>
<span class="line">    CabinNumber INT NOT NULL CHECK (CabinNumber &gt; 0),</span>
<span class="line">    Price DOUBLE PRECISION NOT NULL CHECK (Price &gt;= 0),</span>
<span class="line">    PaymentMethod PaymentMethodType NOT NULL,</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    DepartureDateFK DATE NOT NULL,</span>
<span class="line">    ArrivalDateFK DATE NOT NULL,</span>
<span class="line">    DepartureNameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    DepartureCityFK INT NOT NULL,</span>
<span class="line">    ArrivalNameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    ArrivalCityFK INT NOT NULL,</span>
<span class="line">    ShipFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (</span>
<span class="line">        BookingDate,</span>
<span class="line">        CabinNumber,</span>
<span class="line">        PersonFK,</span>
<span class="line">        DepartureDateFK,</span>
<span class="line">        ArrivalDateFK,</span>
<span class="line">        DepartureNameFK,</span>
<span class="line">        DepartureCityFK,</span>
<span class="line">        ArrivalNameFK,</span>
<span class="line">        ArrivalCityFK,</span>
<span class="line">        ShipFK</span>
<span class="line">    ),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (</span>
<span class="line">        DepartureDateFK,</span>
<span class="line">        ArrivalDateFK,</span>
<span class="line">        DepartureNameFK,</span>
<span class="line">        DepartureCityFK,</span>
<span class="line">        ArrivalNameFK,</span>
<span class="line">        ArrivalCityFK,</span>
<span class="line">        ShipFK</span>
<span class="line">    ) REFERENCES Voyage(</span>
<span class="line">        DepartureDate,</span>
<span class="line">        ArrivalDate,</span>
<span class="line">        DepartureNameFK,</span>
<span class="line">        DepartureCityFK,</span>
<span class="line">        ArrivalNameFK,</span>
<span class="line">        ArrivalCityFK,</span>
<span class="line">        ShipFK</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div><div class="collapsed-lines"></div></div><p>If we look at the DDL, it seems much more complex than the previous ones. But actually, the elements we used are the same. We define the primary key with <code>PRIMARY KEY</code>, and the attributes of the foreign keys with the same data types as the attributes they reference. We also use the <code>NOT NULL</code> constraint to correctly implement what&#39;s indicated in the minimum cardinalities of the associations.</p><p>We declare each foreign key with <code>FOREIGN KEY</code>, which is longer in this case due to the number of attributes that make up each one. The only important thing to keep in mind here is that one of the <code>FOREIGN KEY</code>s is exclusively dedicated to declaring the foreign key to <code>Person</code> (meaning the association between <code>CruiseBooking</code> and <code>Person</code>) while the other models the association with <code>Voyage</code>.</p><p>We do this without mixing attributes of both foreign keys in the same <code>FOREIGN KEY</code> - as this would be an error since we wouldn&#39;t be modeling the conceptual diagram correctly. Each foreign key is independent of the others, so each <code>FOREIGN KEY</code> includes only the attributes that make up each corresponding foreign key.</p><p>To simplify the domain of the <code>PaymentMethod</code> attribute, we can define a <code>TYPE ENUM</code>, since the payment method is an attribute that will likely be used in other parts of the domain. Even if it&#39;s not needed now, it&#39;s possible that in a future expansion of the domain, we might need to include it in the schema. This is why it&#39;s important to declare it to make database management easier in a potential expansion.</p><h3 id="pool-entity" tabindex="-1"><a class="header-anchor" href="#pool-entity"><span><code>Pool</code> entity</span></a></h3><p>In our domain, there are also pools, which are represented in the IS-A hierarchy with the entity <code>Pool</code> as the superclass. This allows people to interact with pools in different ways, as we will see below. So we consider that our domain includes different types of pools, such as cruise pools found on cruise ships modeled with <code>CruiseShip</code>, city pools found in cities, or <code>Olympic</code> pools also found in cities.</p><p>Since they all share common attributes, we do the same as in the <code>Vehicle</code> hierarchy, using a superclass that includes these common attributes like the pool&#39;s name, its address, minimum and maximum depths, or the current state of the pool.</p><p>We also include a <strong><code>1-*</code></strong> association between <code>Pool</code> and <code>City</code> to represent that all pools are located in a city - except for those of type <code>CruiseShip</code>, which are on a cruise ship and not in a city. In that specific case, the semantics of the association are different, as we’ll see later. From this, we can define different types of pools with distinct characteristics, where all of them inherit all the attributes of their superclass, including the association with <code>City</code>.</p><p>As we can see, <code>CityPool</code> and <code>OlympicPool</code> have no issue with this, but <code>CruisePool</code> models pools on cruise ships, so its association with <code>City</code> does not have the same semantics as the others. In other words, the pool is not located in a city but on a cruise ship - so we assume that the associated city is its place of manufacture.</p><p>As you can guess, this is not the only way to model this domain, nor is it the best, since the &quot;locatedAt&quot; semantics indicated in the conceptual diagram&#39;s association between <code>City</code> and <code>Pool</code> does not capture the meaning of that relationship when the pool is of type <code>CruisePool</code>. But once we clarify this, the model is correct in the sense that all essential elements are represented correctly, even if not in the best possible way.</p><h3 id="how-can-we-translate-the-pool-hierarchy-entities-at-the-conceptual-level-to-tables" tabindex="-1"><a class="header-anchor" href="#how-can-we-translate-the-pool-hierarchy-entities-at-the-conceptual-level-to-tables"><span>How can we translate the pool hierarchy entities at the conceptual level to tables?</span></a></h3><p>Once we’ve clarified the semantics of the hierarchy, we can follow the same process as before to implement it at the logical level.</p><figure><img src="https://cdn.hashnode.com/res/hashnode/image/upload/v1754731726248/2494ed94-aca0-401d-abe5-1e91481f336a.png" alt="Part of the entity-relationship diagram where the IS-A hierarchy of pools is represented according to their type." tabindex="0" loading="lazy"><figcaption>Part of the entity-relationship diagram where the IS-A hierarchy of pools is represented according to their type.</figcaption></figure><p>First, we note that the hierarchy is not complete, as we assume that in our domain there are many types of pools, of which we only model 3 with specific entities, while the rest are pools modeled with occurrences of the <code>Pool</code> entity.</p><p>In other words, if a pool is one of the types of the <strong>inheriting entities</strong>, it will be represented as an occurrence of that entity, while if it’s of a different type, it will be represented by an <strong>occurrence of the superclass</strong>. So, in the hierarchy, pools aren’t required to belong to the inheriting entities, making it <strong>incomplete</strong>.</p><p>On the other hand, the types of pools are all disjoint, meaning a pool can’t be both Olympic and cruise at the same time, or city and Olympic at the same time. So the hierarchy is disjoint because there won’t be any pool that is of multiple types at once.</p><p>Just like in the <code>DrivingLicenseRequest</code> hierarchy, pools here are also uniquely identified with a surrogate key in the PoolID attribute, while the rest of the entities in the hierarchy initially do not have any type of identification.</p><p>This might lead us to think that the best way to implement the hierarchy is, once again, with a table for each entity. But this doesn&#39;t necessarily have to be the case because a single table can be used to implement multiple entities at once, using the table&#39;s identifier to distinguish between the entities. This is because we assume that the domain does not impose any restrictions, unlike in the Vehicle hierarchy where each type of vehicle had to have its own identifier.</p><p>Regarding the decision to implement a table for the <strong>superclass</strong>, whenever we have an <strong>incomplete hierarchy</strong>, we’ll need a specific table for the superclass - specifically to store information about pools that don’t belong to any type present in the inheriting entities. This means we need to include a <strong><code>Pool</code> table</strong>.</p><p>Later, to decide whether to use that table to implement all entities in the hierarchy, only some of them, or to include a table for each inheriting entity, we need to look at the number of attributes the inheriting entities have. In this case, we see they have too many attributes, especially <code>CruisePool</code> and <code>CityPool</code>, so the simplest option is to implement a table for each entity in the hierarchy.</p><p>Another option we would have is to use the <code>Pool</code> table to also represent <code>OlympicPool</code> (which has the fewest attributes) and model the rest of the entities with specific tables. But this has disadvantages, such as the division in how we represent each type of pool.</p><p>For example, while we represent <code>OlympicPool</code> with some attributes in <code>Pool</code> that may or may not be <code>NULL</code> depending on whether the pool is of that type, the other types of pools would be represented differently. This can be confusing when querying the database.</p><p>We also need to consider that some foreign keys point to <code>OlympicPool</code>, so those foreign keys would only be valid for tuples in <code>Pool</code> whose corresponding attributes <code>SpectatorMaxCapacity</code> and <code>CompetitionLanes</code> aren’t <code>NULL</code>, greatly complicating database management, creating more constraints, and possibly complicating certain queries.</p><p>Although, no matter how complicated this option is, it would be possible to implement it, and it would be just as valid as implementing a table for each entity. That is, the complexity of an implementation can make it unfeasible but not incorrect - as long as the corresponding constraints are defined to maintain data integrity.</p><p>So even though in this case the simplest option is to use a table per entity, that doesn&#39;t mean there aren&#39;t other correct ways to implement the hierarchy. This means that from the entity-relationship diagram, we can’t infer the exact way it’s finally implemented, although it can be useful for making that decision.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE PoolStatusType AS ENUM (&#39;open&#39;, &#39;closed&#39;, &#39;maintenance&#39;, &#39;renovation&#39;);</span>
<span class="line">CREATE TABLE Pool (</span>
<span class="line">    PoolID SERIAL PRIMARY KEY,</span>
<span class="line">    Name VARCHAR(32) NOT NULL,</span>
<span class="line">    Address VARCHAR(32) NOT NULL,</span>
<span class="line">    MinDepth INT NOT NULL CHECK (MinDepth &gt;= 0),</span>
<span class="line">    MaxDepth INT NOT NULL CHECK (MaxDepth &gt;= MinDepth),</span>
<span class="line">    Status PoolStatusType NOT NULL,</span>
<span class="line">    CityFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (CityFK) REFERENCES City(CityID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>After deciding how to translate the hierarchy to the logical level, we add the table to the relational diagram and code it in the SQL DDL. As you can see, it’s very similar to the <code>Vehicle</code> table, with a surrogate key as an identifier, the entity attributes that characterize all pools, and a foreign key that references the City table. This determines the city where the pool is located (or manufactured in the case of a pool of the type <code>CruisePool</code>).</p><p>Regarding the status of the pool, we can see that it’s modeled here with a <code>Status</code> attribute. We define an <code>ENUM TYPE</code> for it to limit its domain. This design decision is justified because in this hierarchy we’re representing the types of pools in the inheriting entities, not their statuses. So to represent the statuses of the pools, we’ll need to use a different mechanism than <strong>generalization/specialization</strong>, such as a simple <strong><code>Status</code> attribute</strong>.</p><p>There are other ways to model this, but they’d be more complex. This doesn’t make them wrong, but we won’t discuss or show them here.</p><p>To represent this attribute&#39;s data type in the entity-relationship diagram, we have chosen to define a <strong>«Enum»</strong> entity in UML with the possible values the attribute can take. Entities with the <strong>«Enum»</strong> type serve the same purpose as using a <code>TYPE ENUM</code> in SQL. This defines a set of values that can then be used as a data type for an attribute, thus restricting its domain.</p><p>But in general, this doesn’t have to be fully specified at the conceptual level. We could’ve simply used <strong>string</strong> as the data type and omitted this <strong>«Enum»</strong> entity, limiting its domain later at the logical level. Or rather, when the logical model is implemented in the DBMS.</p><p>Still, if we want our design to be as clear and self-descriptive as possible at all levels, we should indicate the possible values that attributes can take at all levels, as restricting the domain implicitly imposes an integrity constraint. We can do this through «Enum» entities, side notes, or by using other applicable standard UML elements.</p><h3 id="cruisepool-entity" tabindex="-1"><a class="header-anchor" href="#cruisepool-entity"><span><code>CruisePool</code> entity</span></a></h3><p>Just as we did in the <code>Vehicle</code> hierarchy, here each type of pool is represented with a dedicated table. This way, when registering a new <code>CruisePool</code> type pool in our system, a tuple will be created in this table where the data characterizing cruise pools is stored. But the data that characterizes it as a pool won’t be stored there, as those can only be stored in the <code>Pool</code> table.</p><p>So to logically model the inheritance of all <code>Pool</code> attributes to the specific type of pool, we’ll use a foreign key to point to the <code>Pool</code> tuple that contains the rest of the pool information. Specifically, we’ll choose <code>PoolID</code> as the foreign key, as it’s the identifier of the pools in our system. We declare it it as the same SERIAL type to reference that same attribute in the <code>Pool</code> table, where it’s the primary key.</p><p>As you can guess, the <code>Pool</code> table not only stores information about pools that aren’t specifically modeled in our system, but it also contains information about pools of each of these types. So, if we want to get information about all the pools in our system, regardless of their type, we just need to query the <code>Pool</code> table.</p><p>This is possible because we have a table for the superclass, whereas in other hierarchies we might not implement it, which would require us to query multiple tables to get information about all the pools in the system. This is not necessarily a problem, but it’s worth considering when implementing the hierarchy or even modeling certain aspects of our domain with hierarchies.</p><p>For example, if we have a <code>Pool</code> and want to know its type, we must check the rest of the tables in the hierarchy to see if there is any tuple referencing that pool. This results in a very computationally expensive operation because it has to go through all the stored data. If our system needs to prioritize efficiency in such a query, it’d be helpful to modify the hierarchy implementation to make sure that this query runs as quickly as possible.</p><p>For instance, adding a redundant attribute in <code>Pool</code> to indicate the type, even though it introduces redundancy and unnecessary additional space, can greatly optimize the latency of certain queries. Just make sure you make these decisions according to project requirements, such as the latency that queries must have, the space the database should occupy, and so on.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE CruisePool (</span>
<span class="line">    PoolID SERIAL PRIMARY KEY,</span>
<span class="line">    DeckNumber INT NOT NULL CHECK (DeckNumber &gt;= 0),</span>
<span class="line">    MaxCapacity INT NOT NULL CHECK (MaxCapacity &gt;= 0),</span>
<span class="line">    WaterTemperature DOUBLE PRECISION NOT NULL,</span>
<span class="line">    SlideCount INT NOT NULL CHECK (SlideCount &gt;= 0),</span>
<span class="line">    ShipFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (PoolID) REFERENCES Pool(PoolID),</span>
<span class="line">    FOREIGN KEY (ShipFK) REFERENCES CruiseShip(ShipID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In addition to the foreign key pointing to <code>Pool</code> (which also serves as the primary key, making this table weak in identification with <code>Pool</code> as its owner entity), we have another foreign key referencing CruiseShip to determine the cruise on which the pool is located. And, since all cruise pools must be on a cruise ship to be of that type, the foreign key pointing to CruiseShip can’t be <code>NULL</code>. It must always reference a valid cruise. This is why we include the <code>NOT NULL</code> constraint, which we don’t do for <code>PoolID</code> because we are declaring it as the primary key.</p><h3 id="citypool-entity" tabindex="-1"><a class="header-anchor" href="#citypool-entity"><span><code>CityPool</code> entity</span></a></h3><p>Another type of pool we can find is a municipal pool, represented by the entity <code>CityPool</code> and implemented with its specific table. Its DDL is very similar to the previous one, with the unique feature that in this case, we have a foreign key pointing to <code>CityPool</code>, which can be directly inferred from the <code>1-*</code> type association connecting <code>CityPool</code> with Entry in the conceptual diagram.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE CityPool (</span>
<span class="line">    PoolID SERIAL PRIMARY KEY,</span>
<span class="line">    MaxCapacity INT NOT NULL CHECK (MaxCapacity &gt;= 0),</span>
<span class="line">    AnnualBudget DOUBLE PRECISION NOT NULL CHECK (AnnualBudget &gt;= 0),</span>
<span class="line">    AccessibilityFeatures VARCHAR(32) NOT NULL,</span>
<span class="line">    FreeWifi BOOLEAN NOT NULL,</span>
<span class="line">    FOREIGN KEY (PoolID) REFERENCES Pool(PoolID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the relational diagram, it&#39;s important that the foreign key <code>PoolID</code> is underlined, indicating that this attribute, despite being a foreign key, is used to uniquely identify the tuples in <code>CityPool</code>. This means that when referencing the primary key of <code>PoolID</code>, the foreign key that refers to it contains exactly the value that identifies the pool in Pool.</p><p>So if a query simply needs the identifier of a pool of a specific type, we don’t need to access the Pool table, as the foreign key attribute of the <code>CityPool</code>, <code>CruisePool</code>, or <code>OlympicPool</code> table, for example, is enough to know it.</p><p>There are even times when we can access data from other tables that are indirectly associated through more levels of association, as in CruiseBooking, where we can access the identifier of a <code>CruiseShip</code> through the value of its foreign key, which doesn&#39;t point directly to CruiseShip, but to Voyage.</p><h3 id="olympicpool-entity" tabindex="-1"><a class="header-anchor" href="#olympicpool-entity"><span><code>OlympicPool</code> entity</span></a></h3><p>Regarding the last type of pool in our schema, there&#39;s <code>OlympicPool</code>, which represents Olympic pools. The implementation of this entity as a table is the same as the previous ones, with the difference that in the entity-relationship diagram, we can see that there are two foreign keys pointing to <code>OlympicPool</code>. Otherwise, the only differences are in the attributes that characterize the type of pool.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE OlympicPool (</span>
<span class="line">    PoolID SERIAL PRIMARY KEY,</span>
<span class="line">    SpectatorMaxCapacity INT NOT NULL CHECK (SpectatorMaxCapacity &gt;= 0),</span>
<span class="line">    CompetitionLanes INT NOT NULL CHECK (CompetitionLanes &gt; 0),</span>
<span class="line">    FOREIGN KEY (PoolID) REFERENCES Pool(PoolID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="entry-entity" tabindex="-1"><a class="header-anchor" href="#entry-entity"><span><code>Entry</code> entity</span></a></h3><p>Continuing with what a person can do in a pool in our system, we have the entity <code>Entry</code>. This is responsible for storing tickets that a person can use to enter a municipal pool, meaning one that is represented by the <code>CityPool</code> entity only.</p><p>To ensure that a person can only access a municipal pool with these tickets, the entity has a <code>1-*</code> association with <code>CityPool</code>, and not directly with <code>Pool</code>, as that would give access to any pool regardless of type. Also, to know which person the ticket belongs to, it also has a <code>1-*</code> association with <code>Person</code>, where a person can have an arbitrary number of tickets, but a ticket can only belong to one person.</p><p>On the other hand, we also have a <code>1-*</code> association where the 1 side is in <code>Entry</code>, modeling that the tickets can have associated penalties, which we’ll see later. So, with all this, we can know that at a logical level, <code>Entry</code> will have 2 foreign keys pointing to other entities, as well as a foreign key from another entity pointing to <code>Entry</code>.</p><p>To uniquely identify the tickets, the most important attribute is <code>EntryTimestamp</code>. This records the exact time the ticket was purchased. But several people can buy tickets at the same time to enter the same pool, leading to multiple tuples with the same <code>EntryTimestamp</code> value, so the primary key must have more attributes to uniquely identify all the tickets.</p><p>Specifically, the primary key needs the foreign key attributes <code>PersonFK</code> and <code>PoolFK</code> to differentiate entries by the person who bought them and the pool they enter, as well as the exact time of purchase. So if we consider the possible situations and combinations of values that can occur for the primary key of <code>Entry</code>, we’ll see that a person can’t buy multiple entries at the exact same moment to enter the same pool.</p><p>This makes sense when the domain states that each ticket is associated with a single person and that a person can’t buy a ticket for someone else. In other words, if a person buys a ticket, they must use it themselves. They can’t buy multiple tickets for several people to enter. This doesn&#39;t have to be the case in all domains - we&#39;re just assuming here that people can&#39;t buy tickets for others.</p><p>In other domains, this might need to be modeled differently depending on the requirements. So, we’ll need to make sure that our model meets these types of requirements imposed by the domain, especially when defining <strong>primary keys</strong> or <code>UNIQUE</code> constraints.</p><p>So by requiring the attributes <code>PersonFK</code> and <code>PoolFK</code> to be present in the primary key, the Entry entity becomes weak in identification with two owning entities, <code>Person</code> and <code>CityPool</code>, respectively. In the DDL, we have explicitly added the <code>NOT NULL</code> constraint to all attributes for clarity, although it wouldn&#39;t be necessary for those present in the primary key.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Entry (</span>
<span class="line">    EntryTimestamp TIMESTAMP NOT NULL,</span>
<span class="line">    Price DOUBLE PRECISION NOT NULL CHECK (Price &gt;= 0),</span>
<span class="line">    PaymentMethod PaymentMethodType NOT NULL,</span>
<span class="line">    AppliedDiscount DOUBLE PRECISION NOT NULL CHECK (AppliedDiscount &gt;= 0),</span>
<span class="line">    Duration INT NOT NULL CHECK (Duration &gt;= 0),</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    PoolFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (EntryTimestamp, PersonFK, PoolFK),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (PoolFK) REFERENCES CityPool(PoolID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>On the other hand, the <code>EntryTimestamp</code> attribute of type <code>TIMESTAMP</code> is named differently from the <code>IssueTime</code> attribute of the <code>BusTicket</code> entity, for example.</p><p>This isn&#39;t very important, but in a real design process, we might be required to use style guides that determine how we should name each attribute depending on its semantics, type, or constraints, as well as when and how we should declare certain constraints. In this specific case, we didn&#39;t follow any style guide - we simply named the attributes as descriptively as possible according to the circumstances. Still, following a style guide offers advantages in system maintainability and ease of administration, among others.</p><h3 id="team-entity" tabindex="-1"><a class="header-anchor" href="#team-entity"><span><code>Team</code> entity</span></a></h3><p>To hold competitions in Olympic pools, our system needs to be able to model sports teams made up of people who participate in these competitions. So we have the entity <code>Team</code>, which represents sports teams that have a reference Olympic pool, are made up of people, and participate in competitions in Olympic pools.</p><p>This is modeled by using the attributes of <code>Team</code> to store the characteristics of the sports teams, such as the name, creation date, uniform color, and so on. We can also use associations with other entities to determine which Olympic pool is the team&#39;s official pool, which people belong to the team, who coaches the team, and which competitions they participate in.</p><p>First, to model the Olympic pool considered the team&#39;s official pool, we’ll use a <code>1-*</code> association with <code>OlympicPool</code>, which becomes a foreign key due to its cardinality. As you can see in the conceptual diagram, the role of the association specifies the semantics, since without it, you can’t directly infer what is being modeled with that association.</p><p>The same applies to the <code>1-*</code> association with <code>Person</code>, which we use to determine who coaches the team, so we need to specify its semantics to avoid confusion about what that association actually models. Although, given their cardinalities, it’s clear that a team can only have one person as a coach, not an arbitrary number of people, so we can rule out that the association models the people who belong to the team.</p><p>On the other hand, besides the foreign keys that Team has, there are others that point to Team and are responsible for modeling the people who make up the team, such as the one from Membership, or the team&#39;s participation in competitions, like the one we see from <code>Participation</code>.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE SportType AS ENUM (&#39;waterpolo&#39;, &#39;swimming&#39;, &#39;diving&#39;);</span>
<span class="line">CREATE TABLE Team (</span>
<span class="line">    Name VARCHAR(32),</span>
<span class="line">    CreationDate DATE NOT NULL,</span>
<span class="line">    ClothColor ColorType NOT NULL,</span>
<span class="line">    Sport SportType NOT NULL,</span>
<span class="line">    Budget INT NOT NULL CHECK (Budget &gt;= 0),</span>
<span class="line">    ContactEmail VARCHAR(32) NOT NULL,</span>
<span class="line">    CoachFK INT NOT NULL,</span>
<span class="line">    HomePoolFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (Name, CoachFK),</span>
<span class="line">    FOREIGN KEY (CoachFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (HomePoolFK) REFERENCES OlympicPool(PoolID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To uniquely identify each team, the attribute that can serve us best from the table itself is <code>Name</code>. But in this domain, we assume that multiple teams can have the same name, so the primary key can’t be formed solely by that attribute.</p><p>So from all the other attributes we have, we finally include the foreign key <code>CoachFK</code> in the primary key, meaning we also use the information of the person who coaches the team to uniquely identify it. This works because we assume that there can’t be multiple teams with the same name coached by the same person.</p><p>At first glance, this might seem entirely possible, but consider that some domain requirements might impose this condition, which we can leverage to define <strong>(<code>Name</code>, <code>CoachFK</code>)</strong> as the primary key. In any case, before making such a decision, make sure that the set of attributes meets the primary key restriction, either due to domain requirements or the semantics of the attributes themselves.</p><p>We can declare foreign keys with <code>FOREIGN KEY</code> referencing the primary key of Person and OlympicPool. We impose the <code>NOT NULL</code> restriction on them since all teams must have a coach and an official Olympic pool. Here, we have also assumed the necessity of these elements, but in other cases it might not be mandatory to have an official pool or a coach - it all depends on the domain.</p><p>If having a coach were not mandatory, we couldn’t include the foreign key attribute <code>CoachFK</code> in the primary key, as it could be NULL and would violate the primary key restriction. So for an entity to be weak in identification and another to be its owner, the association between them must be mandatory, meaning its minimum cardinality on the owner&#39;s side can’t be 0. Finally, we define a <code>TYPE ENUM</code> here for the type of sport the team plays, which is stored in the <code>Sport</code> attribute. But we don’t need to redefine it for the <code>Color</code> attribute, as we had the <code>ENUM ColorType</code> defined earlier, which is the best example of how a data type is <strong>reused</strong> across attributes with the same domain in different entities.</p><h3 id="membership-entity" tabindex="-1"><a class="header-anchor" href="#membership-entity"><span><code>Membership</code> entity</span></a></h3><p>We’ll continue with the semantics of the previous entity. To model the possibility of people being part of a team, the simplest approach would be to include an N-M association between <code>Person</code> and <code>Team</code>. This would be in addition to the <code>1-*</code> association that already exists to model the person who coaches the team. This way, a person can belong to an arbitrary number of teams, while a team can be composed of an arbitrary number of people.</p><p>But since this association requires an intermediate entity to be implemented at the logical level, and we also need to store information about a person&#39;s membership in a team, we’ll introduce the <code>Membership</code> entity. This entity divides the N-M association into several <code>1-*</code> associations, indirectly connecting <code>Person</code> with <code>Team</code>. In this way, each person belonging to a team will have a tuple in this table representing their membership. It’ll store information such as the start or end date of membership or the fee they must contribute to the team to be part of it.</p><p>At the conceptual level, we can see that this entity has many similarities with others like Residence. For example, we define the primary key of this entity with the attribute JoinDate and the foreign keys that determine the <code>Person</code> who belongs to a certain team. This is because the attributes that appear exclusively in the entity can’t uniquely identify each membership. That is, there can be multiple people who started belonging to different teams on the same date, causing multiple tuples in <code>Membership</code> with the same value in their primary key.</p><p>So even though the foreign key attributes don’t explicitly appear at the conceptual level, it’s clear that a <code>Membership</code> tuple must be identified not only by the start date but also by the person and team it relates to. This will avoid situations where multiple tuples with the same person and date are considered equal, or with the same team and start date. So we know it’s a weak entity in identification that’s dependent on <code>Person</code> and <code>Team</code>.</p><p>Since it depends on both entities it’s related to for identification, we could have represented it as an associative entity connected with the possible N-M association between <code>Person</code> and <code>Team</code>. But to make the diagram as clear and close to the logical level as possible, we should instead use an &quot;intermediate&quot; entity like the one represented here with <code>1-*</code> associations.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE PaymentFrequencyType AS ENUM(&#39;monthly&#39;, &#39;anual&#39;, &#39;weekly&#39;, &#39;quarterly&#39;);</span>
<span class="line">CREATE TABLE Membership (</span>
<span class="line">    JoinDate DATE NOT NULL,</span>
<span class="line">    LeaveDate DATE CHECK (</span>
<span class="line">        LeaveDate IS NULL</span>
<span class="line">        OR LeaveDate &gt;= JoinDate</span>
<span class="line">    ),</span>
<span class="line">    FeeAmount INT NOT NULL CHECK (FeeAmount &gt;= 0),</span>
<span class="line">    PaymentFrequency PaymentFrequencyType NOT NULL,</span>
<span class="line">    AutoRenewal BOOLEAN NOT NULL,</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    TeamNameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    CoachFK INT NOT NULL,</span>
<span class="line">    PRIMARY KEY (JoinDate, PersonFK, TeamNameFK, CoachFK),</span>
<span class="line">    FOREIGN KEY (PersonFK) REFERENCES Person(PersonID),</span>
<span class="line">    FOREIGN KEY (TeamNameFK, CoachFK) REFERENCES Team(Name, CoachFK)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To implement the foreign keys, we’ll create the corresponding attributes: <code>PersonFK</code>, which is the foreign key pointing to Person, and (<code>TeamNameFK</code>, <code>CoachFK</code>), where both constitute the other foreign key referencing the team to which the person belongs. Both keys are <strong>not null</strong> because a <code>Membership</code> tuple must associate a person with a team.</p><p>Once we’ve declared the attributes and <code>FOREIGN KEY</code> constraints, we can define the primary key as the set of attributes consisting of <strong><code>JoinDate</code></strong>, the foreign key attribute <code>PersonFK</code>, and the other two attributes <strong>(<code>TeamNameFK</code>, <code>CoachFK</code>)</strong> of the foreign key referencing the team. We can declare them in any order in the <code>PRIMARY KEY</code> constraint, as long as they all appear.</p><p>Finally, according to the domain, we assume that people don’t know exactly when they will stop being members of a team, so <code>LeaveDate</code> doesn’t always have to be defined. This means it can be <code>NULL</code> until the person leaves the team or plans to leave on a specific date. So we have to define a <code>CHECK</code> constraint on that attribute to make sure that it’s either <code>NULL</code> or the date is after JoinDate, as a person can’t leave a team before the start date of membership.</p><h3 id="participation-entity" tabindex="-1"><a class="header-anchor" href="#participation-entity"><span><code>Participation</code> entity</span></a></h3><p>Similarly, a sports team can also participate in sports competitions registered in <code>SwimmingCompetition</code>. So we have an entity called <code>Participation</code> that indirectly links Team with <code>SwimmingCompetition</code> through <code>1-*</code> associations. This is just like we saw earlier with <code>Membership</code>, but with a different meaning. Specifically, what mainly changes is the information stored about the team&#39;s participation in a competition, such as the date they register to participate, their ranking position after the competition, or the time it took to complete the competition.</p><p>To uniquely identify the tuples of <code>Participation</code>, the simplest way is to use a custom database identifier as a surrogate key, just as we have done before with certain entities. But if the domain requirements don’t allow us to include surrogate keys or any additional database-specific identifier, we’ll need to choose a set of attributes that enable identification.</p><p>So if we assume that no team participates more than once in the same competition (as this wouldn&#39;t make sense), we can declare a primary key formed by the foreign keys referencing <code>Team</code> and <code>SwimmingCompetition</code>. This way, we ensure that different tuples of <code>Participation</code> don’t associate the same team with the same competition, as that situation can’t occur.</p><p>As you can see, identifying this entity completely depends on other entities like Team and <code>SwimmingCompetition</code>, meaning there is no attribute at the conceptual level of the entity that forms part of the primary key.</p><p>This isn’t necessarily a bad thing, but rather a consequence of the domain requirements preventing us from using a surrogate key. In fact, this dependency in identification can have certain advantages, such as avoiding additional columns, which might be a domain-imposed requirement (to use the fewest columns possible).</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE Participation (</span>
<span class="line">    RegistrationDate DATE NOT NULL,</span>
<span class="line">    Rank INT NOT NULL CHECK (Rank &gt; 0),</span>
<span class="line">    RecordedTime DOUBLE PRECISION NOT NULL CHECK (RecordedTime &gt;= 0),</span>
<span class="line">    NameFK VARCHAR(32),</span>
<span class="line">    StartDateFK DATE,</span>
<span class="line">    EndDateFK DATE,</span>
<span class="line">    TeamNameFK VARCHAR(32),</span>
<span class="line">    CoachFK INT,</span>
<span class="line">    PRIMARY KEY (</span>
<span class="line">        NameFK,</span>
<span class="line">        StartDateFK,</span>
<span class="line">        EndDateFK,</span>
<span class="line">        TeamNameFK,</span>
<span class="line">        CoachFK</span>
<span class="line">    ),</span>
<span class="line">    FOREIGN KEY (TeamNameFK, CoachFK) REFERENCES Team(Name, CoachFK),</span>
<span class="line">    FOREIGN KEY (NameFK, StartDateFK, EndDateFK) REFERENCES SwimmingCompetition(Name, StartDate, EndDate)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>At the logical level, the foreign key referencing Team has two attributes, which make up the primary key of Team. The foreign key pointing to <code>SwimmingCompetition</code> has three for the same reason. So we’ll use two <code>FOREIGN KEY</code> constraints: one to declare the foreign key pointing to Team and the other for the one pointing to <code>SwimmingCompetition</code>, respectively.</p><p>Note that the <code>FOREIGN KEY</code> constraint only allows one <code>REFERENCES</code> clause. So if we have multiple foreign keys pointing to various entities, we have to use a separate <code>FOREIGN KEY</code> constraint for each foreign key. If we try to declare them all with a single constraint, we would have to indicate the multiple entities/tables being referenced, which means we would need to use multiple <code>REFERENCES</code> statements.</p><p>After declaring the foreign keys and adding their respective <code>NOT NULL</code> constraint, since it’s mandatory for a participation to relate a team with a competition, we declare the foreign key as the set of attributes that form both foreign keys together. So in our system, there can be <code>Participation</code> tuples with different <code>Rank</code> or <code>RegistrationDate</code> values without any problem - but there can’t be multiple tuples with the same value in their primary key (meaning they can’t relate the same team with the same competition multiple times).</p><p>Finally, if we try to reconstruct the conceptual entity from the relational diagram, the first thing we should notice is that all the foreign keys are underlined, and therefore form the primary key. As they are foreign keys, these attributes won’t appear in the conceptual entity of <code>Participation</code>.</p><p>To determine how many foreign keys we actually have, and know how many <code>1-*</code> associations to introduce and with which entities to connect them, we can see that a subset of attributes like <strong>(<code>TeamNameFK</code>, <code>CoachFK</code>)</strong> refers to the same entity - so there will be a <code>1-*</code> relationship with that entity, with the many side in <code>Participation</code>. Doing the same with the attributes <strong>(<code>NameFK</code>, <code>StartDateFK</code>, <code>EndDateFK</code>)</strong>, we see that they all refer to attributes of the same entity. So they form a foreign key that results in a <code>1-*</code> association like the previous one, but connecting with another entity.</p><p>To infer the minimum cardinalities, we should look at the constraints indicated in the relational diagram: which foreign keys can or can’t be <code>NULL</code>, or how many participations each competition must have (as well as the participations each team must have).</p><p>In this case, we haven’t indicated any constraints in the relational diagram for simplicity. But, for example, if we were told that a foreign key can’t be <code>NULL</code> in the relational model, this conceptually translates to its respective <code>1-*</code> association having a minimum cardinality of 1 on the 1 side. Similarly, if there are special constraints that require each team to have 2 participations, for example, then we know that in its corresponding association, the minimum cardinality on the <code>Participation</code> side would be 2. This reverse process is what we initially followed to implement the entity at the logical level, where the attributes that make up each foreign key are inferred, and those selected to declare the primary key.</p><h3 id="swimmingcompetition-entity" tabindex="-1"><a class="header-anchor" href="#swimmingcompetition-entity"><span><code>SwimmingCompetition</code> entity</span></a></h3><p>To model the sports competitions that can take place in an Olympic pool, in the conceptual diagram we have the entity called <code>SwimmingCompetition</code> that’s responsible for storing information about the competitions held in all the Olympic pools registered in the system. In these, any number of sports teams can participate.</p><p>The information that <code>SwimmingCompetition</code> stores mainly depends on the domain and requirements. In this case, we assume that we only need to store the name of the competition, start and end dates that will always be determined, a <code>RecordTime</code> attribute to store any record times achieved during the course of that competition, and the monetary amount of the prize for that competition.</p><p>With these attributes, the simplest way to uniquely identify each tuple in the <code>SwimmingCompetition</code> table is to define the set of attributes <strong>(<code>Name</code>, <code>StartDate</code>, <code>EndDate</code>)</strong> as the primary key.</p><p>For example, there can be competitions in the database with exactly the same name, but they can never have the same start and end dates simultaneously (because that would mean they were the same competition). Ultimately, by declaring this primary key, we’re assuming that there are no different competitions with the same name and start and end dates - so if this condition aligns with the domain requirements, it would be correct.</p><p>Consequently, there can be different competitions in the database with different combinations of values for the primary key attributes - but they might have the <strong>same record time</strong>, or the same prize in <code>PrizeAmount</code>, since there are no restrictions preventing it.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE SwimmingCompetition (</span>
<span class="line">    Name VARCHAR(32),</span>
<span class="line">    StartDate DATE NOT NULL,</span>
<span class="line">    EndDate DATE NOT NULL CHECK (EndDate &gt;= StartDate),</span>
<span class="line">    RecordTime DOUBLE PRECISION CHECK (RecordTime &gt;= 0),</span>
<span class="line">    PrizeAmount INT NOT NULL CHECK (PrizeAmount &gt;= 0),</span>
<span class="line">    PRIMARY KEY (Name, StartDate, EndDate)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the conceptual diagram, we see that the entity has several associations that lead to the existence of a foreign key pointing to <code>OlympicPool</code>, since the competition must necessarily take place in an Olympic pool. So this foreign key references the specific pool where the competition is held, making its existence mandatory. In other words, the foreign key can’t be NULL because, in the conceptual model, we set the minimum cardinality to 1 to ensure that every competition is associated with a pool where it takes place.</p><p>Another peculiarity of this entity is that the attribute <code>RecordTime</code> may not always be defined. For example, when we register a competition in the database and need to provide a value for this attribute, such value might not exist because it’s the first time the competition is being held. So, the simplest way to model it would be to set that attribute to the maximum or minimum possible, depending on how we consider which times are better than others.</p><p>Additionally, since in our domain we also model the possibility of athletes participating in a competition being sanctioned, there is a chance that in a certain competition held for the first time, all participants could be sanctioned. This means that none of them would contribute to initializing the value of the <code>RecordTime</code> attribute. This is why it needs to be allowed to be <code>NULL</code>.</p><p>But this is a decision we must make primarily considering the domain and its requirements, as we may want to initialize the attribute with a default or special value if all athletes are penalized and the counter can’t be initialized, for example.</p><h3 id="sanction-entity" tabindex="-1"><a class="header-anchor" href="#sanction-entity"><span><code>Sanction</code> entity</span></a></h3><p>Given everything a person can do in our domain in relation to other entities, they might break a rule that results in a sanction. So in our schema, we can introduce an IS-A hierarchy where the superclass is the entity <code>Sanction</code>, and its inherited entities are the different types of sanctions we define, all depending on their scope of application.</p><p>Specifically, deciding to use a hierarchy to model sanctions is driven by the specific information that needs to be stored for each type of sanction. For this reason, if we tried to use a single <code>Sanction</code> entity to represent all these types, its semantics would be very complicated (as some attributes would only be useful if the sanction were of a certain type - and the same goes for many others). We would also need to use a specific attribute to represent the sanction type, since otherwise knowing the exact type might depend on which attributes were <code>NULL</code>, and this would complicate queries.</p><p>So with this hierarchy, we can have a set of common attributes for all sanctions in <code>Sanction</code>, such as the monetary amount of the fine, the description, the date of the sanction, or the status, while in the inherited entities, we have specific attributes that characterize each type of sanction.</p><h3 id="how-is-the-is-a-hierarchy-implemented-with-tables" tabindex="-1"><a class="header-anchor" href="#how-is-the-is-a-hierarchy-implemented-with-tables"><span>How is the IS-A hierarchy implemented with tables?</span></a></h3><p>Just as we have done with other hierarchies, we need to analyze it to know how to implement it at the logical level. We need to keep in mind that the conceptual design doesn’t unequivocally determine the implementation that we’ll ultimately carry out, especially when working with IS-A hierarchy. Rather, it’s a decision we should make based not only on the conceptual design itself, but also on the domain and data requirements.</p><p>To do this, we first check whether the hierarchy is complete or not. In this case, all existing sanctions will be of a specific type represented by the inherited entities. This means that all individuals in the hierarchy will belong to one of the sets generated by these entities, which implies that the hierarchy is <strong>complete</strong>.</p><p>On the other hand, the types of sanctions are all disjoint, meaning a sanction can only be of one type, not several at once. This means that the hierarchy is disjoint because no individual will be represented by multiple inherited entities at the same time.</p><p>To identify each sanction, we’ll use a <code>SanctionID</code> attribute in the superclass Sanction, which we’ll implement using a surrogate key. This avoids the inherited entities needing to use their own identifiers, as we assume that the domain requirements don’t require us to identify each type of sanction differently.</p><p>So given the number of attributes each inherited entity has, it’s clear that we’ll need a table to implement each inherited entity. Otherwise, too many <code>NULL</code> values would be generated in the corresponding attributes, complicating both database management and queries, and potentially leading to unnecessary constraints aimed at ensuring schema integrity.</p><p>On the other hand, we have several options for implementing the superclass at the logical level. One option is to duplicate all attributes in each of the tables of the specific sanction types.</p><p>This has advantages, such as identifying each table using the <code>SanctionID</code> attribute inherited from the superclass, but it leads to schema management problems. If we later want to delete, modify, or add an attribute of <code>Sanction</code>, we would have to perform that operation on all the tables of the different sanction types, increasing the likelihood of errors in the process. Also, if we want to query all the sanctions in our database, with this option, we would have to go through all the tuples of all the tables of each sanction type, which could be inefficient because of accessing multiple tables.</p><p>To minimize errors in the database management process, we need to simplify the operations involved as much as possible. To do this, we can implement a specific table for the superclass of the hierarchy in the same way as we did with the Vehicle hierarchy (and for similar reasons).</p><p>Each of the tables for the inherited entities will have a foreign key referencing the superclass table, where we’ll store the information of attributes common to all sanctions. This makes it easier to modify these attributes. It also simplifies other operations, such as adding a new type of sanction. For this, only a new table for that type needs to be created, and we just need to make sure that its foreign key references <code>Sanction</code>.</p><p>If we look at the inherited entities, we’ll see that each one has a <code>1-*</code> association with other entities where the many side is always in the entities of the hierarchy. This means that using a single table to implement the entire hierarchy isn’t a good idea, as it would combine all those foreign keys into one table. This would lead to much more complicated integrity constraints.</p><p>In other words, if a sanction is of a specific type, the attributes and foreign keys of the remaining types must be <code>NULL</code>, so ensuring this for all types involves overly elaborate and complex constraints.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TYPE SanctionStatusType AS ENUM (&#39;created&#39;, &#39;active&#39;, &#39;expired&#39;);</span>
<span class="line">CREATE TABLE Sanction (</span>
<span class="line">    SanctionID SERIAL PRIMARY KEY,</span>
<span class="line">    Amount DOUBLE PRECISION NOT NULL CHECK (Amount &gt;= 0),</span>
<span class="line">    Description VARCHAR(32),</span>
<span class="line">    IssueDate DATE NOT NULL,</span>
<span class="line">    ExpirationDate DATE CHECK (</span>
<span class="line">        ExpirationDate IS NULL</span>
<span class="line">        OR ExpirationDate &gt;= IssueDate</span>
<span class="line">    ),</span>
<span class="line">    Status SanctionStatusType NOT NULL</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the DDL of the <code>Sanction</code> table, we can see that its primary key <code>{SanctionID}</code> consists of a single attribute of type <code>SERIAL</code>. This is a surrogate key that will be used to identify all sanctions in the database, regardless of their type.</p><p>This table also stores the status of the sanction in an attribute, since the type of sanction is represented with inherited entities from the superclass at the conceptual level. So the status must be modeled as an attribute to avoid mixing the semantics of what we represent with each tool of the entity-relationship diagram.</p><p>In other words, we could include new inherited entities that model the states of the sanctions, but we have to consider that each type of sanction could be in any of those states - leading to an unnecessarily complicated multi-level hierarchy.</p><p>Because of this, we should separate the semantics of what we represent with inherited entities from the semantics of the sanction&#39;s status, modeling it with an attribute in the superclass, as any type of sanction can be in any state.</p><p>In this Status attribute, we define a TYPE ENUM to restrict the possible states a sanction can have. But for the Description, if we want to save a description of the sanction written in natural language, we shouldn’t add restrictions unless it’s required by the project specifications.</p><p>A description in natural language can be very diverse, so the simplest approach is not to limit the possible values the attribute can take, not even with a <code>NOT NULL</code> constraint. This can indicate that a sanction has no description, although this isn’t necessarily correct.</p><p>In general, decisions to allow <code>NULL</code> values also depend on the domain and requirements. For example, sanctions may or may not have an expiration date, which is why the <code>CHECK</code> constraint defined on ExpirationDate specifies that this attribute can either be <code>NULL</code> or must hold a date later than the issuance date of the sanction.</p><h3 id="drivingsanction-entity" tabindex="-1"><a class="header-anchor" href="#drivingsanction-entity"><span><code>DrivingSanction</code> entity</span></a></h3><p>Let’s now talk about the different types of sanctions in our system. First, we have <code>DrivingSanction</code>, which are sanctions associated with driver&#39;s licenses. So in the conceptual diagram, it has a <code>1-*</code> association with the <code>DrivingLicense</code> entity, resulting in a foreign key in <code>DrivingSanction</code> that references the driver&#39;s license with the sanction. This refers to the license of the person who committed a traffic violation, leading to the existence of the fine.</p><p>The specific attributes of this type of sanction are store information about why the fine was issued, such as the speed the vehicle was going, as well as the effect the sanction has on the license (like deducting a certain number of points or suspending it for a certain period).</p><p>In its DDL, we can see that all attributes have been declared as <code>NOT NULL</code>, which at first might seem unnecessary in the case of <code>RecordedSpeed</code>, since not all sanctions are caused by speed. But this illustrates that even if an attribute isn’t necessary, it shouldn’t be <code>NULL</code> to be considered unnecessary.</p><p>For example, if a sanction is not related to speed, instead of using a <code>NULL</code> value in the <code>RecordedSpeed</code> attribute, we can use a special value like 0, as long as it respects the integrity constraints and system domain requirements. This allows us to distinguish whether the sanction is related to a possible speeding violation. So we make the decision to allow <code>NULL</code> or not is initially when modeling the entity at the logical level. This works as long as we aren’t forced to use a specific semantics like setting the attribute to 0 when it’s not necessary.</p><p>If we consider whether other attributes can be <code>NULL</code> or not, we can see that PermanentSuspension always has the option to take the value <strong>false</strong> (as the suspension might not be permanent). Similarly, if the suspension is permanent, the <code>SuspensionDays</code> attribute can always be set to 0, or to a different special value. We could also simply ignore its value and check first if the suspension is permanent before accessing the <code>SuspensionDays</code> attribute, among other options.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE DrivingSanction (</span>
<span class="line">    SanctionID SERIAL PRIMARY KEY,</span>
<span class="line">    RecordedSpeed DOUBLE PRECISION NOT NULL CHECK (RecordedSpeed &gt;= 0),</span>
<span class="line">    PointsDeducted INT NOT NULL CHECK (PointsDeducted &gt;= 0),</span>
<span class="line">    SuspensionDays INT NOT NULL CHECK (SuspensionDays &gt;= 0),</span>
<span class="line">    PermanentSuspension BOOLEAN NOT NULL,</span>
<span class="line">    LicenseFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (SanctionID) REFERENCES Sanction(SanctionID),</span>
<span class="line">    FOREIGN KEY (LicenseFK) REFERENCES DrivingLicense(LicenseID)</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>On the other hand, the <code>NOT NULL</code> is indeed necessary for both foreign keys in the table, as <code>SanctionID</code> is the foreign key that references the tuple in <code>Sanction</code> that holds the rest of the sanction information. Its primary key attribute <code>SanctionID</code> serves as the primary key of the <code>DrivingSanction</code> table itself, and it’s the only way to uniquely identify the sanctions. Also, the foreign key that references the driving license that received the sanction can’t be <code>NULL</code> either, because if the sanction is of type <code>DrivingSanction</code>, it must necessarily be associated with a license.</p><h3 id="sportsanction-entity" tabindex="-1"><a class="header-anchor" href="#sportsanction-entity"><span><code>SportSanction</code> entity</span></a></h3><p>Another type of sanction is represented in the entity <code>SportSanction</code>. This models those sanctions that occur in sports competitions, specifically those caused by a sports team while participating in a competition. Like the previous entity, it has attributes that characterize this type of sanction, such as the number of competitions the team is suspended or the name of the referee who issued the sanction.</p><p>In addition to this information, each sanction of this type needs to know which specific team received the sanction, as well as the competition they were participating in when they were sanctioned. So to model this, we have multiple options. We could use two <code>1-*</code> associations to connect <code>SportSanction</code> with <code>Team</code> and with <code>SwimmingCompetition</code>, so that from the sanction you can identify the corresponding team and competition. But this is redundant and unnecessary, as it would lead to two foreign keys that can actually be reduced to one.</p><p>Remember that in our schema, we have an entity called <code>Participation</code> that relates teams to the competitions they participate in. So instead of two <code>1-*</code> associations in <code>SportSanction</code>, we can use just one that connects it with <code>Participation</code>, since from <code>Participation</code> we can determine the team and competition.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE SportSanction (</span>
<span class="line">    SanctionID SERIAL PRIMARY KEY,</span>
<span class="line">    SuspendedCompetitions INT NOT NULL CHECK (SuspendedCompetitions &gt;= 0),</span>
<span class="line">    RefereeName VARCHAR(32) NOT NULL,</span>
<span class="line">    NameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    StartDateFK DATE NOT NULL,</span>
<span class="line">    EndDateFK DATE NOT NULL,</span>
<span class="line">    TeamNameFK VARCHAR(32) NOT NULL,</span>
<span class="line">    CoachFK INT,</span>
<span class="line">    FOREIGN KEY (SanctionID) REFERENCES Sanction(SanctionID),</span>
<span class="line">    FOREIGN KEY (</span>
<span class="line">        NameFK,</span>
<span class="line">        StartDateFK,</span>
<span class="line">        EndDateFK,</span>
<span class="line">        TeamNameFK,</span>
<span class="line">        CoachFK</span>
<span class="line">    ) REFERENCES Participation(</span>
<span class="line">        NameFK,</span>
<span class="line">        StartDateFK,</span>
<span class="line">        EndDateFK,</span>
<span class="line">        TeamNameFK,</span>
<span class="line">        CoachFK</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To implement this in SQL, we can create a table very similar to <code>DrivingSanction</code>, where its primary key is the attribute <code>SanctionID</code>, also declared as a foreign key referencing the Sanction table of the superclass. We can declare attributes in the same way as we have been doing so far, both for the attributes of the entity itself and for the foreign keys. The foreign keys must have the same data types as the attributes they reference.</p><p>In this case, to declare the foreign key that points to <code>Participation</code>, we need as many attributes as its respective primary key has, which is a total of 5. To simplify this process, the ideal approach is to look directly at the <code>PRIMARY KEY</code> constraint of the table we want to reference. Then for each of those attributes, we can declare it in our table with a characteristic name and the corresponding data type. We finally add it to the <code>FOREIGN KEY</code> constraint so that it references the attribute that originated it, as we have already seen.</p><p>For example, if the primary key of <code>Participation</code> is <strong>(<code>NameFK</code>, <code>StartDateFK</code>, <code>EndDateFK</code>, <code>TeamNameFK</code>, <code>CoachFK</code>)</strong>, then we declare an attribute <code>NameFK</code> for the foreign key of <code>SportSanction</code> that points to the <code>NameFK</code> attribute of that primary key, another <code>StartDateFK</code> that points to the <code>StartDateFK</code> attribute of the primary key of <code>Participation</code>, and so on.</p><h3 id="poolsanction-entity" tabindex="-1"><a class="header-anchor" href="#poolsanction-entity"><span><code>PoolSanction</code> entity</span></a></h3><p>To conclude the hierarchy of sanctions, we have <code>PoolSanction</code>. These, as you can guess, are sanctions imposed on people who have entered a <code>CityPool</code> and violated the pool rules. In this case, we store start and end dates as attributes, indicating the period during which the person can’t enter the pool. We can also include an amount as compensation if necessary, or a number of community service hours that the person must complete.</p><p>To determine from the sanction which person and pool are affected by the sanction, we can use a <code>1-*</code> association with <code>Entry</code>. This results in a foreign key in <code>PoolSanction</code> that points to <code>Entry</code> because the many side is placed in <code>PoolSanction</code>. This way, we can identify the entry the person used when they received the sanction.</p><p>Besides the person, the entry also provides information about the pool they will no longer be able to enter freely. The sanction determines when they can re-enter or the action they must take due to being sanctioned.</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE TABLE PoolSanction (</span>
<span class="line">    SanctionID SERIAL PRIMARY KEY,</span>
<span class="line">    BanStartDate DATE,</span>
<span class="line">    BanEndDate DATE,</span>
<span class="line">    CompensationRequired INT NOT NULL CHECK (CompensationRequired &gt;= 0),</span>
<span class="line">    CommunityServiceHours INT NOT NULL CHECK (CommunityServiceHours &gt;= 0),</span>
<span class="line">    EntryFK TIMESTAMP NOT NULL,</span>
<span class="line">    PersonFK INT NOT NULL,</span>
<span class="line">    PoolFK INT NOT NULL,</span>
<span class="line">    FOREIGN KEY (SanctionID) REFERENCES Sanction(SanctionID),</span>
<span class="line">    FOREIGN KEY (EntryFK, PersonFK, PoolFK) REFERENCES Entry(EntryTimestamp, PersonFK, PoolFK),</span>
<span class="line">    CHECK (</span>
<span class="line">        (</span>
<span class="line">            BanEndDate IS NULL</span>
<span class="line">            AND BanStartDate IS NULL</span>
<span class="line">        )</span>
<span class="line">        OR BanEndDate &gt;= BanStartDate</span>
<span class="line">    )</span>
<span class="line">);</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In its DDL, we can see that the table identification is the same as the previous ones, with a primary key composed of the foreign key pointing to Sanction, as well as another foreign key pointing to <code>Entry</code> (which consists of three attributes). In this case, the sanction can impose several conditions on the sanctioned user: it can either prohibit them from entering for a period of time, require them to pay compensation, or serve a certain number of community service hours.</p><p>So if we assume that the domain and requirements don’t force us to store <code>NULL</code> values in any attribute and that we can make any decision about how data is stored in the system, we’ll decide to allow BanStartDate and <code>BanEndDate</code> to be <code>NULL</code> for sanctions that don’t prohibit the sanctioned user from entering the pool. Thus, in the <code>CHECK</code> constraint defined at the end, we see that as an integrity condition for all tuples in the table, both attributes must be either null or the end date must be after the start date of the prohibition. This ensures that only valid data is stored in the table.</p><p>Lastly, we can see that some attributes of the foreign key pointing to <code>Entry</code> are named exactly the same as the attributes they reference, like personFK or <code>PoolFK</code>. This is neither a problem nor an error, although in a larger project where each table has more attributes, we should follow a proper style guide when naming attributes, especially those reserved for foreign keys. This way, we can more clearly understand their purpose without having to spend time analyzing the schema in detail.</p><hr><h2 id="how-to-create-the-database" tabindex="-1"><a class="header-anchor" href="#how-to-create-the-database"><span>How to Create the Database</span></a></h2><p>Now that you understand the domain semantics and have completed the <strong>conceptual</strong> and <strong>logical design phases</strong>, we can implement the logical model on the DBMS.</p><p>The easiest way to do this is by creating a script with a <code>.sql</code> extension that contains all the necessary DDL code to populate the database - that is, the statements we just reviewed where we create tables, data types, and constraints.</p><p>But since we aren’t working with a real project database here, we don&#39;t need to worry about the data that might be in tables that already exist in the database, especially those with the same name as any of the tables we’ll going to create. So for simplicity, before creating them, we’ll execute some DROP statements to remove tables with names matching any of the tables we are going to create. This will make sure that they contain no tuples.</p>`,394)),i("p",null,[e[91]||(e[91]=t("Following this process, we’ll arrive at a DDL script ")),i("a",D,[e[88]||(e[88]=t("like this (")),n(o,{icon:"iconfont icon-github"}),e[89]||(e[89]=i("code",null,"cardstdani",-1)),e[90]||(e[90]=t(")"))]),e[92]||(e[92]=t(" (it’s quite long, so I’ve left it in the gist)."))]),e[125]||(e[125]=r("<p>When we run the script, keep in mind that the statements will execute one by one from top to bottom. So we first use the DROP statements to remove any tables in the database that have the same name as any of those we’ll create.</p><p>This process is equivalent to <strong>deleting</strong> our entire database - that is, our logical model that was once created - so we first need to remove the tables that aren’t <strong>referenced</strong> by any <strong>foreign keys</strong> to maintain integrity while deleting the remaining tables.</p><p>Then, under the same condition, all corresponding tables that aren’t referenced by any foreign keys are successively deleted until no tables remain to be deleted.</p><p>There should now be no tables in our database whose names conflict with the tables in our logical model, so we write the <code>CREATE TABLE</code> statements we saw earlier for each table in the logical model.</p><p>We also need to do this in a specific order, specifically the reverse of the deletion process. Here, we first need to create tables that don’t have any foreign keys pointing to another entity. If we create a table at the beginning that needs to reference another table that hasn&#39;t been created yet, the DBMS will generate an integrity error. So as you can see in the script, we place the statements in an order such that whenever a table with foreign keys pointing to other tables is created, those tables have already been created beforehand.</p>",5)),i("p",null,[e[98]||(e[98]=t("To figure out how we need to order both the DROP and ")),e[99]||(e[99]=i("code",null,"CREATE TABLE",-1)),e[100]||(e[100]=t(" statements, there are ")),i("a",S,[e[93]||(e[93]=t("algorithms (")),n(o,{icon:"fa-brands fa-medium"}),e[94]||(e[94]=i("code",null,"@tharinduimalka915",-1)),e[95]||(e[95]=t(")"))]),e[101]||(e[101]=t(" like ")),i("a",A,[n(o,{icon:"fa-brands fa-wikipedia-w"}),e[96]||(e[96]=t("topological sorting"))]),e[102]||(e[102]=t(" that we can apply to the relational diagram. This way, we treat the database schema as a ")),e[103]||(e[103]=i("strong",null,"directed graph",-1)),e[104]||(e[104]=t(" made up of ")),e[105]||(e[105]=i("strong",null,"nodes (tables)",-1)),e[106]||(e[106]=t(" and ")),e[107]||(e[107]=i("strong",null,"directed edges (foreign keys)",-1)),e[108]||(e[108]=t(". With this algorithm, for example, we can progressively remove minimal or maximal nodes from the graph, creating or deleting the table they represent. But, this is not the only ")),i("a",P,[n(o,{icon:"fas fa-globe"}),e[97]||(e[97]=t("method"))]),e[109]||(e[109]=t(" available."))]),e[126]||(e[126]=r(`<p>Regarding data types and constraints defined in <strong>assertions</strong> or <strong>triggers</strong>, the order of creation is easier to infer. This is because the <code>ENUM</code> or <code>DOMAIN</code> types must always be created before being used in a table&#39;s attribute declaration. So the simplest approach is to create them at the very beginning, or just before we use them for the first time (what we’ve done here).</p><p>On the other hand, it&#39;s best to define assertions or triggers at the end. We also want to give them names descriptive enough of the constraints they model, as their definitions may involve multiple tables that we need to create before defining the constraint. Also, since these elements don’t contain <strong>data (tuples)</strong>, we don’t need to delete them at the start of the script unless we are going to modify the schema itself. In that case, some constraints might become obsolete, meaning they access attributes or tables that no longer exist.</p><p>In summary, with this <strong>SQL script</strong>, we create the tables, data types, and constraints that make up our database schema, ensuring that none of them contain tuples immediately after being created.</p><p>But to run the script, we need to create a database in the DBMS. Let’s use the <code>CREATE DATABASE</code> statement to create a new database with a specific name:</p><div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">CREATE DATABASE ExampleDataBase OWNER postgres;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>If we run this command on the DBMS terminal, we will create a completely empty database named <strong>&quot;exampledatabase&quot;</strong>. Note that PostgreSQL is not case-sensitive for element names or SQL statements. So even if we write an element&#39;s name in uppercase, when we later check the name value stored by the DBMS for the database, we’ll see it in lowercase.</p>`,6)),i("p",null,[e[112]||(e[112]=t("We can also assign an owner user, who will have all the ")),i("a",F,[n(o,{icon:"iconfont icon-postgresql"}),e[110]||(e[110]=t("privileges"))]),e[113]||(e[113]=t(" over that element. By default, we can make the owner user ")),i("a",q,[n(o,{icon:"fa-brands fa-stack-overflow"}),e[111]||(e[111]=t("postgres"))]),e[114]||(e[114]=t(", but we can change it later with a statement like the following:"))]),e[127]||(e[127]=r(`<div class="language-pgsql line-numbers-mode" data-highlighter="prismjs" data-ext="pgsql" data-title="pgsql"><pre><code><span class="line">ALTER DATABASE exampledatabase</span>
<span class="line">OWNER TO user3; /*user3 is a sample user*/</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Once we’ve created the database, we can connect to it using the DBMS command <code>\\c exampledatabase</code>. Finally, we can execute the <code>.sql</code> script with the command <code>\\i /path_to_script/script.sql</code>. The DBMS should then notify us that the <code>DROP</code> statements have had no effect since there is no table with the corresponding name to delete (the database is empty). But, after creating the tables, if we run the script again, the <code>DROP</code> statements will delete them because they are created, preventing the DBMS from giving us these notifications.</p><p>Similarly, if any statements encounter errors that prevent their execution, or in special situations like the one we just mentioned, the DBMS will notify us - but it won’t stop the execution of the script. It will simply move on to execute the next declared statements (at the syntactic level, it executes the next statement we have separated with the corresponding <code>;</code>).</p>`,3))])}const x=u(v,[["render",O],["__file","chapter-10-database-design-process-example.html.vue"]]),B=JSON.parse('{"path":"/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-10-database-design-process-example.html","title":"Chapter 10: Database Design Process Example","lang":"en-US","frontmatter":{"lang":"en-US","title":"Chapter 10: Database Design Process Example","description":"Article(s) > (11/12) How to Design Structured Database Systems Using SQL [Full Book]","category":["Data Science","PostgreSQL","Design","System"],"tag":["blog","freecodecamp.org","data-science","sql","postgres","posgtresql","design","system"],"head":[[{"meta":null},{"property":"og:title","content":"Article(s) > (11/12) How to Design Structured Database Systems Using SQL [Full Book]"},{"property":"og:description","content":"Chapter 10: Database Design Process Example"},{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-10-database-design-process-example.html"}],["meta",{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-10-database-design-process-example.html"}],["meta",{"property":"og:site_name","content":"📚Bookshelf"}],["meta",{"property":"og:title","content":"Chapter 10: Database Design Process Example"}],["meta",{"property":"og:description","content":"Article(s) > (11/12) How to Design Structured Database Systems Using SQL [Full Book]"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:image:src","content":"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png"}],["meta",{"name":"twitter:image:alt","content":"Chapter 10: Database Design Process Example"}],["meta",{"property":"article:author","content":"Daniel García Solla"}],["meta",{"property":"article:tag","content":"blog"}],["meta",{"property":"article:tag","content":"freecodecamp.org"}],["meta",{"property":"article:tag","content":"data-science"}],["meta",{"property":"article:tag","content":"sql"}],["meta",{"property":"article:tag","content":"postgres"}],["meta",{"property":"article:tag","content":"posgtresql"}],["meta",{"property":"article:tag","content":"design"}],["meta",{"property":"article:tag","content":"system"}],["meta",{"property":"article:published_time","content":"2025-08-14T00:00:00.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Chapter 10: Database Design Process Example\\",\\"image\\":[\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1754234568579/a4165e66-85f1-4a81-b85d-a2be8ea19db3.jpeg\\",\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1753355497082/c7f6079b-a88b-4651-a92b-36761151aa80.png\\",\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1754729845339/090a3c2f-0833-4f14-8a58-52fb1a1606ea.png\\",\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1752833986569/a77fb4b1-3632-4d63-ad28-750c6768ef0d.png\\",\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1752916101394/f9b94685-4891-4174-b38d-35c6d164744b.png\\",\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1754731726248/2494ed94-aca0-401d-abe5-1e91481f336a.png\\"],\\"datePublished\\":\\"2025-08-14T00:00:00.000Z\\",\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Daniel García Solla\\",\\"url\\":\\"https://freecodecamp.org/news/author/cardstdani/\\"}]}"]],"date":"2025-08-14T00:00:00.000Z","isOriginal":false,"author":[{"name":"Daniel García Solla","url":"https://freecodecamp.org/news/author/cardstdani/"}],"cover":"https://cdn.hashnode.com/res/hashnode/image/upload/v1755095979245/dfd39c26-3456-4e79-a01c-0b2a82f7a034.png","gitInclude":[]},"headers":[{"level":2,"title":"Database Levels","slug":"database-levels","link":"#database-levels","children":[]},{"level":2,"title":"The Database Design Process","slug":"the-database-design-process","link":"#the-database-design-process","children":[]},{"level":2,"title":"Entity-Relationship to Logical Model","slug":"entity-relationship-to-logical-model","link":"#entity-relationship-to-logical-model","children":[{"level":3,"title":"Person entity","slug":"person-entity","link":"#person-entity","children":[]},{"level":3,"title":"Rental entity","slug":"rental-entity","link":"#rental-entity","children":[]},{"level":3,"title":"CarOwnership entity","slug":"carownership-entity","link":"#carownership-entity","children":[]},{"level":3,"title":"Residence entity","slug":"residence-entity","link":"#residence-entity","children":[]},{"level":3,"title":"ShipAssignment entity","slug":"shipassignment-entity","link":"#shipassignment-entity","children":[]},{"level":3,"title":"City entity","slug":"city-entity","link":"#city-entity","children":[]},{"level":3,"title":"Port entity","slug":"port-entity","link":"#port-entity","children":[]},{"level":3,"title":"CruiseLine entity","slug":"cruiseline-entity","link":"#cruiseline-entity","children":[]},{"level":3,"title":"Vehicle entity","slug":"vehicle-entity","link":"#vehicle-entity","children":[]},{"level":3,"title":"How is this IS-A hierarchy implemented with tables?","slug":"how-is-this-is-a-hierarchy-implemented-with-tables","link":"#how-is-this-is-a-hierarchy-implemented-with-tables","children":[]},{"level":3,"title":"CruiseShip entity","slug":"cruiseship-entity","link":"#cruiseship-entity","children":[]},{"level":3,"title":"Bike entity","slug":"bike-entity","link":"#bike-entity","children":[]},{"level":3,"title":"Car entity","slug":"car-entity","link":"#car-entity","children":[]},{"level":3,"title":"CityBus entity","slug":"citybus-entity","link":"#citybus-entity","children":[]},{"level":3,"title":"CarRegistration entity","slug":"carregistration-entity","link":"#carregistration-entity","children":[]},{"level":3,"title":"DrivingLicenseRequest entity","slug":"drivinglicenserequest-entity","link":"#drivinglicenserequest-entity","children":[]},{"level":3,"title":"How can we model the driving license requests hierarchy at the logical level?","slug":"how-can-we-model-the-driving-license-requests-hierarchy-at-the-logical-level","link":"#how-can-we-model-the-driving-license-requests-hierarchy-at-the-logical-level","children":[]},{"level":3,"title":"RejectedDrivingLicense entity","slug":"rejecteddrivinglicense-entity","link":"#rejecteddrivinglicense-entity","children":[]},{"level":3,"title":"DrivingLicense entity","slug":"drivinglicense-entity","link":"#drivinglicense-entity","children":[]},{"level":3,"title":"BusTrip entity","slug":"bustrip-entity","link":"#bustrip-entity","children":[]},{"level":3,"title":"BusTicket entity","slug":"busticket-entity","link":"#busticket-entity","children":[]},{"level":3,"title":"BusPass entity","slug":"buspass-entity","link":"#buspass-entity","children":[]},{"level":3,"title":"Voyage entity","slug":"voyage-entity","link":"#voyage-entity","children":[]},{"level":3,"title":"CruiseBooking entity","slug":"cruisebooking-entity","link":"#cruisebooking-entity","children":[]},{"level":3,"title":"Pool entity","slug":"pool-entity","link":"#pool-entity","children":[]},{"level":3,"title":"How can we translate the pool hierarchy entities at the conceptual level to tables?","slug":"how-can-we-translate-the-pool-hierarchy-entities-at-the-conceptual-level-to-tables","link":"#how-can-we-translate-the-pool-hierarchy-entities-at-the-conceptual-level-to-tables","children":[]},{"level":3,"title":"CruisePool entity","slug":"cruisepool-entity","link":"#cruisepool-entity","children":[]},{"level":3,"title":"CityPool entity","slug":"citypool-entity","link":"#citypool-entity","children":[]},{"level":3,"title":"OlympicPool entity","slug":"olympicpool-entity","link":"#olympicpool-entity","children":[]},{"level":3,"title":"Entry entity","slug":"entry-entity","link":"#entry-entity","children":[]},{"level":3,"title":"Team entity","slug":"team-entity","link":"#team-entity","children":[]},{"level":3,"title":"Membership entity","slug":"membership-entity","link":"#membership-entity","children":[]},{"level":3,"title":"Participation entity","slug":"participation-entity","link":"#participation-entity","children":[]},{"level":3,"title":"SwimmingCompetition entity","slug":"swimmingcompetition-entity","link":"#swimmingcompetition-entity","children":[]},{"level":3,"title":"Sanction entity","slug":"sanction-entity","link":"#sanction-entity","children":[]},{"level":3,"title":"How is the IS-A hierarchy implemented with tables?","slug":"how-is-the-is-a-hierarchy-implemented-with-tables","link":"#how-is-the-is-a-hierarchy-implemented-with-tables","children":[]},{"level":3,"title":"DrivingSanction entity","slug":"drivingsanction-entity","link":"#drivingsanction-entity","children":[]},{"level":3,"title":"SportSanction entity","slug":"sportsanction-entity","link":"#sportsanction-entity","children":[]},{"level":3,"title":"PoolSanction entity","slug":"poolsanction-entity","link":"#poolsanction-entity","children":[]}]},{"level":2,"title":"How to Create the Database","slug":"how-to-create-the-database","link":"#how-to-create-the-database","children":[]}],"readingTime":{"minutes":101.66,"words":30499},"filePathRelative":"freecodecamp.org/how-to-design-structured-database-systems-using-sql-full-book/chapter-10-database-design-process-example.md","localizedDate":"August 14, 2025","excerpt":"\\n","copyright":{"author":"Daniel García Solla"}}');export{x as comp,B as data};
