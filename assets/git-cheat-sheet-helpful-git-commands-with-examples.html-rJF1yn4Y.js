import{_ as p}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as g,f as t,av as m,au as n,aw as f,ax as u,at as i,e as a,a as c,r as s,o as v}from"./app-n2Oj_rFs.js";const y={},b={id:"frontmatter-title-관련",tabindex:"-1"},w={class:"header-anchor",href:"#frontmatter-title-관련"},k={href:"https://flaviocopes.com/access/",target:"_blank",rel:"noopener noreferrer"},G={class:"table-of-contents"},T={class:"hint-container info"};function x(l,e){const d=s("VPCard"),h=s("SiteInfo"),r=s("FontIcon"),o=s("router-link");return v(),g("div",null,[t("h1",b,[t("a",w,[t("span",null,m(l.$frontmatter.title)+" 관련",1)])]),n(d,f(u({title:"Git > Article(s)",desc:"Article(s)",link:"/programming/git/articles/README.md",logo:"https://chanhi2000.github.io/images/ico-wind.svg",background:"rgba(10,10,10,0.2)"})),null,16),n(h,{name:"Git Cheat Sheet – Helpful Git Commands with Examples",desc:"This Git Cheat Sheet will provide you with a handy list of common (and not so common) commands that will make your life easier when working with Git. You can also download the Git Cheat Sheet in PDF format (along with some other resources) for free b...",url:"https://freecodecamp.org/news/git-cheat-sheet-helpful-git-commands-with-examples/",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",preview:"https://cdn.hashnode.com/res/hashnode/image/upload/v1723993272242/896730cc-3e03-43be-83d9-06d37ebab5a5.jpeg"}),e[50]||(e[50]=t("p",null,"This Git Cheat Sheet will provide you with a handy list of common (and not so common) commands that will make your life easier when working with Git.",-1)),t("p",null,[e[1]||(e[1]=i("You can also download the Git Cheat Sheet in PDF format (along with some other resources) for free by joining my newsletter at ")),t("a",k,[n(r,{icon:"iconfont icon-github"}),e[0]||(e[0]=i("flaviocopes.com/access"))]),e[2]||(e[2]=i("."))]),t("nav",G,[t("ul",null,[t("li",null,[n(o,{to:"#preface"},{default:a(()=>e[3]||(e[3]=[i("Preface")])),_:1})]),t("li",null,[n(o,{to:"#basic-git-commands"},{default:a(()=>e[4]||(e[4]=[i("Basic Git Commands")])),_:1})]),t("li",null,[n(o,{to:"#the-working-directory-and-the-staging-area"},{default:a(()=>e[5]||(e[5]=[i("The Working Directory and the Staging Area")])),_:1})]),t("li",null,[n(o,{to:"#working-with-branches"},{default:a(()=>e[6]||(e[6]=[i("Working with Branches")])),_:1})]),t("li",null,[n(o,{to:"#merging-in-git"},{default:a(()=>e[7]||(e[7]=[i("Merging in Git")])),_:1})]),t("li",null,[n(o,{to:"#git-remotes"},{default:a(()=>e[8]||(e[8]=[i("Git Remotes")])),_:1})]),t("li",null,[n(o,{to:"#amending-commits"},{default:a(()=>e[9]||(e[9]=[i("Amending Commits")])),_:1})]),t("li",null,[n(o,{to:"#stashing-in-git"},{default:a(()=>e[10]||(e[10]=[i("Stashing in Git")])),_:1})]),t("li",null,[n(o,{to:"#git-tagging"},{default:a(()=>e[11]||(e[11]=[i("Git Tagging")])),_:1})]),t("li",null,[n(o,{to:"#reverting-changes-in-git"},{default:a(()=>e[12]||(e[12]=[i("Reverting Changes in Git")])),_:1})]),t("li",null,[n(o,{to:"#viewing-history-logs"},{default:a(()=>e[13]||(e[13]=[i("Viewing History Logs")])),_:1})]),t("li",null,[n(o,{to:"#git-diffs"},{default:a(()=>e[14]||(e[14]=[i("Git Diffs")])),_:1})]),t("li",null,[n(o,{to:"#git-flow"},{default:a(()=>e[15]||(e[15]=[i("Git Flow")])),_:1})]),t("li",null,[n(o,{to:"#exploring-git-references"},{default:a(()=>e[16]||(e[16]=[i("Exploring Git References")])),_:1})]),t("li",null,[n(o,{to:"#how-to-configure-git"},{default:a(()=>e[17]||(e[17]=[i("How to Configure Git")])),_:1})]),t("li",null,[n(o,{to:"#git-security"},{default:a(()=>e[18]||(e[18]=[i("Git Security")])),_:1})]),t("li",null,[n(o,{to:"#how-to-set-aliases-in-git"},{default:a(()=>e[19]||(e[19]=[i("How to Set Aliases in Git")])),_:1})]),t("li",null,[n(o,{to:"#rebasing-in-git"},{default:a(()=>e[20]||(e[20]=[i("Rebasing in Git")])),_:1})]),t("li",null,[n(o,{to:"#what-is-cherry-picking"},{default:a(()=>e[21]||(e[21]=[i("What is Cherry-Picking?")])),_:1})]),t("li",null,[n(o,{to:"#patching-in-git"},{default:a(()=>e[22]||(e[22]=[i("Patching in Git")])),_:1})]),t("li",null,[n(o,{to:"#relative-dates-in-git"},{default:a(()=>e[23]||(e[23]=[i("Relative Dates in Git")])),_:1})]),t("li",null,[n(o,{to:"#git-blame"},{default:a(()=>e[24]||(e[24]=[i("Git Blame")])),_:1})]),t("li",null,[n(o,{to:"#archiving-in-git"},{default:a(()=>e[25]||(e[25]=[i("Archiving in Git")])),_:1})]),t("li",null,[n(o,{to:"#how-to-track-files-in-git"},{default:a(()=>e[26]||(e[26]=[i("How to Track Files in Git")])),_:1})]),t("li",null,[n(o,{to:"#index-manipulation-in-git"},{default:a(()=>e[27]||(e[27]=[i("Index Manipulation in Git")])),_:1})]),t("li",null,[n(o,{to:"#squashing-in-git"},{default:a(()=>e[28]||(e[28]=[i("Squashing in Git")])),_:1})]),t("li",null,[n(o,{to:"#data-integrity-in-git"},{default:a(()=>e[29]||(e[29]=[i("Data Integrity in Git")])),_:1})]),t("li",null,[n(o,{to:"#cleaning-up-in-git"},{default:a(()=>e[30]||(e[30]=[i("Cleaning Up in Git")])),_:1})]),t("li",null,[n(o,{to:"#git-subtree"},{default:a(()=>e[31]||(e[31]=[i("Git Subtree")])),_:1})]),t("li",null,[n(o,{to:"#how-to-search-in-git"},{default:a(()=>e[32]||(e[32]=[i("How to Search in Git")])),_:1})]),t("li",null,[n(o,{to:"#bisecting-in-git"},{default:a(()=>e[33]||(e[33]=[i("Bisecting in Git")])),_:1})]),t("li",null,[n(o,{to:"#git-attributes"},{default:a(()=>e[34]||(e[34]=[i("Git Attributes")])),_:1})]),t("li",null,[n(o,{to:"#git-checkout"},{default:a(()=>e[35]||(e[35]=[i("Git Checkout")])),_:1})]),t("li",null,[n(o,{to:"#git-reflog"},{default:a(()=>e[36]||(e[36]=[i("Git Reflog")])),_:1})]),t("li",null,[n(o,{to:"#how-to-handle-untracked-files-in-git"},{default:a(()=>e[37]||(e[37]=[i("How to Handle Untracked Files in Git")])),_:1})]),t("li",null,[n(o,{to:"#force-pushing-in-git"},{default:a(()=>e[38]||(e[38]=[i("Force Pushing in Git")])),_:1})]),t("li",null,[n(o,{to:"#git-fetching-and-pulling"},{default:a(()=>e[39]||(e[39]=[i("Git Fetching and Pulling")])),_:1})]),t("li",null,[n(o,{to:"#how-to-handle-merge-conflicts-in-git"},{default:a(()=>e[40]||(e[40]=[i("How to Handle Merge Conflicts in Git")])),_:1})]),t("li",null,[n(o,{to:"#working-trees-in-git"},{default:a(()=>e[41]||(e[41]=[i("Working Trees in Git")])),_:1})]),t("li",null,[n(o,{to:"#submodules-in-git"},{default:a(()=>e[42]||(e[42]=[i("Submodules in Git")])),_:1})])])]),e[51]||(e[51]=c('<hr><h2 id="preface" tabindex="-1"><a class="header-anchor" href="#preface"><span>Preface</span></a></h2><p>Welcome to this Git Cheat Sheet! It&#39;s an extensive guide I created to help empower both novice and seasoned developers with the knowledge you need to effectively utilize Git, the most popular version control system in the software industry.</p><p>This cheat sheet is designed to be your go-to resource, whether you&#39;re managing a solo project or collaborating within a large team. By providing clear explanations and practical examples, it should help demystify Git&#39;s complexities and transform them into intuitive, actionable insights.</p><p>Throughout this guide, you will explore a wide array of Git commands and concepts that form the backbone of software version control. From fundamental operations like initializing repositories and committing changes, to more advanced techniques such as branching, merging, and rebasing, this cheat sheet covers it all.</p><p>You&#39;ll also delve into specialized topics like squashing commits, bisecting to debug, handling submodules, and implementing subtrees. All this will help ensure that you&#39;re well-prepared to tackle any challenge that arises in your development process.</p><p>As you progress, you&#39;ll learn how to maintain data integrity, manage multiple working trees, and resolve merge conflicts efficiently. By the end, you&#39;ll not only have a deeper understanding of Git but also the confidence to use it to streamline your workflow and enhance collaboration with peers.</p><div class="hint-container note"><p class="hint-container-title">Prerequisites</p><p>To fully benefit from this cheat sheet, you should have a foundational knowledge of command-line operations and general programming principles. You should also be familiar with using a terminal or command prompt so you can better understand and apply the examples provided. Finally, having a basic grasp of version control concepts will enhance your ability to navigate through this guide effectively.</p></div><hr><h2 id="basic-git-commands" tabindex="-1"><a class="header-anchor" href="#basic-git-commands"><span>Basic Git Commands</span></a></h2><p>In this section, you&#39;ll learn the fundamental Git commands that serve as the building blocks for efficiently managing and navigating your Git repositories.</p><p>Git is a distributed version control system that&#39;s essential for tracking changes in your codebase, collaborating with other developers, and maintaining the integrity of your project history. Understanding these basic commands is crucial for anyone looking to leverage the full power of Git in their development workflow.</p><p>We&#39;ll explore a variety of commands that cover key aspects of Git usage, such as initializing new repositories, committing changes, branching, and merging.</p><p>Each command is explained with a short sentence that describes its purpose along with its syntax, so you can effectively use it in real-world scenarios. Whether you&#39;re setting up a new project or working on an existing codebase, these commands will help you keep your work organized and maintain a seamless workflow.</p><div class="hint-container info"><p class="hint-container-title">git help</p><p>The <code>git help</code> command prints Git help information. It provides a quick reference to Git&#39;s basic usage and the most commonly used Git commands.</p><p>This command is useful when you need a quick reminder of Git&#39;s functionality or want to explore available commands.</p><p>You can also use <code>git help &lt;COMMAND&gt;</code> to display help information for any specific Git command.</p><p>For example, <code>git help git</code> prints the Git help specifically for the Git command itself.</p><p>These help commands are valuable resources for both beginners and experienced users to quickly access information about Git&#39;s features and usage.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git version</code></p><p>The <code>git version</code> command displays the version of Git installed on your system.</p><p>This command is useful for verifying which version of Git you are currently using, which can be important for compatibility with certain features or when troubleshooting issues.</p></div>',16)),t("div",T,[e[46]||(e[46]=t("p",{class:"hint-container-title"},[t("code",null,"git init")],-1)),e[47]||(e[47]=t("p",null,[i("The command "),t("code",null,"git init"),i(" is used to initialize a new Git repository in the current directory.")],-1)),t("p",null,[e[43]||(e[43]=i("This command creates a new subdirectory named ")),n(r,{icon:"fas fa-folder-open"}),e[44]||(e[44]=t("code",null,".git",-1)),e[45]||(e[45]=i(" that contains all the necessary metadata for the new repository."))]),e[48]||(e[48]=t("p",null,"It's typically the first command you run when starting a new project that you want to manage with Git.",-1)),e[49]||(e[49]=t("p",null,"After running this command, you can begin tracking files and making commits in your new Git repository.",-1))]),e[52]||(e[52]=c('<div class="hint-container info"><p class="hint-container-title"><code>git clone &lt;REPOSITORY_URL&gt;</code></p><p>The <code>git clone &lt;REPOSITORY_URL&gt;</code> command creates a copy of a remote Git repository on your local machine.</p><p>It downloads all the files, branches, and commit history, allowing you to start working with the project immediately.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git status</code></p><p>The <code>git status</code> command shows the current state of the Git repository&#39;s working directory and staging area.</p><p>It displays information about which files have been modified, added, or deleted, and whether these changes are staged for the next commit.</p></div><hr><h2 id="the-working-directory-and-the-staging-area" tabindex="-1"><a class="header-anchor" href="#the-working-directory-and-the-staging-area"><span>The Working Directory and the Staging Area</span></a></h2><p>The working directory and the staging area are fundamental concepts in Git that play crucial roles in the version control process.</p><p>The working directory is the environment where you actively make changes to your files, representing the current state of your project. It is essentially a sandbox where you can freely edit, delete, and create files as you develop your project. But these changes are local to your machine and not yet part of the version history.</p><p>On the other hand, the staging area, also known as the index, serves as an intermediary space between the working directory and the repository. It acts as a checkpoint where you can selectively organize changes before they are committed to the repository&#39;s history. This allows you to prepare a set of changes that are logically related, ensuring that each commit is meaningful and coherent.</p><p>The commands below will help you manage changes between the working directory and the staging area. They&#39;ll let you add files to the staging area, remove them, or modify the existing ones, giving you control over what will be included in the next commit.</p><p>By using these commands, you can ensure that only the intended updates are committed, making your project&#39;s history clear and organized. This process is essential for maintaining a clean and understandable history, as it allows you to track the evolution of your project with precision and clarity.</p><div class="hint-container info"><p class="hint-container-title"><code>git checkout .</code></p><p>This command discards all changes in the working directory, reverting files to their last committed state.</p><p>This command is useful for quickly undoing local modifications and restoring the working directory to a clean state.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git reset -p</code></p><p>This command allows you to interactively reset changes in the working directory.</p><p>It provides a way to selectively undo modifications, giving you fine-grained control over which changes to keep or discard.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git add &lt;FILE&gt;</code></p><p>This command adds a specific file to the staging area in Git.</p><p>This prepares the file for inclusion in the next commit, allowing you to selectively choose which changes to include in your version history.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git add -p</code></p><p>Allows you to interactively stage changes from your working directory by breaking them into chunks (hunks).</p><p>This lets you review and selectively add parts of the changes to the index before committing.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git add -i</code></p><p>Enters the interactive mode of adding files.</p><p>Provides a text-based interactive menu where you can select various actions to perform, such as staging individual changes, updating files, or viewing the status.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git rm &lt;FILE&gt;</code></p><p>Removes a file from the working directory and stages the removal.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git rm --cached &lt;FILE&gt;</code></p><p>Removes the specified file from the staging area (index) but leaves it intact in your working directory.</p><p>This effectively un-tracks the file from version control.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git mv &lt;OLD_PATH&gt; &lt;NEW_PATH&gt;</code></p><p>This command is used to move or rename a file or directory within a Git repository.</p><p>It automatically stages the change, making it ready for the next commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git commit -m &quot;&lt;MESSAGE&gt;&quot;</code></p><p>This command is used to create a new commit in your Git repository.</p><p>It saves the changes that have been staged (added to the index) along with a descriptive message.</p><p>This message should briefly explain what changes were made in this commit.</p></div><hr><h2 id="working-with-branches" tabindex="-1"><a class="header-anchor" href="#working-with-branches"><span>Working with Branches</span></a></h2><p>Git branches are parallel lines of development within a Git repository. They allow you to work on different features, bug fixes, or experiments independently from the main codebase.</p><p>Each branch can have its own commit history, and changes made in one branch do not affect others until they are merged. This helps you organize your work, and facilitates collaboration by enabling multiple developers to work on different aspects of a project simultaneously without interfering with each other&#39;s progress.</p><p>In this section we&#39;ll introduce commands that allow you to create, switch, list, rename, and delete branches in your Git repository. These commands help manage parallel lines of development, enabling you to work on different features or bug fixes independently. You&#39;ll also learn how to display commit histories and branch relationships, as well as manage remote branches.</p><div class="hint-container info"><p class="hint-container-title"><code>git branch &lt;BRANCH_NAME&gt;</code></p><p>Creates a new branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git checkout &lt;BRANCH_NAME&gt;</code></p><p>Switches to the specified branch and updates the working directory.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch</code></p><p>Lists all branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch -d &lt;BRANCH_NAME&gt;</code></p><p>Deletes a branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push --delete &lt;REMOTE&gt; &lt;BRANCH&gt;</code></p><p>Deletes a remote branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch -m &lt;OLD_NAME&gt; &lt;NEW_NAME&gt;</code></p><p>Renames a branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git checkout -b &lt;NEW_BRANCH&gt;</code></p><p>Creates and switches to a new branch named <code>&lt;NEW_BRANCH&gt;</code>, based on the current branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git switch &lt;BRANCH&gt;</code></p><p>Switches the working directory to the specified branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git show-branch &lt;BRANCH&gt;</code></p><p>Displays a summary of the commit history and branch relationships for all or selected branches, showing where each branch diverged.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git show-branch --all</code></p><p>Same as above, but for all branches and their commits.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch -r</code></p><p>Lists all remote branches that your local repository is aware of.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch -a</code></p><p>Lists all branches in your repository, including both local and remote branches (the ones the local repository is aware of).</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch --merged</code></p><p>Lists all branches that have been fully merged into the current branch, and can be safely deleted if no longer needed.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git branch --no-merged</code></p><p>Lists all branches that have not been fully merged into your current branch, showing branches with changes that haven&#39;t been integrated yet.</p></div><hr><h2 id="merging-in-git" tabindex="-1"><a class="header-anchor" href="#merging-in-git"><span>Merging in Git</span></a></h2><p>The git merge command is used to combine the changes from one branch into another branch. It integrates the histories of both branches, creating a new commit that includes the changes from both sources.</p><p>This process allows multiple lines of development to be brought together, facilitating collaboration and ensuring that all updates are incorporated into the main project.</p><p>During a merge, conflicts may arise if changes overlap, requiring manual resolution to ensure a coherent final result.</p><div class="hint-container info"><p class="hint-container-title"><code>git merge &lt;BRANCH&gt;</code></p><p>Integrates the changes from the specified branch into your current branch, combining their histories.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git merge --no-ff &lt;BRANCH&gt;</code></p><p>Merges the specified branch into your current branch, always creating a new merge commit even if a fast-forward merge is possible.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git merge --squash &lt;BRANCH&gt;</code></p><p>Combines all the changes from the specified branch into a single commit, preparing the changes for a commit in the current branch without merging the branch’s history.</p><p>This allows you to manually edit the commit message.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git merge --abort</code></p><p>Cancels an ongoing merge process and restores the state of your working directory and index to what it was before the merge started.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git merge -s ours &lt;BRANCH&gt;</code></p><blockquote><p>or <code>git merge —-strategy=ours &lt;BRANCH&gt;</code></p></blockquote><p>These commands are functionally the same, but the second is the expanded (more explicit) version, while <code>git merge -s ours &lt;BRANCH&gt;</code> is the shorthand version (which is commonly used). You&#39;ll see this a few times throughout this guide.</p><p>The <code>git merge —-strategy=ours &lt;BRANCH&gt;</code> command (<code>git merge -s ours &lt;BRANCH&gt;</code> for short) performs a merge using the &quot;ours&quot; strategy, which keeps the current branch&#39;s changes and discards changes from the specified branch.</p><p>This effectively merges the histories without integrating the changes from the other branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git merge --strategy=theirs &lt;BRANCH&gt;</code></p><p>Merges the specified branch into the current branch using the &quot;theirs&quot; strategy, which resolves all conflicts by favoring changes from the branch being merged.</p><p>Note that the &quot;theirs&quot; strategy is not a built-in strategy and usually requires custom scripting or is used with tools to handle conflict resolution.</p></div><hr><h2 id="git-remotes" tabindex="-1"><a class="header-anchor" href="#git-remotes"><span>Git Remotes</span></a></h2><p>Git remotes are references to remote repositories, which are versions of your project hosted on the internet or another network. They enable collaboration by allowing multiple users to share and sync changes with a central repository.</p><p>Common operations with remotes include <code>git fetch</code> to retrieve updates, <code>git pull</code> to fetch and merge changes, and <code>git push</code> to upload local commits to the remote repository.</p><p>Managing remotes involves adding, removing, and renaming remote connections, as well as configuring URLs for seamless collaboration.</p><div class="hint-container info"><p class="hint-container-title"><code>git fetch</code></p><p>Fetches changes from a remote repository but does not merge them into your current branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git pull</code></p><p>Fetches changes from a remote repository and immediately merges them into your current branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push</code></p><p>Uploads your local branch&#39;s changes to a remote repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote</code></p><p>Lists the names of remote repositories configured for your local repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote -v</code></p><p>Displays the URLs of the remote repositories associated with your local repository, showing both the fetched and pushed URLs.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote add &lt;NAME&gt; &lt;REPOSTIORY_URL&gt;</code></p><p>Adds a new remote repository with the specified name and URL to your local repository configuration.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote remove &lt;NAME&gt;</code></p><blockquote><p>or <code>git remote rm &lt;NAME&gt;</code></p></blockquote><p>Deletes the specified remote repository connection from your local git configuration.</p><p><code>git remote rm &lt;NAME&gt;</code> is the shorthand version of the command.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote rename &lt;OLD_NAME&gt; &lt;NEW_NAME&gt;</code></p><p>Changes the name of an existing remote repository connection in your local Git configuration</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote set-url &lt;NAME&gt; &lt;NEW_URL&gt;</code></p><p>Changes the URL of an existing remote repository connection in your local Git configuration.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git fetch &lt;REMOTE&gt;</code></p><p>Retrieves the latest changes from the specified remote repository, updating your local copy of the remote branches without merging them into your local branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git pull &lt;REMOTE&gt;</code></p><p>Fetches changes from the specified remote repository and merges them into your current branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote update</code></p><p>Fetches updates for all remotes tracked by the repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push &lt;REMOTE&gt; &lt;BRANCH&gt;</code></p><p>Uploads the specified branch from your local repository to the given remote repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push &lt;REMOTE&gt; --delete &lt;BRANCH&gt;</code></p><p>Removes the specified branch from the remote repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote show &lt;REMOTE&gt;</code></p><p>Displays detailed information about the specified remote repository, including its URL, fetch and push configurations, and the branches it tracks.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git ls-remote &lt;REPOSITORY&gt;</code></p><p>Lists references (such as branches and tags) and their commit IDs from the specified remote repository.</p><p>This command allows you to view the branches and tags available in a remote repository without cloning it.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push origin &lt;BRANCH&gt; --set-upstream</code></p><p>Pushes the local branch <code>&lt;BRANCH&gt;</code> to the remote repository origin and sets up the local branch to track the remote branch.</p><p>This lets future git push and git pull commands default to this remote branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote add upstream &lt;REPOSITORY&gt;</code></p><p>Adds a new remote named upstream to your local repository, pointing to the specified <code>&lt;REPOSITORY&gt;</code>.</p><p>This is commonly used to track the original repository from which you forked, while origin typically refers to your own fork.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git fetch upstream</code></p><p>Retrieves updates from the upstream remote repository, updating your local references to the branches and tags from that remote without modifying your working directory or merging changes.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git pull upstream &lt;BRANCH&gt;</code></p><p>Fetches updates from the <code>&lt;BRANCH&gt;</code> of the upstream remote repository and merges those changes into your current branch.</p><p>This is often used to integrate changes from the original repository into your own local branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push origin &lt;BRANCH&gt;</code></p><p>Uploads the local branch <code>&lt;BRANCH&gt;</code> to the origin remote repository, making your branch and its commits available on the remote.</p></div><hr><h2 id="amending-commits" tabindex="-1"><a class="header-anchor" href="#amending-commits"><span>Amending Commits</span></a></h2><p>Amending Git commits allows you to modify the most recent commit, typically to correct or update its contents or message. You can do this using the <code>git commit --amend</code> command, which opens the commit in your default text editor for changes.</p><p>Amending is particularly useful for fixing small mistakes or adding forgotten changes without creating a new commit, resulting in a cleaner and more accurate commit history.</p><div class="hint-container info"><p class="hint-container-title"><code>git commit --amend</code></p><p>Modifies the most recent commit, combining staged changes.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git commit --amend -m &quot;&lt;MESSAGE&gt;&quot;</code></p><p>Amends the commit message of the most recent commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git commit --fixup=HEAD</code></p><p>Creates a new commit with the <code>--fixup</code> option that is intended to correct or amend the most recent commit (<code>HEAD</code>).</p><p>The new commit is marked with a <code>fixup!</code> prefix in the commit message and will be used to automatically fix or amend the specified commit during an interactive rebase.</p></div><hr><h2 id="stashing-in-git" tabindex="-1"><a class="header-anchor" href="#stashing-in-git"><span>Stashing in Git</span></a></h2><p>Git stashing is a feature that allows you to temporarily save changes in your working directory that are not yet ready to be committed.</p><p>By using the git stash command, you can set aside these changes and revert your working directory to a clean state, enabling you to switch branches or perform other tasks without losing progress. Later, you can reapply the stashed changes with git stash apply or git stash pop, allowing you to continue where you left off.</p><p>This functionality is especially useful for managing work in progress when you need to address an urgent issue or experiment with a different code path.</p><div class="hint-container info"><p class="hint-container-title"><code>git stash</code></p><p>Temporarily saves your uncommitted changes, allowing you to switch branches or perform other operations without committing incomplete work.</p><p>There&#39;s an older version, <code>git stash save</code>, that allows you to specify a custom message.</p><p>But it&#39;s largely been deprecated in favor of the simpler <code>git stash</code>.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash -m &quot;&lt;MESSAGE&gt;&quot;</code></p><p>Same as above, but stores changes with a message. It also has an older version, <code>git stash save &quot;message&quot;</code>, but <code>git stash -m &quot;message&quot;</code> is preferred for versions of Git 2.13 and newer.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash show</code></p><p>Displays a summary of the changes in the most recent stash entry, showing which files were modified.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash list</code></p><p>Shows all the stashed changes in your repository, displaying them in a numbered list.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash pop</code></p><p>Applies the most recent stash and then immediately removes it from the stash list.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash drop</code></p><p>Removes the most recent stash entry from the stash list without applying it to your working directory.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash apply</code></p><p>Reapplies the most recently stashed changes to your working directory without removing them from the stash list.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash clear</code></p><p>Clears and removes all stashed entries, permanently deleting all saved changes in your stash.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git stash branch &lt;BRANCH&gt;</code></p><p>Creates a new branch named <code>&lt;BRANCH&gt;</code> from the commit where you were before stashing your changes.</p><p>Then it applies the stashed changes to that new branch.</p><p>This command effectively allows you to continue working on your stashed changes in a separate branch, preserving the original context and changes.</p></div><hr><h2 id="git-tagging" tabindex="-1"><a class="header-anchor" href="#git-tagging"><span>Git Tagging</span></a></h2><p>Git tagging is a feature that allows you to mark specific points in your repository&#39;s history as important with a meaningful name, often used for releases or significant milestones.</p><p>Unlike branches, tags are typically immutable and do not change, serving as a permanent reference to a particular commit.</p><p>There are two types of tags in Git: lightweight tags, which are simple pointers to a commit, and annotated tags, which store additional metadata like the tagger&#39;s name, email, date, and a message.</p><p>Tags can be easily created, listed, pushed to remote repositories, and deleted, providing a convenient way to manage and reference key points in your project&#39;s development timeline.</p><div class="hint-container info"><p class="hint-container-title"><code>git tag &lt;TAG_NAME&gt;</code></p><p>Creates a new tag with the specified name pointing to the current commit (typically used to mark specific points in the commit history, like releases).</p></div><div class="hint-container info"><p class="hint-container-title"><code>git tag -a &lt;TAG_NAME&gt; -m &quot;&lt;MESSAGE&gt;&quot;</code></p><p>Creates an annotated tag with the specified name and message, which includes additional metadata like the tagger&#39;s name, email, and date, and points to the current commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git tag -d &lt;TAG_NAME&gt;</code></p><p>Deletes the specified tag from your local repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git tag -f &lt;TAG&gt; &lt;COMMIT&gt;</code></p><p>Forces a tag to point to a different commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git show &lt;TAG_NAME&gt;</code></p><p>Displays detailed information about the specified tag, including the commit it points to and any associated tag messages or annotations.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push origin &lt;TAG_NAME&gt;</code></p><p>Uploads the specified tag to the remote repository, making it available to others.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push origin --tags</code></p><p>Pushes all local tags to the remote repository, ensuring that all tags are synchronized with the remote.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git push --follow-tags</code></p><p>Pushes both commits and tags.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git fetch --tags</code></p><p>Retrieves all tags from the default remote repository and updates your local repository with them, without affecting your current branches.</p></div><hr><h2 id="reverting-changes-in-git" tabindex="-1"><a class="header-anchor" href="#reverting-changes-in-git"><span>Reverting Changes in Git</span></a></h2><p>Reverting changes in Git involves undoing modifications made to a repository&#39;s history. You can do this using several commands, such as <code>git revert</code>. It creates a new commit that negates the changes of a specified previous commit, effectively reversing its effects while preserving the commit history.</p><p>Another method is using <code>git reset</code>, which changes the current HEAD to a specified commit and can update the staging area and working directory depending on the chosen options (<code>--soft</code>, <code>--mixed</code>, or <code>--hard</code>).</p><p>You can also use <code>git checkout</code> to discard changes in the working directory, reverting files to their state in the last commit.</p><p>These tools provide flexibility in managing and correcting changes, ensuring the repository remains accurate and clean.</p><div class="hint-container info"><p class="hint-container-title"><code>git checkout -- &lt;FILE&gt;</code></p><p>Discards changes in the specified file from the working directory, reverting it to the state of the last commit and effectively undoing any modifications that haven&#39;t been staged.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git revert &lt;COMMIT&gt;</code></p><p>Creates a new commit that undoes the changes in the specified commit, effectively reversing its effects while preserving the history.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git revert -n &lt;COMMIT&gt;</code></p><p>Reverts a commit but does not commit the result.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git reset</code></p><p>Resets the current HEAD to the specified state, and optionally updates the staging area and working directory, depending on the options used (<code>--soft</code>, <code>--mixed</code>, or <code>--hard</code>).</p></div><div class="hint-container info"><p class="hint-container-title"><code>git reset --soft &lt;COMMIT&gt;</code></p><p>Moves HEAD to the specified commit, while keeping the index (staging area) and working directory unchanged, so all changes after the specified commit remain staged for committing.</p><p>This is useful when you want to undo commits but keep the changes ready to be committed again.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git reset --mixed &lt;COMMIT&gt;</code></p><p>Moves HEAD to the specified commit and updates the index (staging area) to match that commit.</p><p>But it leaves the working directory unchanged, so changes after the specified commit are kept but are untracked.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git reset --hard &lt;COMMIT&gt;</code></p><p>Moves HEAD to the specified commit and updates both the index (staging area) and working directory to match that commit.</p><p>It discards all changes and untracked files after the specified commit.</p></div><hr><h2 id="viewing-history-logs" tabindex="-1"><a class="header-anchor" href="#viewing-history-logs"><span>Viewing History Logs</span></a></h2><p>Git history refers to the record of all changes made to a repository over time. It includes a chronological sequence of commits, each representing a snapshot of the repository at a specific point.</p><p>This history allows you to track modifications, understand the evolution of the codebase, and collaborate effectively by providing a detailed log of who made changes, when, and why.</p><p>Tools like git log help you navigate this history, offering insights into the development process and aiding in debugging and project management.</p><div class="hint-container info"><p class="hint-container-title"><code>git log</code></p><p>Displays the commits log.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --oneline</code></p><p>Displays a summary of commits with one line each.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --graph</code></p><p>Shows a graphical representation of the commit history.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --stat</code></p><p>Displays file statistics along with the commit history.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --pretty=format:&quot;%h %s&quot;</code></p><p>Formats the log output according to the specified format.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --pretty=format:&quot;%h - %an, %ar : %s&quot;</code></p><p>Provides a more human-readable format for logs.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --author=&lt;AUTHOR&gt;</code></p><p>Shows commits made by the specified author.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --before=&lt;DATE&gt;</code></p><p>Shows commits made before the specified date.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --after=&lt;DATE&gt;</code></p><p>Shows commits made after the specified date (same as <code>git log --since=&lt;DATE&gt;</code>)</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --cherry-pick</code></p><p>Omits commits that are equivalent between two branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --follow &lt;FILE&gt;</code></p><p>Shows commits for a file, including renames.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --show-signature</code></p><p>Displays GPG signature information for commits.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git shortlog</code></p><p>Summarizes git log output by author.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git shortlog -sn</code></p><p>Summarizes git log output by author, with commit counts.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --simplify-by-decoration</code></p><p>Shows only commits that are referenced by tags or branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --no-merges</code></p><p>Omits merge commits from the log.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git whatchanged</code></p><p>Lists commit data in a format similar to a commit log.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff-tree --pretty --name-only --root &lt;COMMIT&gt;</code></p><p>Shows detailed information for a commit tree.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log --first-parent</code></p><p>Only shows commits of the current branch and excludes those merged from other branches.</p></div><hr><h2 id="git-diffs" tabindex="-1"><a class="header-anchor" href="#git-diffs"><span>Git Diffs</span></a></h2><p>Git diffs are a feature in Git that allows you to see the differences between various states in your repository. This can include differences between your working directory and the staging area, between the staging area and the last commit, or between any two commits or branches.</p><p>By displaying line-by-line changes in files, diffs help you review modifications before committing, merging, or applying changes, thus ensuring accuracy and consistency in your codebase.</p><div class="hint-container info"><p class="hint-container-title"><code>git diff</code></p><p>Shows the differences between various states in your repository, such as between your working directory and the index (staging area), between the index and the last commit, or between two commits.</p><p>It displays line-by-line changes in files, helping you review modifications before committing or merging.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff --stat</code></p><p>Shows a summary of changes between your working directory and the index (staging area), helping you see what files have been modified and how many lines have been added or removed.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff --stat &amp;ltCOMMIT&gt;</code></p><p>Views changes between a commit and the working directory.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff --stat &amp;ltCOMMIT1&gt; &amp;ltCOMMIT2&gt;</code></p><p>Provides a summary of changes between two commits, showing which files were altered and the extent of changes between them.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff --stat &amp;ltBRANCH1&gt; &amp;ltBRANCH2&gt;</code></p><p>Summarizes the differences between the two branches, indicating the files changed and the magnitude of changes.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff --name-only &amp;ltCOMMIT&gt;</code></p><p>Shows only the names of files that changed in the specified commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff --cached</code></p><p>Shows the differences between the staged changes (index) and the last commit, helping you review what will be included in the next commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff HEAD</code></p><p>Shows the differences between the working directory and the latest commit (HEAD), allowing you to see what changes have been made since the last commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff &amp;ltBRANCH1&gt; &amp;ltBRANCH2&gt;</code></p><p>Shows the differences between the tips of two branches, highlighting the changes between the commits at the end of each branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git difftool</code></p><p>Launches a diff tool to compare changes.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git difftool &amp;ltCOMMIT1&gt; &amp;ltCOMMIT2&gt;</code></p><p>Uses the diff tool to show the differences between two specified commits.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git difftool &amp;ltBRANCH1&gt; &amp;ltBRANCH2&gt;</code></p><p>Opens the diff tool to compare changes between two branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git cherry &amp;ltBRANCH&gt;</code></p><p>Compares the commits in your current branch against another branch and shows which commits are unique to each branch. It is commonly used to identify which commits in one branch have not been applied to another branch.</p></div><hr><h2 id="git-flow" tabindex="-1"><a class="header-anchor" href="#git-flow"><span>Git Flow</span></a></h2><p>Git Flow is a branching model for Git that provides a robust framework for managing larger projects. It defines a strict branching strategy designed around the project release cycle, with two primary branches (main and develop) and supporting branches for features, releases, and hotfixes.</p><p>This model helps in organizing work, ensuring a clean and manageable history, and facilitating collaboration by clearly defining roles and processes for different types of development work.</p><div class="hint-container info"><p class="hint-container-title"><code>git flow init</code></p><p>Initializes a repository for a git-flow branching model.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git flow feature start &lt;FEATURE&gt;</code></p><p>Starts a new feature branch in git-flow.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git flow feature finish &lt;FEATURE&gt;</code></p><p>Finishes a feature branch in git-flow.</p></div><hr><h2 id="exploring-git-references" tabindex="-1"><a class="header-anchor" href="#exploring-git-references"><span>Exploring Git References</span></a></h2><p>Git references, often referred to as refs, are pointers to specific commits or objects within a Git repository. These can include branches, tags, and other references like HEAD, which points to the current commit checked out in your working directory.</p><p>References are used to keep track of the structure and history of the repository. They help Git efficiently manage and navigate different points in the project&#39;s timeline.</p><p>Refs also provide a way to name and refer to particular commits, making it easier to work with and manipulate the repository&#39;s history.</p><div class="hint-container info"><p class="hint-container-title"><code>git show-ref --heads</code></p><p>Lists references to all heads (branches).</p></div><div class="hint-container info"><p class="hint-container-title"><code>git show-ref --tags</code></p><p>Lists references to all tags.</p></div><hr><h2 id="how-to-configure-git" tabindex="-1"><a class="header-anchor" href="#how-to-configure-git"><span>How to Configure Git</span></a></h2><p>Git configuration involves setting up various options and preferences that control the behavior of your Git environment. This can include specifying your username and email, setting up default text editors, creating aliases for commonly used commands, and configuring global ignore files.</p><p>You can apply configuration settings at different levels: global (affecting all repositories on your system), local (affecting a single repository), and system-wide. These settings ensure a customized and consistent user experience, streamline workflows, and enhance the overall efficiency of version control operations.</p><div class="hint-container info"><p class="hint-container-title"><code>git config --global user.name &quot;Your Name&quot;</code></p><p>Sets the user name on a global level.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global user.email &quot;your_email@example.com&quot;</code></p><p>Sets the user email on a global level.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global core.editor &lt;EDITOR&gt;</code></p><p>Sets the default text editor.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global core.excludesfile &lt;FILE&gt;</code></p><p>Sets the global ignore file.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --list</code></p><p>Lists all the configuration settings.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --list --show-origin</code></p><p>Lists all config variables, showing their origins.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config &lt;KEY&gt;</code></p><p>Retrieves the value for the specified key.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --get &lt;KEY&gt;</code></p><p>Retrieves the value for the specified configuration key.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --unset &lt;KEY&gt;</code></p><p>Removes the specified configuration key.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global --unset &lt;KEY&gt;</code></p><p>Removes the specified configuration key globally.</p></div><hr><h2 id="git-security" tabindex="-1"><a class="header-anchor" href="#git-security"><span>Git Security</span></a></h2><p>Git GPG security involves using GNU Privacy Guard (GPG) to sign commits and tags, ensuring their authenticity and integrity. By configuring a GPG key and enabling automatic signing, you can verify that commits and tags were created by a trusted source. This helps prevent tampering and ensures the integrity of the repository&#39;s history.</p><p>This practice enhances security by providing cryptographic assurance that the changes come from a legitimate contributor.</p><div class="hint-container info"><p class="hint-container-title"><code>git config --global user.signingKey &lt;KEY&gt;</code></p><p>Configures the GPG key for signing commits and tags.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global commit.gpgSign true</code></p><p>Automatically signs all commits with GPG.</p></div><hr><h2 id="how-to-set-aliases-in-git" tabindex="-1"><a class="header-anchor" href="#how-to-set-aliases-in-git"><span>How to Set Aliases in Git</span></a></h2><p>Git aliases are custom shortcuts that you can create to simplify and speed up your workflow by mapping longer Git commands to shorter, more memorable names.</p><p>By configuring aliases in your Git settings, you can quickly execute frequently used commands with less typing. This not only enhances productivity but also reduces the likelihood of errors.</p><p>For example, you can set an alias like git st to replace <code>git status</code>, or <code>git co</code> to replace <code>git checkout</code>.</p><p>You can define aliases globally to apply across all repositories or locally for individual projects, providing flexibility in how you streamline your Git operations.</p><div class="hint-container info"><p class="hint-container-title"><code>git config --global alias.ci commit</code></p><p>Sets git ci as an alias for git commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global alias.st status</code></p><p>Sets git st as an alias for git status.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global alias.co checkout</code></p><p>Sets git co as an alias for git checkout.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global alias.br branch</code></p><p>Sets git br as an alias for git branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git config --global alias.graph &quot;log --graph --all --oneline --decorate&quot;</code></p><p>Creates an alias for a detailed graph of the repository history.</p></div><hr><h2 id="rebasing-in-git" tabindex="-1"><a class="header-anchor" href="#rebasing-in-git"><span>Rebasing in Git</span></a></h2><p>Git rebasing re-applies your changes on top of another branch&#39;s history, creating a cleaner and more linear project history.</p><p>In practice, this helps integrate updates smoothly by avoiding unnecessary merge commits, ensuring that the commit sequence is straightforward, and making it easier to understand the evolution of the project.</p><div class="hint-container info"><p class="hint-container-title"><code>git rebase &lt;BRANCH&gt;</code></p><p>The git rebase command is used to re-apply commits on top of another base tip. It allows you to move or combine a sequence of commits to a new base commit. This is commonly used to:</p><ol><li>Keep a linear project history.</li><li>Integrate changes from one branch into another.</li><li>Update a feature branch with the latest changes from the main branch.</li></ol><p>The basic usage is <code>git rebase &lt;BRANCH&gt;</code>, which will rebase the current branch onto the specified branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git rebase --interactive &lt;BRANCH&gt;</code></p><p>Starts an interactive rebase session, allowing you to modify commits starting from <code>&lt;BASE&gt;</code> up to the current HEAD. This lets you reorder, squash, edit, or delete commits, providing a way to clean up and refine commit history before pushing changes.</p><p>Shorter version: <code>git rebase -i &lt;BRANCH&gt;</code></p></div><div class="hint-container info"><p class="hint-container-title"><code>git rebase --continue</code></p><p>Continues the rebase process after resolving conflicts.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git rebase --abort</code></p><p>Aborts the rebase process and returns to the original branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git fetch --rebase</code></p><p>Fetches from the remote repository and rebases local changes.</p></div><hr><h2 id="what-is-cherry-picking" tabindex="-1"><a class="header-anchor" href="#what-is-cherry-picking"><span>What is Cherry-Picking?</span></a></h2><p>Git cherry-picking is a process that allows you to apply the changes introduced by a specific commit from one branch into another branch. This is particularly useful when you want to selectively incorporate individual changes from different branches without merging the entire branch.</p><p>By using the git cherry-pick command, you can isolate and integrate only the desired commits, ensuring that specific modifications are included in your current branch while avoiding potential conflicts and unwanted changes from other parts of the branch.</p><div class="hint-container info"><p class="hint-container-title"><code>git cherry-pick &lt;COMMIT&gt;</code></p><p>Applies the changes introduced by an existing commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git cherry-pick --continue</code></p><p>Continues cherry-picking after resolving conflicts.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git cherry-pick --abort</code></p><p>Aborts the cherry-pick process.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git cherry-pick --no-commit &lt;COMMIT&gt;</code></p><p>Cherry-picks a commit without automatically committing and allows further changes. Shorter version: <code>git cherry-pick -n &lt;COMMIT&gt;</code></p></div><hr><h2 id="patching-in-git" tabindex="-1"><a class="header-anchor" href="#patching-in-git"><span>Patching in Git</span></a></h2><p>Git patching is a method used to apply changes from one repository to another or from one branch to another within the same repository. It involves creating patch files, which are text files representing differences between commits or branches. These patch files can then be applied to a repository using commands like <code>git apply</code> or <code>git am</code>, allowing changes to be transferred and integrated without directly merging branches.</p><p>Patching is particularly useful for sharing specific changes or updates across different codebases, ensuring that only the intended modifications are applied.</p><div class="hint-container info"><p class="hint-container-title"><code>git apply &lt;PATCH_FILE&gt;</code></p><p>Applies changes to the working directory from a patch file.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git apply --check</code></p><p>Checks if patches can be applied cleanly.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git format-patch &lt;SINCE_COMMIT&gt;</code></p><p>Creates patch files for each commit since the specified commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git am &lt;PATCH_FILE&gt;</code></p><p>Applies patches from a mailbox.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git am --continue</code></p><p>Continues applying patches after resolving conflicts.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git am --abort</code></p><p>Aborts the patch application process.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff &gt; &lt;FILE.PATCH&gt;</code></p><p>Creates a patch file from differences.</p></div><hr><h2 id="relative-dates-in-git" tabindex="-1"><a class="header-anchor" href="#relative-dates-in-git"><span>Relative Dates in Git</span></a></h2><p>Git relative dates allow users to refer to specific points in the repository&#39;s history using human-readable time expressions. For instance, commands like main@{1.week.ago} or @{3.days.ago} enable you to access the state of a branch or view changes made since a certain time period relative to the current date.</p><p>This feature simplifies navigating the repository&#39;s timeline by using intuitive terms like &quot;yesterday,&quot; &quot;2 weeks ago,&quot; or specific dates, making it easier to track and manage the evolution of the codebase without needing to remember exact commit hashes or timestamps.</p><div class="hint-container info"><p class="hint-container-title"><code>git show main@{1.week.ago}</code></p><p>Lets you see the state of your main branch one week ago.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git diff @{3.days.ago}</code></p><p>Shows what changes you&#39;ve made in the last 3 days.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git checkout main@{2.weeks.ago}</code></p><p>Checks out your repository as it was 2 weeks ago.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git log @{1.month.ago}..HEAD</code></p><p>Shows you the log of commits from 1 month ago until now.</p><p><code>@{2024-06-01}</code></p><p><code>@{yesterday}</code></p><p><code>@{&quot;1 week 2 days ago&quot;}</code></p><p>Other usage examples.</p></div><hr><h2 id="git-blame" tabindex="-1"><a class="header-anchor" href="#git-blame"><span>Git Blame</span></a></h2><p>Git blaming is a feature in Git that identifies the last modification made to each line of a file, attributing changes to specific commits and authors. You can do this using the <code>git blame</code> command, which provides a detailed annotation of the file, showing who made changes and when they were made.</p><p>This tool is particularly useful for tracking the history of a file, understanding the evolution of the code, and identifying the source of bugs or changes.</p><p>By pinpointing the exact commit and author responsible for each line, developers can gain insights into the development process and facilitate better collaboration and accountability within a team.</p><div class="hint-container info"><p class="hint-container-title"><code>git blame &lt;FILE&gt;</code></p><p>Shows the last modification for each line of a file.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git blame &lt;FILE&gt; -L &lt;START&gt;,&lt;END&gt;</code></p><p>Limits the blame output to the specified line range.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git blame &lt;FILE&gt; &lt;COMMIT&gt;</code></p><p>Shows the blame information up to the specified commit.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git blame &lt;FILE&gt; -C -C</code></p><p>Shows which revisions and authors last modified each line of a file, with copying detection.</p><p>The <code>-C</code> option detects lines moved or copied within the same file.</p><p>Using it once (<code>-C</code>) detects lines moved or copied within the same file.</p><p>Using the <code>-C</code> option twice (<code>-C -C</code>) makes Git inspect unmodified files as candidates for the source of copy.</p><p>This means it will try to find the origin of copied lines not just in the same file but in other files as well.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git blame &lt;FILE&gt; --reverse</code></p><p>Works backwards, showing who last altered each line in the specified file.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git blame &lt;FILE&gt; --first-parent</code></p><p>Shows who most recently modified each line in a file, following only the first parent commit for merge changes.</p></div><hr><h2 id="archiving-in-git" tabindex="-1"><a class="header-anchor" href="#archiving-in-git"><span>Archiving in Git</span></a></h2><p>Git archiving is a feature that allows you to create archive files, such as .tar or .zip, containing the contents of a specific commit, branch, or tag. This is useful for packaging a snapshot of your repository at a specific point in time, enabling you to distribute or backup the repository&#39;s state without including the entire Git history.</p><p>The git archive command is typically used for this purpose, providing a convenient way to export the current state of the project into a portable format.</p><div class="hint-container info"><p class="hint-container-title"><code>git archive &lt;FORMAT&gt; &lt;TREE-ISH&gt;</code></p><p>Creates an archive file (for example, a .tar or .zip file) containing the contents of the specified tree-ish (like a commit, branch, or tag) in the given format. For example:</p><ul><li><code>git archive --format=tar HEAD</code> creates a .tar archive of the current commit (HEAD).</li><li><code>git archive --format=zip v1.0</code> creates a .zip archive of the files in the v1.0 tag.</li></ul><p>This command is useful for packaging a snapshot of your repository at a specific point in time.</p></div><hr><h2 id="how-to-track-files-in-git" tabindex="-1"><a class="header-anchor" href="#how-to-track-files-in-git"><span>How to Track Files in Git</span></a></h2><p>Git tracking refers to the process of monitoring and managing the files in a repository.</p><p>The command <code>git ls-files</code> lists all files that are being tracked by Git, providing a clear view of the files that are currently under version control. On the other hand, <code>git ls-tree &lt;BRANCH&gt;</code> displays the contents of a tree object for a specified branch, showing the structure and files at that point in the repository.</p><p>Together, these commands help developers understand which files are included in the repository and how they are organized, ensuring efficient tracking and management of the project&#39;s codebase.</p><div class="hint-container info"><p class="hint-container-title"><code>git ls-files</code></p><p>Lists all tracked files.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git ls-tree &lt;BRANCH&gt;</code></p><p>Lists the contents of a tree object.</p></div><hr><h2 id="index-manipulation-in-git" tabindex="-1"><a class="header-anchor" href="#index-manipulation-in-git"><span>Index Manipulation in Git</span></a></h2><p>Git index manipulation involves managing the staging area (also known as the index) where changes are prepared before committing. This can include marking files as &quot;assume unchanged&quot; to temporarily ignore changes, or resetting these markings to track changes again.</p><p>Index manipulation commands, such as <code>git update-index</code>, allow you to control which files are included in the next commit, providing flexibility in handling changes and optimizing the workflow for specific tasks.</p><div class="hint-container info"><p class="hint-container-title"><code>git update-index --assume-unchanged &lt;FILE&gt;</code></p><p>Marks a file as assume unchanged.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git update-index --no-assume-unchanged &lt;FILE&gt;</code></p><p>Unmarks a file as assume unchanged.</p></div><hr><h2 id="squashing-in-git" tabindex="-1"><a class="header-anchor" href="#squashing-in-git"><span>Squashing in Git</span></a></h2><p>Git squashing is the process of combining multiple commits into a single commit. Devs often do this to clean up the commit history before merging changes into a main branch, making the history more concise and easier to read.</p><p>Squashing can be performed using the interactive rebase command (<code>git rebase -i</code>), which lets you selectively merge, reorder, or edit commits. By squashing commits, you can consolidate redundant or minor changes, presenting a clearer narrative of the development process.</p><div class="hint-container info"><p class="hint-container-title"><code>git rebase -i HEAD~&lt;N&gt;</code></p><p>Squashes commits interactively.</p></div><hr><h2 id="data-integrity-in-git" tabindex="-1"><a class="header-anchor" href="#data-integrity-in-git"><span>Data Integrity in Git</span></a></h2><p>Git data integrity refers to the mechanisms and processes Git employs to ensure the accuracy and consistency of data within a repository.</p><p>Git uses cryptographic hashes (SHA-1 or SHA-256) to uniquely identify objects such as commits, trees, and blobs. This hashing not only provides a unique identifier for each object but also ensures that any modification to the object&#39;s content will result in a different hash, thus detecting any corruption or tampering.</p><p>You can use commands like <code>git fsck</code> to verify the connectivity and validity of objects in the database, ensuring the overall health and integrity of the repository.</p><div class="hint-container info"><p class="hint-container-title"><code>git fsck</code></p><p>Verifies the connectivity and validity of objects in the database.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git fsck --unreachable</code></p><p>Finds objects in the repository that are not reachable from any reference.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git prune</code></p><p>Removes unreachable objects.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git gc</code></p><p>Runs a garbage collection process.</p><p>Git garbage collection is a maintenance process that cleans up and optimizes the repository by removing unnecessary files and compressing file revisions to save space.</p><p>This process, triggered by the <code>git gc</code> command, consolidates and deletes unreachable objects, such as orphaned commits and unreferenced blobs, ensuring the repository remains efficient and performant.</p><p>Regular garbage collection helps manage storage effectively and keeps the repository&#39;s structure organized.</p></div><hr><h2 id="cleaning-up-in-git" tabindex="-1"><a class="header-anchor" href="#cleaning-up-in-git"><span>Cleaning Up in Git</span></a></h2><p>Cleaning up in Git involves removing unnecessary files, references, and branches that are no longer needed. This helps to keep your repository organized and efficient.</p><p>Regular cleanup activities, such as pruning remote-tracking branches, deleting untracked files, and removing stale references, ensure that your repository remains manageable and free from clutter.</p><p>In practice, these actions can improve performance, reduce storage requirements, and make it easier to navigate and work within your project.</p><div class="hint-container info"><p class="hint-container-title"><code>git fetch --prune</code></p><p>Removes references that no longer exist on the remote.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git remote prune &lt;NAME&gt;</code></p><p>Prunes all stale remote-tracking branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git fetch origin --prune</code></p><p>Cleans up outdated references from the remote repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git clean -f</code></p><p>Removes untracked files from the working directory, forcing the deletion of files not being tracked by Git.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git clean -fd</code></p><p>Removes untracked files and directories from the working directory, including any files and directories not tracked by Git.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git clean -i</code></p><p>Enters interactive mode for cleaning untracked files.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git clean -X</code></p><p>Removes only ignored files from the working directory.</p></div><hr><h2 id="git-subtree" tabindex="-1"><a class="header-anchor" href="#git-subtree"><span>Git Subtree</span></a></h2><p>Git subtree is a mechanism for managing and integrating subprojects into a main repository. Unlike submodules, which treat the subproject as a separate entity with its own repository, subtrees allow you to include the contents of another repository directly within a subdirectory of your main repository.</p><p>This approach simplifies the workflow by eliminating the need for multiple repositories and enabling seamless integration, merging, and pulling of updates from the subproject. Subtrees provide a flexible and convenient way to manage dependencies and collaborate on projects that require incorporating external codebases.</p><div class="hint-container info"><p class="hint-container-title"><code>git subtree add --prefix=&lt;DIR&gt; &lt;REPOSITORY&gt; &lt;BRANCH&gt;</code></p><p>Adds a repository as a subtree.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git subtree merge --prefix=&lt;DIR&gt; &lt;BRANCH&gt;</code></p><p>Merges a subtree.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git subtree pull --prefix=&lt;DIR&gt; &lt;REPOSITORY&gt; &lt;BRANCH&gt;</code></p><p>Pulls in new changes from the subtree&#39;s repository.</p></div><hr><h2 id="how-to-search-in-git" tabindex="-1"><a class="header-anchor" href="#how-to-search-in-git"><span>How to Search in Git</span></a></h2><p><code>git grep</code> is a powerful search command in Git that allows users to search for specific strings or patterns within the files of a repository. It searches through the working directory and the index, providing a quick and efficient way to locate occurrences of a specified pattern across multiple files.</p><p>This command is particularly useful for developers looking to find instances of code, comments, or text within a project, enabling them to navigate and understand large codebases with ease. With various options and flags, git grep lets you perform targeted searches, making it an essential tool for code analysis and maintenance.</p><div class="hint-container info"><p class="hint-container-title"><code>git grep &lt;PATTERN&gt;</code></p><p>Searches for a string in the working directory and the index.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git grep -e &lt;PATTERN&gt;</code></p><p>Searches for a specific pattern.</p></div><hr><h2 id="bisecting-in-git" tabindex="-1"><a class="header-anchor" href="#bisecting-in-git"><span>Bisecting in Git</span></a></h2><p>Git bisecting is a powerful debugging tool that helps identify the specific commit that introduced a bug or issue in a project. By performing a binary search through the commit history, git bisect efficiently narrows down the range of potential problem commits.</p><p>The process involves marking a known good commit and a known bad commit, and then repeatedly testing intermediate commits to determine whether they are good or bad.</p><p>This iterative approach quickly isolates the faulty commit, allowing developers to pinpoint the exact change that caused the problem. This facilitates faster and more accurate debugging.</p><div class="hint-container info"><p class="hint-container-title"><code>git bisect start</code></p><p>Starts a bisect session.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git bisect bad</code></p><p>Marks the current version as bad.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git bisect good &lt;COMMIT&gt;</code></p><p>Marks the specified commit as good.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git bisect reset</code></p><p>Ends a bisect session and returns to the original branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git bisect visualize</code></p><p>Launches a visual tool to assist with bisecting.</p></div><hr><h2 id="git-attributes" tabindex="-1"><a class="header-anchor" href="#git-attributes"><span>Git Attributes</span></a></h2><p>Git attributes are settings that define how Git should handle specific files or paths within a repository. These attributes are defined in a file named .gitattributes, and they can control various behaviors such as text encoding, line-ending normalization, merge strategies, and diff algorithms.</p><p>By setting attributes, you can ensure consistent behavior across different environments and collaborators, making it easier to manage files with special requirements or complexities.</p><p>For example, you can mark certain files as binary to prevent Git from attempting to merge them, or specify custom diff drivers for more meaningful comparisons.</p><div class="hint-container info"><p class="hint-container-title"><code>git check-attr &lt;ATTRIBUTE&gt; -- &lt;FILE&gt;</code></p><p>Shows the value of a specific attribute for the given file as defined in the .gitattributes configuration, helping you understand how Git is treating the file with respect to attributes like text encoding, merge behavior, or diff handling.</p></div><hr><h2 id="git-checkout" tabindex="-1"><a class="header-anchor" href="#git-checkout"><span>Git Checkout</span></a></h2><p><code>git checkout</code> is a versatile command in Git used to switch between different branches, tags, or commits within a repository. By updating the working directory and index to match the specified branch or commit, it allows you to view or work with the state of the repository at that point.</p><p>You can also use <code>git checkout</code> to create new branches, restore specific files from a commit, or even start a new branch with no history using the <code>--orphan</code> option. This command is essential for navigating and managing different versions of a project&#39;s codebase.</p><div class="hint-container info"><p class="hint-container-title"><code>git checkout &lt;COMMIT&gt;</code></p><p>Updates the working directory and index to match the specified commit, allowing you to view or work with the state of the repository at that commit.</p><p>Just keep in mind that this leaves you in a &quot;detached HEAD&quot; state, meaning you&#39;re not on any branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git checkout -b &lt;BRANCH&gt; &lt;COMMIT&gt;</code></p><p>Creates a new branch named <code>&lt;BRANCH&gt;</code> starting from the specified commit and switches to that branch, allowing you to begin working from that point in the commit history.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git checkout &lt;COMMIT&gt; -- &lt;FILE&gt;</code></p><p>Restores the specified file from a specific commit into your working directory, replacing the current version of the file with the version from that commit without changing the commit history or index.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git checkout --orphan &lt;NEW_BRANCH&gt;</code></p><p>Creates a new branch named <code>&lt;NEW_BRANCH&gt;</code> with no commit history, effectively starting a new branch that begins with a clean working directory and index, as if it were a new repository.</p></div><hr><h2 id="git-reflog" tabindex="-1"><a class="header-anchor" href="#git-reflog"><span>Git Reflog</span></a></h2><p>Git reflog is a powerful tool that records all changes made to the tips of branches and the <code>HEAD</code> reference in a Git repository. This includes actions such as commits, checkouts, merges, and resets.</p><p>By maintaining a history of these changes, reflog allows users to track recent modifications and recover lost commits, even if they are not part of the current branch history. It provides a way to navigate through the repository&#39;s state changes, making it an invaluable resource for debugging and undoing mistakes.</p><div class="hint-container info"><p class="hint-container-title"><code>git reflog</code></p><p>Displays a log of all the changes to the <code>HEAD</code> reference and branch tips, including commits, checkouts, merges, and resets, allowing you to recover lost commits or track recent changes to the repository&#39;s state.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git reflog show &lt;REF&gt;</code></p><p>Displays the reflog for the specified reference (<code>&lt;REF&gt;</code>), showing a log of changes to that reference, including updates to <code>HEAD</code> or branch tips, along with associated commit messages and timestamps.</p></div><hr><h2 id="how-to-handle-untracked-files-in-git" tabindex="-1"><a class="header-anchor" href="#how-to-handle-untracked-files-in-git"><span>How to Handle Untracked Files in Git</span></a></h2><div class="hint-container info"><p class="hint-container-title"><code>git clean</code></p><p><code>git clean</code> removes untracked files and directories from the working directory.</p><p>By default, it only shows what would be removed without actually deleting anything.</p><p>To perform the actual cleanup, you need to use additional flags:</p><ul><li><code>git clean -f</code>: Removes untracked files.</li><li><code>git clean -fd</code>: Removes untracked files and directories.</li><li><code>git clean -fx</code>: Removes untracked files, including those ignored by .gitignore.</li><li><code>git clean -n</code>: Shows which files would be removed without actually deleting them.</li></ul></div><hr><h2 id="force-pushing-in-git" tabindex="-1"><a class="header-anchor" href="#force-pushing-in-git"><span>Force Pushing in Git</span></a></h2><div class="hint-container info"><p class="hint-container-title"><code>git push --force</code></p><p>Forces a push of your local branch to the remote repository, even if it results in a non-fast-forward merge.</p><p>It overwrites the remote branch with your local changes.</p><p>This can be necessary when you have rewritten history (for example, with a rebase) and need to update the remote branch to match your local branch.</p><p>But it can also potentially overwrite others&#39; changes – so use it with caution.</p></div><hr><h2 id="git-fetching-and-pulling" tabindex="-1"><a class="header-anchor" href="#git-fetching-and-pulling"><span>Git Fetching and Pulling</span></a></h2><div class="hint-container info"><p class="hint-container-title"><code>git fetch --all</code></p><p>Retrieves updates from all remote repositories configured for your local repository, fetching changes from all branches and tags without modifying your local branches.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git pull --rebase</code></p><p>Fetches changes from the remote repository and rebases your local commits on top of the updated remote branch, rather than merging them. This keeps the commit history linear and avoids unnecessary merge commits.</p></div><hr><h2 id="how-to-handle-merge-conflicts-in-git" tabindex="-1"><a class="header-anchor" href="#how-to-handle-merge-conflicts-in-git"><span>How to Handle Merge Conflicts in Git</span></a></h2><p>Handling merge conflicts in Git is an essential skill for collaborating on projects with multiple contributors.</p><p>Merge conflicts occur when changes in different branches or commits overlap or contradict each other, preventing an automatic merge. Resolving these conflicts involves reviewing and manually reconciling the differences to ensure that the final code integrates contributions from all parties accurately.</p><p>In practice, effectively managing merge conflicts helps maintain code integrity and facilitates smooth collaboration by ensuring that everyone&#39;s changes are correctly incorporated into the project&#39;s history.</p><div class="hint-container info"><p class="hint-container-title"><code>git mergetool</code></p><p>This launches a merge tool to help you resolve conflicts that arise during a merge or rebase.</p><p>It opens a graphical interface or a text-based tool configured in your Git settings, allowing you to manually resolve conflicts and finalize the merge.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git rerere</code></p><p><code>rerere</code> stands for &quot;reuse recorded resolution&quot; and is a feature that helps automatically resolve conflicts in future merges or rebases by reusing conflict resolutions you&#39;ve previously recorded.</p><p>Once enabled, Git records how you resolved conflicts. If the same conflicts arise again, it can apply the same resolutions automatically.</p></div><hr><h2 id="working-trees-in-git" tabindex="-1"><a class="header-anchor" href="#working-trees-in-git"><span>Working Trees in Git</span></a></h2><p>Working trees in Git allow you to have multiple working directories associated with a single repository. This is particularly useful for working on multiple branches simultaneously without the need to constantly switch branches in the same directory.</p><p>By using working trees, you can easily manage different features, bug fixes, or experiments in isolated environments, improving workflow efficiency and reducing the risk of conflicts.</p><div class="hint-container info"><p class="hint-container-title"><code>git worktree add ../new-branch feature-branch</code></p><p>Creates a new working tree in a directory named &quot;new-branch&quot; based on the &quot;feature-branch&quot;.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git worktree list</code></p><p>Lists all working trees associated with the current repository, showing their paths and the branches they are checked out to.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git worktree remove &lt;PATH&gt;</code></p><p>Removes the specified working tree at the given <code>&lt;PATH&gt;</code>, deleting the working directory and detaching the branch.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git worktree prune</code></p><p>Removes references to nonexistent working trees, cleaning up the working tree list.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git worktree lock &lt;PATH&gt;</code></p><p>Locks the specified working tree at the given <code>&lt;PATH&gt;</code>, preventing it from being pruned.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git worktree unlock &lt;PATH&gt;</code></p><p>Unlocks the specified working tree at the given <code>&lt;PATH&gt;</code>, allowing it to be pruned if necessary.</p></div><hr><h2 id="submodules-in-git" tabindex="-1"><a class="header-anchor" href="#submodules-in-git"><span>Submodules in Git</span></a></h2><p>Submodules in Git are a way to include and manage external repositories within your own repository. They are particularly useful for reusing code across multiple projects, maintaining dependencies, or integrating third-party libraries.</p><p>By using submodules, you can keep your main repository clean and modular, while still ensuring that all necessary components are included and version-controlled.</p><div class="hint-container info"><p class="hint-container-title"><code>git submodule init</code></p><p>Initializes submodules in your repository. This command sets up the configuration necessary for the submodules, but doesn&#39;t actually clone them.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule update</code></p><p>Clones and checks out the submodules into the specified paths. This is typically run after git submodule init.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule add &lt;REPOSITORY&gt; &lt;PATH&gt;</code></p><p>Adds a new submodule to your repository at the specified path, linking it to the specified repository.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule status</code></p><p>Displays the status of all submodules, showing their commit hashes and whether they are up-to-date, modified, or uninitialized.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule foreach &lt;COMMAND&gt;</code></p><p>Runs the specified command in each submodule. This is useful for performing batch operations across all submodules.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule sync</code></p><p>Synchronizes the submodule URLs in your configuration file with those in the .gitmodules file, ensuring they are up-to-date.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule deinit &lt;PATH&gt;</code></p><p>Unregisters the specified submodule, removing its configuration. This doesn&#39;t delete the submodule&#39;s working directory.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule update --remote</code></p><p>Fetches and updates the submodules to the latest commit from their remote repositories.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule set-url &lt;PATH&gt; &lt;NEW_URL&gt;</code></p><p>Changes the URL of the specified submodule to the new URL.</p></div><div class="hint-container info"><p class="hint-container-title"><code>git submodule absorbgitdirs</code></p><p>Absorbs the submodule&#39;s Git directory into the superproject to simplify the structure.</p></div><p>Thank you for reading! I hope this cheatsheet helps you work more easily in Git.</p>',385))])}const R=p(y,[["render",x],["__file","git-cheat-sheet-helpful-git-commands-with-examples.html.vue"]]),E=JSON.parse('{"path":"/freecodecamp.org/git-cheat-sheet-helpful-git-commands-with-examples.html","title":"Git Cheat Sheet – Helpful Git Commands with Examples","lang":"en-US","frontmatter":{"lang":"en-US","title":"Git Cheat Sheet – Helpful Git Commands with Examples","description":"Article(s) > Git Cheat Sheet – Helpful Git Commands with Examples","icon":"iconfont icon-git","category":["Git","Article(s)"],"tag":["blog","freecodecamp.org","git"],"head":[[{"meta":null},{"property":"og:title","content":"Article(s) > Git Cheat Sheet – Helpful Git Commands with Examples"},{"property":"og:description","content":"Git Cheat Sheet – Helpful Git Commands with Examples"},{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/git-cheat-sheet-helpful-git-commands-with-examples.html"}],["meta",{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/git-cheat-sheet-helpful-git-commands-with-examples.html"}],["meta",{"property":"og:site_name","content":"📚Bookshelf"}],["meta",{"property":"og:title","content":"Git Cheat Sheet – Helpful Git Commands with Examples"}],["meta",{"property":"og:description","content":"Article(s) > Git Cheat Sheet – Helpful Git Commands with Examples"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://cdn.hashnode.com/res/hashnode/image/upload/v1723993272242/896730cc-3e03-43be-83d9-06d37ebab5a5.jpeg"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:image:src","content":"https://cdn.hashnode.com/res/hashnode/image/upload/v1723993272242/896730cc-3e03-43be-83d9-06d37ebab5a5.jpeg"}],["meta",{"name":"twitter:image:alt","content":"Git Cheat Sheet – Helpful Git Commands with Examples"}],["meta",{"property":"article:tag","content":"blog"}],["meta",{"property":"article:tag","content":"freecodecamp.org"}],["meta",{"property":"article:tag","content":"git"}],["meta",{"property":"article:published_time","content":"2024-08-20T00:00:00.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Git Cheat Sheet – Helpful Git Commands with Examples\\",\\"image\\":[\\"https://cdn.hashnode.com/res/hashnode/image/upload/v1723993272242/896730cc-3e03-43be-83d9-06d37ebab5a5.jpeg\\"],\\"datePublished\\":\\"2024-08-20T00:00:00.000Z\\",\\"dateModified\\":null,\\"author\\":[]}"]],"prev":"/programming/git/articles/README.md","date":"2024-08-20T00:00:00.000Z","isOriginal":false,"cover":"https://cdn.hashnode.com/res/hashnode/image/upload/v1723993272242/896730cc-3e03-43be-83d9-06d37ebab5a5.jpeg","gitInclude":[]},"headers":[{"level":2,"title":"Preface","slug":"preface","link":"#preface","children":[]},{"level":2,"title":"Basic Git Commands","slug":"basic-git-commands","link":"#basic-git-commands","children":[]},{"level":2,"title":"The Working Directory and the Staging Area","slug":"the-working-directory-and-the-staging-area","link":"#the-working-directory-and-the-staging-area","children":[]},{"level":2,"title":"Working with Branches","slug":"working-with-branches","link":"#working-with-branches","children":[]},{"level":2,"title":"Merging in Git","slug":"merging-in-git","link":"#merging-in-git","children":[]},{"level":2,"title":"Git Remotes","slug":"git-remotes","link":"#git-remotes","children":[]},{"level":2,"title":"Amending Commits","slug":"amending-commits","link":"#amending-commits","children":[]},{"level":2,"title":"Stashing in Git","slug":"stashing-in-git","link":"#stashing-in-git","children":[]},{"level":2,"title":"Git Tagging","slug":"git-tagging","link":"#git-tagging","children":[]},{"level":2,"title":"Reverting Changes in Git","slug":"reverting-changes-in-git","link":"#reverting-changes-in-git","children":[]},{"level":2,"title":"Viewing History Logs","slug":"viewing-history-logs","link":"#viewing-history-logs","children":[]},{"level":2,"title":"Git Diffs","slug":"git-diffs","link":"#git-diffs","children":[]},{"level":2,"title":"Git Flow","slug":"git-flow","link":"#git-flow","children":[]},{"level":2,"title":"Exploring Git References","slug":"exploring-git-references","link":"#exploring-git-references","children":[]},{"level":2,"title":"How to Configure Git","slug":"how-to-configure-git","link":"#how-to-configure-git","children":[]},{"level":2,"title":"Git Security","slug":"git-security","link":"#git-security","children":[]},{"level":2,"title":"How to Set Aliases in Git","slug":"how-to-set-aliases-in-git","link":"#how-to-set-aliases-in-git","children":[]},{"level":2,"title":"Rebasing in Git","slug":"rebasing-in-git","link":"#rebasing-in-git","children":[]},{"level":2,"title":"What is Cherry-Picking?","slug":"what-is-cherry-picking","link":"#what-is-cherry-picking","children":[]},{"level":2,"title":"Patching in Git","slug":"patching-in-git","link":"#patching-in-git","children":[]},{"level":2,"title":"Relative Dates in Git","slug":"relative-dates-in-git","link":"#relative-dates-in-git","children":[]},{"level":2,"title":"Git Blame","slug":"git-blame","link":"#git-blame","children":[]},{"level":2,"title":"Archiving in Git","slug":"archiving-in-git","link":"#archiving-in-git","children":[]},{"level":2,"title":"How to Track Files in Git","slug":"how-to-track-files-in-git","link":"#how-to-track-files-in-git","children":[]},{"level":2,"title":"Index Manipulation in Git","slug":"index-manipulation-in-git","link":"#index-manipulation-in-git","children":[]},{"level":2,"title":"Squashing in Git","slug":"squashing-in-git","link":"#squashing-in-git","children":[]},{"level":2,"title":"Data Integrity in Git","slug":"data-integrity-in-git","link":"#data-integrity-in-git","children":[]},{"level":2,"title":"Cleaning Up in Git","slug":"cleaning-up-in-git","link":"#cleaning-up-in-git","children":[]},{"level":2,"title":"Git Subtree","slug":"git-subtree","link":"#git-subtree","children":[]},{"level":2,"title":"How to Search in Git","slug":"how-to-search-in-git","link":"#how-to-search-in-git","children":[]},{"level":2,"title":"Bisecting in Git","slug":"bisecting-in-git","link":"#bisecting-in-git","children":[]},{"level":2,"title":"Git Attributes","slug":"git-attributes","link":"#git-attributes","children":[]},{"level":2,"title":"Git Checkout","slug":"git-checkout","link":"#git-checkout","children":[]},{"level":2,"title":"Git Reflog","slug":"git-reflog","link":"#git-reflog","children":[]},{"level":2,"title":"How to Handle Untracked Files in Git","slug":"how-to-handle-untracked-files-in-git","link":"#how-to-handle-untracked-files-in-git","children":[]},{"level":2,"title":"Force Pushing in Git","slug":"force-pushing-in-git","link":"#force-pushing-in-git","children":[]},{"level":2,"title":"Git Fetching and Pulling","slug":"git-fetching-and-pulling","link":"#git-fetching-and-pulling","children":[]},{"level":2,"title":"How to Handle Merge Conflicts in Git","slug":"how-to-handle-merge-conflicts-in-git","link":"#how-to-handle-merge-conflicts-in-git","children":[]},{"level":2,"title":"Working Trees in Git","slug":"working-trees-in-git","link":"#working-trees-in-git","children":[]},{"level":2,"title":"Submodules in Git","slug":"submodules-in-git","link":"#submodules-in-git","children":[]}],"readingTime":{"minutes":33.19,"words":9958},"filePathRelative":"freecodecamp.org/git-cheat-sheet-helpful-git-commands-with-examples.md","localizedDate":"August 20, 2024","excerpt":"\\n"}');export{R as comp,E as data};
