import{_ as h}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as u,f as t,av as m,au as o,aw as r,ax as c,a as n,at as a,r as s,o as w}from"./app-C2w16SxA.js";const g={},f={id:"frontmatter-title-관련",tabindex:"-1"},y={class:"header-anchor",href:"#frontmatter-title-관련"};function k(d,e){const i=s("VPCard"),p=s("VidStack"),l=s("FontIcon");return w(),u("div",null,[t("h1",f,[t("a",y,[t("span",null,m(d.$frontmatter.title)+" 관련",1)])]),o(i,r(c({title:"Hacking with iOS – learn to code iPhone and iPad apps with free Swift tutorials",desc:"Learn Swift coding for iOS with these free tutorials – learn Swift, iOS, and Xcode",link:"/hackingwithswift.com/read/README.md",logo:"https://hackingwithswift.com/favicon.svg",background:"rgba(174,10,10,0.2)"})),null,16),e[12]||(e[12]=t("nav",{class:"table-of-contents"},[t("ul")],-1)),e[13]||(e[13]=t("hr",null,null,-1)),o(i,r(c({title:"Loading images with UIImage | Hacking with iOS",desc:"Loading images with UIImage",link:"https://hackingwithswift.com/read/1/5/loading-images-with-uiimage",logo:"https://hackingwithswift.com/favicon.svg",background:"rgba(174,10,10,0.2)"})),null,16),o(p,{src:"youtube/FNrxEvtUl-U"}),e[14]||(e[14]=n(`<p>At this point we have our original table view controller full of pictures to select, plus a detail view controller in our storyboard. The next goal is to show the detail screen when any table row is tapped, and have it show the selected image.</p><p>To make this work we need to add another specially named method to <code>ViewController</code>. This one is called <code>tableView(_, didSelectRowAt:)</code>, which takes an <code>IndexPath</code> value just like <code>cellForRowAt</code> that tells us what row we’re working with. This time we need to do a bit more work:</p><ol><li>We need to create a property in <code>DetailViewController</code> that will hold the name of the image to load.</li><li>We’ll implement the <code>didSelectRowAt</code> method so that it loads a <code>DetailViewController</code> from the storyboard.</li><li>Finally, we’ll fill in <code>viewDidLoad()</code> inside <code>DetailViewController</code> so that it loads an image into its image view based on the name we set earlier.</li></ol><p>Let’s solve each of those in order, starting with the first one: creating a property in <code>DetailViewController</code> that will hold the name of the image to load.</p><p>This property will be a string – the name of the image to load – but it needs to be an <em>optional</em> string because when the view controller is first created it won’t exist. We’ll be setting it straight away, but it still starts off life empty.</p><p>So, add this property to <code>DetailViewController</code> now, just below the existing <code>@IBOutlet</code> line:</p><div class="language-swift line-numbers-mode" data-highlighter="prismjs" data-ext="swift" data-title="swift"><pre><code><span class="line"><span class="token keyword">var</span> selectedImage<span class="token punctuation">:</span> <span class="token class-name">String</span><span class="token operator">?</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>That’s the first task done, so onto the second: implement <code>didSelectRowAt</code> so that it loads a <code>DetailViewController</code> from the storyboard.</p>`,8)),t("p",null,[e[0]||(e[0]=a("When we created the detail view controller, you gave it the storyboard ID “Detail”, which allows us to load it from the storyboard using a method called ")),e[1]||(e[1]=t("code",null,"instantiateViewController(withIdentifier:)",-1)),e[2]||(e[2]=a(". Every view controller has a property called ")),e[3]||(e[3]=t("code",null,"storyboard",-1)),e[4]||(e[4]=a(" that is either the storyboard it was loaded from or nil. In the case of ")),e[5]||(e[5]=t("code",null,"ViewController",-1)),e[6]||(e[6]=a(" it will be ")),o(l,{icon:"iconfont icon-xcode"}),e[7]||(e[7]=t("code",null,"Main.storyboard",-1)),e[8]||(e[8]=a(", which is the same storyboard that contains the detail view controller, so we’ll be loading from there."))]),e[15]||(e[15]=n("<p>We can break this task down into three smaller tasks, two of which are new:</p><ol><li>Load the detail view controller layout from our storyboard.</li><li>Set its <code>selectedImage</code> property to be the correct item from the <code>pictures</code> array.</li><li>Show the new view controller.</li></ol><p>The first of those is done by calling <code>instantiateViewController</code>, but it has two small complexities. First, we call it on the <code>storyboard</code> property that we get from Apple’s <code>UIViewController</code> type, but it’s optional because Swift doesn’t know we came from a storyboard. So, we need to use <code>?</code> just like when we were setting the text label of our cell: “try doing this, but do nothing if there was a problem.”</p><p>Second, even though <code>instantiateViewController()</code> will send us back a <code>DetailViewController</code> if everything worked correctly, Swift <em>thinks</em> it will return back a <code>UIViewController</code> because it can’t see inside the storyboard to know what’s what.</p><p>This will seem confusing if you’re new to programming, so let me try to explain using an analogy. Let’s say you want to go out on a date tonight, so you ask me to arrange a couple of tickets to an event. I go off, find tickets, then hand them to you in an envelope. I fulfilled my part of the deal: you asked for tickets, I got you tickets. But what tickets are they – tickets for a sporting event? Tickets for an opera? Train tickets? The only way for you to find out is to open the envelope and look.</p><p>Swift has the same problem: <code>instantiateViewController()</code> has the return type <code>UIViewController</code>, so as far as Swift is concerned any view controller created with it is actually a <code>UIViewController</code>. This causes a problem for us because we want to adjust the property we just made in <code>DetailViewController</code>. The solution: we need to tell Swift that what it has is not what it thinks it is.</p><p>The technical term for this is “typecasting”: asking Swift to treat a value as a different type. Swift has several ways of doing this, but we’re going to use the safest version: it effectively means, “please try to treat this as a DetailViewController, but if it fails then do nothing and move on.”</p><p>Once we have a detail view controller on our hands, we can set its <code>selectedImage</code> property to be equal to <code>pictures[indexPath.row]</code> just like we were doing in <code>cellForRowAt</code> – that’s the easy bit.</p><p>The third mini-step is to make the new screen show itself. You already saw that view controllers have an optional <code>storyboard</code> property that either contains the storyboard they were loaded from or nil. Well, they also have an optional <code>navigationController</code> property that contains the navigation controller they are inside if it exists, or nil otherwise.</p><p>This is perfect for us, because navigation controllers are responsible for showing screens. Sure, they provide that nice gray bar across the top that you see in lots of apps, but they are also responsible for maintaining a big stack of screens that users navigate through.</p><p>By default they contain the first view controller you created for them in the storyboard, but when new screens are created you can push them onto the navigation controller’s stack to have them slide in smoothly just like you see in Settings. As more screens are pushed on, they just keep sliding in. When users go back a screen – i.e. by tapping Back or by swiping from left to right – the navigation controller will automatically destroy the old view controller and free up its memory.</p>",11)),t("p",null,[e[9]||(e[9]=a("Those three mini-steps complete the new method, so it’s time for the code. Please add this method to ")),o(l,{icon:"fa-brands fa-swift"}),e[10]||(e[10]=t("code",null,"ViewController.swift",-1)),e[11]||(e[11]=a(" – I’ve added comments to make it easier to understand:"))]),e[16]||(e[16]=n(`<div class="language-swift line-numbers-mode" data-highlighter="prismjs" data-ext="swift" data-title="swift"><pre><code><span class="line"><span class="token keyword">override</span> <span class="token keyword">func</span> <span class="token function-definition function">tableView</span><span class="token punctuation">(</span><span class="token omit keyword">_</span> tableView<span class="token punctuation">:</span> <span class="token class-name">UITableView</span><span class="token punctuation">,</span> didSelectRowAt indexPath<span class="token punctuation">:</span> <span class="token class-name">IndexPath</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// 1: try loading the &quot;Detail&quot; view controller and typecasting it to be DetailViewController</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token keyword">let</span> vc <span class="token operator">=</span> storyboard<span class="token operator">?</span><span class="token punctuation">.</span><span class="token function">instantiateViewController</span><span class="token punctuation">(</span>withIdentifier<span class="token punctuation">:</span> <span class="token string-literal"><span class="token string">&quot;Detail&quot;</span></span><span class="token punctuation">)</span> <span class="token keyword">as</span><span class="token operator">?</span> <span class="token class-name">DetailViewController</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token comment">// 2: success! Set its selectedImage property</span></span>
<span class="line">        vc<span class="token punctuation">.</span>selectedImage <span class="token operator">=</span> pictures<span class="token punctuation">[</span>indexPath<span class="token punctuation">.</span>row<span class="token punctuation">]</span></span>
<span class="line"></span>
<span class="line">        <span class="token comment">// 3: now push it onto the navigation controller</span></span>
<span class="line">        navigationController<span class="token operator">?</span><span class="token punctuation">.</span><span class="token function">pushViewController</span><span class="token punctuation">(</span>vc<span class="token punctuation">,</span> animated<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Let’s look at the <code>if let</code> line a bit more closely for a moment. There are three parts of it that might fail: the <code>storyboard</code> property might be nil (in which case the <code>?</code> will stop the rest of the line from executing), the <code>instantiateViewController()</code> call might fail if we had requested “Fzzzzz” or some other invalid storyboard ID, and the typecast – the <code>as?</code> part – also might fail, because we might have received back a view controller of a different type.</p><p>So, three things in that one line have the potential to fail. If you’ve followed all my steps correctly they <em>won’t</em> fail, but they have the <em>potential</em> to fail. That’s where <code>if let</code> is clever: if any of those things return nil (i.e., they fail), then the code inside the <code>if let</code> braces won’t execute. This guarantees your program is in a safe state before any action is taken.</p><p>There’s only one small thing left to do before you can take a look at the results: we need to make the image actually load into the image view in <code>DetailViewController</code>.</p><p>This new code will draw on a new data type, called <code>UIImage</code>. This doesn&#39;t have &quot;View&quot; in its name like <code>UIImageView</code> does, so it&#39;s not something you can view – it&#39;s not something that&#39;s actually visible to users. Instead, <code>UIImage</code> is the data type you&#39;ll use to load image data, such as PNG or JPEGs.</p><p>When you create a <code>UIImage</code>, it takes a parameter called <code>named</code> that lets you specify the name of the image to load. <code>UIImage</code> then looks for this filename in your app&#39;s bundle, and loads it. By passing in the <code>selectedImage</code> property here, which was sent from <code>ViewController</code>, this will load the image that was selected by the user.</p><p>However, we can’t use <code>selectedImage</code> directly. If you remember, we created it like this:</p><div class="language-swift line-numbers-mode" data-highlighter="prismjs" data-ext="swift" data-title="swift"><pre><code><span class="line"><span class="token keyword">var</span> selectedImage<span class="token punctuation">:</span> <span class="token class-name">String</span><span class="token operator">?</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>That <code>?</code> means it might have a value or it might not, and Swift doesn’t let you use these “maybes” without checking them first. This is another opportunity for <code>if let</code>: we can check that <code>selectedImage</code> has a value, and if so pull it out for usage; otherwise, do nothing.</p><p>Add this code to <code>viewDidLoad()</code> inside <code>DetailViewController</code>, <em>after</em> the call to <code>super.viewDidLoad()</code>:</p><div class="language-swift line-numbers-mode" data-highlighter="prismjs" data-ext="swift" data-title="swift"><pre><code><span class="line"><span class="token keyword">if</span> <span class="token keyword">let</span> imageToLoad <span class="token operator">=</span> selectedImage <span class="token punctuation">{</span></span>
<span class="line">    imageView<span class="token punctuation">.</span>image  <span class="token operator">=</span> <span class="token class-name">UIImage</span><span class="token punctuation">(</span>named<span class="token punctuation">:</span> imageToLoad<span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The first line is what checks and unwraps the optional in <code>selectedImage</code>. If for some reason <code>selectedImage</code> is nil (which it should never be, in theory) then the <code>imageView.image</code> line will never be executed. If it has a value, it will be placed into <code>imageToLoad</code>, then passed to <code>UIImage</code> and loaded.</p><p>OK, that’s it: press play or <kbd>Cmd</kbd>+<kbd>R</kbd> now to run the app and try it out! You should be able to select any of the pictures to have them slide in and displayed full screen.</p><p>Notice that we get a Back button in the navigation bar that lets us return back to <code>ViewController</code>. If you click and drag carefully, you’ll find you can create a swipe gesture too – click at the very left edge of the screen, then drag to the right, just as you would do with your thumb on a phone.</p>`,14))])}const I=h(g,[["render",k],["__file","05-loading-images-with-uiimage.html.vue"]]),V=JSON.parse('{"path":"/hackingwithswift.com/read/01/05-loading-images-with-uiimage.html","title":"Loading images with UIImage","lang":"ko-KR","frontmatter":{"lang":"ko-KR","title":"Loading images with UIImage","description":"Article(s) > Loading images with UIImage","category":["Swift","iOS","Article(s)"],"tag":["blog","hackingwithswift.com","crashcourse","swift","xcode","appstore","ios"],"head":[[{"meta":null},{"property":"og:title","content":"Article(s) > Loading images with UIImage"},{"property":"og:description","content":"Loading images with UIImage"},{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/hackingwithswift.com/read/01/05-loading-images-with-uiimage.html"}],["meta",{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/hackingwithswift.com/read/01/05-loading-images-with-uiimage.html"}],["meta",{"property":"og:site_name","content":"📚Bookshelf"}],["meta",{"property":"og:title","content":"Loading images with UIImage"}],["meta",{"property":"og:description","content":"Article(s) > Loading images with UIImage"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"ko-KR"}],["meta",{"property":"article:tag","content":"blog"}],["meta",{"property":"article:tag","content":"hackingwithswift.com"}],["meta",{"property":"article:tag","content":"crashcourse"}],["meta",{"property":"article:tag","content":"swift"}],["meta",{"property":"article:tag","content":"xcode"}],["meta",{"property":"article:tag","content":"appstore"}],["meta",{"property":"article:tag","content":"ios"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Loading images with UIImage\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[]}"]],"isOriginal":false},"headers":[],"git":{"contributors":[{"name":"chanhi2000","email":"chanhi2000@gmail.com","commits":2}]},"readingTime":{"minutes":5.77,"words":1732},"filePathRelative":"hackingwithswift.com/read/01/05-loading-images-with-uiimage.md","excerpt":"\\n"}');export{I as comp,V as data};
