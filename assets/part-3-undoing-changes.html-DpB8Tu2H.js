import{_ as l}from"./plugin-vue_export-helper-DlAUqK2U.js";import{aj as p,am as t,as as h,ao as o,at as m,au as g,ap as s,al as u,aq as i,ar as f,an as a}from"./app-DpiNAgkx.js";const b={},v={id:"frontmatter-title-관련",tabindex:"-1"},w={class:"header-anchor",href:"#frontmatter-title-관련"},y={class:"table-of-contents"};function k(r,e){const c=i("VPCard"),n=i("router-link"),d=i("SiteInfo");return f(),p("div",null,[t("h1",v,[t("a",w,[t("span",null,h(r.$frontmatter.title)+" 관련",1)])]),o(c,m(g({title:"Gitting Things Done - A Visual and Practical Guide to Git [Full Book]",desc:"Introduction Git is awesome. Most software developers use Git on a daily basis. But how many truly understand Git? Do you feel like you know what's going on under the hood as you use Git to perform various tasks? For example, what happens when you us...",link:"/freecodecamp.org/gitting-things-done-book/README.md",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",background:"rgba(10,10,35,0.2)"})),null,16),t("nav",y,[t("ul",null,[t("li",null,[o(n,{to:"#chapter-9-git-reset"},{default:s(()=>e[0]||(e[0]=[a("Chapter 9 - Git Reset")])),_:1}),t("ul",null,[t("li",null,[o(n,{to:"#a-short-reminder-recording-changes"},{default:s(()=>e[1]||(e[1]=[a("A Short Reminder - Recording Changes")])),_:1})]),t("li",null,[o(n,{to:"#introducing-git-reset"},{default:s(()=>e[2]||(e[2]=[a("Introducing git reset")])),_:1})]),t("li",null,[o(n,{to:"#real-life-scenarios"},{default:s(()=>e[3]||(e[3]=[a("Real-Life Scenarios")])),_:1})]),t("li",null,[o(n,{to:"#recap-git-reset"},{default:s(()=>e[4]||(e[4]=[a("Recap - Git Reset")])),_:1})])])]),t("li",null,[o(n,{to:"#chapter-10-additional-tools-for-undoing-changes"},{default:s(()=>e[5]||(e[5]=[a("Chapter 10 - Additional Tools for Undoing Changes")])),_:1}),t("ul",null,[t("li",null,[o(n,{to:"#git-commit-amend"},{default:s(()=>e[6]||(e[6]=[a("git commit --amend")])),_:1})]),t("li",null,[o(n,{to:"#git-revert"},{default:s(()=>e[7]||(e[7]=[a("git revert")])),_:1})]),t("li",null,[o(n,{to:"#git-rebase-as-a-tool-for-undoing-things"},{default:s(()=>e[8]||(e[8]=[a("Git Rebase as a Tool for Undoing Things")])),_:1})]),t("li",null,[o(n,{to:"#git-reflog"},{default:s(()=>e[9]||(e[9]=[a("git reflog")])),_:1})]),t("li",null,[o(n,{to:"#recap-additional-tools-for-undoing-changes"},{default:s(()=>e[10]||(e[10]=[a("Recap - Additional Tools for Undoing Changes")])),_:1})])])]),t("li",null,[o(n,{to:"#chapter-11-exercises"},{default:s(()=>e[11]||(e[11]=[a("Chapter 11 - Exercises")])),_:1}),t("ul",null,[t("li",null,[o(n,{to:"#exercise-1"},{default:s(()=>e[12]||(e[12]=[a("Exercise 1")])),_:1})]),t("li",null,[o(n,{to:"#exercise-2"},{default:s(()=>e[13]||(e[13]=[a("Exercise 2")])),_:1})]),t("li",null,[o(n,{to:"#exercise-3"},{default:s(()=>e[14]||(e[14]=[a("Exercise 3")])),_:1})]),t("li",null,[o(n,{to:"#exercise-4"},{default:s(()=>e[15]||(e[15]=[a("Exercise 4")])),_:1})])])])])]),e[16]||(e[16]=t("hr",null,null,-1)),o(d,{name:"Gitting Things Done - A Visual and Practical Guide to Git [Full Book]",desc:"Introduction Git is awesome. Most software developers use Git on a daily basis. But how many truly understand Git? Do you feel like you know what's going on under the hood as you use Git to perform various tasks? For example, what happens when you us...",url:"https://freecodecamp.org/news/gitting-things-done-book/",logo:"https://cdn.freecodecamp.org/universal/favicons/favicon.ico",preview:"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png"}),e[17]||(e[17]=u(`<p>Did you ever get to a point where you said: &quot;Uh-oh, what did I just do?&quot; I guess you have, just like about anyone who uses Git.</p><p>Perhaps you committed to the wrong branch. Perhaps you lost some code that you had written. Perhaps you committed something that you didn&#39;t mean to.</p><p>This part will give you the tools to rewrite history with confidence, thereby &quot;undoing&quot; all kinds of changes in Git.</p><p>Just like the other parts of the book, this part will be practical yet in-depth - so instead of providing you with a list of things to do when things go wrong, we will understand the underlying mechanisms, so that you will feel confident whenever you get to the &quot;uh-oh&quot; moment. Actually, you will find these moments as opportunities for an interesting challenge, rather than a dreadful scenario.</p><hr><h2 id="chapter-9-git-reset" tabindex="-1"><a class="header-anchor" href="#chapter-9-git-reset"><span>Chapter 9 - Git Reset</span></a></h2><p>Our journey starts with a powerful command that can be used to undo many different actions with Git - <code>git reset</code>.</p><h3 id="a-short-reminder-recording-changes" tabindex="-1"><a class="header-anchor" href="#a-short-reminder-recording-changes"><span>A Short Reminder - Recording Changes</span></a></h3><p>In <a href="#heading-chapter-3-how-to-record-changes-in-git">chapter 3</a>, you learned how to record changes in Git. If you remember everything from this part, feel free to jump to the next section.</p><p>It is very useful to think about Git as a system for recording snapshots of a filesystem in time. Considering a Git repository, it has three &quot;states&quot; or &quot;trees&quot;:</p><ol><li>The <strong>working directory</strong>, a directory that has a repository associated with it.</li><li>The <strong>staging area (index)</strong> which holds the tree for the next commit.</li><li>The <strong>repository</strong>, which is a collection of commits and references.</li></ol><figure><img src="https://freecodecamp.org/news/content/images/2023/12/3_trees.png" alt="The three &quot;trees&quot; of a Git repo" tabindex="0" loading="lazy"><figcaption>The three &quot;trees&quot; of a Git repo</figcaption></figure><p>Note regarding the drawing conventions I use: I include <code>.git</code> within the working directory, to remind you that it is a folder within the project&#39;s folder on the filesystem. The <code>.git</code> folder actually contains the objects and references of the repository, as explained in <a href="#heading-chapter-4-how-to-create-a-repo-from-scratch">chapter 4</a>.</p><h4 id="hands-on-demonstration" tabindex="-1"><a class="header-anchor" href="#hands-on-demonstration"><span>Hands-on Demonstration</span></a></h4><p>Use <code>git init</code> to initialize a new repository. Write some text into a file called <code>1.txt</code>:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">mkdir</span> my_repo</span>
<span class="line"><span class="token builtin class-name">cd</span> my_repo</span>
<span class="line"><span class="token function">git</span> init</span>
<span class="line"><span class="token builtin class-name">echo</span> Hello world <span class="token operator">&gt;</span> <span class="token number">1</span>.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Out of the three tree states described above, where is <code>1.txt</code> now?</p><p>In the working tree, as it hasn&#39;t yet been introduced to the index.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/1_txt_working_dir.png" alt="The file  is now a part of the working dir only" loading="lazy"> <em>The file <code>1.txt</code> is now a part of the working dir only</em></p><p>In order to <em>stage</em> it, to <em>add</em> it to the index, use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> <span class="token number">1</span>.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/1_txt_index.png" alt="Using  stages the file so it is now in the index as well" loading="lazy"> <em>Using <code>git add</code> stages the file so it is now in the index as well</em></p><p>Notice that once you stage <code>1.txt</code>, Git creates a blob object with the content of this file, and adds it to the internal object database (within <code>.git</code> folder), as covered in <a href="#heading-chapter-3-how-to-record-changes-in-git">chapter 3</a> and <a href="#heading-chapter-4-how-to-create-a-repo-from-scratch">chapter 4</a>. I do not draw it as part of the &quot;repository&quot; as in this representation, the &quot;repository&quot; refers to a tree of commits and their references, and this blob has not been a part of any commit.</p><p>Now, use <code>git commit</code> to commit your changes to the repository:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 1&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_1.png" alt="Using  creates a commit object in the repository" loading="lazy"> <em>Using <code>git commit</code> creates a commit object in the repository</em></p><p>You created a new <strong>commit</strong> object, which includes a pointer to a <strong>tree</strong> describing the entire <strong>working tree</strong>. In this case, this tree consists only of <code>1.txt</code> within the root folder. In addition to a pointer to the tree, the commit object includes metadata, such as timestamps and author information.</p><p>When considering the diagrams, notice that we only have a single copy of the file <code>1.txt</code> on disk, and a corresponding blob object in Git&#39;s object database. The &quot;repository&quot; tree now shows this file as it is part of the active commit - that is, the commit object &quot;Commit 1&quot; points to a tree that points to the blob with the contents of <code>1.txt</code>, the same blob that the index is pointing to.</p><p>For more information about the objects in Git (such as commits and trees), refer to <a href="#heading-chapter-1-git-objects">chapter 1</a>.</p><p>Next, create a new file, and add it to the index, as before:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> second <span class="token function">file</span> <span class="token operator">&gt;</span> <span class="token number">2</span>.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> <span class="token number">2</span>.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/2_txt_index.png" alt="The file  is in the working dir and the index after staging it with " loading="lazy"> <em>The file <code>2.txt</code> is in the working dir and the index after staging it with <code>git add</code></em></p><p>Next, commit:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 2&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Importantly, <code>git commit</code> does two things:</p><p>First, it creates a <strong>commit object</strong>, so there is an object within Git&#39;s internal object database with a corresponding SHA-1 value. This new commit object also points to the parent commit. That is the commit that <code>HEAD</code> was pointing to when you wrote the <code>git commit</code> command.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/new_commit_object.png" alt="A new commit object has been created, at first —  still points to the previous commit" loading="lazy"> <em>A new commit object has been created, at first - <code>main</code> still points to the previous commit</em></p><p>Second, <code>git commit</code> <strong>moves the pointer of the active branch</strong> — in our case, that would be <code>main</code>, to point to the newly created commit object.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_updates_active_branch.png" alt=" also updates the active branch to point to the newly created commit object" loading="lazy"> <em><code>git commit</code> also updates the active branch to point to the newly created commit object</em></p><h3 id="introducing-git-reset" tabindex="-1"><a class="header-anchor" href="#introducing-git-reset"><span>Introducing <code>git reset</code></span></a></h3><p>You will now learn how to reverse the process of introducing a commit. For that, you will get to know the command <code>git reset</code>.</p><h4 id="git-reset-soft" tabindex="-1"><a class="header-anchor" href="#git-reset-soft"><span><code>git reset --soft</code></span></a></h4><p>The very last step you did before was to <code>git commit</code>, which actually means two things — Git created a commit object and moved <code>main</code>, the active branch. To undo this step, use the following command:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--soft</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The syntax <code>HEAD~1</code> refers to the first parent of <code>HEAD</code>. Consider a case where I had more than one commit in the commit-graph, say &quot;Commit 3&quot; pointing to &quot;Commit 2&quot;, which is, in turn, pointing to &quot;Commit 1. And consider <code>HEAD</code> was pointing to &quot;Commit 3&quot;. You could use <code>HEAD~1</code> to refer to &quot;Commit 2&quot;, and <code>HEAD~2</code> would refer to &quot;Commit 1&quot;.</p><p>So, back to the command: <code>git reset --soft HEAD~1</code></p><p>This command asks Git to change whatever <code>HEAD</code> is pointing to. (Note: In the diagrams below, I use <code>*HEAD</code> for &quot;whatever <code>HEAD</code> is pointing to&quot;.) In our example, <code>HEAD</code> is pointing to <code>main</code>. So Git will only change the pointer of <code>main</code> to point to <code>HEAD~1</code>. That is, <code>main</code> will point to &quot;Commit 1&quot;.</p><p>However, this command did <strong>not</strong> affect the state of the index or the working tree. So if you use <code>git status</code> you will see that <code>2.txt</code> is staged, just like before you ran <code>git commit</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_status_after_reset_soft.png" alt=" shows that  is in the index, but not in the active commit" loading="lazy"> <em><code>git status</code> shows that <code>2.txt</code> is in the index, but not in the active commit</em></p><p>The state is now:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reset_soft_1.png" alt="Resetting  to &quot;Commit 1&quot;" loading="lazy"> <em>Resetting <code>main</code> to &quot;Commit 1&quot;</em></p><p>(Note: I removed <code>2.txt</code> from the &quot;repository&quot; in the diagram as it is not part of the active commit - that is, the tree pointed to by &quot;Commit 1&quot; does not reference this file. However, it has not been removed from the file system - as it still exists in the working tree and the index.)</p><p>What about <code>git log</code>? It will start from <code>HEAD</code> , go to <code>main</code>, and then to &quot;Commit 1&quot;:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_after_reset_soft.png" alt="The output of " loading="lazy"> <em>The output of <code>git log</code></em></p><p>Notice that this means that &quot;Commit 2&quot; is no longer reachable from our history.</p><p>Does that mean the commit object of &quot;Commit 2&quot; is deleted?</p><p>No, it&#39;s not deleted. It still resides within Git&#39;s internal object database of objects.</p><p>If you push the current history now, by using <code>git push</code>, Git will not push &quot;Commit 2&quot; to the remote server (as it is not reachable from the current <code>HEAD</code>), but the commit object <em>still exists</em> on your local copy of the repository.</p><p>Now, commit again - and use the commit message of &quot;Commit 2.1&quot; to differentiate this new object from the original &quot;Commit 2&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 2.1&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>This is the resulting state:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_2_1.png" alt="Creating a new commit" loading="lazy"> <em>Creating a new commit</em></p><p>I omitted &quot;Commit 2&quot; as it is not reachable from <code>HEAD</code>, even though its object exists in Git&#39;s internal object database.</p><p>Why are &quot;Commit 2&quot; and &quot;Commit 2.1&quot; different? Even if we used the same commit message, and even though they point to the same tree object (of the root folder consisting of <code>1.txt</code> and <code>2.txt</code>), they still have different timestamps, as they were created at different times. Both &quot;Commit 2&quot; and &quot;Commit 2.1&quot; now point to &quot;Commit 1&quot;, but only &quot;Commit 2.1&quot; is reachable from <code>HEAD</code>.</p><h4 id="git-reset-mixed" tabindex="-1"><a class="header-anchor" href="#git-reset-mixed"><span><code>git reset --mixed</code></span></a></h4><p>It&#39;s time to undo even further. This time, use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--mixed</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(Note: <code>--mixed</code> is the default switch for <code>git reset</code>.)</p><p>This command starts the same as <code>git reset --soft HEAD~1</code>. That is, the command takes the pointer of whatever <code>HEAD</code> is pointing to now, which is the <code>main</code> branch, and sets it to <code>HEAD~1</code>, in our example - &quot;Commit 1&quot;.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_mixed_1.png" alt="The first step of  is the same as " loading="lazy"> <em>The first step of <code>git reset --mixed</code> is the same as <code>git reset --soft</code></em></p><p>Next, Git goes further, effectively undoing the changes we made to the index. That is, changing the index so that it matches with the current <code>HEAD</code>, the new <code>HEAD</code> after setting it in the first step.</p><p>If we ran <code>git reset --mixed HEAD~1</code>, then <code>HEAD</code> (<code>main</code>) would be set to <code>HEAD~1</code> (&quot;Commit 1&quot;), and then Git would match the index to the state of &quot;Commit 1&quot; - in this case, it means that <code>2.txt</code> would no longer be part of the index.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_mixed_2.png" alt="The second step of  is to match the index with the new " loading="lazy"> <em>The second step of <code>git reset --mixed</code> is to match the index with the new <code>HEAD</code></em></p><p>It&#39;s time to create a new commit with the state of the original &quot;Commit 2&quot;. This time you need to stage <code>2.txt</code> again before creating it:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> <span class="token number">2</span>.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 2.2&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_2_2.png" alt="Creating &quot;Commit 2.2&quot;" loading="lazy"> <em>Creating &quot;Commit 2.2&quot;</em></p><p>Similarly to &quot;Commit 2.1&quot;, I &quot;name&quot; this commit &quot;Commit 2.2&quot; to differentiate it from the original &quot;Commit 2&quot; or &quot;Commit 2.1&quot; - these commits result in the same state as the original &quot;Commit 2&quot;, but they are different commit objects.</p><h4 id="git-reset-hard" tabindex="-1"><a class="header-anchor" href="#git-reset-hard"><span><code>git reset --hard</code></span></a></h4><p>Go on, undo even more!</p><p>This time, use the <code>--hard</code> switch, and run:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--hard</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Again, Git starts with the <code>--soft</code> stage, setting whatever <code>HEAD</code> is pointing to (<code>main</code>), to <code>HEAD~1</code> (&quot;Commit 1&quot;).</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_hard_1-1.png" alt="The first step of  is the same as " loading="lazy"> <em>The first step of <code>git reset --hard</code> is the same as <code>git reset --soft</code></em></p><p>Next, moving on to the <code>--mixed</code> stage, matching the index with <code>HEAD</code>. That is, Git undoes the staging of <code>2.txt</code>.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_hard_2-1.png" alt="The second step of  is the same as " loading="lazy"> <em>The second step of <code>git reset --hard</code> is the same as <code>git reset --mixed</code></em></p><p>Next comes the <code>--hard</code> step, where Git goes even further and matches the working dir with the stage of the index. In this case, it means removing <code>2.txt</code> also from the working dir.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_hard_3.png" alt="The third step of  matches the state of the working dir with that of the index" loading="lazy"> <em>The third step of <code>git reset --hard</code> matches the state of the working dir with that of the index</em></p><p>So to introduce a change to Git, you have three steps: you change the working dir, the index, or the staging area, and then you commit a new snapshot with those changes. To undo these changes:</p><ul><li>If we use <code>git reset --soft</code>, we undo the commit step.</li><li>If we use <code>git reset --mixed</code>, we also undo the staging step.</li><li>If we use <code>git reset --hard</code>, we undo the changes to the working dir.</li></ul><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_switches.png" alt="The three main switches of " loading="lazy"> <em>The three main switches of <code>git reset</code></em></p><h3 id="real-life-scenarios" tabindex="-1"><a class="header-anchor" href="#real-life-scenarios"><span>Real-Life Scenarios</span></a></h3><h4 id="scenario-1" tabindex="-1"><a class="header-anchor" href="#scenario-1"><span>Scenario #1</span></a></h4><p>So in a real-life scenario, write &quot;I love Git&quot; into a file (<code>love.txt</code>), as we all love Git 😍. Go ahead, stage and commit this as well:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> I love Git <span class="token operator">&gt;</span> love.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 2.3&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_2_3.png" alt="Creating &quot;Commit 2.3&quot;" loading="lazy"> <em>Creating &quot;Commit 2.3&quot;</em></p><p>Also, save a tag so that you can get back to this commit later if needed:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> tag scenario-1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Oh, oops!</p><p>Actually, I didn&#39;t want you to commit it.</p><p>What I actually wanted you to do is write some more love words in this file before committing it.</p><p>What can you do?</p><p>Well, one way to overcome this would be to use <code>git reset --mixed HEAD~1</code>, effectively undoing both the committing and the staging actions you took:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--mixed</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/reset_commit_2_3.png" alt="Undoing the staging and committing steps" loading="lazy"> <em>Undoing the staging and committing steps</em></p><p>So <code>main</code> points to &quot;Commit 1&quot; again, and <code>love.txt</code> is no longer a part of the index. However, the file remains in the working dir. You can now add more content to it:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> and Gitting Things Done <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/adding_love_lyrics.png" alt="Adding more love lyrics" loading="lazy"> <em>Adding more love lyrics</em></p><p>Stage and commit your file:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 2.4&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_2_4.png" alt="Introducing &quot;Commit 2.4&quot;" loading="lazy"> <em>Introducing &quot;Commit 2.4&quot;</em></p><p>Well done!</p><p>You got this clear, nice history of &quot;Commit 2.4&quot; pointing to &quot;Commit 1&quot;.</p><p>You now have a new tool in your toolbox, <code>git reset</code>.</p><p>This tool is super, super useful, and you can accomplish almost anything with it. It&#39;s not always the most convenient tool to use, but it&#39;s capable of solving almost any rewriting-history scenario if you use it carefully.</p><p>For beginners, I recommend using only <code>git reset</code> for almost any time you want to undo in Git. Once you feel comfortable with it, move on to other tools.</p><h4 id="scenario-2" tabindex="-1"><a class="header-anchor" href="#scenario-2"><span>Scenario #2</span></a></h4><p>Let us consider another case.</p><p>Create a new file called <code>new.txt</code>; stage and commit:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> this is a new <span class="token function">file</span> <span class="token operator">&gt;</span> new.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> new.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 3&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_3.png" alt="Creating  and &quot;Commit 3&quot;" loading="lazy"> <em>Creating <code>new.txt</code> and &quot;Commit 3&quot;</em></p><p>(Note: In the drawing I omitted the files from the repository to avoid clutter. Commit 3 includes <code>1.txt</code>, <code>love.txt</code> and <code>new.txt</code> at this stage).</p><p>Oops. Actually, that&#39;s a mistake. You were on <code>main</code>, and I wanted you to create this commit on a feature branch. My bad 😇</p><p>There are two most important tools I want you to take from this chapter. The <em>second</em> is <code>git reset</code>. The first and by far more important one is to whiteboard the current state versus the state you want to be in.</p><p>For this scenario, the current state and the desired state look like so:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/scenario_2.png" alt="Scenario #2: current-vs-desired states" loading="lazy"> <em>Scenario #2: current-vs-desired states</em></p><p>(Note: In following diagrams, I will refer to the current state as the &quot;original&quot; state - before starting the process of rewriting history.)</p><p>You will notice three changes:</p><ol><li><code>main</code> points to &quot;Commit 3&quot; (the blue one) in the current state, but to &quot;Commit 2.4&quot; in the desired state.</li><li><code>feature_branch</code> doesn&#39;t exist in the current state, yet it exists and points to &quot;Commit 3&quot; in the desired state.</li><li><code>HEAD</code> points to <code>main</code> in the current state, and to <code>feature_branch</code> in the desired state.</li></ol><p>If you can draw this and you know how to use <code>git reset</code>, you can definitely get yourself out of this situation.</p><p>So again, the most important thing is to take a breath and draw this out.</p><p>Observing the drawing above, how do you get from the current state to the desired one?</p><p>There are a few different ways of course, but I will present one option only for each scenario. Feel free to play around with other options as well.</p><p>You can start by using <code>git reset --soft HEAD~1</code>. This would set <code>main</code> to point to the previous commit, &quot;Commit 2.4&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--soft</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/scenario_2_1.png" alt="Changing ; &quot;Commit 3 is still there, just not reachable from " loading="lazy"> <em>Changing <code>main</code>: &quot;Commit 3&quot; is still there, just not reachable from <code>HEAD</code></em></p><p>Peeking at the current-vs-desired diagram again, you can see that you need a new branch, right? You can use <code>git switch -c feature_branch</code> for it, or <code>git checkout -b feature_branch</code> (which does the same thing):</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> switch <span class="token parameter variable">-c</span> feature_branch</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/scenario_2_2.png" alt="Creating  branch" loading="lazy"> <em>Creating <code>feature_branch</code> branch</em></p><p>This command also updates <code>HEAD</code> to point to the new branch.</p><p>Since you used <code>git reset --soft</code>, you didn&#39;t change the index, so it currently has exactly the state you want to commit - how convenient! You can simply commit to <code>feature_branch</code>:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 3.1&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_3_1.png" alt="Committing to  branch" loading="lazy"> <em>Committing to <code>feature_branch</code> branch</em></p><p>And you got to the desired state.</p><h4 id="scenario-3" tabindex="-1"><a class="header-anchor" href="#scenario-3"><span>Scenario #3</span></a></h4><p>Ready to apply your knowledge to additional cases?</p><p>Still on <code>feature_branch</code>, add some changes to <code>love.txt</code>, and create a new file called <code>cool.txt</code>. Stage them and commit:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> Some changes <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"><span class="token builtin class-name">echo</span> Git is cool <span class="token operator">&gt;</span> cool.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> cool.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 4&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_4.png" alt="The history, as well as the state of the index and the working dir after creating &quot;Commit 4&quot;" loading="lazy"> <em>The history, as well as the state of the index and the working dir after creating &quot;Commit 4&quot;</em></p><p>Oh, oops, actually I wanted you to create two <em>separate</em> commits, one with each change...</p><p>Want to try this one yourself (before reading on)?</p><p>You can undo the committing and staging steps:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--mixed</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Following this command, the index no longer includes those two changes, but they&#39;re both still in your file system:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reset_commit_4.png" alt="Resulting state after using " loading="lazy"> <em>Resulting state after using <code>git reset --mixed HEAD~1</code></em></p><p>So now, if you only stage <code>love.txt</code>, you can commit it separately:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Love&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_love.png" alt="Resulting state after committing the changes to " loading="lazy"> <em>Resulting state after committing the changes to <code>love.txt</code></em></p><p>Then, do the same for <code>cool.txt</code>:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> cool.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Cool&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_separately.png" alt="Committing separately" loading="lazy"> <em>Committing separately</em></p><p>Nice!</p><h4 id="scenario-4" tabindex="-1"><a class="header-anchor" href="#scenario-4"><span>Scenario #4</span></a></h4><p>To clear up the state, switch to <code>main</code> and use <code>reset --hard</code> to make it point to &quot;Commit 3.1&quot;, while setting the index and the working dir to the state of &quot;Commit 3.1&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> checkout main</span>
<span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--hard</span> <span class="token operator">&lt;</span>SHA_OF_COMMIT_3_<span class="token operator"><span class="token file-descriptor important">1</span>&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/reset_main_commit_3_1.png" alt="Resetting  to &quot;Commit 3.1&quot;" loading="lazy"> <em>Resetting <code>main</code> to &quot;Commit 3.1&quot;</em></p><p>Create another file (<code>another.txt</code>) with some text, and add some text to <code>love.txt</code>. Stage both changes, and commit them:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> Another <span class="token function">file</span> <span class="token operator">&gt;</span> another.txt</span>
<span class="line"><span class="token builtin class-name">echo</span> More love <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> another.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 4.1&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This should be the result:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_more_changes.png" alt="A new commit" loading="lazy"> <em>A new commit</em></p><p>Oops...</p><p>So this time, I wanted it to be on another branch, but not a new branch, rather - an already-existing branch.</p><p>So what can you do?</p><p>I&#39;ll give you a hint. The answer is really short and really easy. What do we do first?</p><p>No, not <code>reset</code>. We <em>draw</em>. That&#39;s the first thing to do, as it would make everything else so much easier. So this is the current state:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/scenario_4.png" alt="The new commit on  appears blue" loading="lazy"> <em>The new commit on <code>main</code> appears blue</em></p><p>And the desired state?</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/scenario_4_1-1.png" alt="We want the &quot;blue&quot; commit to be on another, , branchabel{fig-scenario-4-1}" loading="lazy"> <em>We want the &quot;blue&quot; commit to be on another, <code>existing</code>, branch</em></p><p>How do you get from the current state to the desired state, what would be easiest?</p><p>One way would be to use <code>git reset</code> as you did before, but there is another way that I would like you to try.</p><p>Note that the following commands indeed assume the branch <code>existing</code> exists on your repository, yet you haven&#39;t created it earlier. To match a state where this branch actually exists, you can use the following commands:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> checkout <span class="token operator">&lt;</span>SHA_OF_COMMIT_<span class="token operator"><span class="token file-descriptor important">1</span>&gt;</span></span>
<span class="line"><span class="token function">git</span> checkout <span class="token parameter variable">-b</span> existing</span>
<span class="line"><span class="token builtin class-name">echo</span> <span class="token string">&quot;Hello&quot;</span> <span class="token operator">&gt;</span> x.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> x.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit X&quot;</span></span>
<span class="line"><span class="token function">git</span> checkout <span class="token operator">&lt;</span>SHA_OF_COMMIT_3_<span class="token operator"><span class="token file-descriptor important">1</span>&gt;</span> -- love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit Y&quot;</span></span>
<span class="line"><span class="token function">git</span> checkout main</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>(The command <code>git checkout &lt;SHA_OF_COMMIT_3_1&gt; -- love.txt</code> copies the contents of <code>love.txt</code> from &quot;Commit 3.1&quot; to the index and the working dir, so that you can commit it on the <code>existing</code> branch. We need the state of <code>love.txt</code> on &quot;Commit Y&quot; to be the same as of &quot;Commit 3.1&quot; to avoid conflicts.)</p><p>Now your history should match the one shown in the picture with the caption &quot;We want the &quot;blue&quot; commit to be on another, <code>existing</code>, branch&quot;.</p><p>First, move <code>HEAD</code> to point to existing branch:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> switch existing</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/switch_existing.png" alt="Switch to the  branch" loading="lazy"> <em>Switch to the <code>existing</code> branch</em></p><p>Intuitively, what you want to do is take the changes introduced in &quot;Commit 4.1&quot;, and apply these changes (&quot;copy-paste&quot;) on top of <code>existing</code> branch. And Git has a tool just for that.</p><p>To ask Git to take the changes introduced between a commit and its parent commit and just apply these changes on the active branch, you can use <code>git cherry-pick</code>, a command we introduced in <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a>. This command takes the changes introduced in the specified revision and applies them to the state of the active commit. Run:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> cherry-pick <span class="token operator">&lt;</span>SHA_OF_COMMIT_4_<span class="token operator"><span class="token file-descriptor important">1</span>&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>You can specify the SHA-1 identifier of the desired commit, but you can also use <code>git cherry-pick main</code>, as the commit whose changes you are applying is the one <code>main</code> is pointing to.</p><p><code>git cherry-pick</code> also creates a new commit object, and updates the active branch to point to this new object, so the resulting state would be:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/cherry_pick.png" alt="The result after using " loading="lazy"> <em>The result after using <code>git cherry-pick</code></em></p><p>I mark the commit as &quot;Commit 4.2&quot; since it has a different timestamp, parent and SHA-1 value than &quot;Commit 4.1&quot;, though the changes it introduces are the same.</p><p>You made good progress - the desired commit is now on the <code>existing</code> branch! But we don&#39;t want these changes to exist on <code>main</code> branch. <code>git cherry-pick</code> only applied the changes to the existing branch. How can you remove them from <code>main</code>?</p><p>One way would be to switch back to <code>main</code>, and then <code>reset</code> it:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> switch main</span>
<span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--hard</span> HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>And the result:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reset_cherry_pick.png" alt="The resulting state after resetting " loading="lazy"> <em>The resulting state after resetting <code>main</code></em></p><p>You did it!</p><p>Note that <code>git cherry-pick</code> actually computes the difference between the specified commit and its parent, and then applies the difference to the active commit. This means that sometimes, Git won&#39;t be able to apply those changes due to a conflict.</p><p>Also, note that you can ask Git to <code>cherry-pick</code> the changes introduced in any commit, not only commits referenced by a branch.</p><h3 id="recap-git-reset" tabindex="-1"><a class="header-anchor" href="#recap-git-reset"><span>Recap - Git Reset</span></a></h3><p>In this chapter, we learned how <code>git reset</code> operates, and clarified its three main modes of operation:</p><ul><li><code>git reset --soft &lt;commit&gt;</code>, which changes whatever <code>HEAD</code> is pointing to - to <code>&lt;commit&gt;</code>.</li><li><code>git reset --mixed &lt;commit&gt;</code>, which goes through the <code>--soft</code> stage, and also sets the state of the index to match that of <code>HEAD</code>.</li><li><code>git reset --hard &lt;commit&gt;</code>, which goes through the <code>--soft</code> and <code>--mixed</code> stages, and then sets the state of the working dir to match that of the index.</li></ul><p>You then applied your knowledge about <code>git reset</code> to solve some real-life issues that arise when using Git.</p><p>By understanding the way Git operates, and by whiteboarding the current state versus the desired state, you can confidently tackle all kinds of scenarios.</p><p>In the future chapters, we will cover additional Git commands and how they can help us solve all kinds of undesired situations.</p><hr><h2 id="chapter-10-additional-tools-for-undoing-changes" tabindex="-1"><a class="header-anchor" href="#chapter-10-additional-tools-for-undoing-changes"><span>Chapter 10 - Additional Tools for Undoing Changes</span></a></h2><p>In the previous chapter, you met <code>git reset</code>. Indeed, <code>git reset</code> is a super powerful tool, and I highly recommend to use it until you feel completely comfortable with it.</p><p>Yet, <code>git reset</code> is not the only tool at our disposal. Some of the times, it is not the most convenient tool to use. In others, it&#39;s just not enough. This short chapter touches a few tools that are helpful for undoing changes in Git.</p><h3 id="git-commit-amend" tabindex="-1"><a class="header-anchor" href="#git-commit-amend"><span><code>git commit --amend</code></span></a></h3><p>Consider <a href="https://freecodecamp.org/news/p/f7b355ea-3f22-4613-8218-e95c67779d9f/scenario-1" target="_blank" rel="noopener noreferrer">Scenario #1</a> from the previous chapter again. As a reminder, you wrote &quot;I love Git&quot; into a file (<code>love.txt</code>), staged and committed this file:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/image-52.png" alt="Image" loading="lazy"> <em>The state after creating &quot;Commit 2.3&quot;</em></p><p>And then I realized I didn&#39;t want you to commit it at that state, but rather - write some more love words in this file before committing it.</p><p>To match this state, simply checkout the tag you created, which points to &quot;Commit 2.3&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> checkout scenario-1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>In the previous chapter, when we introduced <code>git reset</code>, you solved this issue by using <code>git reset --mixed HEAD~1</code>, effectively undoing both the committing and the staging actions you took.</p><p>Now I would like you to consider another approach. Keep working at the state of the last introduced commit (&quot;Commit 2.3&quot;, referenced by the tag &quot;scenario-1&quot;), and make the changes you want:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> And I love this book <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Add this change to the index:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Now, you can use <code>git commit</code> with the <code>--amend</code> switch, which tells it to override the commit <code>HEAD</code> is pointing to. Actually, it will create another, new commit, pointing to <code>HEAD~1</code> (&quot;Commit 1&quot; in our example), and make <code>HEAD</code> point to this newly created commit. By providing the <code>-m</code> argument you can specify a new commit message as well:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> commit <span class="token parameter variable">--amend</span> <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 2.4&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>After running this command, <code>HEAD</code> points to <code>main</code>, which points to &quot;Commit 2.4&quot;, which in turn points to &quot;Commit 1&quot;. The previous &quot;Commit 2.3&quot; is no longer reachable from the history.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/commit_amend-1.png" alt="Image" loading="lazy"> <em>The state after using <code>git commit --amend</code> (Commit &quot;2.3&quot; is unreachable and thus not included in the drawing)</em></p><p>This tool is useful when you want to quickly override the last commit you created. Indeed, you could use <code>git reset</code> to accomplish the same thing, but you can view <code>git commit --amend</code> as a more convenient shortcut.</p><h3 id="git-revert" tabindex="-1"><a class="header-anchor" href="#git-revert"><span><code>git revert</code></span></a></h3><p>Okay, so another day, another problem.</p><p>Add the following text to <code>love.txt</code>, stage and commit as follows:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> This is <span class="token function">more</span> tezt <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 3&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_revert_1-1.png" alt="Committing &quot;More changes&quot;" loading="lazy"> <em>The state after committing &quot;Commit 3&quot;</em></p><p>And push it to the remote server:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> push origin HEAD</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Um, oops 😓…</p><p>I just noticed something. I had a typo there. I wrote &quot;This is more tezt&quot; instead of &quot;This is more text&quot;. Whoops. So what&#39;s the big problem now? I <code>push</code>ed, which means that someone else might have already <code>pull</code>ed those changes.</p><p>If I override those changes by using <code>git reset</code>, we will have different histories, and all hell might break loose. You can rewrite your own copy of the repo as much as you like until you <code>push</code> it.</p><p>Once you <code>push</code> the change, you need to be certain no one else has fetched those changes if you are going to rewrite history.</p><p>Alternatively, you can use another tool called <code>git revert</code>. This command takes the commit you&#39;re providing it with and computes the diff from its parent commit, just like <code>git cherry-pick</code>, but this time, it computes the <em>reverse</em> changes. That is, if in the specified commit you added a line, the reverse would delete the line, and vice versa.</p><p>In our case we are reverting &quot;Commit 3&quot;, so the reverse would be to delete the line &quot;This is more tezt&quot; from <code>love.txt</code>. Since &quot;Commit 3&quot; is referenced by <code>main</code> and <code>HEAD</code>, we can use any of these named references in this command:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_revert_2.png" alt="Using  to undo the changes" loading="lazy"> <em>Using <code>git revert</code> to undo the changes</em></p><p><code>git revert</code> created a new commit object, which means it&#39;s an addition to the history. By using <code>git revert</code>, you didn&#39;t rewrite history. You admitted your past mistake, and this commit is an acknowledgment that you made a mistake and now you fixed it.</p><p>Some would say it&#39;s the more mature way. Some would say it&#39;s not as clean a history as you would get if you used <code>git reset</code> to rewrite the previous commit. But this is a way to avoid rewriting history.</p><p>You can now fix the typo and commit again:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> This is <span class="token function">more</span> text <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 3.1&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_revert_3.png" alt="Redoing the changes" loading="lazy"> <em>The resulting state after redoing the changes</em></p><p>You can use <code>git revert</code> to revert a commit other than <code>HEAD</code>. Say that you want to reverse the parent of <code>HEAD</code>, you can use:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> revert HEAD~1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Or you could provide the SHA-1 of the commit to revert.</p><p>Notice that since Git will apply the reverse patch of the previous patch - this operation might fail, as the patch may no longer apply and you might get a conflict.</p><h3 id="git-rebase-as-a-tool-for-undoing-things" tabindex="-1"><a class="header-anchor" href="#git-rebase-as-a-tool-for-undoing-things"><span>Git Rebase as a Tool for Undoing Things</span></a></h3><p>In <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a>, you learned about Git rebase. We then considered it mainly as a tool to combine changes introduced in different branches. Yet, as long as you haven&#39;t <code>push</code>ed your changes, using <code>rebase</code> on your own branch can be a very convenient way to rearrange your commit history.</p><p>For that, you would usually <a href="#heading-how-to-rebase-on-a-single-branch">rebase on a single branch</a>, and use interactive rebase. Consider again this example covered in <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a>, where I worked from <code>feature_branch_2</code>, and specifically edited the file <code>code.py</code>. I started by changing all strings to be wrapped by double quotes rather than single quotes:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/code_py_4-1.png" alt="Changing  into  in " loading="lazy"> <em>Changing <code>&#39;</code> into <code>&quot;</code> in <code>code.py</code></em></p><p>Then, I staged and committed:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> code.py</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 17&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>I then decided to add a new function at the beginning of the file:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/code_py_5-1.png" alt="Adding the function " loading="lazy"> <em>Adding the function <code>another_feature</code></em></p><p>Again, I staged and committed:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> code.py</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 18&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>And now I realized I actually forgot to change the single quotes to double quotes wrapping the <code>__main__</code> (as you might have noticed), so I did that too:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/code_py_6-1.png" alt="Changing  into " loading="lazy"> <em>Changing <code>&#39;__main__&#39;</code> into <code>&quot;__main__&quot;</code></em></p><p>Of course, I staged and committed this change:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">add</span> code.py</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 19&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Now, consider the history:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/history_after_commit_19-1.png" alt="The commit history after introducing &quot;Commit 19&quot;" loading="lazy"> <em>The commit history after introducing &quot;Commit 19&quot;</em></p><p>As explained in <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a>, I got to a state with two commits that are related to one another, &quot;Commit 17&quot; and &quot;Commit 19&quot; (turning <code>&#39;</code>s into <code>&quot;</code>s), but they are split by the unrelated &quot;Commit 18&quot; (where I added a new function).</p><p>This is a classic case where <code>git rebase</code> would come in handy, to undo the local changes before <code>push</code>ing a clean history.</p><p>Intuitively, I want to edit the history here:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/plan_edit_commits_17_18-1.png" alt="These are the commits I want to edit" loading="lazy"> <em>These are the commits I want to edit</em></p><p>I can <code>rebase</code> the history from &quot;Commit 17&quot; to &quot;Commit 19&quot;, on top of &quot;Commit 15&quot;. To do that:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> rebase <span class="token parameter variable">--interactive</span> <span class="token parameter variable">--onto</span> <span class="token operator">&lt;</span>SHA_OF_COMMIT_1<span class="token operator"><span class="token file-descriptor important">5</span>&gt;</span> <span class="token operator">&lt;</span>SHA_OF_COMMIT_1<span class="token operator"><span class="token file-descriptor important">5</span>&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><img src="https://freecodecamp.org/news/content/images/2023/12/rebase_onto_4-1.png" alt="Using  on a single branch" loading="lazy"> <em>Using <code>rebase --onto</code> on a single branch</em></p><p>This results in the following screen:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/interactive_rebase_4-1.png" alt="Interactive rebase" loading="lazy"> <em>Interactive rebase</em></p><p>So what would I do? I want to put &quot;Commit 19&quot; before &quot;Commit 18&quot;, so it comes right after &quot;Commit 17&quot;. I can go further and <code>squash</code> them together, like so:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/interactive_rebase_5-1.png" alt="Interactive rebase - changing the order of commit and squashing" loading="lazy"> <em>Interactive rebase - changing the order of commit and squashing</em></p><p>Now when I get prompted for a commit message, I can provide the message &quot;Commit 17+19&quot;:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/interactive_rebase_6-1.png" alt="Providing a commit message" loading="lazy"> <em>Providing a commit message</em></p><p>And now, see our beautiful history:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/rebase_onto_5-1.png" alt="The resulting history" loading="lazy"> <em>The resulting history</em></p><p>The syntax used above, <code>git rebase --interactive --onto &lt;COMMIT X&gt; &lt;COMMIT X&gt;</code> would be the most commonly used syntax by those who use <code>rebase</code> regularly. The state of mind these developers usually have is to create atomic commits while working, all the time, without being scared to change them later. Then, before <code>push</code>ing their changes, they would <code>rebase</code> the entire set of changes since the last <code>push</code>, and rearrange it so the history becomes coherent.</p><h3 id="git-reflog" tabindex="-1"><a class="header-anchor" href="#git-reflog"><span><code>git reflog</code></span></a></h3><p>Time to consider a more startling case.</p><p>Go back to &quot;Commit 2.4&quot;:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> reset <span class="token parameter variable">--hard</span> <span class="token operator">&lt;</span>SHA_OF_COMMIT_2_<span class="token operator"><span class="token file-descriptor important">4</span>&gt;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Get some work done, write some code, and add it to <code>love.txt</code>. Stage this change, and commit it:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token builtin class-name">echo</span> lots of work <span class="token operator">&gt;&gt;</span> love.txt</span>
<span class="line"><span class="token function">git</span> <span class="token function">add</span> love.txt</span>
<span class="line"><span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&quot;Commit 3.2&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>(I&#39;m using &quot;Commit 3.2&quot; to indicate that this is not the same commit as &quot;Commit 3&quot; we used when explaining <code>git revert</code>.)</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reflog_commit_3-1.png" alt="Another commit" loading="lazy"> <em>Another commit - &quot;Commit 3.2&quot;</em></p><p>I did the same on my machine, and I used the <code>Up</code> arrow key on my keyboard to scroll back to previous commands, and then I hit <code>Enter</code>, and… Wow.</p><p>Whoops.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reflog_commit_3_reset.png" alt="Did I just ?" loading="lazy"> <em>Did I just <code>git reset -- hard</code>?</em></p><p>Did I just use <code>git reset --hard</code>? 😨</p><p>What actually happened? As you learned in the <a href="#heading-chapter-9-git-reset">previous chapter</a>, Git moved the pointer to <code>HEAD~1</code>, so the last commit, with all of my precious work, is not reachable from the current history. Git also removed all the changes from the staging area, and then matched the working dir to the state of the staging area.</p><p>That is, everything matches this state where my work is… gone.</p><p>Freak out time. Freaking out.</p><p>But, really, is there a reason to freak out? Not really… We&#39;re relaxed people. What do we do? Well, intuitively, is the commit really, really gone?</p><p>No. Why not? It still exists inside the internal database of Git.</p><p>If I only knew where that is, I would know the <code>SHA-1</code> value that identifies this commit, and we could restore it. I could even undo the undoing, and <code>reset</code> back to this commit.</p><p>Actually, the only thing I really need here is the <code>SHA-1</code> of the &quot;deleted&quot; commit.</p><p>Now the question is, how do I find it? Would <code>git log</code> be useful?</p><p>Well, not really. <code>git log</code> would go to <code>HEAD</code>, which points to <code>main</code>, which points to the parent commit of the commit we are looking for. Then, <code>git log</code> would trace back through the parent chain, which does not include the commit with my precious work.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reflog_git_log.png" alt=" doesn&#39;t help in this case" loading="lazy"> <em><code>git log</code> doesn&#39;t help in this case</em></p><p>Thankfully, the very smart people who created Git also created a backup plan for us, and that is called the <code>reflog</code>.</p><p>While you work with Git, whenever you change <code>HEAD</code>, which you can do by using <code>git reset</code>, but also other commands like <code>git switch</code> or <code>git checkout</code>, Git adds an entry to the <code>reflog</code>.</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reflog.png" alt=" shows us where  was" loading="lazy"> <em><code>git reflog</code> shows us where <code>HEAD</code> was</em></p><p>We found our commit! It&#39;s the one starting with <code>0fb929e</code>.</p><p>We can also relate to it by its &quot;nickname&quot; - <code>HEAD@{1}</code>. Similar to the way Git uses <code>HEAD~1</code> to get to the first parent of <code>HEAD</code>, and <code>HEAD~2</code> to refer to the second parent of <code>HEAD</code> and so on, Git uses <code>HEAD@{1}</code> to refer to the first <em>reflog</em> parent of <code>HEAD</code>, that is, where <code>HEAD</code> pointed to in the previous step.</p><p>We can also ask <code>git rev-parse</code> to show us its value:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/reflog_revparse.png" alt="Using " loading="lazy"> <em>Using <code>git rev-parse HEAD@{1}</code></em></p><p>Note: In case you are using Windows, you may need to wrap it with quotation marks - like so:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> rev-parse <span class="token string">&quot;HEAD@{1}&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Another way to view the <code>reflog</code> is by using <code>git log -g</code>, which asks <code>git log</code> to actually consider the <code>reflog</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_g.png" alt="The output of " loading="lazy"> <em>The output of <code>git log -g</code></em></p><p>You can see in the output of <code>git log -g</code> that the <code>reflog</code>&#39;s entry <code>HEAD@{0}</code>, just like <code>HEAD</code>, points to <code>main</code>, which points to &quot;Commit 2&quot;. But the parent of that entry in the <code>reflog</code> points to &quot;Commit 3&quot;.</p><p>So to get back to &quot;Commit 3&quot;, you can just use <code>git reset --hard HEAD@{1}</code> (or the <code>SHA-1</code> value of &quot;Commit 3&quot;):</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reflog_reset.png" alt="Image" loading="lazy"> <em><code>git reset --hard HEAD@{1}</code></em></p><p>And now, if you <code>git log</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_log_2.png" alt="Our history is back!!!" loading="lazy"> <em>Our history is back!!!</em></p><p>We saved the day!</p><p>What would happen if I used this command again? And ran <code>git reset --hard HEAD@{1}</code>?</p><p>Git would set <code>HEAD</code> to where <code>HEAD</code> was pointing before the last <code>reset</code>, meaning to &quot;Commit 2&quot;. We can keep going all day:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/git_reset_again.png" alt=" again" loading="lazy"> <em><code>git reset --hard</code> again</em></p><h3 id="recap-additional-tools-for-undoing-changes" tabindex="-1"><a class="header-anchor" href="#recap-additional-tools-for-undoing-changes"><span>Recap - Additional Tools for Undoing Changes</span></a></h3><p>In the previous chapter, you learned how to use <code>git reset</code> to undo changes.</p><p>In this chapter, you extended your toolbox for undoing changes in Git with a few new commands:</p><ul><li><code>git commit --amend</code> - which &quot;overrides&quot; the last commit with the stage of the index. Mostly useful when you just committed something and want to modify that last commit.</li><li><code>git revert</code> - which creates a new commit, that reverts a past commit by adding a new commit to the history with the reversed changes. Useful especially when the &quot;faulty&quot; commit has already been pushed to the remote.</li><li><code>git rebase</code> - which you already know from <a href="#heading-chapter-8-understanding-git-rebase">chapter 8</a>, and is useful for rewriting the history of multiple commits, especially before pushing them.</li><li><code>git reflog</code> (and <code>git log -g</code>) - which tracks all changes to <code>HEAD</code>, so you might find the SHA-1 value of a commit you need to get back to.</li></ul><p>The most important tool, even more important than the tools I just listed, is to whiteboard the current situation vs the desired one. Trust me on this, it will make every situation seem less daunting and the solution more clear.</p><p>There are additional tools that allow you to reverse changes in Git (I will provide links in the <a href="#heading-additional-references-by-part">appendix</a>), but the collection of tools covered here should prepare you to tackle any challenge with confidence.</p><hr><h2 id="chapter-11-exercises" tabindex="-1"><a class="header-anchor" href="#chapter-11-exercises"><span>Chapter 11 - Exercises</span></a></h2><p>This chapter includes a few exercises to deepen your understanding of the tools you learned in Part 3. The full version of this book also includes detailed solutions for each.</p><p>The exercises are found on this repository:</p><p><a href="https://github.com/Omerr/undo-exercises.git" target="_blank" rel="noopener noreferrer">https://github.com/Omerr/undo-exercises.git</a></p><p>Each exercise exists on a branch with the name <code>exercise_XX</code>, so Exercise 1 is found on branch <code>exercise_01</code>, Exercise 2 is found on branch <code>exercise_02</code> and so on.</p><p>Note: As explained in previous chapters, if you work with commits that can be found on a remote server (which you are in this case, as you are using my repository &quot;undo-exercises&quot;), you should probably use <code>git revert</code> instead of <code>git reset</code>. Similar to <code>git rebase</code>, the command <code>git reset</code> also rewrites history - and thus you should refrain from using it on commits that others may have relied on.</p><p>For the purposes of these exercises, you can assume no one else has cloned or pulled code from the remote repository. Just remember - in real life, you should probably use <code>git revert</code> instead of commands that rewrite history in such cases.</p><h3 id="exercise-1" tabindex="-1"><a class="header-anchor" href="#exercise-1"><span>Exercise 1</span></a></h3><p>On branch <code>exercise_01</code>, consider the file <code>hello.txt</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_01_1.png" alt="The file " loading="lazy"> <em>The file <code>hello.txt</code></em></p><p>This file includes a typo (in the last character). Find the commit that introduced this typo.</p><h4 id="exercise-1a" tabindex="-1"><a class="header-anchor" href="#exercise-1a"><span>Exercise (1a)</span></a></h4><p>Remove this commit from the reachable history using <code>git reset</code> (with the right arguments), fix the typo, and commit again. Consider your history.</p><p>Revert to the previous state.</p><h4 id="exercise-1b" tabindex="-1"><a class="header-anchor" href="#exercise-1b"><span>Exercise (1b)</span></a></h4><p>Remove the faulty commit using <code>git commit --amend</code>, and get to the same state of the history as in the end of exercise (1a).</p><p>Revert to the previous state.</p><h4 id="exercise-1c" tabindex="-1"><a class="header-anchor" href="#exercise-1c"><span>Exercise (1c)</span></a></h4><p><code>revert</code> the faulty commit using <code>git revert</code> and fix the typo. Consider your history.</p><p>Revert to the previous state.</p><h4 id="exercise-1d" tabindex="-1"><a class="header-anchor" href="#exercise-1d"><span>Exercise (1d)</span></a></h4><p>Using <code>git rebase</code>, get to the same state as in the end of exercise (1a).</p><h3 id="exercise-2" tabindex="-1"><a class="header-anchor" href="#exercise-2"><span>Exercise 2</span></a></h3><p>Switch to <code>exercise_02</code>, and consider the contents of <code>exercise_02.txt</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_02_1.png" alt="The contents of " loading="lazy"> <em>The contents of <code>exercise_02.txt</code></em></p><p>A simple file, with one character at each line.</p><p>Consider the history (using <code>git lol</code>):</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_02_2.png" alt="Image" loading="lazy"> <em><code>git lol</code></em></p><p>Oh my. Each character was introduced in a separate commit. That doesn&#39;t make any sense!</p><p>Use the tools you&#39;ve acquired to create a history where the creation of <code>exercise_02.txt</code> is all done in a single commit.</p><h3 id="exercise-3" tabindex="-1"><a class="header-anchor" href="#exercise-3"><span>Exercise 3</span></a></h3><p>Consider the history on branch <code>exercise_03</code>:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_03_1.png" alt="The history on " loading="lazy"> <em>The history on <code>exercise_03</code></em></p><p>This seems like a mess. You will notice that:</p><ul><li>The order is skewed. We need &quot;Commit 1&quot; to be the earliest commit on this branch, and have &quot;Initial Commit&quot; as its parent, followed by &quot;Commit 2&quot; and so on.</li><li>We shouldn&#39;t have &quot;Commit 2a&quot; and &quot;Commit 2b&quot;, or &quot;Commit 4a&quot; and &quot;Commit 4b&quot; - these two pairs need to be combined into a single commit each - &quot;Commit 2&quot; and &quot;Commit 4&quot;.</li><li>There is a typo on the commit message of &quot;Commit 1&quot;, it should not have 3 <code>m</code>s.</li></ul><p>Fix these issues, but rely on the changes of each original commit. The resulting history should look like so:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_03_2.png" alt="The desired history" loading="lazy"> <em>The desired history</em></p><h3 id="exercise-4" tabindex="-1"><a class="header-anchor" href="#exercise-4"><span>Exercise 4</span></a></h3><p>This exercise actually consists of three branches: <code>exercise_04</code>, <code>exercise_04_a</code>, and <code>exercise_04_b</code>.</p><p>To see the history of these branches without others, use the following syntax:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> lol <span class="token parameter variable">--branches</span><span class="token operator">=</span><span class="token string">&quot;exercise_04*&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The result is:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_04_1.png" alt="The output of " loading="lazy"> <em>The output of <code>git lol --branches=&quot;exercise_04*&quot;</code></em></p><p>Your goal is to make <code>exercise_04_b</code> independent of <code>exercise_04_a</code>. That is, get to this history:</p><p><img src="https://freecodecamp.org/news/content/images/2023/12/ex_04_2.png" alt="The desired history" loading="lazy"> <em>The desired history</em></p><p><strong>Good luck!</strong></p>`,377))])}const q=l(b,[["render",k],["__file","part-3-undoing-changes.html.vue"]]),C=JSON.parse('{"path":"/freecodecamp.org/gitting-things-done-book/part-3-undoing-changes.html","title":"Part 3 - Undoing Changes","lang":"en-US","frontmatter":{"lang":"en-US","title":"Part 3 - Undoing Changes","description":"Article(s) > (4/6) Gitting Things Done - A Visual and Practical Guide to Git [Full Book]","category":["Git","Article(s)"],"tag":["blog","freecodecamp.org","git"],"head":[[{"meta":null},{"property":"og:title","content":"Article(s) > (4/6) Gitting Things Done - A Visual and Practical Guide to Git [Full Book]"},{"property":"og:description","content":"Part 3 - Undoing Changes"},{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/gitting-things-done-book/part-3-undoing-changes.html"}],["meta",{"property":"og:url","content":"https://chanhi2000.github.io/bookshelf/freecodecamp.org/gitting-things-done-book/part-3-undoing-changes.html"}],["meta",{"property":"og:site_name","content":"📚Bookshelf"}],["meta",{"property":"og:title","content":"Part 3 - Undoing Changes"}],["meta",{"property":"og:description","content":"Article(s) > (4/6) Gitting Things Done - A Visual and Practical Guide to Git [Full Book]"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:image:src","content":"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png"}],["meta",{"name":"twitter:image:alt","content":"Part 3 - Undoing Changes"}],["meta",{"property":"article:author","content":"Omer Rosenbaum"}],["meta",{"property":"article:tag","content":"blog"}],["meta",{"property":"article:tag","content":"freecodecamp.org"}],["meta",{"property":"article:tag","content":"git"}],["meta",{"property":"article:published_time","content":"2024-01-09T00:00:00.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Part 3 - Undoing Changes\\",\\"image\\":[\\"https://freecodecamp.org/news/content/images/2023/12/3_trees.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/1_txt_working_dir.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/1_txt_index.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/2_txt_index.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/new_commit_object.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_updates_active_branch.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_status_after_reset_soft.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reset_soft_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_after_reset_soft.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_2_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_mixed_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_mixed_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_2_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_hard_1-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_hard_2-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_hard_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_switches.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_2_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reset_commit_2_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/adding_love_lyrics.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_2_4.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/scenario_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/scenario_2_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/scenario_2_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_3_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_4.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reset_commit_4.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_love.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_separately.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reset_main_commit_3_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_more_changes.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/scenario_4.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/scenario_4_1-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/switch_existing.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/cherry_pick.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reset_cherry_pick.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/image-52.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/commit_amend-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_revert_1-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_revert_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_revert_3.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/code_py_4-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/code_py_5-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/code_py_6-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/history_after_commit_19-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/plan_edit_commits_17_18-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/rebase_onto_4-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/interactive_rebase_4-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/interactive_rebase_5-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/interactive_rebase_6-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/rebase_onto_5-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reflog_commit_3-1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reflog_commit_3_reset.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reflog_git_log.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reflog.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/reflog_revparse.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_g.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reflog_reset.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_log_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/git_reset_again.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_01_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_02_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_02_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_03_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_03_2.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_04_1.png\\",\\"https://freecodecamp.org/news/content/images/2023/12/ex_04_2.png\\"],\\"datePublished\\":\\"2024-01-09T00:00:00.000Z\\",\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Omer Rosenbaum\\",\\"url\\":\\"https://freecodecamp.org/news/author/omerros/\\"}]}"]],"date":"2024-01-09T00:00:00.000Z","isOriginal":false,"author":[{"name":"Omer Rosenbaum","url":"https://freecodecamp.org/news/author/omerros/"}],"cover":"https://freecodecamp.org/news/content/images/2023/12/Gitting-Things-Done-Cover-with-Photo.png","gitInclude":[]},"headers":[{"level":2,"title":"Chapter 9 - Git Reset","slug":"chapter-9-git-reset","link":"#chapter-9-git-reset","children":[{"level":3,"title":"A Short Reminder - Recording Changes","slug":"a-short-reminder-recording-changes","link":"#a-short-reminder-recording-changes","children":[]},{"level":3,"title":"Introducing git reset","slug":"introducing-git-reset","link":"#introducing-git-reset","children":[]},{"level":3,"title":"Real-Life Scenarios","slug":"real-life-scenarios","link":"#real-life-scenarios","children":[]},{"level":3,"title":"Recap - Git Reset","slug":"recap-git-reset","link":"#recap-git-reset","children":[]}]},{"level":2,"title":"Chapter 10 - Additional Tools for Undoing Changes","slug":"chapter-10-additional-tools-for-undoing-changes","link":"#chapter-10-additional-tools-for-undoing-changes","children":[{"level":3,"title":"git commit --amend","slug":"git-commit-amend","link":"#git-commit-amend","children":[]},{"level":3,"title":"git revert","slug":"git-revert","link":"#git-revert","children":[]},{"level":3,"title":"Git Rebase as a Tool for Undoing Things","slug":"git-rebase-as-a-tool-for-undoing-things","link":"#git-rebase-as-a-tool-for-undoing-things","children":[]},{"level":3,"title":"git reflog","slug":"git-reflog","link":"#git-reflog","children":[]},{"level":3,"title":"Recap - Additional Tools for Undoing Changes","slug":"recap-additional-tools-for-undoing-changes","link":"#recap-additional-tools-for-undoing-changes","children":[]}]},{"level":2,"title":"Chapter 11 - Exercises","slug":"chapter-11-exercises","link":"#chapter-11-exercises","children":[{"level":3,"title":"Exercise 1","slug":"exercise-1","link":"#exercise-1","children":[]},{"level":3,"title":"Exercise 2","slug":"exercise-2","link":"#exercise-2","children":[]},{"level":3,"title":"Exercise 3","slug":"exercise-3","link":"#exercise-3","children":[]},{"level":3,"title":"Exercise 4","slug":"exercise-4","link":"#exercise-4","children":[]}]}],"readingTime":{"minutes":24.73,"words":7418},"filePathRelative":"freecodecamp.org/gitting-things-done-book/part-3-undoing-changes.md","localizedDate":"January 9, 2024","excerpt":"\\n","copyright":{"author":"Omer Rosenbaum"}}');export{q as comp,C as data};
